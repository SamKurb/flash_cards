#separator:tab
#html:true
#guid column:1
#tags column:4
B]I$W<jh!u	Define what is meant by a&nbsp;<i>Proper Relation Algebra</i>	A subalgebra of \((\mathrm{Rel}(X), \emptyset, U, \cup, \cap, \setminus_U, \mathrm{Id}_X, {}^\smile, ;)\) for some non-empty set \(X\) and maximal relation&nbsp;\(U \subseteq X \times X\) (often \(U = X \times X\), but can be any equivalence relation over \( X \)).<br><br>Equivalently: a set of binary relations over&nbsp;\(X\) containing \(\emptyset\) and \(\mathrm{Id}_X\), closed under finite unions, relative complements, converse, and relational composition (e.g. a field of binary relations additionally closed under converse and composition)	binary defn relation_algebra
u`nEa1-]`]	Let \(A = (A, 0, 1, +, \cdot, -, 1', {}^\smile, ;)\) be a finite Relation Algebra. Define what we mean by an atom of \(A\)?	An atom of \(A\) is a minimal non-zero element of \(A\)&nbsp;<br><br>Equivalently: an \(a \in A\) such that \(a \neq 0\), and for every \(b \in A\), if \(0 \leq b \leq a\), then \(b = 0\) or \(b = a\)&nbsp;(There is no non-zero element below \(a\))	binary defn relation_algebra
K/`K5F4wu,	Let \(A = (A, 0, 1, +, \cdot, -, 1', {}^\smile, ;)\) be a Relation Algebra. What is meant by a representation of \(A\)?	A representation of \(A\) is an isomorphism from \(A\) into a <i>proper relation algebra</i><br><br>Equivalently, A representation of \(A\) is an isomorphism \(\theta : A \to \mathrm{Rel}(X)\), for some non-empty base set \(X\).<br><br>That is, \(\theta\) is injective and preserves the relation-algebra operations:<br>\(\theta(1') = \mathrm{Id}_X\),<br>\(\theta(a^\smile) = \theta(a)^\smile\),<br>\(\theta(-a) = \theta(1) \setminus \theta(a)\),<br>\(\theta(a+b) = \theta(a) \cup \theta(b)\), and<br>\(\theta(a;b) = \theta(a);\theta(b)\).	binary defn relation_algebra
"G#:BNM.ZjU"	Let \(X\) be a set such that \(|X| = n\). How many elements does \(\mathcal P(X)\), the powerset of \(X\), have? (i.e. what is \(|\mathcal P(X)|\)?)	\(2^n\)<br><br>Reason:<br>\(\mathcal P(X)\) contains every subset of \(X\). For each element of \(X\), a subset either contains it or does not contain it, giving \(2\) choices per element. Since there are \(n\) elements, the total number of subsets is \(2^n\)	binary relation_algebra
Q$w?EzyD6W	Let \(X\) be a set such that \(|X| = n\). How many elements does \(X \times X\) contain? (i.e. what is \(|X \times X|\)?)	\(n^2\)<br><br>Reason:<br>\(X \times X\) consists of all ordered pairs \((x,y)\) where \(x,y \in X\). There are \(n\) choices for the elementpart and \(n\) choices for the element, giving \(n \cdot n = n^2\) ordered pairs	binary relation_algebra
LlrfxlKvG0	Let \(X\) be a set such that \(|X| = n \). How many binary relations are there over \(X\)?	\(2^{(n^2)}\)<br><br>Reason:<br>A binary relation over \(X\) is any subset of \(X \times X\). Since \(|X \times X| = n^2\), the number of subsets of \(X \times X\) is \(2^{(n^2)}\). Therefore there are \(2^{(n^2)}\) possible binary relations (over \(X\))	binary relation_algebra
Phdo,iof$?	Define an atomic relation algebra	A relation algebra \(A\) is atomic if every non-zero element of \(A\) is above an atom<br><br>That is, for every \(a \in A\), if \(a \neq 0\), then there exists an atom \(b \in A\) such that \(b \leq a\)	binary defn relation_algebra
f7k25Q^+Ba	Define the converse of a binary relation	For a binary relation \(R \subseteq X \times X\), its converse is<br><br>\(R^\smile = \{(y,x) : (x,y) \in R\}\)<br><br>In other words, we take each ordered pair in \(R\) and swap its two elements.	binary defn relation_algebra
sup!G8rHTL	What does it mean for a binary relation to be self-converse?	A binary relation \(R\) is self-converse if it is equal to its converse<br><br>That is, \(R = R^\smile\)<br><br>Equivalently, \(R\) is symmetric: whenever \((x,y) \in R\), we also have \((y,x) \in R\)	binary defn relation_algebra
"OnuGfo4L#/"	Define the composition of two binary relations.	For binary relations \(R,S \subseteq X \times X\), their composition is<br>\(R;S = \{(x,z) : \exists y \in X \text{ such that } (x,y) \in R \text{ and } (y,z) \in S\}\)<br>In other words, \(x\) is related to \(z\) by \(R;S\) if there is some middle element \(y\) such that \(xRy\) and \(ySz\)<br><br>Note:<br>\(R\) is dense if \(R \subseteq R;R\)<br><br>So if \(R;R = R\), or more generally if \(R;R = R + S\) for some relation \(S\), then \(R\) is dense<br><br>That is, whenever \(xRz\), there exists some \(y\) such that \(xRy\) and \(yRz\)<br><br>IMPORTANT:<br>If, for an atom \(a\), we have \(a;a = a\), then \(a\) is both transitive <b>and </b>dense	binary defn relation_algebra
GtKQ-_4Zbc	Define the Boolean order \(\leq\) in a relation algebra	For \(a,b \in A\), we define<br><br>\(a \leq b\) iff \(a + b = b\)<br><br>Equivalently, \(a \leq b\) iff \(a \cdot b = a\)<br><br>In a proper relation algebra, this corresponds to set inclusion: \(\theta(a) \subseteq \theta(b)\)	binary defn relation_algebra
M@&4T/L.bu	Define what is meant by a Boolean algebra	A Boolean algebra is an algebra \((B,0,1,+,\cdot,-)\) where \(+\) is join (similar to set union), \(\cdot\) is meet (similar to set intersection), and \(-\) is complement (similar to set complement wrt a maximal set), satisfying the usual Boolean laws<br><br>Equivalently, it behaves like a field of sets under union, intersection, and complement	binary boolean_algebra defn
DaKRurPoh:	Define what is meant by a field of sets over base set \(X\)	A field of sets \(\mathcal F\) over \(X\) is a non-empty set of subsets of \(X\), i.e. \(\mathcal F \subseteq \mathcal P(X)\), such that \(\mathcal F\) contains \(\emptyset\) and \(X\), and is:<br><br>- closed under set union: if \(R,S \in \mathcal F\), then \(R \cup S \in \mathcal F\);<br>- closed under set intersection: if \(R,S \in \mathcal F\), then \(R \cap S \in \mathcal F\);<br>- closed under complements with respect to \(X\):&nbsp;i.e. if \(A \in \mathcal F\), then \(X \setminus A \in \mathcal F\).	binary defn
E[F;@;teLY	Let \(B = (B, 0, 1, +, \cdot, -)\) be a Boolean algebra. What is meant by a representation of \(B\)?	A representation of \(B\) is an isomorphism from \(B\) into a field of sets<br><br>Equivalently, a representation of \(B\) is an isomorphism \(\theta : B \to \mathcal P(X)\), for some non-empty base set \(X\). So \(B\) is represented as a field of sets (e.g. a subalgebra of the powerset algebra over&nbsp;\(X\))<br><br>That is, \(\theta\) is injective and preserves the Boolean operations:<br>\(\theta(0) = \emptyset\),<br>\(\theta(1) = X\),<br>\(\theta(a+b) = \theta(a) \cup \theta(b)\),<br>\(\theta(a \cdot b) = \theta(a) \cap \theta(b)\), and<br>\(\theta(-a) = X \setminus \theta(a)\)	boolean_algebra defn relation_algebra
pg4Y5FGGHG	Define what is meant by the powerset algebra over base set \(X\)	The powerset algebra over \(X\) is \((\mathcal P(X), \emptyset, X, \cup, \cap, \setminus_X)\)<br><br>It is the Boolean algebra whose elements are all subsets of \(X\)<br><br>Note:<br>-&nbsp;\(\setminus_X\)&nbsp;denotes set complement with respect to \(X\)<br>- \(\mathcal P(X)\) is always a field of sets over \(X\), since it contains every subset of \(X\) and is therefore automatically closed under union, intersection, and complement	boolean_algebra defn relation_algebra
E+1H3_eo[N	If a finite Boolean algebra has \(n\) atoms, how many elements does it have?	\(2^n\)<br><br>Reason:<br>Every element is the join (union) of some subset of the atoms. Since there are \(n\) atoms, there are \(2^n\) possible subsets of atoms, hence \(2^n\) elements<br><br>E.g. every element is made up of atoms. For each atom, it is either included or excluded, giving \(2\) choices per atom	relation_algebra
lPL8kFM=<c	What is meant by \(\mathrm{Rel}(X)\), for set \(X\)?	\(\mathrm{Rel}(X)\) is the set of all binary relations over \(X\)<br><br>Equivalently,<br><br>\(\mathrm{Rel}(X) = \mathcal P(X \times X)\),<br><br>since a binary relation over \(X\) is any subset of \(X \times X\)	defn relation_algebra
lSI/$oFVYH	If \(|X| = n\), how many atoms does \(\mathrm{Rel}(X)\) have?	\(n^2\)<br><br>Reason:<br>In the full relation algebra \(\mathrm{Rel}(X)=\mathcal P(X\times X)\), the atoms are the singleton relations \(\{(x,y)\}\). Since \(X \times X\) has \(n^2\) ordered pairs, there are \(n^2\) atoms.<br><br>Note: <br>- In general, atoms do not need to be singleton relations, but in this case they do.<br><br>-&nbsp;\(\mathrm{Rel}(X)\) has \(2^{n^2}\) elements. In general, a finite Boolean algebra with \(k\) atoms has \(2^k\) elements, so here \(k=n^2\).	relation_algebra
J;}.b0HjUT	What is the difference between a Boolean algebra and a relation algebra?	A Boolean algebra has the operations \((0,1,+,\cdot,-)\), corresponding to empty set, universe, union, intersection, and complement<br><br>A relation algebra is a Boolean algebra with extra relational structure:<br>- an element that acts as the identity, \(1'\),<br>- two additional operations: converse \(({}^\smile)\) and composition \((;)\)<br><br>Note: Every relation algebra is also a Boolean algebra, but not every Boolean algebra is a relation algebra	boolean_algebra relation_algebra

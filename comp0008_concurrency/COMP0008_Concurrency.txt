#separator:tab
#html:true
#guid column:1
#tags column:4
nYj9=;v6Dy	What is a&nbsp;<strong>pipeline hazard</strong>?	A pipeline hazard is a situation where the next instruction in the pipeline cannot safely execute in the next clock cycle, so the processor must stall, flush or otherwise change normal pipeline execution to prevent the instruction from producing incorrect results	defn pipelining week6
Gbd3ViyRHM	Why do pipeline hazards occur?	Pipeline hazards occur because, in a pipeline, multiple instructions overlap in execution, so an instruction may require a hardware resource, data value or control-flow decision (e.g. whether a branch is taken or not taken) that is not yet available<br><br>Examples:<br>- Two instructions need the same single-use hardware resource in the same clock cycle, so one instruction must stall<br>- An instruction needs a result from an earlier instruction, but the result has not yet been produced or made available<br>- The processor does not yet know whether a branch is taken or not taken, so it may not know which instruction to fetch next	pipelining week6
FZ0h=N-&>6	What is a&nbsp;<strong>structural hazard</strong>&nbsp;in a pipelined processor?	"<div>A structural hazard occurs when two or more instructions need the same hardware resource (e.g. memory, ALU, register file) at the same time/in the same clock cycle, but the processor does not have enough resources to support them simultaneously</div><div><br></div>
<div>As a result, one instruction must wait, usually causing a pipeline stall</div>"	defn pipelining week6
gy{gS_8UG.	How can structural hazards be avoided in a pipelined processor?	"<div>Structural hazards can be avoided by designing the processor so that pipeline stages do not need to compete for the same hardware resource in the same clock cycle</div><div><br></div>
<div>This may involve providing enough hardware resources, such as:</div>- separate instruction and data memories/caches, as in the MIPS pipeline<br>- enough register-file ports (i.e access points)<br>- separate functional units (ALU, FPU etc )where needed<br>"	pipelining week6
J@e[Rq%Z,%	What is a&nbsp;data hazard&nbsp;in a pipelined processor?	<div>A data hazard occurs when an instruction depends on a value being produced by an earlier instruction in the pipeline, but that value is not yet available when needed</div><div><br></div><div>In other words, one instruction depends on another instruction that is still in the pipeline<br></div><div><br></div><div>If the dependent instruction executed immediately, it might use an old/incorrect value. The processor may need to use forwarding or stall until the correct value is available<br></div>	defn pipelining week6
H8Ix(RsT}3	What is&nbsp;<strong>forwarding (bypassing)</strong>&nbsp;in a pipelined processor?	"<div>Forwarding, also called bypassing, is a way of resolving some data hazards by passing a result directly from internal pipeline buffers/registers to a later pipeline stage, instead of waiting for the result to be written back to the register file</div><div><br></div><div>It is called bypassing because the value ""bypasses"" the normal route of waiting until it is written back to its usual destination (e.g. register file), and then read again from that destination by the dependent instruction<br></div><div><br></div>
<div>This allows a dependent instruction to use the value as soon as it is available, reducing stalls</div><div><br></div>
<div><strong>Example:</strong>&nbsp;the result of an add&nbsp;instruction can be forwarded directly to the ALU input of a dependent instruction, such as a sub&nbsp;immediately after it</div>"	defn pipelining week6
"iR#uE)EbsP"	Why can forwarding/bypassing not eliminate all data hazards?	"<div>Forwarding can only help once the needed value has actually been produced or retrieved. Sometimes the required value is not available early enough in the pipeline, so there is nothing to forward yet&nbsp;and the hazard is still present</div><div><br></div>
<div>Example: in a load-use hazard, the loaded data is only available after the load’s memory-access stage, so an immediately following instruction that depends on the data may still need to stall until the loaded value is available</div>"	pipelining week6
E>vP`)<nJD	What is a&nbsp;<strong>pipeline stall</strong>?	<div>A pipeline stall (sometimes called a <i>bubble</i>) is when the processor temporarily delays the progress of one or more instructions in the pipeline because the next instruction cannot safely proceed yet (e.g. due to a hazard)</div><div><br></div><div>During a stall/bubble, one or more pipeline stages do no useful work and do not change program state. This gives time for the required data, resource, or control-flow decision to become available</div>	defn pipelining week6
QLJq]&.HsK	What is a&nbsp;<strong>load-use data hazard</strong>&nbsp;in a pipelined processor?	<div>A load-use data hazard is a data hazard where an instruction tries to use a value that an earlier load instruction is still retrieving from memory, before that value is available</div><div><br></div><div>Example:<br>lw&nbsp; &nbsp; $t2, 4($t0)<br>add $t3, $t1, $t2<br></div><div><br></div><div>Here, the add needs $t2, but at the time it needs it within the pipeline, $t2 is still being loaded from memory. Since there is no value to forward yet, the pipeline may need to stall</div>	defn pipelining week6
eydCUZJH!{	What is a&nbsp;<strong>pipeline</strong>&nbsp;in a processor?	"<div>A pipeline is a way of organising instruction execution into a sequence of stages, where each stage performs one part of the instruction (e.g. fetch, decode, execute). The classic MIPS pipeline has <strong>5 pipeline stages</strong></div><div><br></div>
<div>With pipelining, multiple instructions can be in different stages at the same time, allowing the processor to overlap instruction execution and improve throughput: the number of instructions completed per unit time<br></div>"	defn pipelining week6
"ye7#E8V!FK"	What are the&nbsp;five stages of the classic MIPS pipeline?	"<div>The five stages are:</div><br>1.)&nbsp;<b>Instruction Fetch (IF):</b> fetch the instruction from instruction memory<br>2.)&nbsp;<b>Instruction Decode/Register Read (ID):</b> decode the instruction and read registers<br>3.)&nbsp;<b>Execute/Address Calculation (EX):</b> perform the ALU operation or calculate a memory address<br>4.)&nbsp;<b>Memory Access (MEM):</b> read from or write to data memory,&nbsp;if needed (e.g. for load/save instructions), otherwise instruction just passes through this stage<br>5.)&nbsp;<b>Write Back (WB):</b> write the result back to the register file (if the instruction produces a register result)<br><ul>
</ul>
<div>Each stage performs a specific part of instruction execution, allowing multiple instructions to be processed simultaneously in different stages<br></div>"	defn pipelining week6
n&D)X&6u/*	What happens during the <strong>Instruction Fetch (IF)</strong> stage of the MIPS pipeline?	The processor fetches the next instruction from memory using the Program Counter (PC) as the byte address<br><br>By default, it also computes the next PC as <strong>PC + 4</strong> to point to the next sequential instruction, adding 4 because each MIPS instruction is 4 bytes long. Though the PC may be changed at a later pipeline stage if the instruction is a branch or jump<br><br>Note: Fixed-length instructions make instruction fetch simpler, because the processor knows where the next sequential instruction starts, so it can always increment the PC by a set amount, in this case 4	pipelining week6
y6*kqX:t4f	What happens during the <strong>Instruction Decode (ID)</strong> stage of the MIPS pipeline?	"The processor decodes the instruction to determine what operation is needed, while also reading the source register values from the register file<br><br><div>For instructions with a 16-bit immediate, the immediate is also sign-extended so it can be used later, e.g. for address calculation or branch offsets</div><div><br></div>
<div>Note: MIPS makes this stage easier to pipeline because, for instructions that read registers, the source register fields are in consistent positions across instruction formats, such as R-type and I-type instructions. This lets register reading begin while the instruction is being decoded</div>"	defn pipelining week6
ch2&+zy8F@	What happens during the <strong>Execute (EX)</strong> stage of the MIPS pipeline?	The processor uses the ALU (arithmetic logic unit) to perform the main computation needed by the instruction<br><br>Examples:<br>- For arithmetic/logical/shift instructions, it performs the related ALU operation, e.g. addition, AND or shifting<br>- For lw/sw, it calculates the effective memory address, e.g. base register + offset<br>- For branches, it may compare register values and help determine the branch outcome, e.g. taken or not taken	defn pipelining week6
KWA$E{[cM(	What happens during the&nbsp;<strong>Memory Access (MEM)</strong>&nbsp;stage of the MIPS pipeline?	the processor accesses data memory&nbsp;to read/write if the instruction is a load/store<br><br>- For load instructions, it <i>reads </i>data from memory uisng the effective address calculated in EX<br>- For store instructions, it <i>writes&nbsp;</i>data to memory using the effective address calculated in EX<br>- For other instructions, this stage does no meaningful work, but the instruction still passes through it	defn pipelining week6
"e![H*u##]F"	What happens during the <strong>Write Back (WB)</strong> stage of the MIPS pipeline?	During the WB stage, the processor writes a result back to the register file if the instruction produces a register result<br><br><div><div>Examples:</div>- add $t0, $t1, $t2 writes the ALU result to $t0</div><div>- lw $t0, 4($s0) writes the loaded memory value to $t0.</div><div>- sw $t0, 4($s0) does not write back to a register, so it does no meaningful work during the WB stage.&nbsp;Its useful work happened in MEM, where it wrote to data memory</div>	defn pipelining week6
q7>ls~t*-H	What is a&nbsp;<strong>control hazard</strong>&nbsp;in a pipelined processor?	"<div>A control hazard, also called a <strong>branch hazard</strong>, occurs when the processor does not yet know which instruction address to fetch next because the outcome of a branch or jump is not known<br></div><div><br></div>
<div>For example, the processor may not yet know whether to fetch the next sequential instruction at PC + 4 or fetch from a branch/jump target address</div><div><br></div><div>To handle this, the processor may stall, use <strong>branch prediction</strong> to guess the next instruction address, or flush instructions if it fetched from the wrong control-flow path<br></div>"	defn pipelining week6
A)aIcQ]]X=	What are the&nbsp;<strong>three main ways of handling a control hazard</strong>&nbsp;in a pipeline?	"1.) Stalling: Wait until the branch/jump outcome is known before fetching or allowing the next-path instruction to proceed. Simple and correct, but wastes cycles<br>
2.)&nbsp;Branch prediction: Guess the next control-flow path and continue fetching. Faster if correct, but wrong-path instructions may need to be flushed if the prediction is wrong<br>3.)&nbsp;Delayed branching: Always execute the instruction immediately after the branch, called the delay slot. The compiler/assembler tries to fill this slot with an instruction that is safe to execute, e.g. by reordering instructions safely"	pipelining week6
e;tbz=`.2r	How does&nbsp;<strong>stalling</strong>&nbsp;handle control hazards?	"<div>By waiting until the branch/jump outcome is known before continuing down a control-flow path</div><div><br></div>
<div>This guarantees correctness because the processor does not proceed with the wrong next instruction address, but it wastes cycles and reduces performance</div>"	pipelining week6
"Nm#rE.l9Gb"	What is&nbsp;branch prediction&nbsp;in a pipelined processor?	"<div>Branch prediction is a technique for handling control hazards where the processor guesses the branch outcome, e.g. taken or not taken, and fetches instructions along the predicted control-flow path instead of waiting for the branch to be resolved</div><div><br></div>
<div>If the prediction is correct, this can avoid stalls and improve performance. If the prediction is wrong, the wrong-path instructions must be flushed and execution restarts from the correct path, causing a performance penalty</div>"	defn pipelining week6
IOA1x,t39G	How do&nbsp;stalling&nbsp;and&nbsp;branch prediction&nbsp;compare for handling control hazards?	"<strong>Stalling:</strong> waits until the branch outcome is known. Simple and correct, but wastes cycles because the pipeline is not doing useful work<br><br><strong>Branch prediction:</strong>&nbsp;Attempts to guess the control-flow path and continues fetching. It can improve performance when correct, but if wrong, wrong-path instructions must be flushed, causing wasted work.<br><ul>
</ul>
<div>Modern processors usually prefer branch prediction because it often gives better overall performance on average than always stalling<br></div>"	pipelining week6
Iw&GMdB;m`	What is <strong>delayed branching</strong>?	<div>Delayed branching is a technique for handling control hazards where the branch takes effect after a delay of one instruction</div><div><br></div> <div>The instruction immediately after the branch, called the delay slot, is always executed whether or not the branch is taken</div><div><br></div> <div>The compiler/assembler tries to fill the delay slot with an instruction that is safe to execute in both cases (branch taken or not taken), so the processor avoids wasting a cycle. If no safe instruction exists, the slot may be filled with a nop&nbsp;</div>	defn pipelining week6
DtdEwtGQ1{	What is&nbsp;<strong>instruction latency</strong>?	"<div></div><div><div>Instruction latency is the time it takes for a single instruction to complete from start to finish</div><div><br></div>
<div>In a simple 5-stage MIPS pipeline, each instruction still takes roughly 5 cycles to pass through the 5 pipeline stages.&nbsp;</div><div><br></div><div>Note: Pipelining overlaps multiple instructions to improve throughput, but it does not make an individual instruction finish faster e.g. the latency is not reduced</div></div>"	defn pipelining week6
bLQ>}bixnS	What is&nbsp;instruction throughput?	<div>Instruction throughput is the number of instructions a processor completes per unit of time, e.g. instructions per second</div><div><br></div><div>Note: In a pipeline, throughput improves because multiple instructions are overlapped: once the pipeline is full, the processor may complete roughly one instruction per clock cycle in the ideal case</div>	defn pipelining week6
HQdR^^!A:(	Does&nbsp;pipelining&nbsp;reduce instruction latency or improve instruction throughput?	"<div>Pipelining mainly improves instruction throughput, not individual instruction latency<br></div><div><br></div>
<div><div>It overlaps multiple instructions so the processor can complete instructions at a higher rate (more throughput), but each instruction still has to pass through the pipeline stages (so latency is not reduced)</div></div><div><br></div><div>In other words, pipelining does not make a single instruction finish faster, instead it lets the processor complete more instructions per unit of time</div>"	pipelining week6
h8kKlGEE|V	What is the&nbsp;principle of locality?	"<div>The principle of locality is the observation that programs tend to access only a small portion of memory repeatedly over a short period of time, rather than accessing all memory locations uniformlyrandomly</div><div><br></div>
<div><div>This happens because programs often:</div><div>- reuse the same memory locations, called <strong>temporal locality</strong></div><div>- access nearby memory locations soon after one another, called <strong>spatial locality</strong>.</div></div><div><br></div><div><strong>Note:</strong> Locality is what makes caches effective: the cache can keep recently used or nearby data close to the processor, increasing the chance of fast future accesses<br></div>"	defn memory week6
IH?$o_8(U	What are the&nbsp;two main types of locality?	"<div>The two main types of locality are:</div>
<div>1.) <b>Temporal locality: </b>Recently accessed memory locations are likely to be accessed again soon.<br>
2.) <strong>Spatial locality:</strong>&nbsp;Memory locations near recently accessed/referenced locations are likely to be accessed again soon</div>"	defn memory week6
y(TH~}QKX(	What is&nbsp;temporal locality?	"<div>Temporal locality (locality in time) is the tendency for a program to access/reference the same memory location again soon after it was recently accessed/referenced</div><div><br></div>
<div>Example: a loop variable or frequently used instruction may be accessed repeatedly while a loop runs</div>"	defn memory week6
GCGkZsEer4	What is&nbsp;spatial locality?	"<div>Spatial locality (locality in space) is the tendency for a program to access/reference memory locations near a recently accessed/referenced location</div><div><br></div>
<div>Example: if a program accesses one element of an array, it is likely to access nearby elements soon after, because array elements are stored next to each other in memory</div>"	defn memory week6
z8v]88Xq+`	What is the&nbsp;<strong>memory hierarchy</strong>?	"<div>The memory hierarchy is the organisation/arrangement of storage into levels, from small/fast/expensive memory close to the processor to large/slow/cheap memory further away</div><div><br></div>
<div>The typical order is:</div>
<div>registers -&gt; caches (L1, L2, L3) -&gt; main memory (RAM) -&gt; secondary storage (HDD, SSD)<br></div><div><br></div>
<div>The goal is to make memory accesses fast on average while still providing large storage capacity</div>"	defn memory week6
inH?/)3R6h	What is the goal of a&nbsp;<strong>memory hierarchy</strong>?	"<div>The goal of a memory hierarchy is to make memory appear almost as large and cheap as the lower levels of the hierarchy, while providing access speeds close to the upper, faster levels</div><div><br></div>
<div>It works by exploiting locality -&nbsp;keeping recently used and nearby data in faster upper levels, such as cache, while storing the full program/data in larger, slower lower levels</div>"	memory week6
"ON/#nL1y+^"	What is a&nbsp;<strong>block</strong>/<strong>line</strong>&nbsp;in a memory hierarchy?	<div>A block (also called a line), is the minimum unit of data that can be present or absent in a cache</div><div><br></div><div>It is also the unit of data transferred between adjacent levels of the memory hierarchy<br></div><div><br></div><div>For example, when data is brought from main memory into cache, the cache usually transfers a whole block between memory levels, not just the single byte or word that was needed. This may bring in more data than the processor requested, but it helps exploit spatial locality, since nearby data may be needed soon</div>	defn memory week6
I{Xvj!,UgV	What is a&nbsp;cache?	"<div>A cache is a small, very fast memory that stores copies of data from a larger, slower level of the memory hierarchy, such as main memory</div><div><br></div>
<div>It improves performance by letting the processor quickly access data that is likely to be needed soon, avoiding slower accesses to lower memory levels</div><div><br></div><div>SIDENOTE: In this hardware context, “cache” usually means CPU cache. More generally, a cache is any storage that keeps likely-to-be-reused data for faster future access. For example, a program might cache expensive function-call results in a data structure or variable, even though that data is not necessarily stored in the CPU cache<br></div>"	defn memory week6
O))d$>Mh:f	What is the general order of the&nbsp;memory hierarchy, from closest to furthest from the processor?	"<div>The usual order is:</div>
<div>1.) Registers<br>
2.) Cache (different levels e.g. L1, L2, L3)<br>
3.) Main memory / RAM<br>
4.) Secondary storage e.g. SSD/HDD</div><div><br></div>
<div>As you move down the hierarchy, storage generally becomes larger, cheaper per bit, and slower<br></div>"	memory week6
JBb~k+b}O/	Why is the&nbsp;<strong>principle of locality</strong>&nbsp;essential for caches to work well?	"<div><div>Caches are much smaller than main memory, so they can only store a small subset of all possible data</div><div><br></div>
<div>Locality makes this useful because it tells us that programs do not access memory randomly. They tend to reuse recently accessed data (temporal locality) and access nearby data soon after (spatial locality)</div><div><br></div>
<div>Therefore, the processor does not need fast access to all of memory to be extremely fast - it mainly needs fast access to the small part of memory likely to be used soon. This is what caches try to store, exploiting the principle of locality</div></div>"	memory week6
H*kp4$+gIZ	What is a&nbsp;<strong>cache hit</strong>?	"<div>A cache hit occurs when the data requested by the processor is found in a block in the cache</div><div><br></div><div>The requested data can then be returned from the fast upper level of the memory hierarchy instead of being fetched from slower main memory<br></div><div><br></div>
<div><strong>Note:</strong> Cache hits are desirable because they can make memory access orders of magnitudes faster</div>"	defn memory week6
wk%<JDBm*t	What is a <strong>cache miss</strong>?	"<div>A cache miss occurs when the data requested by the processor is not found in the cache</div><div><br></div><div>The data must then be fetched from a lower, slower level of the memory hierarchy, which can delay execution</div><div><br></div>
<div>Note: Cache misses are undesirable because they can take many extra cycles and may cause a pipeline stall</div>"	defn memory week6
cGb}0,),ue	What is a&nbsp;<strong>cache update</strong>&nbsp;after a cache miss?	"<div>A cache update is when the cache stores the memory block fetched from a lower level after a cache miss</div><div><br></div>
<div>This is useful because future accesses to the same block may become cache hits, exploiting temporal and spatial locality</div>"	defn memory week6
wOODO7`{xF	What happens in the memory hierarchy after a&nbsp;<strong>cache miss</strong>?	<div>After a cache miss, the memory system fetches the block containing the requested data from a lower level of the hierarchy, such as main memory or a lower-level cache<br></div><div><br></div><div>Since an entire block is fetched, nearby data stored close to the requested address may also be brought in, exploiting spatial locality</div><div><br></div><div><div>The cache is then updated by placing this block into the cache, possibly replacing/evicting an older block, and the requested data is supplied to the processor. This means future accesses to the same block may become cache hits</div></div>	memory week6
Dy5VmK|>B$	How does a memory hierarchy exploit temporal locality, and how does this improve performance?	<div>A memory hierarchy exploits temporal locality by keeping recently accessed data in faster upper levels of the hierarchy that are closer to the processor, such as the cache</div><div><br></div><div>This improves performance because repeated accesses to the same data are more likely to bw cache hits, avoiding slower accesses to lower levels of the hierarchy (e.g. main memory) and reducing the time the processor spends waiting for memory.<br></div>	memory week6
p(g-l0j6BM	How does a&nbsp;<strong>memory hierarchy exploit spatial locality</strong>, and how does this improve performance?	"<div>A memory hierarchy exploits <strong>spatial locality</strong> by moving a whole <strong>block/line</strong> of nearby/contiguous memory locations at a time into upper, faster levels of the hierarchy, such as the cache<br></div><div><br></div>
<div>If the processor later accesses nearby locations in that block, those accesses are more likely to be cache hits, reducing time spent waiting for lower, slower memory levels</div>"	memory week6
MQ-l+ujvEM	What is a&nbsp;direct-mapped cache?	"<div>A direct-mapped cache is a cache where each memory block can go in exactly one possible location within the cache</div><div><br></div>
<div>That location is chosen using the index bits of the memory address, which are part of the address used to select the cache entry to check</div><div><br></div>
<div>This makes the cache simple and fast to check, but can cause conflict misses when multiple frequently used memory blocks map to the same cache location, since many addresses may share the same index bits</div>"	defn memory week6
mn4;<SUnf2	What is a&nbsp;<strong>cache index</strong>?	The cache index is the part of the memory address used to select which cache entry or set to look in. It usually comes from lower address bits, excluding any offset bits<br><br>Note: In a direct-mapped cache, it identifies the only possible cache location where the requested memory block could be stored.&nbsp;	defn memory week6
mtl{_jh;X`	What is a&nbsp;<strong>cache tag</strong>?	"<div>A cache tag is the part of the memory address stored alongside a cache block to identify which memory block is currently in that cache entry/set</div><div><br></div>
<div>This is needed because many different memory blocks can map to the same cache index. The cache compares the tag from the requested address with the stored tag to check whether the selected cache entry contains the correct block</div><div><br></div><div>Example: Suppose memory addresses are 5 bits long, the cache index is the lower 3 bits, and the tag is the upper 2 bits. To access address 10011, the cache looks at entry 011. But many addresses could share index 011, so the cache checks whether the stored tag matches 10.<br></div>"	defn memory week6
B057ZN=n8m	What is a&nbsp;<strong>valid bit</strong>&nbsp;in a cache, and why is it needed?	A valid bit is a bit stored with each cache entry that indicates whether the entry currently contains valid dataIt is needed because cache entries may initially contain random, old or invalid data<br><br>A cache access is only a hit if the tag matches <strong>and</strong> the valid bit is set	defn memory week6
BQqc)Mw//N	How does a direct-mapped cache decide whether a memory access is a hit?	<div>The cache uses the index bits of the address to select the cache entry. It then compares the tag from the requested address with the stored tag in that entry. The access is a hit only if the tags match and the valid bit is set - otherwise it is a miss</div>	memory week6
rD/%p>Ol~]	Why are the lowest 2 bits of a MIPS address ignored when selecting a word within a cache?	"<div>MIPS is byte-addressed, but a word is 4 bytes and word addresses are aligned to multiples of 4</div><div><br></div>
<div>Therefore, the lowest 2 address bits only select a byte within the word, not which word/block is being accessed. So these bits are <b>byte-offset bits</b> and are ignored when selecting or identifying a whole word in the cache<br></div>"	memory week6
ExKFJ.LShH	If a direct-mapped cache has 2<sup>n</sup>&nbsp;blocks, how many index bits are needed?	<div>It needs n index bits</div><div><br></div><div>This is because n<sup>&nbsp;</sup>bits can represent 2<sup>n&nbsp;</sup>possible values, allowing the cache to select one of the 2<sup>n</sup>&nbsp;cache blocks/entries<br></div><div><br></div><div>Note: Direct-mapped caches usually have a power-of-2 number of entries so the index bits can be used directly to select the cache entry</div>	memory week6
ea^3df2][z	What are&nbsp;<strong>word offset bits</strong>&nbsp;in a cache address?	"<div>Word offset bits are the address bits used to select which <strong>word within a multiword cache block</strong> is being requested</div><div><br></div>
<div>After the cache has identified the correct block, the word offset tells it which specific word inside that block should be returned to the processor</div><div><br></div>
<div><strong>Note:</strong> If each cache block contains only one word, no word offset bits are needed</div>"	memory week6
BOJX`^eFQa	If a cache block contains 2<sup>m&nbsp;</sup>words, how many word offset bits are needed?	"<div>It needs m word offset bits</div><div><br></div>
<div>This is because m bits can represent 2<sup>m</sup> possible values, so they are enough to select one of the 2<sup>m</sup>&nbsp;words within the cache block<br></div><div><br></div><div><strong>Note:</strong> These bits select the word inside the block; separate byte offset bits select the byte inside a word<br></div>"	memory week6
qsL4yS+yrI	How are address bits typically split for a direct-mapped cache with multiword blocks?	"<div>For a 32-bit address, the address is typically split as, from most significant bits to least significant bits, as:<br></div><div><br></div>
<div>Tag | Index | Word offset | Byte offset&nbsp;</div><div><br></div>
<div><b>- Tag:</b> identifies which memory block is stored in the selected cache entry<br>
<b>- Index:</b> selects the cache block/entry<br>
<b>- Word offset:</b> selects the word inside the cache block/entry<br>
<b>- Byte offset:</b> selects the byte inside the word<br></div>"	memory week6
o%G!w-IYoH	What is the&nbsp;<strong>byte offset</strong>&nbsp;in a MIPS memory address?	"<div>The byte offset is the lowest part of the address that selects a specific byte within a word</div><div><br></div>
<div>In MIPS, a word is 4 bytes, so the lowest 2 address bits select one of the 4 bytes within the word</div>"	defn memory week6
eG+-g^r0Dy	For a 32-bit address, direct-mapped cache with 2<sup>n</sup>&nbsp;cache entries/blocks and 2<sup>m</sup>&nbsp;words per block, where one word is 4 bytes, how many tag bits are needed?	"<div>The number of tag bits is =&nbsp; 32 − n − m − 2</div><div><br></div>
<div>This is because the 32-bit address is split into:</div><div>- n index bits</div><div>- m word offset bits</div><div>- 2 byte offset bits</div><div>- the remaining bits are the tag</div><br>"	memory week6
muh8Mrn^MO	Why is the total amount of storage needed for a cache larger than just the amount of data it holds?	Total cache storage is larger than the data capacity because each cache entry stores extra metadata as well as data<br><br>For example, in a direct-mapped cache, each entry stores the data block plus a tag and a valid bit, so the total number of bits required is greater than the number of data bits alone	memory week6
vKX0PfVBEo	How many bits are needed to implement a&nbsp;direct-mapped cache?	"<div>The total number of bits needed is:</div><div>number of cache entries × (data block size + tag field size + valid field size)<br></div><div><br></div>
<div>So for a simple direct-mapped cache with 2<sup>n</sup>&nbsp;entries:</div>
<div>total bits = 2<sup>n</sup> × (block size + tag size + valid field size)</div><div><br></div>
<div>This is because each cache entry must store the actual data block, plus metadata: the tag used to identify the memory block and the valid bit used to check whether the entry contains meaningful data</div>"	memory week6
"eZMN9Sb{i#"	How many total bits are required for a direct-mapped cache with 16 KiB of data and 4-word blocks (word size 32 bits), assuming a 32-bit address?	16 KiB is 2<sup>14&nbsp;</sup>bytes which is 4096 (2<sup>12</sup>) words if tthe word size is 4 bytes<br><br>If the block size is 4 words (2<sup>2</sup>) then the cache needs 4096/4 = 1024 (2<sup>10</sup>) blocks<br><br>Each cache block stores:<br>- Data: in this case 4 * 32 = 128 bits&nbsp;<br>- A tag field: in this case 32 - 10 index bits - 2 word offset bits - 2 byte offset bits = 18 bits<br>- A valid field: 1 bit<br><br>So in total the amount of bits needed for the cache is is 2<sup>10</sup>&nbsp;* (128 + 18 + 1) = 2<sup>10</sup>&nbsp;* 147 bits, which is around 18.4KiB. <br><br>So even though the cache stores 16KiB of data, the metadata overhead means it actually requires 18.4KiB of storage in total	memory week6
KlvY2!W!iX	What is the trade-off of increasing cache block size?	"<div>Increasing block size can reduce the miss rate because each miss brings in more nearby data, exploiting spatial locality</div><div><br></div>
<div>However, very large blocks can hurt performance because the cache can hold fewer total blocks, causing more competition between blocks and more evictions. They also increase the miss penalty, because more data must be fetched from lower memory on each miss</div><div><br></div>
<div>So larger blocks help up to a point, but after that they can make misses more costly or more frequent</div>"	memory week6
NyJA:*P3qF	What is the trade-off of decreasing&nbsp;cache block size?	"<div>Decreasing block size can reduce the miss penalty, because less data has to be fetched from lower memory after a cache miss</div><div><br></div>
<div>However, smaller blocks exploit spatial locality less effectively, because each miss brings in less nearby data. This can increase the miss rate if the processor soon needs adjacent memory locations that aren't in the cache</div><div><br></div>
<div>So smaller blocks make each miss cheaper, but may cause misses to happen more often</div>"	memory week6
i3Tz9Br*?,	What is the&nbsp;<strong>early restart</strong>&nbsp;technique in cache miss handling?	"<div>Early restart is a technique where the processor resumes execution as soon as the <strong>requested word</strong> of the missed cache block has been returned</div><div><br></div>
<div>Instead of waiting for the entire cache block to be fetched, the processor continues once the word it actually needed is available. This can reduce the <strong>effective miss penalty</strong> by hiding part of the block transfer time, especially for instruction accesses<br></div>"	memory week6
L4/fU((zE7	What is an <strong>in-order processor</strong>?	"<div></div><div>An in-order processor (like MIPS processors) is a processor that executes instructions in the same order they appear in the program (e.g. in memory)</div><div><br></div>
<div>This means if one instruction is delayed, for example by a cache miss or data hazard, later instructions usually cannot skip ahead and execute first. As a result, stalls can have a large performance impact</div>"	memory week6
f|i%^**kPP	How does a processor handle a&nbsp;<strong>cache miss</strong>?	"<div>On a cache miss, the requested data is not in the cache, so the processor cannot get it from the fast upper level of the memory hierarchy</div><div><br></div>
<div>The memory system fetches the required block from a lower level within the hierarchy, updates the cache with that block, and then supplies the requested data to the processor</div><div><br></div>
<div>In an in-order processor such as MIPS processors, this typically causes the pipeline to <strong>stall</strong> while the processor waits for the data</div>"	memory week6
pTrdZ?6*!u	Why does a&nbsp;<strong>cache miss</strong>&nbsp;usually cause a pipeline stall?	"<div>A cache miss requires data to be fetched from a lower, slower memory level, which can take many cycles</div><div><br></div>
<div>In an in-order processor, later instructions cannot safely continue if they depend on the missing data (e.g. there is a data hazard), so the processor stalls the pipeline until the data is available</div>"	memory week6
GwvcH&TC8W	What does it mean for the cache and memory to be <b>consistent</b>?	<div>The cache and main memory are consistent when they store the same value for the same memory address<br></div><div><br></div><div>This matters because if the processor writes a new value into the cache but main memory still has the old value, then main memory is no longer up to date</div><div><br></div><div>Note: A write-through cache keeps them consistent by updating both the cache and main memory on every write</div>	defn memory week6
ue._YNXp%a	What is a&nbsp;<strong>write-through cache</strong>?	"<div>A write-through cache is a cache where every processor write updates both the cache and the next lower level of the memory hierarchy</div><div><br></div>
<div>For example, if the processor writes a new value to an address that is currently stored in the cache, the cache is updated and the same new value is also written to main memory</div><div><br></div>
<div>This keeps memory consistent with the cache, but it can be slower because every write must also go to lower memory (which is slower)</div>"	defn memory week6
h1dk!&v(EZ	What are&nbsp;pipeline registers, and why are they needed?	"<div>Pipeline registers are temporary registers placed between pipeline stages. They store the values and control signals that an instruction produces in one stage, so that the same instruction can use them in its next stage on a later clock cycle<br></div><div><br></div>
<div>They are needed because multiple instructions are in the pipeline at once, with different instructions occupying different stages. Pipeline registers keep each instruction’s data/control information separate as it moves forward</div><div><br></div>
<div>Note: They can also be used for forwarding, e.g. sending a value from a pipeline register directly to the ALU input of a dependent instruction that needs it<br></div>"	defn pipelining week6
mL9L>{$IP|	What does it mean to <strong>flush</strong> an instruction from the pipeline?	"<div>Flushing means cancelling/removing an instruction that has already entered the pipeline, so it does not update registers or memory</div><div><br></div>
<div>This is usually needed when handling a control hazard after the processor has fetched instructions from the wrong control-flow path (e.g. after a mispredicted branch)</div><div><br></div>
<div>Example: if the processor fetches PC+4&nbsp;but a branch is actually taken, the incorrectly fetched sequential instruction may be flushed</div>"	defn pipelining week6
xdQ`Qe<<vd	What is dynamic branch prediction?	"<div>Dynamic branch prediction uses the runtime history of previous branch outcomes to predict future branch outcomes based on previous branch behaviour</div><div><br></div>
<div>Instead of using a fixed rule like “always predict not taken,” the processor records how branches behaved in the past and uses that information to predict future outcomes</div><div><br></div>
<div>Example: if a loop branch was taken many times before, the processor may predict it will be taken again</div><div><br></div>
<div>Note: This works well because many branches, especially loop branches, behave predictably across repeated executions, so a processor using dynamic branch prediction can take advantage of this pattern to be more efficient</div>"	defn pipelining week6
qvqp5I6_s=	What is a branch delay slot?	"<div>A branch delay slot is the instruction position immediately after a delayed branch</div><div><br></div>
<div>In classic MIPS, this instruction is executed whether or not the branch is taken, so the compiler/assembler tries to put a useful instruction there that is safe in both cases</div>"	defn pipelining week6

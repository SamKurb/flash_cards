#separator:tab
#html:true
#guid column:1
#tags column:4
mVmHS?Jxj[	What is the definition of a <i>'connected component'</i>	A maximal set of connected vertices/nodes	Graphs
o+EStZ&S0,	What is the definition of a Digraph?	Directed graph<br><br>A set of vertices/nodes connected pairwise by directed edges/arcs, with each edge being unidirectional - having a direction from one vertex to another	Graphs
u?!j^x(i.i	What is the meaning of the <i>'degree'</i> of a vertex/node in an <i>undirected</i> graph?	The amount of nodes it is connected to via both incoming and outgoing edges	Graphs
"PTtxB#G(GK"	What is the meaning of the 'in/out degree' of a vertex/node in a directed graph?	In degree - the number of <i>incoming</i> edges/arcs<br>out degree - the number of <i>outgoing</i> edges/arcs	Graphs
wghx8?e2t0	What is the API graphs? (just function signatures)	addEdge(int v, int w)<br><br>Adj(int v) -&gt; int[]<br><br>V() -&gt; int()<br><br>E() -&gt; int()	Graphs
N;K)Ms8gyo	What does the addEdge(v, w) API call for a <i>directed</i> graph do?	Adds a unidirectional edge going from v to w<br><br>Amounts to setting adj[v][w] = True in the adjacency matrix, or adding w to the adjacency list at adj[v]	Graphs
P-.WYk+8C&	What does the Adj(v) API call for both&nbsp;<i>undirected</i>&nbsp;and&nbsp;<i>directed&nbsp;</i>graphs do?	Returns the list of vertices connected to v via outgoing edges<br><br>So for digraph 1 -&gt; 2 -&gt; 3, adj(2) returns [3],<br>for undirected graph 1 &lt;-&gt; 2 &lt;-&gt; 3 adj(2) returns [1,3]	Graphs
c^G=AOUb|1	What do the V() and E() API calls for <i>undirected</i> and <i>directed </i>graphs do?	They are the same for both types of graphs<br><br>V() - returns the number of vertices<br>E() - returns the number of edges	ADTs Graphs
xKj^&qb+y!	What is an adjacency list? How do they differ between undirected and directed graphs?	An adjacency list is a vertex-indexed array containing lists representing which vertices a given vertex is connected to.<br><br>For undirected graphs - this includes vertices/nodes connected via outgoing and incoming edges, but for directed graphs only vertices/nodes connected via outgoing edges are stored.	Graphs
IiS/PBY[|4	In the context of digraphs, what is meant by two vertices being 'strongly connected'?	Two vertices x, y are strongly connected if there is both a directed path from x to y and from y to x.<br><br>Note:<br>Strong connectivity is an equivalence relation, so<br><br>- x is strongly connected to itself (reflexivity)<br>- if x is strongly connected to y, then y is strongly connected to x (symmetry)<br>- if x is strongly connected to y and y to z, then x is strongly connected to z (transitivity)	Graphs
OJkSY2$K6o	What is the definition of a <i>'strong component'</i>	A maximal set of <i>strongly</i>&nbsp;connected vertices	Graphs
4^c}<n+^b	What does it mean for a digraph to be <i>'strongly connected'?</i>	A directed graph is strongly connected if there exists a directed path from any given node to every other node	Graphs
IAp=an4@r.	What does it mean for an undirected graph to be 'connected'?	An undirected graph is said to be connected if there exists a path from any given node to every other node<br><br>Note: A graph is connected iff it consists of only 1 connected component	
c]EL4u8^:f	Would [(1,'a') , (2, 'b')] be a valid list in Haskell?	Yes.&nbsp;<br><br>The type of each tuple is (Int, Char), and because they are all the same, the list's contents are homogenous and thus it is allowed	Expression Haskell
"Dpg{_}#[6R"	Would [(1, 2) , (2, 'b')] be a valid list in Haskell?	The first tuple's type is (Int, Int), the second tuple's type is (Int, Char). These are not the same, thus the list is heterogenous and so it is not valid&nbsp;	Expression Haskell
hsimM6gi*q	What is a '<i>type'&nbsp;</i>in Haskell?&nbsp;	A type is a collection of related values. <br>- All types start with a capital letter	defn Haskell keyword
tZh(_XV7Pa	How does a '<i>type definition'</i> work? With a small example	We use notation&nbsp;<br>e :: T<br>to mean that evaluating the expression/function e will produce a value of type T<br><br>For example<br>False :: Bool means that evaluating False will give a boolean&nbsp;	defn keyword
xJ[EYb-|=E	What is <i>'type inferencing'</i>?&nbsp;	Type inference is the process used by the Haskell compiler to automatically determine the type of every expression <em>before</em> it is evaluated<br><br>Type inferencing is also why you do not need to explicitly annotate everything with their types<br>	defn Haskell keyword
K$SV@/`xXr	What is meant by Haskell programs being <i>'type safe'</i>? Why are they type safe?	Haskell programs are called type safe because type errors can never occur during evaluation<br><br>This is because the compiler does rigorous type checking at compile time, ensuring that only well-typed programs can run	defn Haskell keyword
vni8|CeNg2	What is meant by a program being <i>'well-typed'</i>?	A well-typed expression is one where all parts of the expression follow the type rules (i.e 5 + True never occurs)<br><br>A well-typed program is one where every expression is well-typed	defn Haskell keyword
PsAo5a,;|O	What is the meaning of '<i>void'</i> with respect to C?	"Void is a keyword in C with the meaning of ""no type"" or ""nothing"". <br>It can be used to:<br>- specify that a function does not return anything <br>- specify that a function takes no arguments ( i.e int main(void) ) <br>- set the ""data type"" of a pointer to be generic, so that it can point to any data type"	C defn keyword
bMMC^1?-:0	What is the meaning of <i>'linking'</i> with respect to C?	Linking is the final stage of the compilation process, and is when multiple object files ( compiled .o files) and libraries are combined into a single executable program by the linker.<br><br>	C defn keyword
D2Eq>I>j&g	What is the meaning of <i>'literal value'</i> with respect to C?	<div>A <em>literal value</em> is a fixed constant whose souce code contains its value.</div><div>- Its value is known at compile time</div><div>- It is not a variable or identifier</div>- Stored in read-only memory	C defn keyword
"Hfx3v#MvY}"	What is the meaning of <i>'function declaration'</i> with respect to C?	A function declaration tells the compiler:<br>- What the name of the function is<br>- The return type of the function<br>- The types and number of parameters<br><br>This allows the function to be called and used before it is defined.	C defn keyword
IjzgTkY&-)	With respect to C, what is <i>'input buffering'&nbsp;</i>and why is it a thing?	Input buffering refers to the temporary storage of user input in a buffer (in memory) before it is processed by the program.<br><br>- Buffering is used to improve efficiency by reducing system calls, as it allows the program to batch data before it is sent to the program<br><br>- Without buffering, you would not be able to edit input before submitting as it would be immediately send to the program	C defn keyword
ILjpm|Tn;G	What is <i>'function definition' </i>with respect to C?	A function definition is the part of a program where the actual code/body of the function is written. This tells the compiler what the function does, and also relates the body of a function to its name/identifier.	C defn keyword
Nn|H>GBD[^	What is <i>'file scope'</i> with respect to C?	File scope refers to when a variable or function is accessible anywhere in the program from its point of declaration to end of file. <br><br>Functions always have file scope, while variables have file scope only if they are declared outside of any function	C defn keyword
wnZ%OnvW1o	What is the type signature of foldr1?	foldr1 :: (a -&gt; a -&gt; a) -&gt; [a] -&gt; a<br><br>i.e <br>takes two parameters:<br>- a binary function that works on type a and returns type a<br>- a list with contents of type a<br>returns:<br>- something of type a	functionDef Haskell
nP6bf;CtVh	Provide a recursive definition (w/o type signature) of <i>foldr1</i>	foldr1 f [x] = x&nbsp;<br>foldr1 f (x:xs) = f x (foldr1 f xs)&nbsp;<br><br>Remember:<br>foldr1 is just foldr (right to left processing), that uses the last element of the list as an implicit initial accumulator, where as foldr uses an explicit one passed in via a parameter<br>This also means that foldr1 only works on on non-empty list inputs and will throw an error if it is empty	functionDef Haskell
IP+8_pILOM	What is<i> 'point-free'</i> or <i>'point-less'</i> style in Haskell?	A function definition is point‑free if you never name its arguments (the “points”) and instead build the function solely by composing other functions<br><br>For example,<br>This is point free:<br>allLow :: String -&gt; Bool<br>allLow = foldr1 (&amp;&amp;) . map isLower<br><br>This isnt<br>allLow cs = foldr1 (&amp;&amp;) . map isLower cs&nbsp; -- cs is mentioned so not point free	defn Haskell keyword
mgt]*t.T^I	What does it mean for a language to be <i>'statically typed'</i>? is C statically typed? Haskell?	A language is statically typed if the type of every variable and expression is known at compile time. This means the compiler checks types before the program runs.<br><br>C - Yes, compiler knows because we do explicit type declarations<br>Haskell - Yes, compiler knows due to its type inference system (can also know thru explicit declarations)	C defn Haskell keyword
sZMk)5J@zy	What does it mean for a language to be <i>'strongly typed'</i>?	"A strongly typed language is a language with strict type rules regarding type conversions and interactions between different types.<br><br>- Not complying with these rules = error&nbsp;<br><br>""All type errors are found at compile time in Haskell, so it is strongly typed"" - Graham Hutton (not 100% sure if this is a correct defn)"	defn Haskell keyword
B2;m:Licrg	What does it mean for a function to be <i>'higher order'</i>?	A function is called&nbsp;<i>higher order</i>&nbsp;if it takes a function as an argument or returns a function as a result	defn Haskell keyword
Kl.$f]`Bdu	What is <i>'functional programming</i>'? What is meant by a 'functional language'?	Generally speaking<br><b>Functional programming</b> is a <i>style </i>of programming in which the basic method of computation is applying functions to arguments<br><br>A <b>functional language&nbsp;</b>is one that&nbsp;<i>supports</i>&nbsp;and&nbsp;<i>encourages</i>&nbsp;the functional style of programming.	defn Haskell keyword
lLWmQWWI,!	Define <i>'Pattern matching'</i> with respect to Haskell	Pattern matching is a way to deconstruct data structures by checking a value against a pattern i.e (x:xs) for lists. <br>It also allows functions to be defined by cases, based on the shape of an input.&nbsp;<br>i.e<br>f (a,b) = ...<br>f [a] = ...&nbsp;<br>So f will act differently (potentially) on lists and tuples	defn Haskell keyword
H;L%y,w/(~	Provide an overview of how memory is structured in a C program.	"Top to bottom:<br><u>Stack</u><b>&nbsp;</b>- Stores stack frames used by functions for storage of local and parameter variables.&nbsp;<br><u>Free space</u> - Shrinks and grows as more memory is used/frees. If this runs out, program fails<br><u>Heap</u> - Used for storing dynamically allocated data. Accessed by pointers/<br><u>Globals</u> - Stores static variables as well as variables declared outside of functions - have a lifetime that is the same as the program.<br><u>Constants</u> - Stores initialised, read-only data such as literal values<br><u>Code</u> - Can be read-only, or stored in flash memory.<br><br><img src=""paste-c1133a84cb87e306bde9454bff3017589e3d557c.jpg"">&nbsp;"	C
-s4Iyz?2/	<div>How are local and parameter variables are <i>allocated </i>in</div><div>memory when a function is called</div>	"<table><tbody><tr><th><div><span style=""font-weight: 400;"">-&nbsp; When a function is called, it is allocated a stack frame, which is a temporary data store for all that is needed for the function to execute.&nbsp;</span></div><div><span style=""font-weight: 400;""><br></span></div><div><span style=""font-weight: 400;"">- Parameter variables are allocated space on the stack frame as soon as the function is called, and local variables are allocated space as soon as they are defined.</span></div></th></tr></tbody></table><br>"	C
NT^|inP.Pw	What is the meaning of <i>'scope'&nbsp;</i>with respect to C?&nbsp;	Scope of a variable/identifier refers to which area of program it is valid and can be referenced<br><br>i.e from point of declaration/definition to end of scope<br><br>e.g. if a variable is defined within a block of code like a for loop, it will be have local scope to that block of code and will be inaccessible outside it	C defn keyword
b<SdL9SbWX	What is the meaning of '<i>file scope'</i>&nbsp;with respect to C?&nbsp;	File scope refers to when a variable/function identifier is accessible in any part of the program from when it is declared to end of file<br><br>Functions always have file scope, variables have file scope if they are declared outside of any specific block of code i.e outside of any functions	C keyword
NUA5bpKy{K	What is the meaning of '<i>lifetime'</i>&nbsp;with respect to C?&nbsp;	Lifetime refers to the time period for which a variable or object exists within memory/is valid.&nbsp;	C defn keyword
L0_zvbxxw+	What is the meaning of <i>'pointer arithmetic'</i> with respect to C?	The user of arithmetic on pointers; i.e allowing the code to acceses locations in memory relative to another known location<br><br>for example *(ptr + 5)	C defn keyword
NGbuSgI%KH	What is the meaning of <i>'dereferencing'</i> with respect to C?	Dereferencing refers to retrieve a value stored at the address of a pointer using the '*' operator	C defn keyword
"JY9Qv#*zK("	What is the meaning of 'function composition' with respect to Haskell? What is the syntax for explicit function composition?	Refers to using one function's return value as the argument of another<br><br>Syntax: f . g x&nbsp;<br>which is equivalent to f . (g x)<br>(the operator is the dot '.')	defn Haskell keyword
"zR#)fsP/x7"	What is the meaning of a&nbsp;<i>'basic type'</i>&nbsp;in Haskell?	A basic typeis one defined in the Prelude package	defn Haskell keyword
c+(g:tfY&;	What is the definition&nbsp;<i>'Tail recursion'?&nbsp;</i>&nbsp;Provide an example of a tail-recusrive Prelude function	A recursion is <i>tail‑recursive</i> when the recursive call is the function’s very last action, with no work (no arithmetic or combining of results) done after it returns.<br><br>Any “remaining work” is carried forward through extra parameters (called <i>accumulators</i>)<br><br>Foldl is an example of one, but&nbsp;<i>not</i>&nbsp;foldr&nbsp;	defn Haskell keyword
nlU.)dyNRY	What are two advantages of <i>currying </i>(function currying)	- Allows us to use/create partially applied functions, which are useful to use as parameters to higher-order functions<br>- Allows the creation of lambda/anonymous functions	Haskell
"g&$*|bw#UY"	"Provide&nbsp;<i>three</i>&nbsp;examples of pattern matching ""patterns"" you can use in Haskell"	List pattern: i.e f (x:xs)<br>Tuple pattern: i.e f (a,b)<br>Wildcard pattern: i.e f _ (basically means 'anything else')	Haskell
e%@s>C+B*H	Explain what a <i>polymorphic type</i> is	Types that contain one or more type variables (e.g. <i>a, b</i>), allowing a single function or data type to operate the same way over values of many different types<br>- Allows super generalised functions	defn Haskell keyword
"C_eS#xuJ*."	Define&nbsp;<i>'lazy evaluation' </i>with respect to&nbsp;<b>Haskell</b> and provide an example of when it is used	"Lazy evaluation refers to the fact that Haskell only evaluates/computes expressions when their results are needed for a given operation<br><br>- For example, in the case of operations involving infinite lists i.e ""take 10 [1..]"", Haskell will not attempt to generate the entire list but will instead only generate what is needed i.e the first 10 elements"	defn Haskell keyword
ulsix?@|-~	Provide the type signature of foldr, and explain in detail why it is the way it is	foldr :: (a -&gt; b -&gt; b) -&gt; b -&gt; [a] -&gt; b<br>foldr takes as parameters a function, accumulator value (base case) and a list to work on.<br><br>The reason the type signature involves two potentially different types (a, b) is because the value foldr is building up (the accumulator/result) can be a different type from the elements of the list, because we can provide any accumulator we wish.&nbsp;<br><br>If we only used 'a', we would be forced to have the result of foldr be the same as the type of the list which is not always waht we want - i.e we might want to fold an [Int] into a Bool<div></div>	defn Haskell
A}h[x._lCj	Define the term&nbsp;<i>'Imperative Programming'</i>	Imperative programming is a programming paradigm where programming is essentially specifying a sequence/series of statements/commands we want the computer to do in order to achieve a designed outcome<br><br>- Makes use of mutable memory to store information; a compuation transofrms the initial state of memory to a final one	C defn keyword
FRA&ey!)X}	Define what is meant by a&nbsp;<i>'Recursive function'.&nbsp;</i>Provide 2 advantages of using recursion	A <em>recursive function</em> is one that calls itself within its own definition to solve a problem by breaking it into smaller subproblems. Always has a recursive and base case.<br><br>One advantage of recursive functions over non-recursive ones is it can often lead to elegantly written code<br><br>Another advantage of recursive functions is that it allows us to reason on the behaviour of a program using mathematical principles such as induction, because recursion aligns with the idea of induction.	C defn
l]32TAW?&w	Explain the meaning of '<i>Overflow</i>' and when it occurs	"Overflow occurs when an arithmetic operation produces a result that exceeds the range that can be represented with a given data type. i.e we assign a variable a value that it cannot hold<br><br>For example, if integers are 32 bits, and we assign to it a value that exceeds what 32 bits can represent, then overflow will occur.<br><br>EXTRA:<br>- The outcome of overflow depends, sometimes it leads to a runtime exception, sometimes the number ""wraps around"" using modular arithmetic which can produce unexpected results"	C defn Haskell keyword
FgZe|E.ajX	Explain what a&nbsp;<i>'curried function'</i>&nbsp;is in Haskell	A curried function in Haskell is one that, despite appearing to take multiple arguments, is internally represented as a sequence of single‑argument functions.<br><br>- Each function in the sequence consumes one argument and returns another function until all arguments have been supplied, at which point a final result is produced	C defn Haskell
B^]OrPR+Yr	Provide a brief explanation of what&nbsp;<i>'Do'</i>&nbsp;is in Haskell	Do is essentially syntactic sugar for the Monad typeclass, which allows monadic code (sequences of computations) to be written in an imperative style	defn Haskell
uulD0tuPu1	Explain what a&nbsp;<i>'Guarded function'</i>&nbsp;is in Haskell	"A guarded function is one that makes use of guards, allowing the function to operate under different conditions<br>- Similar to piecewise functions in mathematics<br>- Guards are syntactic sugar for a long series of ""if else then"" statements"	defn Haskell
g2*3vx%;^&	State the result of: 1.0 / 3 + (-1) in Haskell, and whether or not an error would occur	-0.66666666...<br>integer 3 promoted to double <br>1.0/3.0 = 0.33333...<br>then 0.3333... -1 <br>= -0.6666...	Expression Haskell
hAb:{,@2%m	"State the result of: ""Abe"" &lt; ""Abbey"" in Haskell, and whether or not an error would occur"	"False<br>Haskell compares strings via <i>lexicographical</i> order, like a dictionary.&nbsp;<br>Third letter of ""Abe"" is 'e', which is greater than 'b', the third letter of ""Abbey"""	Expression Haskell
E_i-FkT=0F	State the result of: 12 == 12.0000000000000001, and whether or not an error would occur	True<br>Haskell doubles have roughly 15 decimal digits of precision. This number will get rounded down to the nearest double value that can be represented, which is 12, hence it evaluates to true because it compares 12 with 12.0	Expression Haskell
N;uZLBT2ko	State the result of: 7 * -4 in Haskell, and whether or not an error would occur	"Leads to an error. <br>'-' here is interpreted as a function rather than part of a literal ""-4"".<br>To remove error, need to do 7 * (-4)"	Expression Haskell
v{)YLN[.1K	What is the <i>'pre-processor'&nbsp;</i>with respect to C?	"<div>The preprocessor is the very first program invoked during the compilation process, which gets source files ready for compilation It:</div><div>- Handles #include directives by inserting header file content</div><div>- Processes #define macros (i.e simple text substitutions</div><div>- Evaluates conditional‐compilation directives (i.e #if, #ifdef, #ifndef</div><div>- Strips comments</div>
<div><br></div>"	C defn keyword
Mo7DM~Z(:8	What is an <i>'Argument'</i>? a <i>'Parameter'</i>? How are they different to one another?	An argument is a value passed to a function in a function call<br><br>A parameter is a variable defined in the function definition, that is initialised by an argument variable<br><br>i.e <br>def function(int a) - 'a' here is a&nbsp;<i>parameter</i>&nbsp;<br>function(78) - '78' here is an&nbsp;<i>argumetn</i><br>	C defn keyword
Jau0Q-=7.o	What are the ASCII values of '0', 'A', and 'a'?	0 = 48<br>A = 65<br>a = 97	C
ml!x<w|EgU	What is an <i>'escape character'?</i>	"An escape character is a special symbol (usually '\') that has the meaning of ""what follows should be interpreted differently""<br><br>For example, using the escape character with n gives '\n', which is the new line character. The reason it does not get interpreted as the character 'n' is because of the escape character"	C defn
ILEh+W)EXk	what does <i>'strcpy'&nbsp;</i>do?&nbsp;	strcpy(char* dest, char* str) copies the string 'str' into 'dest', including the null terminator '\0'<br><br>- Relies on source string having a null terminator '\0' at the end, otherwise the function might corrupt memory&nbsp;	C defn functionDef
p]esx=PEhP	what does&nbsp;<i>'strcat'&nbsp;</i>do?&nbsp;	strcat(char* str1, char* str2) appends a copy of str2 to the end of str1, overwriting '\0'<br>- Relies on str1 having a null terminator '\0' at the end, otherwise it wont know where to append to and may corrupt memory<br>	C functionDef
oc>ftT~VsC	what does&nbsp;<i>'strchr'&nbsp;</i>do?&nbsp;	strchr (char* str, char c) returns the the index posiiton of the first occurence of the character in the string, or null if not found&nbsp;<br><br>- Relies str having a null terminator '\0' at the end, otherwise it wont know when the end of the string is and will continue looking out of bounds	C functionDef
Igr5~^<ZeS	what does&nbsp;<i>'strcmp&nbsp;</i>do?&nbsp;	strcmp(char* str1, char* str2) compares two strings element-wise until it finds a difference<br><br>str1 &gt; str2 - returns positive int<br>str1 == str2 - returns zero<br>str 1 &lt; str2 - returns negative int<br><br>characters are compared using ASCII values, so 'A' &lt; 'a', because 65 &lt; 97	C functionDef
o@W{8qP^T)	Provide a definition of the term&nbsp;<i>'Abstraction'</i>	"Abstraction is the process of removing unecessary or irreleveant detail of something while including the more important ones, in order to make that 'something' easier to use (in the event of an API for example) or easier to solve (in the case of a problem)<br><br>""Removing distinctions to emphasise commonality"""	defn keyword
Poq&uZ/5?&	What does defining a variable do? How does this differ from declaring a variable?	A definition introduces a&nbsp;<i>name, type&nbsp;</i><b>and</b>&nbsp;it allocates memory.<br>A declaration is the same, but it does&nbsp;<b>not</b>&nbsp;allocate memory. Just gives a name and type<br><br>int a; - this is a DEFINITION and it allocates enough memory for an integer<br>extern int a; - this is a DECLARATION and it does not allocate memory	C defn
rus.ir7>i&	"What happens when a variable goes <i>""out of scope""</i>?"	When a variable goes out of scope, the memory that was given to it is&nbsp;<i>deallocated,&nbsp;</i>but the bytes are <b>not</b>&nbsp;<i>zeroed</i><br><br>The memory is not destroyed or removed - this means that if a different variable happens to be allocated that memory later on, it will take on the value of that old variable.&nbsp;<br><br>	C defn Haskell
A0V?{{q?,7	Explain in what order the different parts of a for loop are evaluated i.e for (int i = 0 ; i &lt; 10 ; i++)	The <i>first </i>part of the for loop (int i = 0) is evaluated&nbsp;<i>once</i>&nbsp;before the start of the loop<br>The <i>conditional</i> part of the for loop (i &lt; 10) is evaluated&nbsp;<i>before</i>&nbsp;each evaluation of the loop body<br>The <i>third</i> part of the for loop (i++) is evaluated&nbsp;<i>after</i>&nbsp;each evaluation of the loop body	C
g&JbBS8^oO	&nbsp;What is&nbsp;<i>'size_t'</i>&nbsp;in C?	"size_t is an unsigned integer ""type"" (its not really a type but a typedef'd alias for unsigned long)<br>It can be used to represent value sizes in&nbsp;<i>bytes</i><br><br>Format specifier for printing - '%zu"""	C defn
gt41A!V.o1	How does&nbsp;<i>'scanf'</i>&nbsp;work in C? is scanf considered safe?	"- scanf reads formatted input from stdin and stores what it gets into memory specified by pointer arguments. For strings, automatically adds a '\0'<br>- if it fails to read or input doesnt match format, it will leave whatever input was typed in inside of stdin, so it will need to be flushed by other code<br><br>scanf(char* format, ...) where ""..."" represents 1 or more pointers passed as arguments<br><br><b>NOT SAFE&nbsp;</b>- if scanf is NOT used with a width specifier i.e '%50s', it will read 50 characters which may overwrite/corrupt memory if the pointer it is supplied is only meant for say 20 characters<br><br>i.e int n = scanf(""%10s, %5s"", str1, str2);<br>- Reads ≤10 chars into str1, matches “, ”, then ≤5 chars into str2.<br>- If &gt;10 chars appear before “, ”, scanf stops at 10, fails to match “, ”, and returns 1<br><br><br>"	C defn functionDef
P@Dt{x2`t3	How does <i>'fgets'&nbsp;</i>work?	fgets(char* str, int n, FILE *stream) is a function that can be used to read characters from a specified stream (i.e stdin), saving what is read to a string<br><br>Takes as parameters:<br>- The target destination to which it will write what is pulled from the stream<br>- The size of the input buffer to use (accounting for '\0', n = 10 will make it read n-1 characters from stream)<br>- The stream to read from<br><br>Any leftover data that is not read is kept in the stream<br>	C defn functionDef
Obm@d/gztR	What does&nbsp;<i>'sizeof'&nbsp;</i>do? what is the return type?	sizeof(Expression) where 'Expression' can be a data type or variable of any type<br><br>Returns a size_t (size in bytes) value representing the size of a type or variable. <br>sizeof(int) returns the number of bytes the compiler uses to store an int<br><br>NOTE: if you create an array of size 30, i.e char arr[30] (30 bytes), then pass it into a function and attempt to call sizeof on it, you will&nbsp;<b>NOT</b>&nbsp;get the amount of memory taken up by the array itself, but the size of the pointer used to reference it (because arrays get decayed into pointers), so sizeof will return 8 bytes instead of the expected 30	C defn
uo1A5$V39,	What is the difference between a&nbsp;<i>'function'&nbsp;</i>and a&nbsp;<i>'procedure'</i>?	"A function returns some value, a procedure does not <br>- Therefore a ""function"" with return type void is technically a procedure"	C defn keyword
p1]0z<N]+q	What are the 3 main default file streams in C?	<u>stdin </u>(standard input) - connected by default to keyboard<br><u>stdout</u> (standard output) - connected by default to terminal<br><u>stderr</u> (standard error) - connected by default to error output in terminal<br>	C defn
EuV<Z)M*m)	What is the&nbsp;<i>'EOF'</i>&nbsp;macro in C?	EOF is a constant macro defined in stdlib that is used to denote the end of a file (useful for file handling)<br><br>So you can do something like<br>while ((ch = getchar()) != '\n' &amp;&amp; ch != EOF);	C defn
GJi^h,q>sk	Explain what this syntax does: '&lt;', in the context of C I/O redirection	This syntax is used to redirect the stdin stream from the file it is connected to by default<br><br>'&lt;' i.e a.out &lt; inputFile - makes it so that stdin is defaulted to 'inputFile'. That is, the program starts with the contents of inputFile in stdin<br>	C defn
"u$|D,#YH?8"	Explain what this syntax does: '&gt;', in the context of C I/O redirection	This syntax is used to redirect the stdoutstream from the file it is connected to by default<br><br>'&gt;' i.e a.out &gt; outputFile - makes it so that stdout is defaulted to 'outputFile'. Will overwrite existing file contents with program output<br><br>	C defn
"oXP/#DD{:["	Explain what this syntax does: '&gt;&gt;', in the context of C I/O redirection	This syntax is used to redirect the stdin stream from the file it is connected to by default<br><br>'&gt;&gt;' i.e a.out &gt;&gt; outputFile - makes it so that stdout is defaulted to 'outputFile'. Will <i>append </i>program output to existing file contents&nbsp;<br>	C defn
xP8H`Cu*8l	Explain what this syntax does: '|', in the contect of C I/O redirection	This syntax is used to <i>pipe </i>the stdout of one program into the stdin of another<br><br>'|' i.e a.out | otherProgram- sends stdout through a pipe to the stdin of 'smth'<br><br>This is an example if&nbsp;<i>pipelining</i>	C defn
g1@-X}a|(w	Explain what the following syntax does: '2&gt;' and '2&gt;&gt;', in the contect of C I/O redirection	This syntax is used to redirect the stderr stream from the file it is connected to by default<br><br>'2&gt;' i.e 2&gt; outputFile - <i>overwrites </i>contents of outputFile with stderr i.e basically makes an error log<br>'2&gt;&gt;' i.e 2&gt;&gt; outputFile - same as above but&nbsp;<i>appends</i>&nbsp;instead of&nbsp;<i>overwriting</i>	C defn
lUX1jZF}Y,	What is the meaning of '<i>local scope'</i>&nbsp;with respect to C?&nbsp;	<div>A variable has local (aka 'block') scope if it is declared inside a pair of braces {... } - for example, within a function body, a loop, or an 'if'&nbsp;statement. Such a variable:</div><div>- Exists <strong>only</strong> from its point of declaration to the matching closing brace.</div><div>- Is <b>inaccessible/</b>not&nbsp;<b>valid</b> outside that block.</div><div>- Is created anew each time the block is entered (for loops, function calls, etc.)</div>	C defn keyword
D3bG%_=?+$	What is a '<i>compound statement'</i>&nbsp;with respect to C?	compound statement (also called a <em>block</em>) is a sequence of zero or more declarations and/or statements enclosed in curly braces	C defn keyword
kyT5[DgCp)	What does it mean for an algorithm to be&nbsp;<i>'in-place'</i>?	An algorithm is called <i>in-place</i> if it uses &lt;= c log(n) memory	defn
"p?.om1#T`Y"	What does it mean for a sorting algorithm to be <i>'stable'</i>?	Stability means that an algorithm maintains the relative order of equal elements in an input array while/after sorting<br><br>i.e&nbsp;An algorithm is said to be <i>stable</i>&nbsp;if two items with equal keys appear in the same order in the sorted output as they appear in the input array<br>	defn
C99JIJkRpB	What are the worst, average and best case costs for <i>selection sort</i>?	Worst: ~N<sup>2</sup><br>Average: ~N<sup>2</sup>&nbsp;<br>Best: ~N<sup>2</sup>	cost
rhAt}PXDvz	Is <i>selection sort</i>&nbsp;stable? In-place?	In-place: Yes<br>Stable: No	heapsort trait
rc-bG%mlvm	What are the worst, average and best case costs for&nbsp;<i>insertion sort</i>?	Worst: ~N<sup>2</sup>&nbsp; &nbsp; &nbsp;(also Big Theta(N<sup>2</sup>)<br>Average: ~N<sup>2</sup>&nbsp;(also Big Theta(N<sup>2</sup>)<br>Best: ~N&nbsp; &nbsp; &nbsp; &nbsp; &nbsp;(also Big Theta(N)<br>	cost
La.6BJfUsr	Is&nbsp;<i>insertion sort</i>&nbsp;stable?&nbsp;In-place?	In-place: Yes<br>Stable: Yes<br><br>It is the&nbsp;<i>only</i>&nbsp;sorting algorithm (that we learn) that is both stable and in-place	trait
lt<V~T4}vZ	What are the worst, average and best case costs for&nbsp;<i>merge sort</i>?	Worst: ~N lg N<br>Average: ~N lg N<br>Best: ~N lg N<br><br>Note: uses O(N) memory	cost
k(%r2?z$4K	What are the worst, average and best case costs for&nbsp;<i>quick sort</i>?	Worst: N<sup>2</sup><br>Average: ~N lg N<br>Best: ~N lg N<br><br>In practise it is the fastest sorting algorithm in the <i>average </i>case	cost quicksort
x,>_DVM5Ao	What are the worst, average and best case costs for&nbsp;<i>heap sort</i>?	Worst: ~N lg N<br>Average: ~N lg N<br>Best: ~N lg N<br>Overall: Theta(NlogN)<br>unstable and In-place counterpart to merge sort (which is stable but not in-place)	cost
O(+uIGv<U)	Explain what is a&nbsp;<i>'type class'</i> and how is it used	A typeclass is a way of grouping types that support a common set of operations (methods or functions).<br>- It lets you write generic&nbsp;<i>polymorphic</i>&nbsp;functions that work over any type that implements the required interface	defn Haskell
dK<s]j8rWa	"Show how square bracket notation is translated to
pointer notation when an arary element is <i>accessed </i>or <i>assigned </i>to"	Given an array: arr[],<br>arr[index] is the same as *(arr + index)<br>arr[index] = 5 is the same as *(arr + index) = 5	C
zc&=TRIG;m	What is a&nbsp;<i>'virtual address space'&nbsp;</i>with respect to C?	The contiguous range of memory addresses that a C program 'sees' and can use.<br><br>It is an isolated, private block of addresses allocated by its process, which the OS and MMU (memory management unit) map onto physical memory (RAM)	C defn keyword
j~S_8]u2$j	What is a&nbsp;<i>'process'&nbsp;</i>in an operating system? What does it do?	What:<br>A process is the created by the OS as an abstraction for a program that is running. Each process is allocated processor time, and each process is&nbsp;<i>strictly isolated</i>&nbsp;from all other processes<br><br>Do:<br>- Defines a virtual address space for a program<br>- Manages resources needed by the program	C defn keyword
PsBm=fr{m=	How does the OS ensure one process cannot access another’s memory?	By using virtual memory: each process gets its own isolated address space that the hardware (MMU) enforces	C
gNnK:R+5JY	Name three benefits of virtual memory	1) Process isolation (safety), <br>2) Ability to run programs larger than RAM via swapping<br>3) Simplified programming model (each process sees its own flat contiguous address space)	C
Q5yjI6:GUY	What is a <em>PID?</em>	Process identifier - unique integer given to each process by the OS for tracking and control	C defn
n&gxC=/{]3	Define what a&nbsp;<i>'pointer'</i>&nbsp;is with respect to C. What does it hold?	A pointer is a virtual memory address (not physical memory)<br>- Holds the address of the&nbsp;<i>first</i>&nbsp;bye of a value within memory	C defn keyword
"iKUS}#S:Mo"	Are pointers and arrays the same thing in C?	"<b>No.</b> They are distinct data types. An <i>array </i>is a fixed‐size, contiguous block of memory, while a <i>pointer </i>is a variable that holds a virtual memory address<br><br>Arrays <i>decay </i>to pointers in most expressions, but they are <b>not </b>pointers themselves and cannot be reassigned the way pointer variables can<br>i.e<br>char arr[]&nbsp; = ""hello""<br>arr = ""hi"" is NOT allowed - cannot reassign array once defined"	C defn
HiC>?H~*)k	What is <i>'array decay'</i> in C? Why does it occur?	Array decay is the the implicit conversion where, in most expressions, an array’s name becomes a pointer to its first element<br><br>It is needed because C does not support passing arrays by value (would be memory inefficient), so instead it lets you refer to its elements relative to the the memory location of the first	C defn keyword
JKYuyIs*MM	How do you access struct fields when the identifier is a pointer? when it's not?	Given a struct with identifier 's'<br>If <i>pointer</i>, use '-&gt;' syntax:<br>s -&gt; field1<br>if <i>not</i>, use '.' syntax:<br>s.field1	C
iMoFl:vD(3	What does <i>'typedef'</i>&nbsp;do in C, and how do you use it?	"Creates an alias for an existing type, making complex types easier to read and write<br><br>format: typedef existing_type new_name;<br><br>with struct:<br>typedef struct Point {<br>&nbsp;&nbsp;&nbsp; int x, y;<br>} Point;&nbsp; // now we can write ""Point a.."" instead of ""struct Point a.."""	C defn keyword
F-K-;*7g]s	What lives in the <i>Code </i>segment of program's virtual address space?	The compiled machine instructions of the program	C defn
gXJl)s]f~:	What lives in the <i>Constants</i> segment of a C program's virtual address space?<br>	"Literal values (e.g. 2, ""abc""), typically read-only, possibly stored in flash/ROM on embedded systems"	C defn
"hU#=PsHug&"	What lives in the <i>Globals </i>segment of a C program's virtual address space?	Static variables and variables declared outside any function - they exist for the program’s entire lifetime (same lifetime as program)	C defn
r`.f;v+q6*	What lives in the <i>Heap&nbsp;</i>segment of a C program's virtual address space?	Dynamically allocated memory (via malloc, calloc), accessed by pointers and manually managed by the programmer (via free).	C defn
mgvCzf*}a{	What lives in the <i>Stack </i>segment of a C program's virtual address space?	Local and function-parameter variables; their lifetime is limited to the scope of the function call. Stack frame created for each function call and placed on stack	C defn
hYdR3L/[I7	What is <i>'Free Space'</i>&nbsp;with respect to a C program's virtual address space layout?	Unused virtual memory between heap and stack; it shrinks as the heap grows or the stack grows. Need free space because we dont know how much memory will be needed beforehand	C defn keyword
"hufPgZ2O#T"	What is the lifetime of <i>file scope</i>, <i>local scope</i> and <i>dynamically allocated</i> variables?	Lifetime is determined by scope:<br>- File scope variables have a lifetime that is the same as the program; memory is allocated once when program starts and deallocated when ends<br>- Local scope variables have a lifetime that depends on the block of code they are local to; memory allocated when local scope is entered, deallocatted when exited<br>- Dynamically allocated variables have a lifetime that depends on when the variables are free'd by the user	C
HqjXhkISR/	What are&nbsp;<i>'argv'</i>&nbsp;and&nbsp;<i>'argc'</i>&nbsp;with respect to C? What do they contain?	They are (potential) parameters of the main function that the OS/runtime passes in when aprogram starts<br><br>'argc' -&nbsp;<u>arg</u>ument <u>c</u>ount&nbsp;is an <b>int </b>representing the number of CLA's (command like arguments)<br><br>'argv' - <u>arg</u>ument <u>v</u>ector): an <b>array of char*</b> pointers where each element points to a (null terminated) string representing the i-th CLA. argv[0] will always be the program name by convention	C defn
lrtX2.rg_.	What does malloc do in C? How do you use it?	Used for dynamic memory allocation<br>Allocates a specified number of bytes on the heap and returns a pointer to the first byte of that block<br><br>For example, for 100 ints we do: malloc(sizeof(int) * 100)<br>NOTE:<br>- do NOT need cast the pointer that is returned&nbsp;<br>- Need to manually free memory that is allocated using free()	C functionDef
ERv%tt~O17	What does calloc do in C? How do you use it?	Used for dynamic memory allocation<br>Allocates a specified number of <i>zero'd out</i> bytes on the heap and returns a pointer to the first byte of that block<br>- Takes as parameters number of elements and size of each element<br><br>For example, for 100 ints we do: calloc(100, sizeof(int))<br><br>- do NOT need cast the pointer that is returned&nbsp;<br>- Need to manually free memory that is allocated using free()	C functionDef
o6rG>cP(gL	What does realloc&nbsp;do in C, and how do you use it?	Resizes a previously allocated memory block to a new size<br>Takes as parameters a pointer to the first byte of the block and the new size needed<br>- Automatically frees old block and copies over memory to new location (if needed)<br><br>NOTE: Always assign assign realloc's&nbsp;return to a temporary pointer to avoid losing the original block on failure, or pointer to that memory might forever be lost, leading to memory leak	C functionDef
Pjr;KvEsJm	What is an&nbsp;<i>'Object file'</i>&nbsp;with respect to C?	The result of compiling <strong>one</strong> C source file (a translation unit) into machine code and metadata, but <strong>before</strong> linking<br>- Contains machine (binary) code that is&nbsp;<i>device specific</i>	C defn
DZjy)c|Tpl	What is a <i>'segmentation fault'</i> in C, and what common mistakes cause it?	A runtime error where a program tries to access memory it isn't allowed to (not allocated/deallocated), causing the OS to terminate it<br><br>COMMON CAUSES:<br>- Dereferencing a NULL pointer<br>- Attempting to use free'd memory<br>- Accessing memory out of array bounds<br>- Stack overflow	C defn keyword
DPK?Ud|;,/	what does&nbsp;<i>'strstr'&nbsp;</i>do? (C function)	strstr(char* string, char* substring)<br><br>Attempts to locate a substring within another string, returns:<br>- 0 if no match<br>- 1 if substring found	C defn functionDef
izU+DI^c0_	"What is&nbsp;<i>'#include'&nbsp;</i>and what does it do?"	It is a <i>preprocessor directive</i> that inserts the contents of the specified file into the source text	C defn keyword
h^Zfz`=p9$	What is a&nbsp;<i>'translation unit'</i>&nbsp;with respect to C?	A translation unit is the output of the C preprocessor for one source file. A translation unit is then converted into an object file by the compiler	C defn keyword
d+!hd&(x4^	"What is&nbsp;<i>'#define'&nbsp;</i>and what does it do?"	"It is a <i>preprocessor directive</i>&nbsp;that tells the preprocessor to subtitute all occurences of something with something else&nbsp;<br>i.e #define PI 3.14<br><br>will substitute all instances of 'PI' with 3.14"	C defn keyword
Ij5))`IE<{	Provide a recursive definition (w/o type signature) of foldr	foldr _ acc [] = acc<br>foldr f acc (x:xs) = f x (foldr f acc xs)	functionDef Haskell
O/tV3hQKYl	Explain the meaning of the term&nbsp;<i>'list comprehension'</i>&nbsp;with respect to Haskell	List comprehension refers to haskell's compact syntax for building lists by specifying an output expression as well as 1 or more&nbsp;<i>qualifiers.&nbsp;</i>Very similar to set builder notation in maths<br><i>Qualifiers</i>&nbsp;can be generators, guards/filters or let-bindings. For example:<br><br>Generator: x &lt;- [1..100]<br>Guard: x &gt; 10<br>let-binding: let y = f x<br><br>NOTE: Qualifiers are evaluated left to right, [ x | even x, x &lt;- [1..10] ] would lead to an error because the x is not yet assigned a value when 'even' is called	defn Haskell keyword
BENd:P=4=Y	Define the term <i>'Monadic Programming'</i>&nbsp;with respect to Haskell	Monadic programming is a style of programming that uses monads, which are type constructors that pair values with extra context (such as failure, state, or I/O) - together with two core operations that obey the monad laws:&nbsp;<br>- return<br>- bind/sequence (&gt;&gt;=, has syntactic sugar&nbsp;<i>&lt;-</i>).<br>Monads provide a succinct way to sequence functions while abstracting side-effects, all while maintaining the purity of the functions.	defn Haskell keyword
iaTYpT0?Wl	What does the function application function, '$' do in Haskell? Briefly explain what it does and provide an example of its usage	Type signature: ($) :: (a -&gt; b) -&gt; a -&gt; b<br><br>In Haskell, normal function application has really high precedence, so f a b c is the same thing as saying (((f a) b) c)<br>$ on the otherhand has very low precedence, so writing f $ a b c is the same as saying f (a b c).<br><br>Note: $ is right associative. Using a '$' is the same as enclosing everything to the right in a pair of brackets, kinda like '.' in theory of comp.<br><br>Another example: f(g (zx)) = f $ g $ z x	functionDef Haskell
D0b/A`<WX!	Provide the type signature and definition of the '$' (<i>function application</i>) function in Haskell	($) :: (a- -&gt; b) -&gt; a -&gt; b<br>f $ x = f x<br><br>Takes a function and an expression, then applies the function to that expression	functionDef Haskell
"OKc#yojx.{"	Provide the type signature and definition of the '.' (<i>function composition</i>) function in Haskell	(.) :: (b -&gt; c) -&gt; (a-&gt; b) -&gt; a -&gt; c<br>f . g = \x -&gt; f (g x)<br><br>Note: g has to go from a to b, because f goes from b to c. g(x) is the input to f so the output type of g must be the same as the input type of f	functionDef Haskell
zhRF;^Oukz	What is the type signature and definition of the library function&nbsp;<i>elem</i>	<div>elem :: (Eq a) =&gt; a -&gt; [a] -&gt; Bool</div><div>elem _ [] = False<br></div><div><div>elem n (x : xs)</div><div>&nbsp; | n == x = True</div><div>&nbsp; | otherwise = elem n xs</div></div>	functionDef Haskell
D0rY.s0L!o	What is the type signature and definition of the library function&nbsp;<i>Length</i>	<div><div>Length :: [a] -&gt; Int</div><div>Length [] = 0</div><div>Length (x : xs) = 1 + myLength xs</div></div>	functionDef Haskell
"zF(U#Rne~K"	What does the library function <i>fromIntegral </i>do in Haskell? When should it be used?	It converts a value of any&nbsp;<i>Integral</i>&nbsp;type (e.g.&nbsp;<i>Int&nbsp;</i>or&nbsp;<i>Integer</i>) into any desired type that’s an instance of the&nbsp;<i>Num&nbsp;</i>class.<br><br>Used whenever we have an <i>Int&nbsp;</i>or <i>Integer</i>&nbsp;but need it in a context that requires a different <i>Num</i>&nbsp;type (e.g. comparing to a <i>Double</i>, or dividing to get a fractional result)<br><br>i.e if we have 'a &gt; b' where 'a' is an <i>Int </i>and 'b' is a <i>Float</i>, we would actually need to do 'fromIntegral a &gt; b' because just 'a &gt; b' would throw an error. fromIntegral converts 'a' into a <i>Float </i>so that the comparison can happen. ('&gt;' requires that both operants have the same time)&nbsp;	functionDef Haskell
i}}+VN&C~e	Explain the meaning of the term&nbsp;<i>'expression'</i>&nbsp;with respect to C	Any combination of operators, literals, variables, function calls, etc, that the compiler can evaluate to yield a value or side effects<br><br>For example:<br>a + b<br>function(x)<br>42 (constants are also expressions)	C defn keyword
M9nP<@un7(	What is <i>'extern'&nbsp;</i>with respect to C?	Extern is a&nbsp;<i>storage-class&nbsp;</i>keyword that:<br><div><i>Declares&nbsp;</i>a name (variable or function) with <i>external linkage&nbsp;</i>(tells the compiler that the actual definition is inside of a different .c file)</div><div><br></div><div>NOTE:&nbsp;</div><div>- if you do 'extern int a', then a is only declared, not defined, meaning it is not allocated memory</div><div>- you&nbsp;<i>can</i>&nbsp;do 'extern int a = 5' to define an extern variable, but this can only be at file scope (and it does nothing), otherwise there will be an error</div><div><br></div>	C defn keyword
u]<!43KJTK	What is&nbsp;<i>'static'&nbsp;</i>with respect to C?	"<div>Static is a&nbsp;<span style=""font-style: italic;"">storage class</span> keyword, that can be used in three main ways:</div><div>- When prefixed to a global variable/function, it specifies <span style=""font-style: italic;"">internal linkage, </span>so the name’s visibility is kept to the file in which it is declared</div><div>- When prefixed to a local (block‑scope) variable, static gives it the same lifetime as the program, scope does not change</div><div>- Can be used to specify minimum array length within a function’s parameter. For example,</div><div>void fn (int array[static 10])&nbsp;</div><div>specifies that the function takes an array of atleast length 10.<br></div><div><br></div>"	C defn keyword
J3PqVZ^Fkp	Define&nbsp;<i>'Polymorphism'</i>&nbsp;with respect to Haskell	A value is&nbsp;<i>polymorhpic</i>&nbsp;if there is more than one type it can have<br>- Polymorphism refers to the ability to write function or data types that can operate the same way on values of different types. This is achieved through&nbsp;<i>type variables</i><br>For example:<br>id :: a -&gt; a (id works with arbitrary type a)<br>addNum :: Num a =&gt; a -&gt; a -&gt; a (addNum only works with types that are a part of the Num Typeclass)	defn Haskell keyword
t_W=RMnZoL	Explain what the term&nbsp;<i>'Guarded Function'</i>&nbsp;means with respect to Haskell	A guarded function is one that makes use of guards, allowing the function to operate differently under certain conditions.	defn Haskell keyword
O[KY5Yg;2b	What is the type signature and definition of the library function <i>filter</i>	<div><div>filter :: (a -&gt; Bool) -&gt; [a] -&gt; [a]</div><div>filter _ [] = []</div><div>filter p (x : xs)</div><div>&nbsp; | p x = x : filter p xs</div><div>&nbsp; | otherwise = filter p xs</div></div>	defn functionDef Haskell
Q(E0yJH[q&	What does the library function <i>filter&nbsp;</i>do in Haskell?&nbsp;	filter takes a predicate and a list, and returns a new list containing only the elements that satisfy the predicate.&nbsp;<br>i.e it includes each element&nbsp;from the input list for which the predicate 'p x' evaluates to <i>True</i>	defn functionDef Haskell
pE|ZB&R4AT	What does the library functions&nbsp;<i>foldr</i>&nbsp;do in Haskell? Is it tail-recursive?	foldr takes a binary function, a starting (initial) value, and a list, then reduces the list from the right using the function. It is&nbsp;<i>not</i>&nbsp;tail-recursive, but is lazy so works with infinite lists<br><br>- It can be thought of as replacing each cons, ':', operator of a listwith the function that is being applied, and the empty list at the end with the base case<br>- For example, given a list a = [1,2,3], that desugars to 1:(2:(3:[])), <br>foldr (+) 0 a is like doing 1 + 2 + 3 + 0 (replace all cons with function, replace empty list with base case)	defn functionDef Haskell
m}>>2k)_!(	What does the library functions&nbsp;<i>foldr1</i>&nbsp;do in Haskell? Is it tail-recursive?	foldr1 is like foldr, but it does not take an explicit starting value. Instead, it uses the last element of the list as the initial value, and then folds the rest of the list from the right.&nbsp;<i>Not</i>&nbsp;tail-recursive<br>- Requires a non-empty list as input or will lead to an error as there will be no base case<br><br>	defn functionDef Haskell
EOq[??Zl=<	What does the library functions&nbsp;<i>foldl</i>&nbsp;(fold left) do in Haskell? Is it tail-recursive?	foldl processes a list from left to right using a function and a starting value. It starts by combining the initial value with the first element, then keeps folding the result with the next element until the list is done. Foldl is <i>tail-recursive</i><br>- Note: foldl does <i>not</i>&nbsp;work with infinite lists as it is not lazy	defn functionDef Haskell
vs>~C|/V`r	What does the library function&nbsp;<i>map&nbsp;</i>do in Haskell?&nbsp;	map takes a function and a list, and returns a new list where the function has been applied to each element of the original list	defn functionDef Haskell
cRA92!1f_6	What is the type signature and definition of the library function&nbsp;<i>map</i>	<div><div>map :: (a -&gt; b) -&gt; [a] -&gt; [b]</div><div>map _ [] = []</div><div>map f (x : xs) = f x : map f xs</div></div>	defn functionDef Haskell
yW}F1gCOAh	How do you output error messages in Haskell?	"Using the error function<br>error :: [Char] -&gt; a<br>Example usage:<br>function x = error ""this is an error"""	Haskell
e}r=$!u/]H	What is the type signature and definition of the library function&nbsp;<i>take</i>	<div>take :: Int -&gt; [a] -&gt; [a]</div><div>take _ [] = []</div><div>take n (x : xs)</div><div>&nbsp; | n &lt;= 0 = []</div><div>&nbsp; | otherwise = x : take (n - 1) xs</div>	defn functionDef Haskell
FCj2[G]seL	What is the type signature and definition of the library function&nbsp;<i>unzip</i>	<div><div>unzip :: [(a, b)] -&gt; ([a], [b])</div><div>unzip ((a, b) : xs) = (a : as, b : bs)</div><div>&nbsp; where</div><div>&nbsp; &nbsp; (as, bs) = unzip xs</div><div>unzip [] = ([], [])</div></div><div><br></div><div>Unzip takes a list of tuples and returns a tuple of two lists (basically opposite of zip)<br></div>	defn functionDef Haskell
w9eE3%G6_]	What does the library function&nbsp;<i>unzip&nbsp;</i>do in Haskell?&nbsp;	unzip takes a list of tuples and returns a tuples of lists: one containing all the first elements, and the other containing all the second elements	defn functionDef Haskell
d~8{KPO7@F	What does the '!!' function do in Haskell?	"Infix function used to access a specific element of a list i.e<br>[0..10] !! 5&nbsp; &nbsp; &nbsp; gives 5<br>""haskell"" !! 2&nbsp; gives 's'"	defn functionDef Haskell
EGMOqmGAtr	What does the library function&nbsp;<i>take&nbsp;</i>do in Haskell?&nbsp;	<i>take</i> takes an integer n and a list, and returns a list containing the first n elements of the original list. If the list has fewer than n elements, it returns the whole list. Works with infinite lists<br><br>i.e take 5 [2,4..] gives [2,4,6,8,10]	defn functionDef Haskell
C.1mvOe*ZK	What is the type signature and definition of the library function&nbsp;<i>take</i>	<div><div>take :: Int -&gt; [a] -&gt; [a]</div><div>take _ [] = []</div><div>take n (x : xs)</div><div>&nbsp; | n &lt;= 0 = []</div><div>&nbsp; | otherwise = x : take (n - 1) xs</div></div>	defn functionDef Haskell
eIhJg2|bIS	Explain what the operator '|' does in C	This is the&nbsp;<i>bitwise OR</i>&nbsp;operator, and it takes two integers, compares their binary representations&nbsp;bit by bit, and gives a result where each bit is 1 if at least one of the input bits at that position is 1<br>i.e<br>5 | 3 <br>which is<br>0101 | 0011 <br>= 0111 = 7	C defn
QB9@I^B3g3	Explain what the operator '&amp;' does in C	This is the&nbsp;<i>bitwise AND</i>&nbsp;operator, and it takes two integers, compares their binary representations&nbsp;bit by bit, and gives a result where each bit is 1 iff both of the bits at that position are 1, otherwise 0<br>i.e<br>5 &amp; 3<br>which is<br>0101 &amp; 0011<br>= 0001 = 1	C defn
b%1{I}-.Fc	Explain what the operator '^' does in C	This is the&nbsp;<i>bitwise XOR&nbsp;</i>operator, and it takes two integers, compares their binary representations&nbsp;bit by bit, and gives a result where each bit is 1 iff only&nbsp;<i>one</i>&nbsp;of the bits at that position is 1 (i.e they are different), 0 otherwise<br>i.e<br>5 ^ 3<br>which is<br>0101 ^ 0011<br>= 0110 = 6	C defn
P;]]ChM*LK	Explain what the operator '~' does in C	This is the&nbsp;<i>bitwise NOT&nbsp;</i>operator, and it takes one integer and flips all of its bits i.e 0's become 1's and vice versa.&nbsp;<br>NOTE: If applied to signed integers, result is interpreted in two's complement, so might be a negative number<br>i.e<br>~5<br>which is<br>~0000010<br>= 11111010 = -6 (in 2's complement)	C defn
hg};4R])ZT	Explain what the operator '&lt;&lt;' does in C	"This is the&nbsp;<i>bitwise left shift&nbsp;</i>operator, and it takes an integer and shifts all the bits to the left by the given number of positions. Leftmost bits are discarded, rightmost are filled with 0's to fill the ""gap"". Shifting to the left by 1 is the same as multiplying by 2<br>i.e<br>5 &lt;&lt; 1<br>which is<br>00000101 &lt;&lt; 1<br>= 00001010 = 10"	C defn
G2ux40hCW0	Explain what the operator '&gt;&gt;' does in C	"This is the&nbsp;<i>bitwise right shift&nbsp;</i>operator, and it takes an integer and shifts all the bits to the right by the given number of positions. rightmost bits are discarded, leftmost are filled with 0's to fill the ""gap"". Shifting to the right by 1 is the same as dividing by 2<br>i.e<br>10 &gt;&gt; 1<br>which is&nbsp;<br>00001010 &gt;&gt; 1<br>= 00000101 = 5"	C defn
c9QX{|UC?9	Explain the term&nbsp;<i>'QuickCheck'</i>&nbsp;with respect to Haskell	QuickCheck is a Haskell library that tests properties of functions using automatically generated random inputs. <br>- Instead of writing specific test cases, we define general rules (properties/predicates), and QuickCheck checks that they hold for many different values	defn Haskell keyword
ta@C}[zA*H	What does the library function&nbsp;<i>all&nbsp;</i>do in Haskell?	<i>all </i>takes a predicate and a list, and returns <i>True </i>if the predicate returns <i>True </i>for every element in the list.&nbsp;<br>i.e It returns&nbsp;<i>True</i>&nbsp;if all elements of the list satisfy the predicate, <i>False </i>otherwise	defn functionDef Haskell
Nz1h`p<)]5	What does the library function&nbsp;<i>any&nbsp;</i>do in Haskell?	any takes a predicate and a list, and returns <i>True </i>if the predicate returns <i>True </i>for any element in the list.<br>i.e it returns <i>True&nbsp;</i>if there exists some element of the list that satisfies the predicate	defn functionDef Haskell
zh-[DM?X1j	Explain what a&nbsp;<i>'struct'&nbsp;</i>is with respect to C	A C struct is a user defined data structure that allows multiple variables of different types to be grouped under one name. Essentially the C way of defining abstract data types, allows multiple values to be passed to or returned from a function using one name	C defn
cYpm^J[T^b	What data structure is used in the <i>quick-find</i> approach to union-find? How is it used?	Data structure: Integer array of length N where N is the number of objects being modelled<br>How is it used: id[i] for object i is the connected component to which i belongs to<br>- If two objects have the same value in the array at their respective index, they belong to the same connected component	union-find
x))-L)BbB:	How do the <i>find&nbsp;</i>and&nbsp;<i>union</i>&nbsp;operations work in the eager (<i>quick-find</i>) approach to union-find?	Given an integer array id[] representing the connected component an object belongs to,<br><br>find(u, v): <b>id[u]</b> == <b>id[v]</b><br>union(u,v): change all nodes 'i' whose <b>id[i]</b> is the same as <b>id[u]</b> to be <b>id[v]</b> instead	union-find
s>52cOkYS*	What are the <i>worst case</i>&nbsp;<i>costs</i> of all operations (init, union, find) for the eager (<i>quick-find</i>) approach to union-find?	init: <i>O(N)</i>&nbsp; &nbsp; &nbsp; &nbsp;(need to append every object to id one by one)<br>union: <i>O(N)</i>&nbsp; &nbsp;(need to iterate through id[]<b> </b>and change all necessary id's)<br>find: <i>O(1)</i>&nbsp; &nbsp; &nbsp; &nbsp;(simple array access and comparison)	union-find
IJOg*p[>$^	What data structure is used in the&nbsp;<i>quick-union</i>&nbsp;approach to union-find? How is it used?	Data structure: Integer array of length N where N is the number of objects being modelled<br>How is it used: id[i] for object i is the parent of i. If an object i is its own parent, then it is a&nbsp;<i>root</i><br>- two objects&nbsp;<i>u, v&nbsp;</i>are connected if they have the same root	union-find
"o||W`vNo#P"	How do the&nbsp;<i>find&nbsp;</i>and&nbsp;<i>union</i>&nbsp;operations work in the lazy (<i>quick-union</i>) approach to union-find?	find(u, v): root(u) == root(v)<br>union(u, v): set id of root(u) to the id of root(v) - so we first find the roots of u and v, then make the root of u a child of the root of v	union-find
Bzk,_lawB]	What are the&nbsp;<i>worst case</i>&nbsp;<i>costs</i>&nbsp;of all operations (init, union, find) for the lazy (<i>quick-union</i>) approach to union-find?	"init: <i>O(N)</i>&nbsp; &nbsp; &nbsp; &nbsp;(need to append every object to id one by one)<br>union:&nbsp;<i>O(N)</i> &nbsp; (need to iterate through id[]<b>&nbsp;</b>and change all necessary id's)<br>find:&nbsp;<i>O(N)</i> &nbsp; &nbsp; &nbsp;(simple array access and comparison)<br><br>union and find are both N because they require us to travel up the tree to find the roots of&nbsp;<i>u, v,&nbsp;</i>which might require us to ""visit"" almost every object<br><br>Note: average case for union is much lower than N"	union-find
O.wEsdb{jz	What additional data structure is used in the <i>weighted quick-union&nbsp;</i>approach to union-find? How is it used?	Data structure: An extra array to keep track of the size of each tree <br>- size[i] tells us the size of the connected component object i is a part of. This is used to decide which root should become the child of the other in union operations	union-find
Hc)D10LaKs	What change is made to the find and union operations using the&nbsp;<i>quick-union</i>&nbsp;when swapping to the&nbsp;<i>weighted quick-union</i>&nbsp;approach to the union-find problem?	find(u, v): no change<br>union(u, v): instead of always making the root of u the child of the root of v, we instead consult the size array to see which connected component u and v are a part of is smaller<br>- the root of the smaller connected component will be child of the larger one. <br>- Once a union is made, the side of the connected component of v is updated to be the size of u + v	union-find
p^S9XmRsKN	What are the&nbsp;<i>worst case</i>&nbsp;<i>costs</i>&nbsp;of all operations (init, union, find) for the <i>weighted quick-union</i>&nbsp;approach to union-find?	init:&nbsp;<i>O(N)</i>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;&nbsp;<br>union:&nbsp;<i>O(log N)</i>&nbsp; &nbsp;<br>find:&nbsp;<i>O(log N)</i>&nbsp; &nbsp; &nbsp;&nbsp;<br>- Note: uses more space as we also need a size array	union-find
EHhUO(O/&p	What are the main bottlenecks in performance for distributed algorithms (require communication between different devices)?	Latency: start-up cost per message<br>Bandwidth: rate of data transfer	algorithmic::analysis
m{act=|^1p	What are the three main ways of analysing the performance of algorithms?	- Empirical observations<br>- Mathematical models<br>- Order of growth classifications/estimations	algorithmic::analysis
gA9<M,K7FG	What are the drawbacks of analysing algorithms through empirical observations?	- Requires us to implement the algorithm and run it a number of times for testing. This can sometimes take too much investment upfront for more complex algorithms<br>- Performance testing can be system-dependent. Can lead to unexpected results<br>- Hard to extrapolate performance on data for larger input sizes. If we only measures for input size 100000, we wont know for sure how it would behave for input sizes larger than that	algorithmic::analysis
cQ@JO.GPMD	What are the two main types of factors when performing empirical analysis on algorithms? Provide examples of each	1. System <i>independent </i>factors:<br>&nbsp; &nbsp;- Algorithm being used<br>&nbsp; &nbsp;- Input data<br>2. System <i>dependent </i>factors<br>&nbsp; &nbsp;- Hardware (CPU, memory, cache)<br>&nbsp; &nbsp;- Software (prog. language, compiler)<br>&nbsp; &nbsp;- System (OS, background running apps)<br>	algorithmic::analysis
oXB]Z{a]d<	What do the&nbsp;<i>costs</i>&nbsp;and&nbsp;<i>frequencies</i>&nbsp;of operations in an algorithm depend on?	Cost: depends on machine and compiler<br>Frequency: depends on algorithm and input data	algorithmic::analysis
Iw(pp}1~KW	What are the two main things we do when mathematically modelling an algorithm's performance?	1. Focus on the most costly and frequently executed operations<br>2. Ignore lower order terms (use tilde '~' notation)<br>Ultimately, we wish to express the running time of an algorithm as a function of input size N<br>- For example 1.5N<sup>2</sup>&nbsp;+ 5N + 2 becomes ~1.5N<sup>2</sup>	algorithmic::analysis
J*Ibl&U-A$	What common algorithm patterns lead to an order of growth of <i>logN, N, NlogN</i>&nbsp;and <i>N<sup>2</sup></i>?	<i>logN -&nbsp;</i>Algorithms that divide the problem in half i.e binary search<br><i>N</i> - single loop algorithms<br><i>NlogN</i>&nbsp;- Divide and conquer algorithms i.e mergesort<br><i>N<sup>2</sup>&nbsp;-&nbsp;</i>Algorithms with double/nested loops	algorithmic::analysis
JcD|_A]7r@	What does Big O represent?	"The <i>upper bound</i> of an algorithm's order of growth<br><br>""Average case O(N)"" means that in the average case, the algorithm will have an order of growth <i>no worse</i> than N<br><br>Algorithms with performance 2N, NlogN could both be classified as having O(N<sup>2</sup>) order of growth<br>"	algorithmic::analysis
nGP/b[Jy>d	What does Big Omega represent?	"The <i>lower bound</i> of an algorithm's order of growth<br><br>""Average case Omega(N)"" means that in the average case, the algorithm will have an order of growth <i>no better</i> than N<br><br>Algorithms with performance 2N, NlogN could both be classified as having Omega(1) order of growth (but this would not be very informative)<br>"	algorithmic::analysis
Mj=mP;:J$A	What does Big Theta represent?	Big Theta is used to classify an algorithm.<br>- It represents both the upper and lower bound of an algorithm - so it can only be used for algorithms where the upper and lower bounds are very similar (if we consider the tightest bounds). <br>- If it is the case that an algorithm has the same lower and upper bound, then it means it cannot be improved further.<br><br>A function with growth model&nbsp;<i>N<sup>2</sup></i>+5&nbsp;<i>NlogN </i>can be classified as Big Theta(<i>N<sup>2</sup></i>) because the lower and upper bounds are both <i>N<sup>2</sup>&nbsp;</i>	algorithmic::analysis
AQc:H]VKa;	What does Tilde (~) notation represent? How do we get it?	Tilde (~) notation represents an approximation of what the order of growth of an algorithm is.<br>- To get tilde notation, we take the leading term of the function representing an algorithm's order of growth,&nbsp;<i>with</i>&nbsp;the constants.<br>So&nbsp;<i>2N<sup>2</sup>&nbsp;+ 10N + 50&nbsp;</i>= ~<i>2N<sup>2</sup></i>	algorithmic::analysis
IgdG)X+^=h	What is meant by an&nbsp;<i>'Abstract Data Type'?</i>	"An abstract data type is a specification of a data structure’s <i>behavior -&nbsp;</i>the set of operations it supports (API) and their semantics - without prescribing <i>how</i> those operations are implemented or how data is laid out/organised in memory. <br>- ADT's focus on the ""what"" rather than the ""how"""	ADTs
bxG9ucb@sQ	What is meant by a&nbsp;<i>'Data Structure'?</i>	"Data structures are concrete reperesentations of data/implementations of an ADT.<br><div>A concrete implementation of one or more ADTs that specifies how data is organised in memory and how each operation is carried out.&nbsp;</div><div>- Data structures cover the ""how""</div>"	datastructures
w*G3xC;u_y	What is the difference between <i>linear </i>and <i>non-linear </i>Abstract Data Types (ADTs)?	Linear ADTs:<br>- Store elements in a sequential order where each element (not necessarily first and last) has exactly one predecessor and successor<br><br>Non-linear ADTs:<br>- Organise elements in a hierarchical or networked manner, where each element can be related to multiple others (not necessarily one predecessor or successor)<br>- This means that operations may 'branch' or follow multiple paths from a given element	ADTs
"ucDIbb#wg."	Provide key examples of linear ADTs	List<br>Stack<br>Queue<br>Deque<br>Set<br>Map	ADTs
DQ3$?BaWyG	Provide key examples of non-linear ADTs	Trees<br>Graphs	ADTs
bEC!Ye=hr]	Provide the key characteristics of the&nbsp;<i>'Array'</i>&nbsp;ADT as well as its API	Array: An ADT that holds a <i>fixed-size</i> collection of <i>ordered </i>elements accessible by an index<br><br>API:<br>set(index, element) - changes the element at a given index to another<br>get(index) - retrieves the element at a given index	ADTs
Ju(1Kp/dLW	Provide the key characteristics of the&nbsp;<i>'List'</i>&nbsp;ADT as well as its API	List: An ADT that stores&nbsp;<i>sequential</i>&nbsp;elements. Has no concept of an index<br>A list consists of a&nbsp;<i>head</i>, which is the first element, and a&nbsp;<i>tail</i>, which are all the other elements.<br>API:<br>append(item) - adds an item to the end of the list<br>prepend(item) - adds an item to the start of the list (i.e new head)<br>head() - gives the first element of the list<br>tail() - gives all elements besides the head	ADTs
p40[LYizN3	What are the key differences between the&nbsp;<i>'List'</i>&nbsp;and&nbsp;<i>'Array'</i>&nbsp;ADTs?	- Lists are not fixed-size, arrays are<br>- Lists have no concept of an index, instead we use the head and tail to access elements<br>- Lists only support adding elements to the start and end of the list - no direct way to modify any given element unlike arrays	ADTs
Af!;bq4CCT	Provide the key characteristics of the&nbsp;<i>'Stack'</i>&nbsp;ADT as well as its API	"Stack: An ADT that is a list, with restrictions on where we can add/remove elements. Has the concept of a ""bottom"" and ""top"". We can only ever access the ""top""<br>- Stacks are LIFO; Last-in, first-out data types<br>API:<br>push(item) - Add a new element to the top of the stack<br>pop() - Remove the element at the top of the stack and return it<br>isEmpty() - Returns whether or not the stack is empty ()"	ADTs
v6.DI@2D1%	Provide the key characteristics of the&nbsp;<i>'Set' </i>and&nbsp;<i>'Multiset'</i> ADTs as well as their API	Set: An ADT that is used to store&nbsp;<i>unindexed, unordered,&nbsp;</i>and&nbsp;<i>unrepeated</i>&nbsp;elements<br>Multiset (aka bag): An ADT that is the same as a&nbsp;<i>set</i>, but allows&nbsp;<i>repeated/duplicated</i>&nbsp;elements<br>API (same for both):<br>insert(item) - Adds an element to the set/multiset<br>remove(item) - Removes an item from the set/multiset<br>contains(item) - Returns whether or not an element is a part of the set/multiset	ADTs
B3z=(eyZo*	Provide the key characteristics of the&nbsp;<i>'Map'</i>&nbsp;ADT as well as its API	Map (aka <i>dictionary</i>&nbsp;or <i>associative array</i>): A list that can hold data in <i>key, value</i>&nbsp;pairs. Keys must be unique and can only hold one value<br>API:<br>insert(key, value) - Add a key, value pair to the map<br>remove(key) - Remove a key, value pair from the map<br>update(key, value) - Update the value of a key<br>lookup(key) - Return the&nbsp;<i>value</i>&nbsp;associated to a given key	ADTs
EN<.jU06mp	Provide the key characteristics of the&nbsp;<i>'Priority Queue'</i>&nbsp;ADT as well as its API	Priority Queue: Essentially a queue except items are inserted/removed based on a given priority (might not always place item at the tail, depending on priority)<br>API:<br>enqueue(item priority): Enqueues an item, encoding its priority<br>dequeue(): Removes an item with the highest priority	ADTs
jj%?jA{XEr	Provide an overview of the&nbsp;<i>'Array'</i>&nbsp;data structure and the <i>time complexity</i> of its associated API	Arrays are a linear data structure used to store homogenous (of the same type) items in&nbsp;<i>contiguous</i>&nbsp;memory.&nbsp;<br>- Often used to implement the array and stack ADTs<br>- Requires knowledge of the&nbsp;<i>base</i>&nbsp;address of the array, so that it can be used to access the locations of other elements relative to it&nbsp;<br>API costs:<br>get(index)&nbsp;-&nbsp;<i>O(1)</i><br>set(index, element) -&nbsp;<i>O(1)</i>	array datastructures
FFF:HUq@<k	Provide an overview of the&nbsp;<i>'Stack'</i>&nbsp;data structure when implemented using an <i>array.&nbsp;</i>How is the problem of resizing typically tackled?	Can be implemented using an array data structure<br>- Must keep a pointer to where the top of the stack is currently located<br>- Limited by the array's size, resizing requires&nbsp;<i>O(N)&nbsp;</i>time<br>Resizable stack works as follows:<br>- When array is full, create new array twice the size and copy items over<br>- If array reaches &lt;25% capacity, halve the size<br>- invariant: At any given point, array should be between 25%-100% full	array datastructures stack
kU=csczWKO	Provide the best, worst and amortised complexities of a&nbsp;<i>stack</i>&nbsp;implemented via a&nbsp;<i>resizable array</i>	<b>Push:&nbsp;</b><br>Best case:&nbsp;<i>O(1)</i><br>Worst case:&nbsp;<i>O(N)&nbsp;</i>(leads to resize)<br>Amortised: <i>O(1)<br></i><b>Pop:&nbsp;</b><br>Best case:&nbsp;<i>O(1)</i><br>Worst case:&nbsp;<i>O(N)&nbsp;&nbsp;</i>(leads to resize)<br>Amortised:&nbsp;<i>O(1)</i>	array datastructures stack
KyYqAkQ4/,	What problem arises when implementing a Queue ADT using an array?	As we dequeue, the front index (head) moves forward, leaving unused slots at the beginning. <br>- Eventually the rear (tail) reaches the end of the array - which means that even if there is free space at the front, we cannot add any more elements because the tail is the only place to which we can add elemens to. This is also called the&nbsp;<i>False overflow</i>&nbsp;problem<br>- The naive fix (shifting all elements back) is O(n) per operation, which is not efficient enough	array datastructures queue
I8,Y:VE^Aw	What is the solution to the <i>'False overflow'</i>&nbsp;problem that arises when implementing a Queue ADT using a fixed-size array?	Use a <i>circular array&nbsp;</i>where both front and rear indices wrap around to the beginning of the array via modulo arithmetic. This lets us reuse freed slots in <i>O(1)</i> time per enqueue/dequeue without ever shifting elements.<br>- The new tail position after enqueuing an element is calculated as follows (same for head):<br>tail = (tail + 1) % capacity	array datastructures queue
p-prqm(kuL	Provide an overview of the&nbsp;<i>'Linked list'</i>&nbsp;data structure. Name its three variants	A <i>linked list</i> is a data structured that is used to maintain a <i>dynamically sized&nbsp;</i>collection of <i>heterogenous </i>(of different type) items.&nbsp;<br>- Often used to implement stack and queue ADTs<br>- Linked lists consist of nodes, which contain data, and a reference to the next node in the list. <br>- Requires us to maintain a reference to the&nbsp;<i>head</i>&nbsp;of the linked list so that we can usue it as a starting point<br>Three main variants:<br>Singly linked lists<br>Doubly linked lists<br>Circular linked lists	datastructures linkedlist
BeRlZ3lFC}	Provide an overview of&nbsp;<i>'singly linked'</i>,&nbsp;<i>'doubly linked'</i>&nbsp;and '<i>circular'</i>&nbsp;linked lists	"Singly linked - each node maintains a reference to&nbsp;<i>one</i>&nbsp;other node (the next one)<br>Doubly linked - each node maintains a reference to&nbsp;<i>two</i>&nbsp;other nodes (the previous and next ones)<br>Circular - The ""final"" element of the list maintains a reference to the ""first"", so it wraps around&nbsp;"	datastructures linkedlist
lVU!!JK]Aj	What are the pros and cons of using a linked list data structure over an array?	Pros:<br>- Can store elements of different types<br>- Can be dynamic in size<br>Cons:<br>- Slow random access. Cannot directly access an&nbsp;<i>nth</i>&nbsp;element of a linked list, which we can easily do using a normal array<br>- Larger memory overhead, instead of just storing data, we also store references to other nodes	array datastructures linkedlist
NumAlw2@$T	How do we add an element (append) to a singly linked list?	Without considering that we may be deleting the head/tail,<br>- First find the tail (or use a reference you keep to it)<br>- Set the next pointer of the tail to point to the new node<br>- Set next pointer of the new node to be null<br>Pseudocode:<br>tail.next = newItem<br>newItem.next = null<br>tail = newItem	datastructures linkedlist
tbW`dl,RZP	How do we prepend an element to a linked list?	- Set the new node's next pointer to be the head of the linked list<br>- Set the head reference to point to the new node that we added<br>Pseudocode:<br>newItem.next = head<br>head = newItem	datastructures linkedlist
kI~jbhPgDP	How do we delete an element from a singly linked list?	- Start at the head and locate the&nbsp;<i>predecessor</i>&nbsp;of the node we want to delete<br>- Set the&nbsp;<i>predecessor's</i>&nbsp;next pointer to point to the&nbsp;<i>successor</i>&nbsp;of the node we want to delete<br>Pseudocode:<br>while (currNode.next != targetNode)<br>&nbsp; &nbsp; currNode = currNode.next<br>currNode.next = targetNode.next	datastructures linkedlist
G@8Av;M}b3	How do we delete an element from a doubly linked list?	- Access the <i>predecessor</i>&nbsp;and <i>successor&nbsp;</i>of the node we wish to delete using the prev/next pointers<br>- Set the <b>next </b>pointer of the&nbsp;<i>predecessor</i>&nbsp;to be the&nbsp;<i>successor</i>&nbsp;of the node we wish to delete<br>- Set the&nbsp;<b>prev</b>&nbsp;pointer of the&nbsp;<i>sucessor</i>&nbsp;to be the&nbsp;<i>predecessor</i>&nbsp;of the node we wish to delete<br>Pseudocode:<br>prevNode = targetNode.prev<br>nextNode = targetNode.next<br>prevNode.next = nextNode<br>nextNode.prev = prevNode	datastructures linkedlist
r-[/xVqARq	How do we implement the <i>stack&nbsp;</i>API using a linked list data structure? Provide their&nbsp;<i>worst case</i> time complexities	Note: head of LL = top of stack<br>push:&nbsp;<br>newNode.next = head<br>head = newNode<br>pop:<br>targetNode = head<br>head = head.next<br>return targetNode	datastructures linkedlist stack
E+D8WZ6>>o	What are the <i>time complexities</i> of <i>stack operations</i> when implemented via a <i>linked list</i>?	Pop: Worst case&nbsp;<i>O(1)<br></i>Push: Worst case&nbsp;<i>O(1)</i><i><br></i>	cost datastructures linkedlist stack
bsT<f<O^B2	How do we implement the&nbsp;<i>queue&nbsp;</i>API using a linked list data structure?&nbsp;	<b>Note:</b>&nbsp;we add to tail, remove from head<br><b>enqueue</b>:<br>tail.next = newNode<br>newNode.next = null<br>tail = newNode<br><b>dequeue</b>:<br>targetNode = head<br>head = head.next<br>return targetNode	datastructures linkedlist queue
"gxvH_4f9w#"	What are the&nbsp;<i>time complexities</i>&nbsp;of&nbsp;<i>queue operations</i>&nbsp;when implemented via a&nbsp;<i>linked list</i>?	Enqueue: Worst case&nbsp;<i>O(1)<br></i>Dequeue: Worst case&nbsp;<i>O(1)</i><i><br></i>	cost datastructures linkedlist queue
sLhsZ|/0@~	Provide a concise overview of what a&nbsp;<i>'deque'&nbsp;</i>ADT is	A&nbsp;<i>double-ended queue</i>&nbsp;(aka <i>deque</i>) is a generalisation of a stack + queue that supports adding and removing items from either the front or the back<br>API:<br>addFirst(item): adds an item to the front<br>addLast(item): adds an item to the back<br>removeFirst(item): remove and return item from the front<br>removeLast(item): remove and return item from the back	ADTs
QS4U`}P7,w	What is the key idea of the&nbsp;<i>'Selection Sort'</i>&nbsp;algorithm?	Assuming we are sorting an array a[] in&nbsp;<i>ascending</i>&nbsp;order:<br>- Scan array left to right<br>- In the i'th iteration, locate the index&nbsp;<i>min</i> of the&nbsp;<i>smallest remaining entry</i>&nbsp;in the array&nbsp;&nbsp;<br>- Swap a[i] with a[min]<br>- Repeat this&nbsp;<i>N</i>&nbsp;times for an array of length&nbsp;<i>N</i><br>Essentially, we build up a sorted sub-array on the left side of <i>i</i>. That is, on the&nbsp;<i>ith</i>&nbsp;iteration,&nbsp;<i>i</i>&nbsp;elements on the leftmost side of the array will be in sorted order<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp;	sorting
QcbI$LW[F+	What is the time complexity of the&nbsp;<i>'Selection Sort'</i>&nbsp;algorithm?	<i>O(N<sup>2</sup>)&nbsp;</i>in all cases<br>Has Big Theta (N<sup>2</sup>)	cost sorting
b+jxZ^+J%&	What is the key idea of the&nbsp;<i>'Insertion Sort'</i>&nbsp;algorithm?	Assuming we are sorting an array a[] in&nbsp;<i>ascending</i>&nbsp;order:<br>- Scan array left to right<br>- In iteration i, swap a[i] with&nbsp;each<i> larger entry</i>&nbsp;to its&nbsp;<i>left</i><br>- Repeat this&nbsp;<i>N</i>&nbsp;times where&nbsp;<i>N</i>&nbsp;is the number of elements in the array<br>Note: Similar to sorting a deck of cards - take an element, find its position, place it there<br>We are essentially building up a sorted sub-array on the left side of the array. and finding the correct position of each unsorted element within this sub-array and placing it there (by repeatedly swapping it with preceding elements if in the wrong position)	cost sorting
x%jTnN$1t:	What is the key idea behind the <i>'Merge Sort'</i>&nbsp;algorithm? What is it an example of?	- Recursively divide an array into two halves<br>- Repeat until each array is of size 1 (implicitly sorted)<br>- Merge two divided arrays into one sorted one<br>- Keep merging sorted arrays until we have our final sorted version of the original array<br>Merge sort is an example of a&nbsp;<i>divide and conquer</i>&nbsp;algorithm	cost mergesort sorting
D;$XkBuuhL	How do we perform the&nbsp;<i>merge step</i>&nbsp;of the&nbsp;<i>merge sort</i>&nbsp;algorithm? Given an array a[] and its copy aux[]	The two sub-arrays we are meging will be from a[lo] to a[mi] and from a[mid+1] to a[hi]<br>Maintain 3 pointers. i to aux[lo], j to aux[mid+1], k to a[lo]<br>1. Compare element at index i to the element at index j of aux<br>- if aux[i] &lt;= aux[j], then a[k] becomes aux[i]. Increment i and k<br>- if aux[i] &gt; aux[j], then a[k] becomes aux[j]. Increment j and k<br>2. Repeat until one of the following happens:<br>3.a) If i exceeds mid, then we have exhausted all elements of that sub-array, so copy over the remaining elements (j to hi) of the other sub-array into a[]<br>3.b) if j exceeds hi, then we have exhausted all elements of its sub-array, so do a similar procedure as in 3.a<br>3.c) if k exceeds hi, then we have successfully merged and can stop	mergesort sorting
D=X1oa51oX	Provide <i>pseudocode&nbsp;</i>for&nbsp;<i>merge sort</i>	"<div style=""text-align: left;"">The main pattern is&nbsp;<i>sort, sort, merge</i></div><div style=""text-align: left;"">sort(a[], aux[], lo, hi):</div><div style=""text-align: left;"">&nbsp; &nbsp; if (hi &lt;= lo) return; // sub-array of size 1, implicitly sorted</div><div style=""text-align: left;"">&nbsp; &nbsp; mid = lo + (hi-lo) / 2;</div><div style=""text-align: left;"">&nbsp; &nbsp; sort(a, aux, lo, mid);</div><div style=""text-align: left;"">&nbsp; &nbsp; sort(a, aux, mid+1, hi);</div><div style=""text-align: left;"">&nbsp; &nbsp; merge(a, aux, lo, mid, hi);</div>"	mergesort sorting
gFqrk8r`Pz	What are some practical improvements that can be made to merge sort?	1. Before performing a merge, we can check if the two sub-arrays we are about to merge are already sorted relative to eachother. i.e we can check that <br>a[mid] &lt;= a[mid+1]. If this is true, return and dont perform the unecessary merge<br><br>2. Swap the role of a[] and aux[] at each recursive call, so that we dont need to keep copying over elements to aux at each merge step<br>Usual merge call looks like: merge(a, aux, lo, mid, hi)<br>Change this to: merge(aux, a, lo, mid, hi)&nbsp;	mergesort sorting
p~juEMU7P[	Is&nbsp;<i>merge sort</i>&nbsp;stable?&nbsp;In-place?	In-place: No (uses&nbsp;<i>O(N) </i>memory)<br>Stable: Yes	mergesort sorting trait
rH{@zcf$eM	What is the key idea behind the&nbsp;<i>'Quick Sort'</i>&nbsp;algorithm?	1. Shuffle array a[] (to avoid worst case scenario)<br>2. Partition a[] such that for some element j (called the <i>pivot</i>, usually first element)<br>&nbsp; - All elements left of j are less than or equal to j<br>&nbsp; - All elements right of j are greater than or equal to j<br>&nbsp; - This makes it so that j is in its final sorted position<br>3. Repeat from 2 on the left and right partitions we have created. This makes it so that at the end, every element has been the pivot once and so every element is in its final, sorted position	quicksort sorting
c{?3|)Ts.o	How do we perform the&nbsp;<i>partitioning step&nbsp;</i>of the&nbsp;<i>quicksort</i>&nbsp;algorithm?	Given a pivot element at a[lo] (first element of sub-array) we want to find a position for the pivot such that all elements left of it are &lt;=, and all elements to the right are &gt;=:<br>Maintain two pointers - one at lo+1, another at hi<br>Repeat the following steps until i and j cross (i.e i &gt; j)<br>1.) Scan i from left to right as long as a[i] &lt; pivot<br>2.a) Scan j from right to left as long as a[j] &gt; pivot<br>2.b) If scanning for both has stopped, swap a[i] with a[j]<br>3. Once i and j cross, swap pivot with a[j] (because all elements left of a[j] will be less than pivot, and all elements right greater)<br>&nbsp;&nbsp;	quicksort sorting
nq]~~WA_sT	Is&nbsp;<i>quick sort</i>&nbsp;stable?&nbsp;In-place?	In-place: Yes<br>Stable: No	quicksort sorting trait
m0C[[eHJ{s	What are some practical improvements we can make to the&nbsp;<i>Quick Sort</i>&nbsp;algorithm?	- For small sub-arrays (i.e size &lt;10), use insertion sort to avoid recursion overhead<br>- Pick a pivot value that is an estimation of the median of the array's elements (e.g median of 3 random items)&nbsp;	quicksort sorting
A8<@3Y>8bx	What is a&nbsp;<i>'Binary Tree'?</i>	A&nbsp;<i>binary tree</i>&nbsp;is a tree data structure/ADT where each node has <i>at most</i> 2 children, referred to as the left/right child	datastructures trees
PUS45?y9E7	What does it mean for a binary tree to be&nbsp;<i>complete?</i>	<div>A binary tree is <strong>complete</strong> if it satisfies both of these conditions:</div><div>1. All levels except possibly the last are completely filled</div><div>2.&nbsp;In the last level, all nodes are as far left as possible (i.e., there are no gaps between nodes).</div>	datastructures trees
xHe2qj|NPu	With respect to height, what is a key property of&nbsp;<i>complete binary trees?&nbsp;</i>What does this property let us do?	Their height grows at a rate of&nbsp;<i>logN</i>&nbsp;where&nbsp;<i>N</i>&nbsp;is the number of nodes. In other words, height only increases when a node reaches a new power of 2<br>The height of a complete binary tree with N nodes will always be the floor of <i>logN </i>(base 2)<br>- Because of this property, we can represent complete binary trees in a flattened form via arrays&nbsp;	datastructures trees
cn]Q5Ve^j&	How do we access the parent and children of a node in a&nbsp;<i>Binary Heap?</i>	Assuming we begin storing elements at index 1, Given a node at index i<br><i>Parent </i>is at index i/2<br>Children:<br><i>Left child</i>&nbsp;is at index 2i<br><i>Right child</i>&nbsp;is at index 2i+1<br>	binaryheap datastructures trees
qr_q>uT=~_	What is are the key properties of the elements of a&nbsp;<i>binary heap</i>&nbsp;represented by an array?	- The element at index 1 has highest priority<br>- An element at index i has a priority greater than elements at indices &gt;i, and lower priority than elements at indices &lt;i.<br>- This means that the children of any given node should always have lower priority	binaryheap datastructures trees
waJ}mQ_Q>6	Outline the procedure of inserting (enqueuing) an element into a binary heap	- Add the element to the next available space in the array<br>-&nbsp;<i>'Swim'</i>&nbsp;the element up to its correct position (sometimes referred to as <i>propogating</i>&nbsp;it up the heap)<br><br>&nbsp; &nbsp; - This means compare the element's key to its parent, and swap if they are in the incorrect order (if the parent's priority is lower). Repeat this until the newly added element is in the correct position within the heap	binaryheap datastructures trees
Q56P-ap,6D	Outline the procedure of removing (dequeuing) an element from a binary heap	The root has the element with the largest priority, so that is what we need to remove<br><br>- Swap root with node at the end of the array (rightmost leaf of the lowest depth within the tree), remove and return the last node<br><br>-&nbsp;<i>'Sink'</i>&nbsp;the new root (call it R) down to restore heap order<br>&nbsp; - This is done by comparing the R's&nbsp; key with the keys of its children. If R has a lower priority than either one, swap it with the child that has higher priority of the two.<br><br>- Repeatedly compare R with its children and swap if needed until R is in a position where its priority is greater than its children	binaryheap datastructures trees
fh)<lPv$0v	What are the time complexities of <i>Heap&nbsp;</i>operations when implemented via an <i>Unordered Array?</i>	Insert:&nbsp;<i>O(1)</i><br>Delete max (highest prio):&nbsp;<i>O(M)&nbsp;</i>where&nbsp;<i>M</i>&nbsp;is the size of the heap<br>Find max:&nbsp;<i>O(M)&nbsp;<br>O(M)</i> because we need to linearly search in either case	cost heap
ur3GwG-]ef	What are the time complexities of&nbsp;<i>Heap&nbsp;</i>operations when implemented via an&nbsp;<i>Ordered Array?</i>	Insert:&nbsp;<i>O(M)&nbsp;</i>because we need to find correct position and shift over elements<br>Delete max (highest prio):&nbsp;<i>O(1)&nbsp;</i>where&nbsp;<i>M</i>&nbsp;is the size of the heap<br>Find max:&nbsp;<i>O(1)&nbsp;</i>	cost heap
O[dTbf]?U0	What are the time complexities of&nbsp;<i>Heap&nbsp;</i>operations when implemented via a&nbsp;<i>Binary Heap?</i>	Insert:&nbsp;<i>O(logM)</i><br>Delete max (highest prio):&nbsp;<i>O(</i><i>logM</i><i>)&nbsp;</i><br>Find max:&nbsp;<i>O(1)&nbsp;</i><br>Where&nbsp;<i>M</i>&nbsp;is the size of the heap	binaryheap cost
knJ!?U!H=B	What is the main idea behind the&nbsp;<i>'Heap Sort'</i>&nbsp;algorithm?	1. Create a max-heap ordered array with all&nbsp;<i>N</i>&nbsp;keys we are trying to sort<br>2. Repeatedly perform remove-max operations until all keys have been removed. Place removed elements into an array<br>3. Resulting array will be sorted	heapsort sorting
h@qLoY?h}X	What is the procedure when constructing a&nbsp;<i>binary heap in-place&nbsp;</i>from an unordered array<i>?&nbsp;</i>(heapify)	We will essentially just perform&nbsp;<i>Sink</i>&nbsp;operations on all nodes that have children until heap order is created<br>Let N be the array length (1-indexed), then the last non-leaf node will be at index N//2<br>- For each index from N//2 to 1 (the root), perform a&nbsp;<i>sink</i>&nbsp;operation<br>- Once we have reached the root and performed a&nbsp;<i>sink</i>&nbsp;on it, the array will satisfy heap order and so will be heapified	heapsort sorting
vH%d@eRn;V	What is the procedure of getting a <i>sorted array </i>from a&nbsp;<i>binary heap&nbsp;</i>that satisfies heap order, in-place? (sortdown/heap deconstruction)	"We will essentially perform repeated <i>delete-max</i> operations on the heap, forming a sorted sub-array where the ""deleted"" nodes end up. Heap length gets reduced by 1 per deletion<br>Let N be the array length (1-indexed)<br>- Perform a <i>delete-max</i> operation, reduce heap length by 1. Element at index N will be part of the sorted sub-array<br>- Repeat this for a total of&nbsp;<i>N</i>&nbsp;times. Once we perform the N'th <i>delete max</i> operation, the resulting array will in sorted state."	heapsort sorting
N/{m4-h6z=	Pseudocode for&nbsp;<i>heapify</i>&nbsp;(heap construction) and&nbsp;<i>sortdown&nbsp;</i>(heap deconstruction)?	Heapify:<br>for (int i = N//2 ; i &gt;= 1 ; k--)<br>&nbsp; &nbsp;sink(i);<br>Sortdown:<br>while(N &gt; 1):<br>&nbsp; &nbsp;swap(a[1], a[N]);<br>&nbsp; &nbsp;N--;<br>&nbsp; &nbsp;sink(1);	heapsort sorting
HjZYl5N<&e	What are the worst, average and best case costs of&nbsp;<i>Heap Sort?</i>	Worst: ~N lg N<br>Average: ~N lg N<br>Best: ~N lg N<br><br>Note: Makes poor use of cache	cost heapsort sorting
x,{g.]^B%&	In what situations is it good to use an array data structure?	- The amount of input data is known in advance, and does not change much<br>- Not performing alot of insertions<br>- Need fast random access	array datastructures
"gnJ-#55O(V"	What are some <i>common assumptions</i> with respect to&nbsp;<i>symbol tables?</i>	- Keys are unique<br>- Values are not null<br>- Keys have a&nbsp;<i>total order relation<br></i><br>Total order relation:<br>Antisymmetry - if a &lt;= b and b &lt;= a then a = b<br>Transitivity - if a &lt;= b and b &lt;= c then a &lt;= c<br>Totality - a &lt;= b or b &lt;= a	ADTs symboltable
OmO4Wm@+33	What are the three things needed to have a total order relation?	Total order relation:<br>Antisymmetry - if a &lt;= b and b &lt;= a then a = b<br>Transitivity - if a &lt;= b and b &lt;= c then a &lt;= c<br>Totality - a &lt;= b or b &lt;= a	
O[1cWuL_{@	How would a&nbsp;<i>linked list</i>&nbsp;implementation of a&nbsp;<i>symbol table</i>&nbsp;ADT work? How would get and put operations be performed?	We would use an unordered linked list with nodes that would contain <i>key, value</i> pairs<br><br>Get(key) - We would need to linearly scan the entire list until we find a matching key, or exhaust the list without finding one<br>Put(key, value) - We would need to scan through the entire list and:<br>&nbsp;- If we find a matching key, replace the associated value with the new one<br>&nbsp;- If a matching key is not found, add the&nbsp;<i>key, value</i>&nbsp;pair to the front of the list	datastructures linkedlist symboltable
r>ba9NgiH&	What would be the costs of&nbsp;<i>get&nbsp;</i>and<i>&nbsp;put</i>&nbsp;operations with a&nbsp;<i>linked list</i>&nbsp;implementation of a&nbsp;<i>symbol table?</i>	Average/worst case for both:<br>Get(key) -&nbsp;<i>O(N)</i>&nbsp;<br>Put(key, value) -&nbsp;<i>O(N)</i>&nbsp;<br>Linear because we need to perform a linear search on the linked list for either operation	cost datastructures linkedlist symboltable
DH7!e^xMUv	How would an <i>ordered array&nbsp;</i>implementation of a&nbsp;<i>symbol table</i>&nbsp;ADT work? How would get and put operations be performed?	We would need to have 2 data structures:<br>- An <i>ordered</i> array of keys, on which we perform binary searches<br>- Another array containing the keys' associated values<br><br>Get(key) - Binary search on keys[] array, then lookup value in values[] if found<br>Put(key, value) - Binary search on keys[] array, <br>- If matching key found, replace associated value<br>- If not found, perform insertion sort with new <i>key, value </i>pair	datastructures linkedlist symboltable
l?k5W)[^bD	What would be the cost of&nbsp;<i>get&nbsp;</i>and<i>&nbsp;put</i>&nbsp;operations with an&nbsp;<i>ordered array</i>&nbsp;implementation of a&nbsp;<i>symbol table?</i>	Worst case:<br>Get(key) -&nbsp;<i>O(log N)</i>&nbsp; &nbsp; &nbsp; (binary search)<br>Put(key, value) -&nbsp;<i>O(N)</i>&nbsp; (insertion)	cost datastructures linkedlist symboltable
jxE`.B}5-Y	What is the definition if the&nbsp;<i>tree</i>&nbsp;ADT?	A tree is a&nbsp;<i>non-linear</i>&nbsp;abstract data type that can either be:<br>- Empty<br>- A tree consisting of one node called the root, and zero or more&nbsp;<i>disjoint</i>&nbsp;(sub)trees called children	ADTs trees
xndS+-|F(&	What does it mean for a&nbsp;<i>binary tree</i>&nbsp;to be in&nbsp;<i>symmetric order?&nbsp;</i>What does it guarantee?	A binary tree is in symmetric order if each node has a key, and every node's key is:<br>- Larger than all keys in its left subtree<br>- Smaller than all keys in its right subtree<br><br><i>Symmetric order</i>&nbsp;guarantees that an <i>in-order&nbsp;</i>traversal of a tree yields the keys in&nbsp;<i>ascending&nbsp;</i>order	ADTs trees
Jngq93fAi>	What is a&nbsp;<i>binary search tree?</i>	A binary search tree is a&nbsp;<i>binary tree</i>&nbsp;in&nbsp;<i>symmetric order</i>	ADTs trees
ss)AN_[(PB	What data structure do we typically use to <i>implement&nbsp;</i>a&nbsp;<i>binary search tree?</i>&nbsp;	"An ""extended"" linked list where:<br>- Each node has a data item and two pointers,&nbsp;<br>- A left pointer to its left child<br>- A right pointer to its right child"	ADTs BST trees
GTu}Bm>x{	How do we perform&nbsp;<i>get</i>&nbsp;operations using a&nbsp;<i>binary search tree?</i>	Get(key)<br>If current node has the target key, return value<br>If current node's key &lt; target key, go to <i>left </i>child<br>If current node's key &gt; target key, go to <i>right</i> child	ADTs BST trees
Qn<q3MYYCH	How do we perform <i>put&nbsp;</i>operations using a&nbsp;<i>binary search tree?</i>	Put(key, value):<br>If current node's key = new key, update its value to the new value<br>If current node's key &lt; new key, go to <i>left</i> child<br>If current node's key &gt; new key, go to <i>right</i> child<br>If current node is null. insert new node with the specified&nbsp;<i>key, value</i>&nbsp;pair	ADTs BST trees
sPQzf]yewz	What 4 things does a&nbsp;<i>binary search tree</i>&nbsp;<b>node </b>need to have?	1. A variable for its <i>key</i><br>2. A variable for its <i>value</i><br>3. A pointer to its <i>left child</i><br>4. A pointer to its <i>right child</i>	BST datastructures trees
M}BR[a`_s,	What are the&nbsp;<i>average</i>&nbsp;and&nbsp;<i>worst case</i>&nbsp;costs for&nbsp;<i>get&nbsp;</i>and&nbsp;<i>put</i>&nbsp;operations in a <i>symbol table</i> implemented via an&nbsp;<i>unordered linked list?</i>	Worst case:<br>Search:&nbsp;<i>N</i><br>Insert:&nbsp;<i>N</i><br><br>Average case:<br>Search:&nbsp;<i>N/2</i><br>Insert:&nbsp;<i>N</i>	cost symboltable
AzxD9nVl+&	What are the&nbsp;<i>average</i>&nbsp;and&nbsp;<i>worst case</i>&nbsp;costs for&nbsp;<i>get&nbsp;</i>and&nbsp;<i>put</i>&nbsp;operations in a&nbsp;<i>symbol table</i>&nbsp;implemented via an ordered array<i>?</i>	Worst case:<br>Search:&nbsp;<i>log N</i><br>Insert:&nbsp;<i>N</i><br><br>Average case:<br>Search: <i>logN</i><br>Insert: <i>N/2</i>	cost symboltable
"Ef#z*DBsxK"	What are the&nbsp;<i>average</i>&nbsp;and&nbsp;<i>worst case</i>&nbsp;costs for&nbsp;<i>get&nbsp;</i>and&nbsp;<i>put</i>&nbsp;and <i>delete</i>&nbsp;operations in a&nbsp;<i>symbol table</i>&nbsp;implemented via a <i>binary search tree?</i>	Worst case (when the tree is essentially just a linked list):<br>Search:&nbsp;<i>N&nbsp;</i><br>Insert:&nbsp;<i>N</i><br>Delete:&nbsp;<i>N</i><br><br>Average case:<br>Search:&nbsp;<i>clog N&nbsp;&nbsp;</i><br>Insert:&nbsp;<i>clog N</i><br>Delete: <i>sqrt N</i><br>where&nbsp;<i>c</i>&nbsp;is some constant<br><br>NOTE: If deletions are allowed, average case for&nbsp;<i>all</i>&nbsp;operations becomes&nbsp;<i>sqrt N</i>&nbsp;because the tree's structure will not be as optimal	cost symboltable
pCbkiql_35	What are some <i>additional</i>&nbsp;operations we may want to perform using <i>symbol table? </i>Why are they possible?	- Finding the min/max element<br>- Ordered iteration = iterate through all keys in ascending/descending order)<br>- Ceiling(k) = The smallest key &gt;= k<br>- Floor(k) = the greatest key &lt;= k<br>- Rank(k) = number of keys in table strictly less than k)<br>- Select(i) = return the i'th smallest key)<br>- Delete = Removing a <i>key, value</i>&nbsp;pair if it exists<br><br>Possible because of the&nbsp;<i>total order</i>&nbsp;relation between keys	cost symboltable
Ot)tR|%Y{Z	What is the procedure for performing a&nbsp;<i>delete-min</i>&nbsp;operation on a&nbsp;<i>binary search tree?</i>	1. Repeteadly traverse to the <i>left </i>until we find a node with a <i>null left link</i><br>2. If the node we end at has a right child, make is so that the left pointer of the&nbsp;<i>parent</i>&nbsp;of the node pointers to that&nbsp;<i>right child</i>&nbsp;<br>i.e<br>def delMin(node)<br>&nbsp; if (node.left == null)<br>&nbsp; &nbsp; &nbsp; return node.right<br>&nbsp; node.left = delMin(node.left)<br>&nbsp; return node	BST symboltable
xr]mB-W(N&	How do we perform&nbsp;<i>Hibbard deletion</i>&nbsp;on a&nbsp;<i>binary search tree?</i>&nbsp;(deleting a given node)	delete(k)<br>First, search for node&nbsp;<i>n</i>&nbsp;with key&nbsp;<i>k</i><br>Consider 3 cases:<br>1.&nbsp;<i>n</i>&nbsp;has no children - delete by setting the parent link to it to null<br>2.&nbsp;<i>n</i>&nbsp;has 1 child - delete by making the parent point to the child<br>3.&nbsp;<i>n</i>&nbsp;has 2 children, 3 step process, we need to replace <i>n</i>&nbsp;with its <i>successor</i>:<br>- Find successor, call it&nbsp;<i>x,</i>&nbsp;of&nbsp;<i>n&nbsp;</i>(go to right subtree, then traverse left until no more nodes are left)<br>- Delete&nbsp;<i>x</i>&nbsp;from the tree (we essentially do a delete min on the right subtree of <i>n</i>)<br>- Replace&nbsp;<i>n</i>&nbsp;with&nbsp;<i>x</i>	BST symboltable
rs<hSbA}+I	What is a&nbsp;<i>2-3 search tree?&nbsp;</i>What is the nature of its nodes?	A 2-3 search tree is a&nbsp;<i>balanced</i> tree where each node can be of two types:<br>A 2-node, with <i>1 key </i>and&nbsp;<i>2 children</i><br>A 3-node, with&nbsp;<i>2 keys&nbsp;</i>and <i>3&nbsp;children<br></i>	2-3tree symboltable
C:wp:u@Xq$	How are 2-nodes and 3-nodes ordered relative to their children in a 2-3 search tree?	2 Node with key <i>k</i>:<br>Left sub-tree has keys <i>smaller </i>than <i>k</i><br>Right sub-tree has keys&nbsp;<i>larger</i>&nbsp;than&nbsp;<i>k</i><br><br>3 Node with keys <i>x</i>&nbsp;and <i>y </i>where <i>x </i>&lt; <i>y</i>:<br>Left sub-tree has keys <i>smaller </i>than&nbsp;<i>x&nbsp;</i>(&lt; x)<br>Middle sub-tree has keys <i>larger </i>than&nbsp;<i>x</i>&nbsp;but&nbsp;<i>smaller</i>&nbsp;than&nbsp;<i>y&nbsp;</i>(x &lt; k &lt; y)<br>Right sub-tree has keys&nbsp;<i>larger&nbsp;</i>than&nbsp;<i>y </i>(&gt; y)	2-3tree symboltable
qvf]O75UN)	What are the key properties of a 2-3 seach tree?	- Nodes are in&nbsp;<i>symmetric order</i>. This means that an in-order traversal yields keys in&nbsp;<i>ascending</i>&nbsp;order<br>- Perfect balance: every path from the root to a null link has the&nbsp;<i>same length</i>	2-3tree symboltable
HU^!veid9|	What is the procedure for performing an&nbsp;<i>in-order</i>&nbsp;traversal on a&nbsp;<i>2-3 tree?</i>	1.) Start at root<br>2.) Traverse left until null link found<br>If 2 Node:<br>- Enqueue key, go to right sub-tree and repeat from 2<br>If 3 Node:<br>- Enqueue smaller key, visit middle sub-tree and repeat from 2. Once that is done, enqueue larger key, go to right sub-tree and repeat from 2	2-3tree symboltable
jU:};(cDJC	What is the procedure of performing a&nbsp;<i>get</i>&nbsp;operation on a&nbsp;<i>2-3 tree?</i>	get(key):<br>If 2-node: Same procedure as in a BST<br>If 3-node:<br>- Compare search key against those in node<br>- Find interval containing search key (left, middle, right)<br>- Recursively follow associated link	2-3tree symboltable
zb0*TF$C9(	How do we perform a&nbsp;<i>put&nbsp;</i>operation on a&nbsp;<i>2-3 tree?&nbsp;</i>	"First search to find the place where the node would&nbsp;<i>usually</i>&nbsp;be inserted (i.e in a BST put)<br>If the to-be parent of the new node is a 2-node:<br>- Instead of making the new node be a child of it, turn the ""parent"" into a 3-node with the new key<br>If the to-be parent of the new node is a 3-node:<br>1.) Add the new key to the 3-node to make a temporary 4-node<br>2.) Move the middle key of the 4-node to its parent node, and make split the 3-node (from which we moved a key) into two 2-nodes<br>3.) If after propogating the middle key up to its parent, we create another 4-node, repeat from 2. If root ends up being a 4-node, we increase the height of the tree by 1"	2-3tree symboltable
r_gvQQ-Hp)	What are the&nbsp;<i>worst</i>&nbsp;and&nbsp;<i>best</i>&nbsp;cases for a 2-3 tree's height?	Worst case:&nbsp;<i>logN&nbsp;</i>(base 2) if all nodes are 2-nodes<br>Best case:&nbsp;<i>logN</i>&nbsp;(base 3) if all nodes are 3-nodes<br>where&nbsp;<i>N</i>&nbsp;is the number of nodes	2-3tree symboltable
EhUku;gl:%	What are the&nbsp;<i>average</i>&nbsp;and&nbsp;<i>worst case</i>&nbsp;costs for&nbsp;<i>get&nbsp;</i>and&nbsp;<i>put</i>&nbsp;and&nbsp;<i>delete</i>&nbsp;operations in a&nbsp;<i>symbol table</i>&nbsp;implemented via a&nbsp;<i>2-3 tree?</i>	Worst case:<br>Search:&nbsp;<i>clog N</i><br>Insert:&nbsp;<i>clog N</i><br>Delete:&nbsp;<i>clog N</i><br><br>Average case:<br>Search:&nbsp;<i>clog N&nbsp;&nbsp;</i><br>Insert:&nbsp;<i>clog N</i><br>Delete:&nbsp;<i>clog N<br></i><br>where&nbsp;<i>c</i>&nbsp;is some constant	2-3tree cost symboltable
Osm&<~a>[=	What is a <i>left-leaning red-black</i> (LLRB) BST?	LLRB's are a simplification of 2-3 trees (easier to code) - this means they are a also a balanced tree<br>An LLRB is a BST such that:<br>- Every link between nodes is either red or black. Red links represent a 3-node connection&nbsp;<br>- Every path from the root to a null link has the same number of black links<br>- No node has 2 outgoing red links, and there should not be 2 red links in a row<br>- All red links go to the left<br><br>Note: There is a 1-1 correspondence (bijection) between 2-3 trees and LLRBs	datastructures redblack symboltable
"s`tAMD#:sv"	How do we represent red/black links in an <i>LLRB BST?</i>	Instead of having explicitly coloured edges, we give each node a colour property representing the colour of the&nbsp;<i>incoming</i>&nbsp;link to it. <br>- So if a parent has a left link that is red, then the left child of the parent will be coloured red	datastructures redblack symboltable
L6h[BwChF3	How do we perform a&nbsp;<i>get</i>&nbsp;operation in an LLRB BST?	Exact same as in a normal BST, just ignore colours	datastructures redblack symboltable
LP<1@V1U47	What are the 3 <i>operations</i> we perform on an LLRB BBST to maintain its <i>key properties</i> and <i>balance, </i>and when do we do these operations<i>?</i>	Left rotation - used when a node has a right-leaning red link<br>Right rotation - used when there are two left-leaning red links in a row<br>Colour flip - used when there is a node with 2 outgoing red links (left and right)	datastructures redblack symboltable
zDcCU=m=o?	What is the procedure in performing a&nbsp;<i>left rotation</i>&nbsp;in an <i>LLRB BST?</i>	Given a node&nbsp;<i>N</i>&nbsp;and its red-linked <i>right child</i>&nbsp;<i>X</i>, we:<br>1.) Set the&nbsp;<i>right child</i>&nbsp;of&nbsp;<i>N</i>&nbsp;to be the&nbsp;<i>left child</i>&nbsp;of&nbsp;<i>X</i><br>2.)<i>&nbsp;</i>Set the&nbsp;<i>left child</i>&nbsp;of&nbsp;<i>X</i>&nbsp;to be&nbsp;<i>N</i>&nbsp;(<i>N</i>&nbsp;is now a child of&nbsp;<i>X</i>)<br>3.)&nbsp;Set the colour of&nbsp;<i>X</i>&nbsp;to be the colour of&nbsp;<i>N</i><br>4.)&nbsp;Set the colour of&nbsp;<i>N</i>&nbsp;to be <i>red</i>	datastructures redblack symboltable
odRQrqJu{I	What is the procedure in performing a&nbsp;<i>right rotation</i>&nbsp;in an&nbsp;<i>LLRB BST?</i>	Given a node&nbsp;<i>N</i>&nbsp;and its red-linked<i> left child</i>&nbsp;<i>X</i>, we:<br>1.) Set the&nbsp;<i>left child</i>&nbsp;of&nbsp;<i>N</i>&nbsp;to be the&nbsp;<i>right child</i>&nbsp;of&nbsp;<i>X</i><br>2.)<i>&nbsp;</i>Set the&nbsp;<i>right child</i>&nbsp;of&nbsp;<i>X</i>&nbsp;to be&nbsp;<i>N</i>&nbsp;(<i>N</i>&nbsp;is now a child of&nbsp;<i>X</i>)<br>3.)&nbsp;Set the colour of&nbsp;<i>X</i>&nbsp;to be the colour of&nbsp;<i>N</i><br>4.)&nbsp;Set the colour of&nbsp;<i>N</i>&nbsp;to be&nbsp;<i>red</i>	datastructures redblack symboltable
m2_DqkjSq?	What is the procedure in performing a <i>colour flip</i>&nbsp;in an&nbsp;<i>LLRB BST?</i>	Given a node&nbsp;<i>N</i>&nbsp;and its red-linked left and right children, we:<br>- Set the colour of <i>N</i>&nbsp;to be red<br>- Set the colour of the <i>children </i>to be&nbsp;<i>black</i>	datastructures redblack symboltable
P0U@%uo|Hi	How do we perform a&nbsp;<i>put</i>&nbsp;operation on an <i>LLRB BST?</i>	1.) Perform a standard BST insert, colouring the new node/link <i>red</i><br>2.) On the parent of the new node, perform the following operations in this order <i>if necessary</i>&nbsp;<br>3.a) Left rotation if right link is red. If this happens, current node we are considering becomes right child<br>3.b) Right rotation if there are two left-leaning red links in a row. If this happens, current node we are considering becomes left child<br>3.c) Colour flip if both left and right links are red<br>4.) Repeat from 3.a on the <i>parent </i>of the node we are currently considering (this happens naturally due to recursion)	datastructures redblack symboltable
Q5vJDp4mW=	What is the&nbsp;<i>worst case</i>&nbsp;scenario for the <i>height </i>of an <i>LLRB BST?</i>	Height is &lt;=&nbsp;<i>2 log N</i>&nbsp;where&nbsp;<i>N</i>&nbsp;is the number of nodes	cost datastructures redblack symboltable
d*WTE&Sy!.	What are the&nbsp;<i>average</i>&nbsp;and&nbsp;<i>worst case</i>&nbsp;costs for&nbsp;<i>get&nbsp;</i>and&nbsp;<i>put</i>&nbsp;and&nbsp;<i>delete</i>&nbsp;operations in a&nbsp;<i>symbol table</i>&nbsp;implemented via an <i>LLRB BST?</i>	Worst case:<br>Search: <i>2log N</i><br>Insert: <i>2log N</i><br>Delete: <i>2log N</i><br><br>Average case:<br>Search: <i>1log N&nbsp;&nbsp;</i><br>Insert: <i>1log N</i><br>Delete: <i>1log N</i>	cost datastructures redblack symboltable
EgCrF[_pQT	What is the definiton of a&nbsp;<i>Graph?</i>	A <i>graph </i>is a set of <i>vertices/nodes</i> connected pairwise by <i>edges/arcs</i>	ADTs Graphs
rc]3D)~++N	What is meant by the term&nbsp;<i>'degree'</i>&nbsp;with respect to a graph node/vertex?	The number of edges&nbsp;<i>incident</i>&nbsp;to a vertex (the number of edges with an endpoint at a vertex)	ADTs Graphs
HK`ZCZh,eQ	What does it mean for an&nbsp;<i>edge&nbsp;</i>to be&nbsp;<i>incident to</i>&nbsp;a vertex/node in a graph? How does this differ between <i>directed </i>and <i>undirected </i>graphs?	An&nbsp;<i>edge</i>&nbsp;is&nbsp;<i>incident to</i>&nbsp;a vertex if it has an&nbsp;<i>endpoint</i>&nbsp;at a node<br>- Meaning is the exact same for both undirected and directed graphs.&nbsp;<br>e.g.&nbsp;<br>In a directed graph a -&gt; b, a and b are both incident to the edge<br>In an undirected graph a &lt;-&gt; b, a and b are also both incident to the edge	ADTs Graphs
"h]b##1Vj:i"	What is the definition of a&nbsp;<i>cycle</i>&nbsp;in a&nbsp;<i>graph?</i>	A path where the first and last vertex/node is the same	ADTs Graphs
vl{kiK7L4?	What is the definition of a <i>path&nbsp;</i>in a&nbsp;<i>graph?</i>	A sequence of vertices connected by edges	ADTs Graphs
no+P;]Uq}_	What is the&nbsp;<i>length</i>&nbsp;of a path with respect to graphs?	The&nbsp;<i>length</i>&nbsp;of the path is the number of edges traversed from one node to another	ADTs Graphs
n7=2_caa62	What are the <i>space </i>and <i>time </i>(for operations)<i>&nbsp;</i>costs of implementing an&nbsp;<i>undirected/directed graph</i>&nbsp;via an&nbsp;<i>adjacency matrix? </i>(they are the same)	Space (for representation):&nbsp;<i>V</i><sup><i>2</i></sup><br>Time (for operations):<br>addEdge: 1&nbsp;<br>Edge(v, w)?: 1<br>Iterate over connected vertices: V<br><br>Where&nbsp;<i>V</i>&nbsp;is the number of&nbsp;<i>vertices</i>,&nbsp;<i>E</i>&nbsp;the number of&nbsp;<i>edges</i>	cost Graphs
px;V[vUL-<	What are the&nbsp;<i>space&nbsp;</i>and&nbsp;<i>time&nbsp;</i>(for operations)<i>&nbsp;</i>costs of implementing an&nbsp;<i>undirected graph</i>&nbsp;via an&nbsp;<i>adjacency list?</i>	Space (for representation): <i>V + E</i><br>Time (for operations):<br>addEdge: 1&nbsp;<br>Edge(v, w)?: degree(v)<br>Iterate over connected vertices: degree(v)<br><br>Where&nbsp;<i>V</i>&nbsp;is the number of <i>vertices</i>,&nbsp;<i>E</i>&nbsp;the number of <i>edges</i>	ADTs cost Graphs
v7&OPmr%}g	What is the main procedure for performing&nbsp;<i>DFS&nbsp;</i>on a graph?	Maintain 2 aux arrays:<br>visited[] - To mark which nodes have already been visited<br>edgeTo[] - To mark from which node we got to a given node (to retrace steps)<br><br>Starting with a given node, and at each node we visit:<br>- Set&nbsp;<i>visited</i>&nbsp;flag for current node to True<br>- Recursively visit all other nodes accessible from the current node, if node&nbsp;<i>already&nbsp;</i>visited, return instantly<br>- For each node that we travel to from a given node&nbsp;<i>N,</i>&nbsp;set the edgeTo of those nodes to be&nbsp;<i>N</i>	DFS Graphs
z3G=)XOYCp	What things can we do after&nbsp;<i>pre-processing</i>&nbsp;a graph using&nbsp;<i>DFS? </i>How?	1.) Answer the question of whether or not there is a path from the source node to another - Can do by checking whether or not a node was marked visited during the search<br>2.) Find a path to a node from another (if it exists) - Can do by retracing steps form the edgeTo array, keep checking edgeTo of current node (and pushing to a stack), then set current node to what is in edgeTo. Repeat until we reach source node&nbsp;<br>3.) Answer the question of whether or not 2 vertices belong to the same&nbsp;<i>connected component</i>. Can do by seeing if their cc is the same within the cc[] array we use when computing connected components	DFS Graphs
dy8T<6(=P@	What is the main procedure for performing <i>BFS&nbsp;</i>on a graph?	Maintain 3 aux arrays:<br>visited[] - To mark which nodes have already been visited<br>edgeTo[] - To mark from which node we got to a given node (to retrace steps)<br>distToSource[] - Used to keep track of distance from source. In unweighted we do +1, weighted we add the edge weight<br><br>Starting by enqueuing source node, then for each node we visit:<br>- Set&nbsp;<i>visited</i>&nbsp;flag for current node to True<br>- Enqueue all nodes accessible from current node<br>- For each node that we travel to from a given node&nbsp;<i>N,</i>&nbsp;set the edgeTo of those nodes to be&nbsp;<i>N,&nbsp;</i>and the distance to be&nbsp;<i>N's</i>&nbsp;+1/edge weight<br>- After processing a node, dequeue and process another node. Repeat until queue is empty	BFS DFS Graphs
wNTl,ie(;?	What things can we do after&nbsp;<i>pre-processing</i>&nbsp;a graph using&nbsp;<i>BFS?&nbsp;</i>How?	1.) Answer the question of whether or not there is a path from the source node to another - Can do by checking whether or not a node was marked visited during the search<br>2.) Find a path to a node from another (if it exists) - Can do by retracing steps form the edgeTo array, keep checking edgeTo of current node (and pushing to a stack), then set current node to what is in edgeTo. Repeat until we reach source node&nbsp;<br>3.) Know the distance of a node from the source by consulting the distanceToSource[] array. This is also the&nbsp;<i>shortest possible path</i>&nbsp;to that node from the <i>source</i>	BFS Graphs
g!0H)S.@c[	In what cases should we use a <i>hash table</i> over a <i>symbol table?</i>	- If we do not require operations that need the sorted nature of symbol tables (min, max, range etc)&nbsp;<br>- If our keys are simple, like primitive data types (strings, ints)	hashtable symboltable
NoH8*FMach	What is the <i>key idea</i> behind a <i>hash table?</i>	The key idea is to reduce the symbol table ADT into an array<br>- This is done through a hash function, which transforms&nbsp;<i>keys</i>&nbsp;into integers that we can use as indexes to an array where corresponding values are stored	hashtable symboltable
yR@x1r9]H?	What is the meaning of a&nbsp;<i>collision</i>&nbsp;with respect to&nbsp;<i>hash functions? </i>(for hash tables)	<div><div><div><div><div>A <i>collision </i>occurs when two distinct keys produce the <i>same </i>hash value under the <i>hash function</i> (i.e, they map to the <i>same </i>slot/index in the hash table)</div></div></div></div></div><div></div>	hashtable symboltable
w!2|sC9)m}	What are the 3&nbsp;<i>core properties</i>&nbsp;we aim to satisfy when designing a&nbsp;<i>hash function?</i>	1.) The mapping must be deterministic<br>2.) The mapping can be computed very fast<br>3.) The probability of <i>collisions </i>is very low	hashtable symboltable
"siX4^8#m:g"	What is&nbsp;<i>modular hashing</i>&nbsp;with respect to <i>hash functions?</i>	Common practise for creating a hash function (we take modulo of some number to generate hashed value)<br>1.) Choose array size&nbsp;<i>M</i>&nbsp;to be a prime number, then:<br>2.a) For integer key&nbsp;<i>n&nbsp;</i>map to <i>abs(n) % M</i><br>2.b) For double key&nbsp;<i>x&nbsp;</i>map by performing modular hashing on the binary representation of&nbsp;<i>x</i>&nbsp;<br><br>2.c) For a string key <i>s</i>:<br>- Pick a small prime <i>R </i>(e.g. 31), start with a running total h = 0.<br>- For each character <i>c</i> in the string: Multiply <i>h</i> by <i>R</i>.<br>- Add the integer code of <i>c</i> (e.g. its ASCII or Unicode value).<br>- Reduce the result mod <i>M</i>.<br>After we’ve processed all characters, <i>h</i> is the hash value<div></div>	hashtable symboltable
yT3M.PlDq$	Provide an overview of how we would implement a&nbsp;<i>hash table</i>&nbsp;using&nbsp;<i>seperate chaining</i>&nbsp;for&nbsp;<i>collision management</i>	For a data set of size&nbsp;<i>N,</i>&nbsp;<br>Data structures used: Array of&nbsp;<i>M</i>&nbsp;(much smaller than <i>N</i>)&nbsp;<i>linked lists</i>&nbsp;<br><br>Operations:<br>1.) Hash(key): Maps key to index&nbsp;<i>i</i>&nbsp;in range [0,&nbsp;M-1]<br>2.) put(key, value): Put value at the <i>front </i>of the&nbsp;<i>ith</i>&nbsp;linked list/chain if not already there (i being the value the key maps to under hash function)<br>3.) get(key): Linearly scan the&nbsp;<i>ith</i>&nbsp;linked list/chain for the key and retrieve the value if found	hashtable symboltable
wS>PW7!3W|	When implementing a&nbsp;<i>hash table</i>&nbsp;with&nbsp;<i>seperate chaining</i>, what do typically choose the size of the array containing linked lists to be?	If we have&nbsp;<i>N</i>&nbsp;keys to hash, we typically have an array of&nbsp;<i>N/5</i>&nbsp;linked lists	hashtable symboltable
HL}S?e5OM5	What is meant by the term&nbsp;<i>'uniform hashing assumptions'&nbsp;</i>with respect to hash tables?	Every key is roughly equally likely to be mapped to any index under the <i>hash function</i>. So no index will many more keys mapped to compared to another	hashtable symboltable
iSA`s[:!3f	What are the&nbsp;<i>worst&nbsp;</i>and&nbsp;<i>average</i>&nbsp;case time complexities for&nbsp;<i>hash table</i>&nbsp;operations<i>&nbsp;</i>with <i>seperate chaining?</i>	Under uniform hashing assumptions,<br>Worst case:<br>Search:&nbsp;<i>logN<br></i>Insert:&nbsp;<i>logN<br></i>Delete:&nbsp;<i>logN</i><br><br>Average case:<br>Search:&nbsp;<i>3-5<br></i>Insert:&nbsp;<i>3-5<br></i>Delete:&nbsp;<i>3-5</i>	cost hashtable symboltable
n43us7j2q}	Provide an overview of how we would implement a&nbsp;<i>hash table</i>&nbsp;using&nbsp;<i>linear probing</i>&nbsp;for&nbsp;<i>collision management</i>	For a data set of size&nbsp;<i>N,</i>&nbsp;<br>Data structures used: Two arrays of size&nbsp;<i>M</i>&nbsp;(&gt;<i>N</i>), one for <i>keys </i>and one for&nbsp;<i>values</i><br><br>Operations:<br>1.) Hash(key): Maps key to index&nbsp;<i>i</i>&nbsp;in range [0,&nbsp;M-1]<br>2.) put(key, value): Hash key to index <i>i,&nbsp;</i>check keys array if there is space, if not, try&nbsp;<i>i+1, i+2</i>&nbsp;etc until space is found. When found, place <i>key </i>at that index, and <i>value </i>at that index of <i>values </i>array<br>3.) get(key): Hash key to index&nbsp;<i>i</i>, check keys array if matching key is found, if not try&nbsp;<i>i+1, i+2&nbsp;</i>until key is found (return <i>value </i>stored in <i>value</i>&nbsp;array at same index) or empty space is reached (return null)<br><br>Note: we are guaranteed to have space for every key because the array is of size <i>M &gt; N</i>	cost hashtable symboltable
t3rH]a]oCz	What are the&nbsp;<i>worst&nbsp;</i>and&nbsp;<i>average</i>&nbsp;case time complexities for&nbsp;<i>hash table</i>&nbsp;operations<i>&nbsp;</i>with <i>linear probing?</i>	Under uniform hashing assumptions,<br>Worst case:<br>Search:&nbsp;<i>logN<br></i>Insert:&nbsp;<i>logN<br></i>Delete:&nbsp;<i>logN</i><br><br>Average case:<br>Search:&nbsp;<i>3-5<br></i>Insert:&nbsp;<i>3-5<br></i>Delete:&nbsp;<i>3-5</i>	cost hashtable symboltable
NBmz}Wp>;`	When implementing a&nbsp;<i>hash table</i>&nbsp;with&nbsp;<i>linear probing</i>, what do typically choose the sizes of the&nbsp;<i>keys </i>and&nbsp;<i>values</i>&nbsp;arrays to be?	If we have&nbsp;<i>N</i>&nbsp;keys to hash, we typically have both arrays be if size&nbsp;<i>2*N</i>	hashtable symboltable
fL=|KjkuZA	Pros of implementing a&nbsp;<i>hash table</i>&nbsp;via&nbsp;<i>seperate chaining</i>&nbsp;vs&nbsp;<i>linear probing </i>relative to eachother?	Seperate chaining:<br>- Easier to implement<br>- Performance degrades more gracefully<br>- Less sensitive to poorly designed hash functions<br><br>Linear probing:<br>- Less wasted space<br>- Better performance on larger data sets	hashtable symboltable
fk.FU()[4M	What do we do with the arrays used when implementing a&nbsp;<i>hash table</i>&nbsp;via&nbsp;<i>linear probing</i>&nbsp;to maintain good performance?	Dynamically resize the array such that the amount of spaces occupied at any time is around 50% of the array's size	hashtable symboltable
oRku>MV)Vx	What are the time complexities of&nbsp;<i>BFS</i>&nbsp;and&nbsp;<i>DFS?&nbsp;</i>(they are both the same)	In&nbsp;<i>all</i>&nbsp;cases (best, average, worst)<br>Big Theta (<i>V + E</i>)<br>where&nbsp;<i>V</i>&nbsp;is the number of vertices/nodes in the graph,&nbsp;<i>E</i>&nbsp;the number of edges/arcs	BFS cost DFS
xk6wnixEBo	What is the general procedure of computing <i>connected components</i> via&nbsp;<i>DFS?</i>	Maintain an extra auxiliary array cc[] to keep track of what connected component a vertex belongs to<br>- Will perform (potentially) several DFS. Need a <i>count</i> variable, which increments at the&nbsp;<i>end</i>&nbsp;of every DFS<br>- As we traverse the nodes, set the cc[] of every node visited to be the value of the current&nbsp;<i>count.&nbsp;</i>So every node visited in one <i>DFS</i> has the same cc[] value (because they are apart of the same component)<br>- At the end of a&nbsp;<i>DFS</i>, start another&nbsp;<i>DFS</i>&nbsp;on any unvisited node, after incrementing&nbsp;<i>count</i>	DFS
D`$FS`G-1B	What does the addEdge(v, w) API call for an <i>undirected</i>&nbsp;graph do?	Adds a bidirectional edge going from v to w<br><br>Amounts to setting adj[v][w] and adj[w][v] to True in the adjacency matrix, or adding w to the adjacency list at adj[v], and v to the adjacency list at adj[w]	
hy+KAql@hT	What are the&nbsp;<i>space&nbsp;</i>and&nbsp;<i>time&nbsp;</i>(for operations)<i>&nbsp;</i>costs of implementing a&nbsp;<i>directed graph</i>&nbsp;via an&nbsp;<i>adjacency list?</i>	Space (for representation): <i>V + E</i><br>Time (for operations):<br>addEdge: 1&nbsp;<br>Edge(v, w)?: outdegree(v)<br>Iterate over connected vertices: outdegree(v)<br><br>Where&nbsp;<i>V</i>&nbsp;is the number of&nbsp;<i>vertices</i>,&nbsp;<i>E</i>&nbsp;the number of&nbsp;<i>edges</i>	cost Graphs
vO^)o?igr@	What are 3 applications of <i>search </i>algorithms (<i>DFS, BFS</i>) on problems that can be modelled via <i>digraphs?</i>	1.) Program flow analysis - checking if any code is unreachable<br>2.) Garbage collection - find all objects reachable, free those unreachable<br>3.) Web crawling - start from a webpage to be crawled and visit all reachable websites	Graphs
uFbE1mc!H/	What <i>condition </i>needs to be fulfilled for a graph to be able to have&nbsp;a&nbsp;<i>topological order?</i>	It needs to be a DAG (directed acyclic graph)	Graphs
xDt!CZ?>}*	What is a&nbsp;<i>DAG </i>with respect to <i>graphs?</i>	A DAG (directed acyclic graph) is a <i>directed </i>graph with&nbsp;<i>no cycles</i>	Graphs
nAEl$Pa~:T	What is&nbsp;<i>topological</i>&nbsp;<i>order</i> with respect to&nbsp;<i>graphs?</i>	Topological order is a way to order the nodes in a <i>directed acyclic graph</i> (DAG) such that for every <i>directed</i> edge u → v, vertex u appears <i>before</i>&nbsp;v in the sequence of vertices. <br>	Graphs
ld%vF2xAib	How do we get the&nbsp;<i>topological</i>&nbsp;<i>order</i>&nbsp;of a&nbsp;<i>DAG?</i>&nbsp;(i.e perform a <i>topological sort</i>)	1.) Check that the graph is a&nbsp;<i>DAG</i>&nbsp;<br>2.) Run a DFS, returning the vertices in&nbsp;<i>reverse post-order&nbsp;</i><br>- To get reverse post order, we add each node to a stack&nbsp;<i>after</i>&nbsp;it has been&nbsp;<i>fully processed.&nbsp;</i>This gives us post-order<br>- Then pop all contents of the stack to get the contents in&nbsp;<i>reverse</i>&nbsp;order, which gives us&nbsp;<i>reverse post order</i>	Graphs
vYa/x%8%(@	How do we obtain a graph's nodes in&nbsp;<i>post-order?</i>	Perform <i>DFS,&nbsp;</i>once a node has been&nbsp;<i>fully processed&nbsp;</i>(visited all adjacent nodes), add it to a data structure. Final order of nodes is post-order	Graphs
k6PF^:i!5M	What is the general procedure of computing whether or not a <i>digraph</i> has a <i>directed cycle? </i>(i.e checking if it is a <i>DAG</i>)	Slight modification of&nbsp;<i>DFS,</i><br>- Maintain an <i>extra data structure</i> - a&nbsp;<i>boolean array</i>&nbsp;representing whether or not a node is currently being processed ( onCallStack[] )<br>- As we traverse nodes in the DFS, set boolean flag&nbsp;for nodes that have <i>open DFS calls</i> (are currently being processed) to True. Once a node is <i>fully processed</i> (checked all its neighbours), set&nbsp;to False.<br>- If at any point we are visiting a node and&nbsp;<i>it its boolean flag is already set to True</i>, then we have a cycle, so graph is not a <i>DAG</i><br>- If we process the entire graph without any issues, graph is a&nbsp;<i>DAG</i>	Graphs
"duW-+y#QZ8"	What is the key idea/procedure in using the K<i>osaraju-Sharir</i>&nbsp;<i>algorithm</i>&nbsp;to compute&nbsp;<i>strongly connected components?</i>	1.) Reverse the edges in the graphs - this works because reversing the edges does&nbsp;<i>not</i>&nbsp;change the strong components<br>2.) Compute reverse post-order of reversed version of graph<br>3.) Run&nbsp;<i>DFS</i>&nbsp;on the non-reversed (original) graph, visiting unmarked vertices in the reverse post-order we calculated earlier<br>4.) As we traverse, keep track of&nbsp;the&nbsp;<i>current strongly connected component</i>&nbsp;in the same way we would when computing connected components for&nbsp;<i>undirected graphs</i>	Graphs
ym%vxvCV$:	What is a&nbsp;<i>spanning tree</i>&nbsp;of a <i>graph? Minimum</i>&nbsp;<i>spanning tree?</i>	A&nbsp;<i>spanning tree</i>&nbsp;of a graph G is a subgraph T such that it is both a <i>tree </i>(connected and acyclic) and <i>spanning </i>(includes all vertices)<br><br>A minimum spanning tree of an undirected and positively edge-weighted graph is a&nbsp;<i>spanning tree </i>where<i> </i>the <i>sum of the edgeweights is minimum</i>	Graphs
J.(~8+aTd;	What property of a <i>edge-weighted undirected graphs</i> dictates <i>how many minimum spanning trees</i> it may have?	Whether or not it has distinct edges with duplicate edge weights<br><br>Duplicate edge weights = &gt;1 MST<br>No duplicate edge weights = 1 MST	Graphs
NnGPt3OC}2	With respect to&nbsp;<i>graphs,</i>&nbsp;what is a&nbsp;<i>cut</i>&nbsp;and&nbsp;<i>crossing edge?</i>&nbsp;How can we use them in calculating the&nbsp;<i>MST</i>&nbsp;of a graph?	A <i>cut </i>in a graph is a <i>partition </i>of its vertices into 2 nonempty sets<br><br>A <i>crossing edge </i>between the 2 sets of a cut is an edge <i>connecting</i> a vertex in one set to a vertex in the other<br><br>Key property: The crossing edge of&nbsp;<i>minimum weight</i>&nbsp;between the two sets of a cut is an edge in the&nbsp;<i>MST.&nbsp;</i>We can use this property to calculate the&nbsp;<i>MST</i>	Graphs MST
FWk+/tnbGf	What is the key idea/procedure behind using the&nbsp;<i>greedy algorithm</i>&nbsp;for computing the&nbsp;<i>minimum spanning tree</i>&nbsp;of a graph?	"Start by ""colouring"" all edges&nbsp;<i>grey</i>. Edges that we know are a part of the MST will be coloured&nbsp;<i>black</i>&nbsp;<br>1.) At random, find a&nbsp;<i>cut</i>&nbsp;of the&nbsp;<i>MST</i>&nbsp;that has&nbsp;<i>no black</i>&nbsp;<i>crossing edges</i><br>2.) Colour the min-weighted crossing-edge&nbsp;<i>black</i>&nbsp;(it is a part of the MST)<br>3.) Repeat until&nbsp;<i>V-1</i>&nbsp;edges have been coloured black (an MST has <i>V-1</i>&nbsp;edges, where <i>V</i>&nbsp;is the number of vertices)"	Graphs MST
NX_IR>1ptM	What is a&nbsp;<i>key property</i>&nbsp;of&nbsp;<i>MSTs</i>&nbsp;with respect to the number of&nbsp;<i>vertices </i>and <i>edges?</i>	An MST will have&nbsp;<i>V-1</i>&nbsp;edges where&nbsp;<i>V</i>&nbsp;is the number of <i>vertices</i>	Graphs MST
GMM32qn=+p	What is the key idea/procedure behind using&nbsp;<i>Kruskal's algorithm</i>&nbsp;for computing the&nbsp;<i>minimum spanning tree</i>&nbsp;of a graph?	1.) Consider edges in&nbsp;<i>ascending</i>&nbsp;order of weight. Using a min-heap<br>2.) Add the next edge (obtained by delMin operation on heap) to the&nbsp;<i>MST</i>, unless doing so would create a&nbsp;<i>cycle</i>&nbsp;<br>3.) Repeat until either all edges have been considered, or&nbsp;<i>V-1</i>&nbsp;edges have been added to the&nbsp;<i>MST</i>	Graphs MST
LT`scyPjKu	With respect to&nbsp;<i>Kruskal's</i>&nbsp;algorithm, what 2 methods can we use to check whether or not adding an edge v-w creates a cycle in the&nbsp;<i>MST?&nbsp;</i>What are the timy complexities of each method?	1.) Run&nbsp;<i>DFS&nbsp;</i>to see if&nbsp;<i>w</i>&nbsp;is already reachable from&nbsp;<i>v</i>. If it is, then adding the edge v-w would create&nbsp;<br>Cost:&nbsp;<i>O(V) </i>per DFS run, so would need to run&nbsp;<i>every</i>&nbsp;time we consider an edge. Too inefficient<br>2.) Make use of the&nbsp;<i>weighted quick-union</i>&nbsp;version of the union-find data structure to check whether or not v and w are in the same connected component<br>Cost:&nbsp;<i>O(log V) </i>per check	Graphs MST
v|J[7i~baR	Provide a breakdown of the different costs of operations needed for&nbsp;<i>Kruskal's algorithm</i>, as well as the overall time complexity for computing an&nbsp;<i>MST.</i>	&nbsp; &nbsp; &nbsp;Operation&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;|&nbsp; Frequency&nbsp; |&nbsp; &nbsp;Cost<br>Build priority queue |&nbsp; &nbsp; &nbsp; &nbsp; &nbsp;1&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; |&nbsp;<i>E log E</i><br>&nbsp; &nbsp; &nbsp;Delete min&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; |&nbsp; &nbsp; &nbsp; &nbsp; &nbsp;E&nbsp;&nbsp;<i>&nbsp; &nbsp; &nbsp; &nbsp;|&nbsp;&nbsp; log E</i><br>&nbsp; &nbsp; &nbsp; &nbsp; Union&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;|&nbsp; &nbsp; &nbsp; &nbsp; &nbsp;V&nbsp; &nbsp; &nbsp; &nbsp; &nbsp;|&nbsp; &nbsp;<i>log V</i><br>&nbsp;Find (connected?)&nbsp; |&nbsp; &nbsp; &nbsp; &nbsp; &nbsp;E&nbsp; &nbsp; &nbsp; &nbsp; &nbsp;|&nbsp; &nbsp;<i>log V</i><br><br>Worst case order of growth:&nbsp;<i>E log E</i>	cost Graphs MST
vwrTQ+P61H	What is the key idea/procedure behind using&nbsp;<i>Prim's algorithm</i>&nbsp;for computing the&nbsp;<i>minimum spanning tree</i>&nbsp;of a graph?	We start with vertex 0 and&nbsp;<i>greedily</i>&nbsp;grow the <i>MST</i><br>1.) Add to the&nbsp;<i>MST</i>&nbsp;the min-weighted edge with&nbsp;<i>exactly one endpoint</i>&nbsp;in the current&nbsp;<i>MST</i><br>2.) Stop when&nbsp;<i>V-1</i>&nbsp;edges have been added	
qi-[{Qmz!s	With respect to&nbsp;<i>Prim's</i>&nbsp;algorithm, what 2 methods can we use to find the&nbsp;<i>min-weighted edge</i>&nbsp;with&nbsp;<i>exactly 1 endpoint</i>&nbsp;already in the&nbsp;<i>MST?&nbsp;</i>What are the associated <i>time complexities?</i>	1.) Try all edges linearly&nbsp;<br>Cost:&nbsp;<i>O(E)</i><br>2.) Use a <i>min-heap/priority</i> queue containing all edges&nbsp;<i>with atleast one endpoint in T</i><br>Cost:&nbsp;<i>O(log E)</i>	cost Graphs MST
g5SjOFb)<:	Provide an overview is how we implement&nbsp;<i>Prim's&nbsp;</i>algorithm using a&nbsp;<i>priority queue</i>	Keys will be edges, priority will be based on edge weight<br>1.) Delete-min to determine next edge,&nbsp;<i>v-w,</i> to consider<br>2.) Disregard edge if both endpoints already in&nbsp;<i>MST&nbsp;</i>(i.e v, w already in <i>MST </i>/ is already marked)<br>- Otherwise, let v be the vertex already in the&nbsp;<i>MST,&nbsp;</i>w the vertex we are adding<br>3.a) add edge v-w to <i>MST</i><br>3.b) Mark w as being part of&nbsp;<i>MST</i><br>3.c) Add any edge incident to w to the priority queue, unless both endpoints are already in the&nbsp;<i>MST</i>	Graphs MST
P`+~Bd;QCN	Provide a breakdown of the different costs of operations needed for&nbsp;<i>Prim's algorithm</i>, as well as the overall time complexity for computing an&nbsp;<i>MST.</i>	&nbsp; &nbsp; &nbsp;Operation&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;|&nbsp; Frequency&nbsp; |&nbsp; &nbsp;Cost<br>&nbsp; &nbsp; &nbsp;Delete min&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; |&nbsp; &nbsp; &nbsp; &nbsp; &nbsp;E&nbsp;&nbsp;<i>&nbsp; &nbsp; &nbsp; &nbsp;|&nbsp;&nbsp; log E</i><br>&nbsp; &nbsp; &nbsp; &nbsp; Insert&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;|&nbsp; &nbsp; &nbsp; &nbsp; &nbsp;E&nbsp; &nbsp; &nbsp; &nbsp; &nbsp;|&nbsp; &nbsp;<i>log E</i><br><br>Worst case order of growth:<br>Time -&nbsp;<i>E log E</i><br>Space - <i>E</i>	Graphs MST
Q0L6-<>oCY	What are the 3 variants of the&nbsp;<i>shortest path problem</i>&nbsp;we may want to solve?	Source-Sink:<br>- Shortest path from one specific vertex to another&nbsp;<br><br>Single source:<br>- Shortest path from one vertex to any other in a graph<br><br>All pairs:<br>- Shortest path between all paris of vertices in a graph&nbsp;	Graphs shortestpath
LDLt}>%j2.	How is a&nbsp;<i>weighted digraph</i>&nbsp;typically represented?	"As a vertex-indexed array of bags (adjacency lists)<br>- Each ""list"" is a bag of&nbsp;<i>weighted, directed&nbsp;</i>edges coming out of a vertex"	Graphs shortestpath
EP2?!5ZHwF	When considering the&nbsp;<i>single-source</i>&nbsp;<i>shortest path</i>&nbsp;problem on&nbsp;<i>graphs,&nbsp;</i>in what way can we represent the SPT (shortest path tree)?	Using two vertex-indexed arrays:<br>distTo[v]: gives the <i>length</i>&nbsp;of the&nbsp;<i>shortest path</i>&nbsp;from the source to vertex&nbsp;<i>v</i>&nbsp;<br>edgeTo[v]: gives the&nbsp;<i>last edge</i>&nbsp;used to traverse to&nbsp;<i>v</i>&nbsp;on the shortest path from source to vertex v (can use this to reconstruct the shortest path)&nbsp;	Graphs shortestpath
zz@Sm=v(Ds	What does it mean to&nbsp;<i>'relax an edge'&nbsp;</i>with respect to the&nbsp;<i>shortest path problem</i>&nbsp;on&nbsp;<i>graphs?</i>	Essentially updating our current best estimate of the shortest path to a vertex<br><br>Given an edge v -&gt; w, relaxing this edge means to compare the overall distance of the path to&nbsp;<i>w</i>&nbsp;through&nbsp;<i>v</i>&nbsp;with the current known shortest distance to&nbsp;<i>v</i><br>- If distance to v + weight of v -&gt; w is smaller than the current known shortest path to&nbsp;<i>w</i>, then going through edge v -&gt; w yields a shorter path, hence we update:<br>- Update distTo[w] to reflect this<br>- Update edgeTo[w] to use the edge from v	Graphs shortestpath
pQ-PNmVBU*	What is the&nbsp;<i>key idea/procedure</i>&nbsp;for <i>Dijkstra's algorithm? </i>What is the assumption we make with respect to the&nbsp;<i>graph</i>&nbsp;we use the algorithm on?	Assumption: Digraph has non-negative edge weights<br>Initialisation: distTo array with distTo[s] = 0 for source&nbsp;<i>s</i>, distTo[v] = inf&nbsp;and for all other vertices&nbsp;<i>v&nbsp;</i><br>Key idea: <br>- Consider vertices in increasing distance from&nbsp;<i>s </i>(via priority queue)<br>- Add vertex to SPT (shortest path tree) and <i>relax </i>all <i>outgoing </i>edges<br>- Repeat until all edges are part of the SPT	Graphs shortestpath
IvxgZ_(G~x	What are the&nbsp;<i>costs</i>&nbsp;associated with implementing&nbsp;<i>Dijkstra's</i>&nbsp;<i>algorithm</i>&nbsp;for computing&nbsp;<i>shortest path</i>, for both an&nbsp;<i>unordered array&nbsp;</i>implementation of a&nbsp;<i>priority queue</i>, and a&nbsp;<i>binary heap</i>&nbsp;implementation?	Unordered array:<br>insert() -&nbsp;<i>1</i><br>delMin() -&nbsp;<i>V </i><br>decreaseKey() -&nbsp;<i>1 </i><br>Overall order of growth (worst case):&nbsp;<i>V<sup>2<br></sup></i><br>Binary heap:<br>insert() -&nbsp;<i>log V</i><br>delMin() -&nbsp;<i>log V</i><br>decreaseKey() -&nbsp;<i>log V</i><br>Overall order of growth (worst case): <i>E log V</i><br><br>Binary heap better for&nbsp;<i>sparse</i>&nbsp;graphs,&nbsp;<br>Unordered array better for&nbsp;<i>dense</i>&nbsp;graphs	cost Graphs shortestpath
qja)thbC~J	What is the&nbsp;<i>key idea/procedure</i>&nbsp;for the&nbsp;<i>Edge-weighted DAG algorithm </i>for computing&nbsp;<i>shortest path?&nbsp;</i>What is the assumption we make with respect to the&nbsp;<i>graph</i>&nbsp;we use the algorithm on?	Assumption: Digraph is <i>acyclic</i> (it is a <i>DAG</i>)<br>Initialisation: distTo array with distTo[s] = 0 for source&nbsp;<i>s</i>, distTo[v] = inf&nbsp;and for all other vertices&nbsp;<i>v&nbsp;</i><br>Key idea:<br>- Consider vertices in <i>topological order</i>&nbsp;<br>- Add vertex to SPT (shortest path tree) and&nbsp;<i>relax&nbsp;</i>all of its outgoing edges<br>- Repeat until all vertices have been exhausted	Graphs shortestpath
I}BHj|d_)8	What is the&nbsp;<i>time complexity</i>&nbsp;of the&nbsp;<i>edge-weighted DAG</i>&nbsp;algorithm for computing&nbsp;<i>shortest path?</i>	Worst case: E + V (dominated by cost of topological sort via <i>DFS</i>)	cost Graphs shortestpath
wof%Src1D0	Under what conditions does there <i>not </i>exist an SPT (shortest path tree) for a <i>digraph?</i>	When there are <i>negative cycles</i><br><br>- A negative cycle is a cycle where the <i>sum </i>of the edgeweights is <i>negative</i>	Graphs shortestpath
wS}6A!tY+B	What is the&nbsp;<i>key idea/procedure</i>&nbsp;for&nbsp;<i>Bellman-Ford's algorithm?&nbsp;</i>What is the assumption we make with respect to the&nbsp;<i>graph</i>&nbsp;we use the algorithm on?	Assumption: Digraph has no <i>negative cycles</i><br>Initialisation: distTo array with distTo[s] = 0 for source&nbsp;<i>s</i>, distTo[v] = inf&nbsp;and for all other vertices&nbsp;<i>v&nbsp;</i><br>Key idea:<br>- Relax all edges&nbsp;<i>V</i>&nbsp;times	Graphs shortestpath
rb!~5*pnHd	What are some&nbsp;<i>practical improvements</i>&nbsp;we can make to the Bellman-Ford algorithm?	1.) If distTo[v] does not change during a pass (i.e, relaxing all edges makes no difference) then stop the algorithm as SPT has already been computed<br>2.) Maintain a queue of vertices whose distTo[] has changed (i.e, a queue of vertices who were affected by relaxing an edge) - only relax outgoing edges for nodes in the queue each pass	Graphs shortestpath
zgfcT.zT&S	What are the&nbsp;<i>costs</i>&nbsp;associated with implementing&nbsp;<i>Bellman Ford's</i>&nbsp;<i>algorithm</i>&nbsp;for computing&nbsp;<i>shortest path</i>, for both a&nbsp;<i>queue-based&nbsp;</i>implementation and a <i>non queue-based</i> one?	Queue-based (time):<br>- Average case:&nbsp;<i>E+V</i><br>- Worst case:&nbsp;<i>E * V</i><br>non queue-based (time)<br>- Average case:&nbsp;<i>E*V</i><br>- Worst case:&nbsp;<i>E * V</i><br><br>Space (both):&nbsp;<i>V</i>	cost Graphs shortestpath
cRv0F&z0n{	What is the meaining of the term <i>'radix'&nbsp;</i>with respect to&nbsp;<i>strings?</i>	<i>Radix </i>refers to the <i>size </i>of the <i>alphabet </i>from which string characters are taken from	strings
coCy-Gt-+&	What is the&nbsp;<i>main idea/procedure</i>&nbsp;behind&nbsp;<i>key-indexed counting?</i>&nbsp;What are the assumptions we make?	"Assumption: Keys being compared are integers in [0, R-1] where R is the radix<br>Key idea:<br>We count the frequencies of each value in&nbsp;<i>R</i>, and use that to figure out where in the sorted array each value will start and end<br>1.) Count frequencies of each&nbsp;<i>value in R</i>&nbsp;using&nbsp;<i>key as index</i>&nbsp;<br>2.) Compute cumulative frequencies (to know where each letter starts in the final array. <br>- We are essentialy counting ""how many items smaller than x do we have"". If there are 5 items smaller, <i>x</i>&nbsp;values should start at index 5 in sorted array)<br>3.) Iterate through original array, putting each element into an auxiliary array at the indices specified in the cumulative frequency table. Each time we place an element, increment the value in the table so we know where to place the next letter with the same key<br>4.) Copy aux contents into original array<br><br>"	strings
QVb*xr>E|9	What are the&nbsp;<i>space&nbsp;</i>and&nbsp;<i>time&nbsp;</i>complexities of&nbsp;<i>key-indexed counting?</i>	Time: ~N+R<br>Space: ~N+R<br>Where&nbsp;<i>R</i>&nbsp;is the radix	cost strings
N(fCh`ukh[	Is&nbsp;<i>key-indexed counting in-place? Stable?</i>	In-place: No<br>Stable: Yes	strings trait
jJ@Msk_UN(	What is the&nbsp;<i>main idea/procedure</i>&nbsp;behind&nbsp;<i>LSD Radix sort?</i>&nbsp;What are the assumptions we make?	Stands for: Least-significant digit first string (radix) sort<br>Assumption: Keys all have the <i>same, </i>relatively<i> small length&nbsp;</i>&nbsp;<br>Main idea: <br>1.) Consider each character position of each string in turn, right to left<br>2.) Perform <i>key-indexed counting</i>&nbsp;on each string based on the character at the current position we are considering<br>- Once we have reached the end (leftmost character) of each string and performed the final&nbsp;<i>key-indexed&nbsp;count,&nbsp;</i>all strings will have been sorted in<i> lexicographical order.&nbsp;<br></i>NOTE: This works because KIC is stable	strings
dc4m:?Z;;g	What are the&nbsp;<i>space&nbsp;</i>and&nbsp;<i>time&nbsp;</i>complexities of&nbsp;<i>LSD Radix Sort?</i>	Time: ~W * N+R -&gt;&nbsp;<i>O(N)</i><br>Space: ~N+R -&gt;&nbsp;<i>O(N)</i><br>Where&nbsp;<i>R</i>&nbsp;is the radix	cost strings
d]Z-)0wax9	Is&nbsp;<i>LSD Radix Sort in-place? Stable?</i>	In-place: No<br>Stable: Yes (it is based on <i>key-indexed counting</i>, which is stable)	strings trait
K%-~9:[-_>	What is the&nbsp;<i>main idea/procedure</i>&nbsp;behind&nbsp;<i>MSD Radix Sort?</i>&nbsp;What are the assumptions we make?	Assumptions: None. Works with variable size strings<br>Key idea: Kind of like quicksort/bucket sort<br>1.) Consider each character position of the strings left to right<br>2.) Partition the strings using <i>key-indexed counting&nbsp;</i>based on their character at this position<br>3.) Recursively repeat from 2 on each partition created, until the bucket/partition of strings we are comparing only contains a single string&nbsp;	strings
"jcIGR#[j%#"	Provide an overview of what an&nbsp;<i>R-way trie</i>&nbsp;is	- A data structure that is a <i>tree </i>representation of a <i>symbol table</i>, used for storing strings in a way that provides <i>fast lookup</i> when <i>searching</i><br>- Each node represents/stores <i>1 character</i><br>- Each node has exactly&nbsp;<i>R</i>&nbsp;children - 1 for&nbsp;<i>each possible character </i>(this makes it fast but also very space inefficient)<br>- Nodes/characters that represent the end of a string can also contain&nbsp;<i>values</i>&nbsp;associated to that string. <br>- This is also how we know that a sequence of characters we have traversed through represents a full string - value = string end<br>Where&nbsp;<i>R</i>&nbsp;is the radix<br>	strings tries
"PU#!`g?Z2M"	Provide an overview of how an&nbsp;<i>R-way Trie&nbsp;</i>node is represented	Each node has:<br>- A <i>value </i>that is either NULL or an arbitrary value<br>- A&nbsp;<i>character-indexed&nbsp;</i>array, representing the node's connection to all other nodes (NULL = not in trie)<br>NOTE: <br>- Characters not explicitly represented in node, implicitly defined by link index instead<br>- Keys not explicitly represented either, instead implicitly represented by node location in trie	strings tries
y+m.M(j?ir	What are the&nbsp;<i>time</i>&nbsp;and&nbsp;<i>space</i>&nbsp;complexities of performing get and put operations on an&nbsp;<i>R-way trie?</i>	Time:<br>Get:&nbsp;<i>O(N)</i><br>Put:&nbsp;<i>O(N)</i><br><br>Space: Extremely inefficient for large&nbsp;<i>radix</i>. Each node will need to have a radix-sized array	cost strings tries
zGzDR[RzS^	What key things does the java compiler do to source code?&nbsp;	- Translates source code into bytecode, producing a .class file<br>Performs:<br>- Parsing (checks if code follows correct syntax)<br>- Analysis (checks if code is correct i.e scope rules are followed)<br>- Typechecking (and more)<br>	
lXA^3q)9|w	What key things does the java compiler <i>not&nbsp;</i>do to source code?&nbsp;	Does&nbsp;<b>not</b>&nbsp;<i>analyse </i>or&nbsp;<i>optimise</i>&nbsp;bytecode	
L4m*lEHmR{	What are the key features of the&nbsp;<i>JVM (Java virtual machine)?</i>	It consists of both an&nbsp;<i>interpreter</i>&nbsp;and a <i>just-in-time (JIT) compiler</i>&nbsp;<br>- Not an OS-style virtual machine<br>- Has a garbage collector, that automatically deallocates memory for objects with no reference	ke
bKHz]5V@s?	Provide a basic overview of what the <i>just-in-time (JIT) compiler&nbsp;</i>does	- Compiles and optimises frequently executed code at run-time<br>- JVM tracks how often code runs; frequently used code flagged as a&nbsp;<em>hotspot</em><br>- Once a threshold is passed, JIT compiles the hotspot into machine code for faster execution/efficiency	keyword
L[!1$f|!:y	Provide a basic overview of what the&nbsp;<i>Java virtual machine (JVM)&nbsp;</i>does	- The JVM executes bytecode produced by the&nbsp;<i>java compiler</i>&nbsp;(.class files)<br>- Loads, links and initialises classes dynamically&nbsp;<i>at runtime</i>	keyword
DE^~hQMhEl	How does java achieve <i>platform independence?</i>	- Java source code is <i>compiled into bytecode</i>, a platform-neutral format<br>- All Java compilers produce the <i>same bytecode</i> regardless of the platform<br>- But <i>JVMs are platform-specific</i>, interpreting the same bytecode differently. So the same bytecode can be run on different platforms using different JVMs	
mP.taGUEv,	What is meant by a language being&nbsp;<i>strongly typed?&nbsp;</i>Is java a&nbsp;<i>strongly typed&nbsp;</i>language?	A strongly typed language is a language that enforces strict type rules regarding type conversions and interactions between different types.<br><br>Yes - Java is strongly typed, with type checking being done at&nbsp;<i>compile-time</i>	
PpU?mpFrta	Provide an <i>overview </i>of the 2 <i>main types</i> in Java	1.) Primitive types<br>- Built-in, simple data types i.e&nbsp;<i>int</i>&nbsp;<br>- Not objects, stored directly in memory (though they do have wrapper classes)<br>- Offer faster performance but less functionality<br><br>2.) Class/reference types<br>- Represent objects and custom data types (i.e String)<br>- Hold a&nbsp;<i>reference</i>&nbsp;to objects in the heap<br>- Can access methods and fields/attributes of an object	
M7F)alB<8=	Provide a concise definition of the terms&nbsp;<i>'autoboxing'</i>&nbsp;and&nbsp;<i>'unboxing'&nbsp;</i>with respect to Java. Why does it matter?	"Autoboxing - automatic <i>conversion </i>from a <i>primitive </i>type to its <i>wrapper </i>class&nbsp;<br>i.e in the expression Integer x = 5; the int ""5"" is <i>autoboxed </i>to an object of class Integer<br><br>Unboxing - Automatic <i>conversion </i>from <i>wrapper </i>class to&nbsp;<i>primitive type</i>&nbsp;<br>i.e if we then do int y = x; the Integer x is <i>unboxed </i>into the primitive type int<br><br>Why - Allows seamless use of primitive with collections/generics (which require objects, not primitives)"	keyword
"cg%v^%1#^5"	What is the difference in doing<i>&nbsp;assignment</i>&nbsp;using&nbsp;<i>class</i>&nbsp;vs&nbsp;<i>primitive</i>&nbsp;types?	- For primitive types, the <i>value</i>&nbsp;(binary bits) are copied into the variable<br>- For&nbsp;<i>class/reference</i>&nbsp;types, the <i>object reference</i>&nbsp;is copied into the variable instead. Not the object itself - so the two variable will reference the same object	
lIK(=WVVAv	What is the difference in using the <i>comparison</i> (==)<i>&nbsp;</i>operator<i>&nbsp;</i>on&nbsp;<i>class</i>&nbsp;vs&nbsp;<i>primitive</i>&nbsp;types? How do we compare strings in Java?	- For <i>primitive types,&nbsp;</i>compares <i>values </i>(are the values the same?)<br>- For <i>class types</i>, compares&nbsp;<i>object references </i>(do they reference the same object in memory?)	
M~m:`pzRtU	How do we compare the contents of&nbsp;<i>Strings&nbsp;</i>in java?	"Doing str1 == str2 only compares whether or not str1 and str2&nbsp;<i>reference </i>the same object in memory, not necessarily the contents of the string<br><br>Instead need to use .equals() or .compareTo() method i.e <br><br>.equals() checks if contents are the same and returns a boolean indicating the answer so ""abc"".equals(""abc"") - returns True<br><br>.compareTo() compares the contents in&nbsp;<i>lexicographical/dictionary&nbsp;</i>order, so for str1.compareTo(str2) will return:<br>- 0 if str1 is equal to str2<br>- Negative number if str1 is ""less than"" str2<br>- Positive number if str1 is ""greater than"" str2<br>"	
d_-)Te-h9:	Pattern matching syntax for case expressions?	Matches cases on the&nbsp;<i>actual type</i>&nbsp;of the object being referenced, not the type of the variable (i.e Object)<br><br>switch (obj)&nbsp;<br>{<br>&nbsp; case String s -&gt; ...<br>&nbsp; case Integer s -&gt; ...<br>&nbsp; default -&gt; ...<br>}	
f.W_z30NY_	Provide an example of switch <i>statement </i>syntax	switch(val)<br>{<br>&nbsp; case 1:<br>&nbsp; &nbsp; ...;<br>&nbsp; &nbsp; break;<br>&nbsp; case 2:<br>&nbsp; &nbsp; ...;<br>&nbsp; &nbsp;break;<br>&nbsp; default:<br>&nbsp; &nbsp; break;<br>}	
t&2!$::vI%	Provide an example of switch <i>expression&nbsp;</i>syntax, including an example of a&nbsp;<i>compound statement</i> (different from statement), explaining how the&nbsp;<i>yield</i>&nbsp;keyword works	"Yield - used to <i>""return""</i> a value from a&nbsp;<i>case block </i>(i.e in a compound statement)&nbsp;within a <i>switch</i>&nbsp;<i>expression.&nbsp;</i>Tells java what value to assign to variable inside of a compound statement<br><br>int result = switch(val)<br>{<br>&nbsp; case ""a"" -&gt; 1;<br>&nbsp; case ""b"" -&gt; {&nbsp; //compound statement<br>&nbsp; &nbsp; if (something)<br>&nbsp; &nbsp; &nbsp; yield 2;<br>&nbsp; &nbsp; else<br>&nbsp; &nbsp; &nbsp; yield 3;<br>&nbsp; }<br>&nbsp; default -&gt; 0;&nbsp;&nbsp;<br>}"	
0N.s^lkrI	What is the meaning of&nbsp;<i>'scope'&nbsp;</i>with respect to Java? What are the 3 main types of scope?	Scope of a variable/named element refers to which area of program it is valid and can be referenced. Scope applies to&nbsp;<i>variables</i>,&nbsp;<i>methods,&nbsp;</i>and&nbsp;<i>classes&nbsp;</i>(via private/public)<br><br>Main types:<br>1 - Local scope, inside a block or method<br>2 - Method scope, accessible within the whole method<br>3 - Class scope, accessible by all class members	keyword
q(na5ftAaX	What is the meaning of&nbsp;<i>'lifetime'&nbsp;</i>with respect to Java? What is the lifetime of&nbsp;<i>local, parameter,&nbsp;</i>and&nbsp;<i>instance</i>&nbsp;variables?	Lifetime refers to the time period for which a variable or object exists within memory/is valid<br><br>Local - Lifetime of scope (exist only while scope is active)<br>Parameter - Same as local<br>Instance - Exist as long as the object holding them exists; same lifetime as that object	keyword
NhgHq/p^70	Explain what the term&nbsp;<i>'method'&nbsp;</i>means&nbsp;with respect to Java. Provide an overview of the 2 main types of <i>methods</i>	'Method' is a general term for a named block of code in a class - essentially the object-oriented version of functions<br><br>Two main types:<br>1.) Instance method <br>- Belong to an&nbsp;<i>instance</i>&nbsp;of a class (requires an object to be called)<br>- Declared <i>without </i>the&nbsp;static keyword<br>- Can access <i>instance variables</i> directly<br><br>2.) Static method<br>- Belongs to class itself, not any object (does not require an object to be called)<br>- Declared&nbsp;<i>with</i>&nbsp;static keyword<br>- Can access only&nbsp;<i>static variables</i>&nbsp;directly	
f|J%*Z(F7I	Provide a basic overview of&nbsp;<i>arrays</i>&nbsp;in Java, and the syntax for creating them	-&nbsp;<i>Fixed-size</i>&nbsp;data structure for storing values of the same type. Can be of size 0<br><i>-&nbsp;</i>Arrays in Java are&nbsp;<i>objects</i>, not primitives<br>- Can store values of any type - both&nbsp;<i>primitive</i>&nbsp;and&nbsp;<i>class/reference</i>&nbsp;types<br>- Arrays know their own size. Accessed through&nbsp;<i>length</i>&nbsp;attribute i.e arr.length<br>i.e<br>int[] numbers = new int[5];	
Cx(X_Q`.W(	Provide an example of <i>enhanced for loop&nbsp;</i>syntax	Given an array arr[] of ints, we can do<br>for (int n : arr)<br>{<br>&nbsp; ...<br>}<br><br>Equivalent to python syntax<br>for n in arr:	
ot2CDX99Sz	What is the meaning of the term&nbsp;<i>'container class'</i>&nbsp;with respect to java?	A container class is a class that holds or stores other objects i.e ArrayList	
B3p*k9ngrL	What is the syntax for accessing and appending elements to an <i>ArrayList</i>&nbsp;in&nbsp;<i>Java?</i>	for an arraylist a,<br>a.get(index)<br>a.add(item)	
"QpR%>#[lVV"	What is a <i>stream</i> in Java? Provide an overview on how they work	<div>A <i>stream </i>in Java is a pipeline of elements from a data source (like an array or list) that supports a sequence of operations to process those elements.</div><div>- Elements <i>flow </i>through one or more <i>intermediate operations</i> (like map() or filter() ), which transform or filter the data</div><div>- Finally, a <i>terminal operation</i> (like sum() or collect() ) is used to produce a result or side-effect<br>in Java is a pipeline of elements from a data source (like an array or list) that supports a sequence of operations to process those elements.</div>	
C-VV%(4.S&	How would we typically use a stream to sum the values in an array a?	Example:&nbsp;<br>int sum = Arrays.stream(a).sum();<br>- Arrays.stream(a) is a&nbsp;<i>static&nbsp;</i>method that creates an&nbsp;<i>IntStream</i>&nbsp;from an array<br>- .sum() is a&nbsp;<i>terminal operation</i>&nbsp;that reduces the stream to a single value by summing each element and producing a result	
wWW(5DV`O9	How would we use an <i>IntStream</i>&nbsp;to iterate through and sum the values in an array a?	int sum = IntStream.range(0, a.length).map(i -&gt; a[i]).sum();<br><br>- IntStream.range(0, a.length) generates a stream of index values from 0 to a.length-1<br>- map(i -&gt; a[i]) uses a lambda function to map each index value to the corresponding array value<br>- .sum() is the terminal operation adding up/reducing the stream into one value	
c,f2bQJi(}	Provide a few example of commonly used&nbsp;<i>static methods</i>&nbsp;in <i>java.util.Arrays</i>	1.) Arrays.sort(array) - sorts an array in-place<br>2.) Arrays.equals(a, b) - checks if 2 arrays have the same content (in the same indices)<br>3.) Arrays.binarySearch(array, key) - searches for a value in a&nbsp;<i>sorted</i>&nbsp;array<br>4.) Arrays.copyOf(array, newLength) - copies an array to new size, kind of like realloc<br>5.) Arrays.toString(array) - returns a readable string of an array i.e [1,2,3]&nbsp;	
d*lJgM^F`.	What is the meaning of the term&nbsp;<i>'Class'</i>&nbsp;with respect to <i>OOP?</i>	A class a template/blueprint/mold for the creation of objects in OOP. A class:<br>- Defines the&nbsp;<i>structure</i>&nbsp;and&nbsp;<i>behaviour</i>&nbsp;of an&nbsp;<i>instance object</i><br>- Is a collection of&nbsp;<i>instance variables/attributes,</i>&nbsp;representing the&nbsp;<i>state</i>&nbsp;of an object or its&nbsp;<i>value</i><br>- Is a collection of&nbsp;<i>instance methods</i>, which can be called on the object, representing the object's&nbsp;<i>behaviour</i>	
hr}Hu2th_d	Define the Computer Science&nbsp;term&nbsp;<i>'Abstraction'</i>&nbsp;	Abstraction is the process of&nbsp;<i>hiding the internal details</i>&nbsp;of an object or system and showing only the&nbsp;<i>essential features</i>&nbsp;<br>- Helps reduce complexity by focusing only on what is relevant to the user/person<br>- Abstraction refers to the act of <i>removing unnecessary</i><b> </b>or <i>extra details</i> from a problem so it's easier to understand and solve by focusing on the most important/distinguishing aspects<br>- With respect to OOP, allows us to focus on <i>what </i>an object does rather than&nbsp;<i>how</i>&nbsp;it does it	keyword
t-TB:}GMX,	What are some key benefits of using <i>Classes</i>&nbsp;in programming?	Modularity - Classes make it easy to <i>break down/divide</i> complex systems into manageable self-contained chunks<br>Code reusability - Can define a class once and create multiple objects from it<br>Encapsulation - Classes let us keep internal details private (<i>information hiding</i>) and expose only what is absolutely necessary through public methods<br>	
"ndj6#5YW}f"	What does the <i>'private' keyword </i>mean when used with a variable or method in Java?	Note: A class defined a scope<br>- Declaring a&nbsp;<i>method</i>&nbsp;or&nbsp;<i>variable</i>&nbsp;as&nbsp;<i>private</i>&nbsp;means that it can be accessed <i>only</i>&nbsp;within the scope of the class i.e within a method body or instance variable initialisation&nbsp;<br>- Lets us protect the <i>internal state&nbsp;</i>of an object as well as do <i>information hiding</i>	keyword
h3-Ar/Gv/E	What does the '<i>public' keyword&nbsp;</i>mean when used with a variable or method in Java?	Declaring a method or variable as <i>public </i>means that it can be accessed from any other class where the class is visible<br>- At <i>compile-time</i>, methods and variables declared as <i>public </i>can be used by any code where the <i>class is in scope</i> (i.e through an import statement)<br>- At <i>run-time</i>, methods and variables declared <i>public </i>can be accessed by anything that <i>has a reference</i> to an <i>object</i> of the class	keyword
xF4,pr.G6Y	What is the meaning of the term&nbsp;<i>'encapsulation'</i>&nbsp;with respect to <i>OOP?</i>	Encapsulation is really 2 things:<br>- The concept of <i>bundling data </i>(variables) and <i>methods </i>that operate on that data into a single unit (a class)<br>- The practice of hiding/protecting the <i>internal state</i> and <i>implementation details</i> of an object, and exposing only a controlled <i>public interface</i> for interaction (for information hiding and to proect against mistakes)	keyword
b46t{Ii2kg	What is a <i>'reference'</i>&nbsp;in Java?	- A variable of&nbsp;<i>class/reference type</i>&nbsp;holds a&nbsp;<i>reference</i>&nbsp;to an object<br>- The reference itself is&nbsp;a value denoting the (abstract) location of an object (it is not a memory address like with a pointer)<br>- A reference does&nbsp;<i>not</i>&nbsp;store the object itself, only a reference to it<br>	keyword
jFRZ.jecz9	What is the <b>CIA Triad</b>, and what are the names of the three properties it defines?	The CIA Triad is a general set of Information Security (InfoSec) properties that any system&nbsp;processing, storing, or transmitting&nbsp;information must maintain to be secure. A given system will usually have to strike a balance between satisfying these 3 properties, as some may need to be prioritised more than others.<br><br>The three properties are:<br>-<b> C</b>onfidentiality<br>-<b> I</b>ntegrity<br>- <b>A</b>vailability	week1
"kUq19%v#Td"	What is meant by <b>Information Security</b> (InfoSec)?	<b>Information Security&nbsp;</b>(InfoSec) is the practice of protecting information and its systems (things that handle the information) from unauthorised access, use, disclosure, disruption, modification, or destruction. These systems typically <b>process, store, and transmit</b>&nbsp;information.	defn week1
kI6Yf}1Y8C	In the context of the CIA Triad, what is the property of <i>Confidentiality</i>?	Confidentiality is the property that the information in the system is <b>not </b>made avaialble or disclosed to unauthorised individuals, entities or processes (e.g. malware).<br><br>Equivalently, it is the property that ensures information is accessed&nbsp;<b>only</b>&nbsp;by authorised individuals, entities or processes	CIA confidentiality defn week1
"e#VjtjpAQo"	In the context of the CIA Triad, what is the property of&nbsp;<i>Integrity</i>?	<div><b>Integrity</b> is the property that information is <b>not</b> altered or destroyed in an unauthorised manner.</div><div><br></div><div>Equivalently, it is the property that ensures information remains <b>accurate, complete, and trustworthy</b> throughout its entire life cycle (during processing, storage, and transmission).</div>	CIA defn Integrity week1
J@,:]S0RBK	What is meant by the&nbsp;<i>correctness</i>&nbsp;of data/information?	Data/information is <b>correct</b> if it is both <b>accurate</b> (contains the right value) and <b>complete</b> (nothing is missing)	defn week1
mo_Rxe>Srn	In the context of the CIA Triad, what is the property of&nbsp;<i>Availability, </i>and&nbsp;<b>why </b>is it important?	<b>Availability</b> is the property that ensures information and systems are accessible and usable on demand by an authorised entity. It represents the degree of <i>accessibility</i> of a system's resources.<br><br>For an information system to be useful and serve its purpose, its resources must be available when required, which is what the <b>Availability</b> property requires.	availability CIA defn week1
kY/A-aRPkp	In the context of <b>Information Security</b>, what is the property of <i>Privacy</i>?	<div><b>Privacy</b> is the right of an individual or entity to control how their personal information is collected, used, and shared.</div><div><br></div><div>It is the idea that Information should not be visible or accessible to others unless the owner explicitly allows it/provides consent.</div>	InfoSec privacy week1
erYC3ZBViE	In the context of&nbsp;<b>Information Security</b>, what is the difference between Privacy and Confidentiality?	Confidentiality is about the system preventing unauthorised access to entities/individuals, whereas Privacy is about the individual choosing if their data should be accessible in the first place.	CIA confidentiality InfoSec privacy week1
p{)q)H/=t(	In the context of&nbsp;<b>Information Security</b>, what is the property of <i>Authenticity</i>?	<b>Authenticity</b> is the property that ensures the system can prove that a person, entity, or message is actually who or what they claim to be. It allows the system to verify the truth of a claim so that an imposter cannot pretend to be a legitimate user (e.g. via some authentication system).&nbsp;	authenticity defn InfoSec week1
qB(b<|[q;>	In the context of&nbsp;<b>Information Security</b>, what is the property of&nbsp;<i>Non-repudiation</i>?	<b>Non-repudiation</b> is the property that ensures an entity cannot deny performing a specific action, like performing a transaction or sending a message. It requires the system to generate and maintain evidence (like digital signatures or logs) that links the action specifically to that user.	defn InfoSec non-repudiation week1
jc[~y+[[HV	In the context of&nbsp;<b>Information Security</b>, what is the property of&nbsp;<i>Anonimity</i>?	<i>Anonimity</i>&nbsp;is the property that ensures a person or entity using the system cannot be identified. It also means that an individual’s real identity cannot be derived from the information or activities recorded within the system.	anonimity defn InfoSec week1
"d8(#6f?[Ir"	In the context of the <b>Information Security</b>, what is the property of&nbsp;<i>Forward secrecy</i>?	<div><b>Note:&nbsp;</b>Forward secrecy is a property of communications systems in particular</div><div><b><br></b></div><div><b>Forward Secrecy </b>is the property that ensures past communications within the system remain secure even if the system is compromised in the future.</div><div>Equivalently, it ensures that an adversary who records encrypted information today cannot access or read it later, even if they eventually gain full control over the system (e.g. they gain access to the main encryption key).</div>	defn Forward-secrecy InfoSec week1
uz^urEgsOV	What are the 2 main approaches in defining the <b>security </b>of a system?&nbsp;	<ol><li><b>Binary model:</b> The approach that defines a system as either <b>secure</b> or <b>not secure </b>(no inbetween). The rule is that if a system meets every single security requirement/property, it is considered secure. Otherwise, it is considered insecure.</li><li><div><b>Risk-based Model:</b>&nbsp;A system is considered secure if it satisfies the security properties we care about to a degree that is acceptable. The idea is that since we know no system is 100% secure, we focus on making the level of security atleast good enough, balancing the cost of protection against the likelihood and damage of an attack.</div></li></ol>	InfoSec week1
edw4lCcDk}	What is the meaning of&nbsp;<i>threat modelling</i>?	Threat modelling is the process of identifying, categorising and prioritising potential security risks to a system&nbsp;<b>before</b>&nbsp;they happen	defn week1
KPV<y:2XyR	What is the <b>STRIDE</b> acronym/mnemonic in <i>Threat Modelling,&nbsp;and what do the letters stand for?</i>	<b>STRIDE</b> is a model used to identify and categorise threats into six main types of security threats.<br><br>The letters stand for:<br><b>S</b>poofing&nbsp;<br><b>T</b>ampering&nbsp;<br><b>R</b>epudiation<br><b>I</b>nformation Disclosure<br><b>D</b>enial of service&nbsp;<br><b>E</b>levation of privilege&nbsp;	defn STRIDE Threat_modelling week1
vpAJXZ[oH`	In the context of <b>STRIDE</b>, what does&nbsp;<i>Spoofing&nbsp;</i>involve, and what security property is violated by&nbsp;it??	<i>Spoofing </i>violates <b>Authenticity</b>. It occurs when an entity pretends to be a different person, system or message to gain access to a restricted system.	STRIDE Threat_modelling week1
r2r27`*YM.	In the context of <b>STRIDE</b>, what does&nbsp;<i>Tampering&nbsp;</i>involve, and what security property is violated by&nbsp;it??	<i>Tampering </i>violates <b>Integrity</b>. It involves the unauthorised modification of data, code or communication.	STRIDE Threat_modelling week1
OEn!2zMet4	In the context of <b>STRIDE</b>, what does&nbsp;<i>Information Disclosure </i>involve, and what security property is violated by&nbsp;it?	<i>Information Disclosure</i> violates <b>Confidentiality</b>. It involves exposing private or sensitive data to individuals who are not authorised to see it.	STRIDE Threat_modelling week1
ELWzV/E2_H	In the context of <b>STRIDE</b>, what does&nbsp;<i>Denial of Service&nbsp;</i>involve, and what security property is violated by&nbsp;it??	<i>Denial of Service</i> violates <b>Availability</b>. It occurs when an attacker crashes or slows down a system so that legitimate users cannot access it	STRIDE Threat_modelling week1
eL%W*2loK{	In the context of <b>STRIDE</b>, what does&nbsp;<i>Elevation of Privilege&nbsp;</i>involve, and what security property is violated by&nbsp;it??	<i>Elevation of Privilege</i> violates <i>Authorisation</i>. It occurs when a user gains more permissions or access rights to a system than they are supposed to have.	STRIDE Threat_modelling week1
MIFs,Ck$Ug	In the context of <b>STRIDE</b>, what does&nbsp;<i>Repudiation </i>involve, and what security property is violated by&nbsp;it?	<i>Repudiation </i>violates <b>Non-repudiation</b>. It occurs when a user denies performing an action because the system lacks sufficient evidence to prove they did it.	STRIDE Threat_modelling week1
OVEUo{-;%-	What is meant by a&nbsp;<i>threat</i>, and how does it differ from a&nbsp;<i>vulnerability?</i>	"A threat is a potential event or action that could cause harm, while a vulnerability is a weakness or flaw in a system that makes a threat possible. They differ in the sense that a threat is the ""what"", while a vulnerability is the ""how""<br>"	defn week1
E)q/HFc@,O	In the context of a <b>Web Server</b>, give two examples of a security <i>Threat</i>, an associated <i>Vulnerability</i>, and the <i>Security Principle</i> violated.	<div>1.)</div><div><b>Threat:</b> An attacker performing a Denial of Service (DoS) attack to take the web server down.</div><div><b>Vulnerability:</b>&nbsp;A lack of security measures within the system, such as <b>rate limiters</b> or <b>backup servers</b>, to handle excessive traffic or system failure.</div><div><b>Principle violated:</b> Availability</div><div><br></div><div>2.)</div><div><b>Threat:</b> An attacker gaining administrative access&nbsp;to take full control of the server</div><div><b>Vulnerability:</b> Running outdated server software that has a known, unpatched security flaw (a security exploit)</div><div><b>Principle Violated:&nbsp;</b>Authorisation</div>	question week1
"r#Ip#.uO)F"	In <b>Threat modelling</b>, what is <i>Likelihood </i>and what two main factors influence it?	"<div><b>Likelihood</b> is the probability that a threat will successfully exploit a vulnerability in the system.
It is mainly influenced by:</div><ol><li><b>Attacker Motivation: </b>How much will they benefit from exploiting the system? (e.g. For a bank, the likelihood of an attack is high as people want money).&nbsp;</li><li><b>Ease of Exploitation: </b>How easy is exploiting the vulnerability? (e.g. Does the attacker have the tools/skills to break the vault, and is the risk worth it?).</li></ol>"	Threat_modelling week1
C6NlMuLJv|	In <b>Threat modelling</b>, what is&nbsp;<i>Impact</i>?	<b>Impact</b> is the (moreso qualitative) measure of the total harm, loss or damage caused to an organisation if a threat is successfully exploited using a vulnerability within the system.	Threat_modelling week1
on`x2{~4OA	When creating a <i>threat model</i>, what are the 5 main factors we consider?	Threats<br>Vulnerabilities<br>Likelihood (of a threat exploiting a vulnerability)<br>Impact (Qualitative cost)<br>Cost (Quantitative cost)	Threat_modelling week1
y})^$Aj$0V	In <b>Information Security</b>,<i>&nbsp;</i>what is a&nbsp;<i>Security Policy&nbsp;</i>and how does it differ from a <i>Security Mechanism</i>?	A <b>Security Policy</b> is a definition of the set of rules, roles and procedures an organisation must follow to protect its assets and interests, whereas a <b>Security Mechanism</b> is the technical tool or method that enforces the policy	defn InfoSec Threat_modelling week1
Q:EG2oXvIW	"Under what conditions is a system formally considered <b>""secure""</b>?"	A system is considered secure if an <b>adversary</b>, constrained by a specific <i>threat model</i>, cannot violate the <i>security policy</i>.	defn InfoSec Threat_modelling week1
qwnQ)J<4R;	What is a <i>Security Arguement</i>?	A <b>rigorous argument</b> or proof grounded by evidence demonstrating that the chosen <b>security mechanisms</b> are successfully maintaining the <b>security policy</b>, subject to the assumptions of the <b>threat model</b>.	defn InfoSec Threat_modelling week1
PH1^=;<S<A	What six (or seven...) key factors must a <b>Security Policy</b> address to be effective?	"A good policy should address:<br><b>Threats:</b> What/Who are we afraid of? (The Adversary)<br><b>Vulnerabilities:</b> Where are the system's weaknesses? <br><b>Likelihood:</b> How probable is a successful attack?<br><b>Impact:</b> How much harm will an attack cause if it happens?
<br><b>Protection:</b> What specific <b>security mechanisms </b>will we use to enforce the policy?<br>(User) <b>Goals: </b>Why are people using the system? How do we make the policy strict enough to maintain security but lax enough to make the system <b>usable </b>for the user's intended goals?"	human_centred InfoSec week1
D4`ZoMD@0w	In the context of <b>Human-Centred Security,</b><i>&nbsp;</i>what is meant by&nbsp;<i>usability?</i>	Usability is a&nbsp;measure of how well a specific user in a specific context can use a<br>product/design to achieve a defined goal effectively, efficiently and<br>satisfactorily.	human_centred InfoSec week1
DtPeqP`,Z8	What are the risks of making a security policy too restrictive or complex?	Overly secure systems can become <b>unusable,&nbsp;</b>causing:<ul><li><div><b>Workarounds:</b> Users bypass annoying security to save time (e.g., writing passwords on post it notes).</div></li><li><div><b>Non compliance (productivity-first behaviour):</b> Users prioritise <b>productivity</b> over safety to finish their work (e.g. transferring files over a fast tool like dropbox rather than a secure in-house one)</div></li><li><div><b>Human Error:</b> Complex rules increase the likelihood of accidental mistakes or using predictable patterns (e.g. reusing the same password with small changes)</div></li></ul>	human_centred week1
HDdJ%s8;nV	What are the <i>Saltzer and Schroeder</i> principles?	"A foundational set of <b>8 design principles</b> used to build secure and reliable computer systems. They provide a philosophy for security design, focusing on <b>simplicity</b>, <b>least privilege</b>&nbsp;and restricting access to the absolute minimum required for a task.<br><br>Read through this atleast once:&nbsp;<a href=""https://shostack.org/blog/the-security-principles-of-saltzer-and-schroeder"">https://shostack.org/blog/the-security-principles-of-saltzer-and-schroeder</a>"	defn saltzer_schroeder week2
QwmRItLDK{	What are the <b>8 security design principles</b> defined by <i>Saltzer and Schroeder</i>?	1.&nbsp;<b>E</b>conomy of Mechanism<br>2.  <b>L</b>east Privilege<br>3.  <b>L</b>east Common Mechanism<br>4. <b>F</b>ail-Safe Defaults<br><br>5. <b>C</b>omplete Mediation<br>6. <b>O</b>pen Design<br>7.  <b>P</b>sychological Acceptability<br>8. <b>S</b>eparation of Privilege<br><br>Mnemonic (courtesy of professor...): ELLF COPS	defn saltzer_schroeder week2
t3{4LUf]9g	What is the <i>Saltzer and Schroeder&nbsp;</i>pinciple of&nbsp;<b>Economy of Mechanism?</b>	<div>Keep the design and implementation of security measures as <b>simple and small</b> as possible.</div><div><br></div><div>Complex designs are harder to test, verify and more likely to contain hidden bugs or vulnerabilities. The idea is that the simpler the systems, the easier they are to understand, and the less likely they are to contain flaws</div>	defn saltzer_schroeder week2
zH];Fh6A5T	What is the&nbsp;<i>Saltzer and Schroeder&nbsp;</i>pinciple of&nbsp;<b>Fail-safe Defaults?</b>	Base access decisions on <b>permission</b> rather than exclusion. The default action of the system should be to <b>deny access</b> to everyone (denial of service) unless a user is <b>explicitly allowed</b> to enter. <br><br>The idea is that it is better for an <i>authorised</i> entity to accidentally be <b>denied </b>access (easy to detect and fix), than to allow an <i>unauthorised </i>entity to accidentally be <b>granted </b>access (which could happen if the default is to give everyone access unless there is a reason not to, and is hard to detect and fix)	defn saltzer_schroeder week2
oL&L2$jRI-	What is the&nbsp;<i>Saltzer and Schroeder&nbsp;</i>pinciple of&nbsp;<b>Complete Mediation?</b>	Every access to a system resource must be continuously checked for authority. Instead of just verifying permissions at login or initial access, the system must actively mediate and validate all subsequent actions. <br><br>This continuous verification ensures that if a user's rights are revoked while they are still logged in/accessing the system, they are immediately prevented from further interacting with the protected resource	defn saltzer_schroeder week2
PY>4I&l0d4	What is meant by <b>Security by Obscurity, </b>and<b> </b><i>why </i>does it<b> fail?</b>	"<b>Security by Obscurity</b> is a a flawed approach to <b>system security&nbsp;</b>where protection relies entirely on hiding how the system works (its code, architecture or mechanics). It assumes attackers simply won't figure out the hidden flaws.<br><br>It fails because secrecy is fragile. Once an attacker reverse-engineers or leaks the ""secret"" design, the entire defense can collapse since there may be no actual strong barriers (like proper encryption or access controls) stopping them."	defn week2
L}o]e+;!xp	What is the&nbsp;<i>Saltzer and Schroeder&nbsp;</i>pinciple of&nbsp;<b>Open Design?</b>	The security of a system should not depend on the secrecy of its design or implementation. Instead of relying on <b>Security by Obscurity,</b>&nbsp;the architecture should be assumed public, with protection relying entirely on specific, easily changeable secrets like passwords or cryptographic keys.&nbsp;<br><br>The core idea is that a system must remain secure even if an attacker knows exactly how it is designed. Security should rely on them not having the <i>means</i> (e.g. a cryptographic key) to bypass it, rather than not knowing how it works	defn saltzer_schroeder week2
D|6U</1@s!	What is the&nbsp;<i>Saltzer and Schroeder&nbsp;</i>pinciple of&nbsp;<b>Separation of Privilege?</b>	<div>A system should require multiple independent conditions, keys or parties to authorise access to highly sensitive resources. Rather than letting one mechanism or person control the entire decision, authorisation/access is divided into multiple distinct requirements, kind of like a secure bank vault that requires two separate keys held by two different people to open.</div><div><br></div><div>This ensures that a single point of failure, such as one compromised password or a single rogue employee, cannot independently breach the system.<br></div>	defn saltzer_schroeder week2
FV:E-s%^98	What is the&nbsp;<i>Saltzer and Schroeder&nbsp;</i>pinciple of&nbsp;<b>Least Privilege?</b>	Every user, program or process must operate using only the absolute <b>minimum permissions necessary </b>to complete its specific job. By strictly limiting access rights to only what is required, we minimise the <i>impact </i>of a security breach.<br><br>For instance, if an attacker compromises a standard account, they are trapped within those limited, non-admin permissions. This prevents them from causing widespread damage or taking over the entire system.	defn saltzer_schroeder week2
bOp!Txv&[I	What is the&nbsp;<i>Saltzer and Schroeder&nbsp;</i>pinciple of&nbsp;<b>Least Common Mechanism?</b>	The security design should minimise the amount of shared resources or state between different users and processes. The idea is that shared mechanisms, like shared memory, temporary files or network paths, creates channels through which information could leak from one user to another, or where one user's actions could disrupt everyone else's. Isolating users prevents potential cross-interference.<br><br>For example on a computer with multiple user accounts, one user should not be able to see the files of another user, even on the same computer.	defn saltzer_schroeder week2
o{Vh07.n![	What is the&nbsp;<i>Saltzer and Schroeder&nbsp;</i>pinciple of&nbsp;<b>Psychological Acceptability?</b>	Security mechanisms must be easy to use and transparent. If a security requirement is too complex or frustrating, users will find workarounds to bypass it, such as writing passwords on post it notes or leaving secure doors open.<br><br>The core idea is that a system is secure only if the people using it readily accept the measures and follow them routinely without feeling burdened.	defn saltzer_schroeder week2
JVY%0>}Ei?	What is the&nbsp;<b>security design&nbsp;principle</b>&nbsp;of <i>Defense in Depth?</i>	Design the system with multiple layers of security mechanisms so it can resist an attack even if one layer is bypassed. <br><br>This ensures that a single vulnerability or point of failure does not allow an attacker to gain complete control of the system. As a last resort, if an error cannot be handled, intentionally crash or shut down the system. A temporary outage is always safer than remaining online and allowing potential data theft or unauthorised access.	defn week2
OWm9qK:J|g	What is the&nbsp;<b>security design&nbsp;principle</b>&nbsp;of&nbsp;<i>Design for Updating</i><b>?</b>	Plan for the safe and reliable installation of security patches. Since no system remains permanently free from vulnerabilities forever, developers must build a secure, structured mechanism to update the system as new flaws are discovered.	defn week2
j[cQ2PiIuG	What is the&nbsp;<b>security design&nbsp;principle</b>&nbsp;of&nbsp;<i>Prudent Paranoia?</i>	Assume the system operates in a hostile environment and will inevitably face attacks from adversaries.<br><br>Design defenses anticipating that breaches will occur, without underestimatting the effort an adversary will go to. Security design should be proactive rather than reactive.<br><br>Do not trust any user, input or internal component without verification.	defn week2
"s|#Q^?s&Wl"	What is the&nbsp;<b>security design&nbsp;principle</b>&nbsp;of&nbsp;<i>Privacy Promotion?</i>	Design systems to protect user privacy by default (to abide with GDPR laws). Minimise the collection, use and retention of personal data to only what is absolutely necessary.<br><br>Ensure that sensitive information is never exposed, tracked without consent or used beyond its original and intended purpose.	defn week2
gJ&=Rd`[ZJ	What is a <b>URL</b>, and what are its 3 main components?	<div>A <b>URL&nbsp;</b>(Uniform Resource Locator) is a standard string used to locate resources/files on a network. Its 3 main components (with respect to example URL&nbsp;<code>https://website.com/hi</code>) are:&nbsp;</div><div><b>&nbsp;</b></div><div><b>1.) Protocol Specifier:</b> Specifies the network protocol (rules of communication) to be used (e.g.&nbsp;<code>https://)</code></div><div><b>2.) Domain Name (Host):</b> The server's human-readable address (e.g.&nbsp;<code>website.com</code>).</div><div><b>3</b>.)&nbsp;<b>Page Location (Path):</b> The specific file or directory to be requested from that server (e.g.&nbsp;<code>/hi.html</code>)<br></div>	defn networking week2
G|DgU,2:yw	What is the <b>Domain Name System</b> (DNS)?&nbsp;	The DNS is a distributed, hierarchical naming system used to translate human-readable domain names (e.g. google.com) into numerical <b>IP addresses</b>. <br><br>It <b>consists of</b> a global network of <b>domain name servers</b> that partition and store data across different levels (Root, TLD, Authoritative). This allows computers to locate a specific host server so that <b>requests</b> can be <b>routed</b> to it.	defn networking week2
t_]?VEW)r]	What is the difference between&nbsp;the <b>Domain Name System (DNS)</b>&nbsp;and a&nbsp;<i>Domain Name Server?</i>	<div></div>The <b>Domain Name Sysem </b>is&nbsp;the overall system, protocol and global hierarchy used to translate names to IP addresses, whereas a <b>Domain Name Server</b>&nbsp;is a specific <b>computer/server</b>&nbsp;within that system that stores naming data and responds to requests.<br>	defn networking week2
"eP#Ff8w!ne"	In the context of <b>Networking</b>, what is&nbsp;<i>Domain Name Resolution?</i>	Domain Name Resolution is the process performed by the <b>DNS</b> to translate a human-readable domain name into its numerical <b>IP address</b>.<br><br><div>This involves a <b>top-down search</b> through the DNS hierarchy: the <b>Root</b> server redirects the query based on the <b>extension</b> (e.g. .com) to a <b>TLD</b> server, which then redirects the query based on the <b>domain name</b> (e.g. bob.com) until it reaches the <b>Authoritative</b> server, which provides the final <b>IP address</b><br></div>	defn networking week2
vViVUW=IhI	What are the two main <b>Networking </b>steps required to access a <i>website?</i>	<b>1.) Locating the IP address:</b>&nbsp;Using the&nbsp;<b>DNS</b>&nbsp;to translate the human-readable domain name (found within the URL) into the host's numerical&nbsp;<b>IP address</b>.<br><br><b>2.)&nbsp;Requesting the Content:</b>&nbsp;Using the IP address obtained from the&nbsp;<b>DNS&nbsp;</b>to send&nbsp;<b>data packets</b>&nbsp;containing the request (abiding by the network protocol specified in the URL) the host server so it can send the website content back. This is usually either done through&nbsp;<b>Leased Lines&nbsp;</b>or the&nbsp;<b>Internet's infastructure (Internet backbone)</b>	networking week2
Bl`eG!8$^~	In the context of&nbsp;<b>Networking,&nbsp;</b>what is a&nbsp;<i>Packet?</i>	<div>A packet is a <b>formatted </b>(format specified by network protocol) <b>unit of data</b> that travels across a network. Large files or requests are broken down into a sequence of smaller chunks (packets) to make them easier to transmit and route efficiently.</div><div><br></div><div>A packet contains two main things:</div><div>1.)&nbsp;<b>The Payload</b>, which is the&nbsp;actual piece of content being sent (e.g., part of a photo or a line of code)</div><div>2.)&nbsp;<b>The Header</b>, which contains the metadata required for delivery and reassembly. It identifies the <b>source </b>and <b>destination IP addresses</b> for routing and includes <b>sequence numbers</b> so the receiving computer knows how to put the packets back together in the correct order. It also contains other information, like a packet's Time to Live (TTL)</div>	defn networking week2
kd:ggB(f1/	In the context of&nbsp;<b>Networking</b><i>,</i>&nbsp;what are&nbsp;<b><i>Leased Lines</i></b> and what are their trade-offs?	<div>A leased line is a <b>direct, private connection</b> used as a method of transmitting data between two specific locations. It allows data to travel through an uncontended path that bypasses the public internet.<br></div><div><br></div><div><b>Pros:</b></div><ul><li><b>Incredibly fast: </b>Since the line is dedicated, there is no competition for bandwidth</li><li><b>Reliable:&nbsp;</b>Network performance is alot more consistent because there is no typical public network congestion</li><li><b>Secure: </b>Data is transmitted over a private path, making it much harder to intercept</li></ul><div><b>Cons:</b></div><ul><li><b>Incredibly expensive: </b>The cost of renting or maintaining a dedicated physical connection is significantly higher than using existing public infrastructure</li></ul>	defn networking week2
"Dbl(yt*s#q"	What is the <b>Internet Backbone</b>?	Essentially the physical infastructure of the internet. It is the global network of high-speed fiber-optic cables and core routers that provides the main data routes used to connect different networks (global networks, ISPs) across the world.	defn networking week2
w[e{}dJk%8	In&nbsp;<b>Networking</b>, what is a packet's Time to Live (TTL) and why is it necessary?	"<div><b></b>TTL is a value (usually 8 bits) in the <b>packet header</b> that sets a limit on how long a packet can travel through the network.&nbsp;</div><div><br></div><div>Its purpose is to prevent packets from getting stuck in an infinite loop (going around in a circle) if they can't find their destination.</div><div><br></div><div>Every time a packet passes through a router (a ""hop""), the TTL value is decreased by one. If the TTL reaches zero before the packet reaches its destination, the packet is ""killed"" or discarded.</div>"	networking week2
AIB=Y7qHW/	&nbsp;What is the main difference between <i>HTTP </i>and <i>HTTPS?</i>	<div><b>HTTP</b> (Hypertext Transfer Protocol) and <b>HTTPS</b> (Hypertext Transfer Protocol <i>Secure</i>) are both networking protocols, however, unlike HTTP, HTTPS requires that the entire communication be <b>encrypted.</b></div><div><b><br></b></div><div>This means that if HTTPS packets are intercepted/stolen (e.g. via a Man-in-the-middle attack), the data is unreadable to an attacker without the proper decryption key. In contrast, HTTP transmits data in <b>plaintext</b> with no encryption, meaning any intercepted data is immediately readable by unauthorised parties.</div>	networking week2
m:QN.nE4b<	What is the complete end-to-end process of accessing a website, from URL entry to the request being fulfilled? (long answer)	<b></b><div><div>First, the requesting device uses the <b>DNS</b> to translate the human-readable domain name from the <b>URL</b> into the host's numerical <b>IP address</b>.</div><div><br></div><div>Once the IP address is identified, the device sends a request following the rules of the <b>network protocol</b> (like HTTPS) specified in the URL. This request is split into many <b>packets</b>, each containing a <b>header</b> (with its sequence number and <b>Time to Live,&nbsp;</b>among other things) and the <b>payload </b>(actual data) to navigate the physical network infrastructure - either the public <b>Internet Backbone</b> or a private <b>Leased Line</b>.</div><div><br></div><div>When the <b>host server</b> receives the request packets, it processes them and prepares a <b>response</b> containing the website's data. The response is then sent to the requesting device (host server know the requester's <b>IP address</b>). This response is also split into <b>packets</b> with their own headers and TTL values to safely navigate back through the routers without getting stuck.</div><div><br></div><div>When the packets arrive back at the requesting device, it uses the sequence numbers in the headers to <b>reconstruct</b> the data so the browser can <b>render the HTML</b> and display the site.</div></div>	networking week2
xSz>r:MS8x	What are two examples of <b>human limitations</b> that impact security? (4 marks)	<div>One limitation is <b>human memory</b>. Humans can struggle to remember long, complex and unique passwords for every account, often leading to password reuse or writing passwords down, which can be a problem for the security of a system that depends on passwords.</div><div><br></div><div>Another limitation is that humans tend to have a <b>productivity-first mindset</b> (e.g. a preference for efficiency). This means that if a security policy is deemed too slow or inconvenient for a user, they may attempt to circumvent or find workarounds for the security policy, which could create vulnerabilities.</div><div><br></div><div>(sidenote) Other possible limitations:&nbsp;</div><div><ul><li>Susceptibility to social engineering (might reveal information to someone they shouldnt reveal it to)</li><li>Limited technical literacy (humans might not have a good mental model of how things like the internet work, leading to wrong assumptions e.g. the padlock icon doesnt mean that a site is safe, only that it is encrypted)</li><li>Human fatigue e.g. a tired person might press the wrong button and accidentally grant permissions to something they shouldn't have</li><li>Human ego/Optimism bias e.g. a person may think that there is no way&nbsp;<i>they</i>&nbsp;would fall for a cyber attack, causing them to maybe neglect certain security precautions (like having a good password)&nbsp;</li></ul></div>	human_centred question week1
lgF:foF&mK	Requesting more permissions than needed is a violation of which of the&nbsp;<i>Saltzer and Schroeder's</i> design principles?<br><br>Briefly&nbsp;<b>describe&nbsp;</b>the principle, and&nbsp;<b>why&nbsp;</b>it is being violated.	This is a violation of the principle of<b> Least Privilege</b>. The principle states that every user or program should operate using only the <b>minimum set of privileges</b> necessary to complete their task. Requesting excess permissions creates unnecessary security risks if the account or program is compromised, making it a violation of this principle.	question saltzer_schroeder week2
B!,<M(%}:a	A new user account is created with full administrative access by default, requiring an admin to manually restrict them.&nbsp;<br><br>This is a violation of which&nbsp;<i>Saltzer and Schroeder</i>&nbsp;principle<b>?&nbsp;</b>Briefly <b>describe </b>the principle, and <b>why </b>it is being violated.	This is a violation of the <b>Fail-safe Defaults</b> principle. The principle states that access decisions should be based on permission rather than exclusion, meaning the default state should be to&nbsp;<b>deny</b> access rather than grant it. By defaulting to <b>allowing access,&nbsp;</b>the principle is violated.	question saltzer_schroeder week2
tlxZ.pqcj<	A company uses a secret, proprietary encryption algorithm that has never been peer-reviewed, believing secrecy makes it more secure. <br><br>This is a violation of which&nbsp;<i>Saltzer and Schroeder</i>&nbsp;principle<b>?&nbsp;</b>Briefly&nbsp;<b>describe&nbsp;</b>the principle, and&nbsp;<b>why&nbsp;</b>it is being violated.	This is a violation of the <b>Open Design</b> principle. The principle states that the security of a system should not depend on the secrecy of its design or its overall implementation, but rather on specific secrets like cryptographic keys. <br><br>In this example, the company is relying on <b>Security by Obscurity</b>, which violates the principle because the security is tied to the secrecy of the algorithm itself rather than its robustness against an attacker that may have full knowledge of how it works (e.g. in the event that the secret security design is leaked).	question saltzer_schroeder week2
EYGJ?^T64b	A single administrative password allows an employee to access the company’s email server, financial records and employee medical files all at once. <br><br>This is a violation of which&nbsp;<i>Saltzer and Schroeder</i>&nbsp;principle<b>?&nbsp;</b>Briefly&nbsp;<b>describe&nbsp;</b>the principle, and&nbsp;<b>why&nbsp;</b>it is being violated.	"<div>This is a violation of the <b>Separation of Privilege</b> principle. The principle states that access to critical data/functions should depend on more than one condition or ""key"" being met.</div><div><br></div><div>This scenario violates the principle because it utilizes a <b>single point of failure. </b>The system should have instead separated access to the email server, financial records and medical files between different passwords or authorised individuals to ensure one compromise/leak doesn't expose everything.</div>"	question saltzer_schroeder week2
l.e$+Dff~b	Multiple departments in a firm use the same shared network drive to store sensitive data.&nbsp;<br><br>This is a violation of which&nbsp;<i>Saltzer and Schroeder</i>&nbsp;principle<b>?&nbsp;</b>Briefly&nbsp;<b>describe&nbsp;</b>the principle, and&nbsp;<b>why&nbsp;</b>it is being violated.	<div>This is a violation of the <b>Least Common Mechanism</b> principle. The principle states that users or entities should not share mechanisms to access resources to prevent information leakage or interference.</div><div><br></div><div>This violates the principle because the shared drive creates a <b>common path</b> -&nbsp;if an attacker compromises one user's access within a particular department, the shared nature of the mechanism allows them to access every other department's data easily.</div>	question saltzer_schroeder week2
d|Ss%[)L?E	A security architecture is designed with thousands of complex, overlapping rules that are difficult for staff to maintain. <br><br>This is a violation of which&nbsp;<i>Saltzer and Schroeder</i>&nbsp;principle<b>?&nbsp;</b>Briefly&nbsp;<b>describe&nbsp;</b>the principle, and&nbsp;<b>why&nbsp;</b>it is being violated.	This is a violation of the <b>Economy of Mechanism</b> principle. The principle states that the design and implementation of security measures should be kept as <b>simple</b> and <b>small</b> as possible. <br><br>This complex design violates the principle due to the <b>excessively complicated architecture</b>, which is harder to verify and much more prone to <b>human misconfiguration</b> or <b>logical oversight.</b> This makes it inherently less secure than a simpler system.	question saltzer_schroeder week2
fPC2&r8KYF	A system verifies a user's identity at login but does not re-check permissions when they access specific files later in the session. <br><br>This is a violation of which&nbsp;<i>Saltzer and Schroeder</i>&nbsp;principle<b>?&nbsp;</b>Briefly&nbsp;<b>describe&nbsp;</b>the principle, and&nbsp;<b>why&nbsp;</b>it is being violated.	<div>This is a violation of the <b>Complete Mediation</b> principle. The principle states that every access to every object must be continuously checked for authority <b>every single time</b>.</div><div><br></div><div>This violates the principle because the system only checks for authority at <b>login </b>and fails to verify authority at the actual <b>point of access</b>, allowing for unauthorised data access if a user's privileges are changed or revoked while they are still logged in.</div>	question saltzer_schroeder week2
r]T<5QUm,$	A company requires employees to complete a 10-step biometric scan and change their 20-character password every 6 to 7 days.&nbsp;<br><br>This is a violation of which&nbsp;<i>Saltzer and Schroeder</i>&nbsp;principle<b>?&nbsp;</b>Briefly&nbsp;<b>describe&nbsp;</b>the principle, and&nbsp;<b>why&nbsp;</b>it is being violated.	"This is a violation of the <b>Psychological Acceptability</b> principle. The principle states that security mechanisms should be easy to use and not too complex/frustrating, so that users routinely and automatically apply them correctly, rather than feeling encouraged to find workarounds to bypass them.<br><br>This violates the principle because the<b>&nbsp;</b>burden&nbsp;of following these mechanisms may discourage users from following the rules, inevitably leading them to find ""shortcuts"" that bypass the security measures entirely, for instance writing down their passwords on easily accessible pieces of paper."	question saltzer_schroeder week2
j`yr,b5-Q`	What is <b>Cryptography </b>and what core <b>security properties</b>&nbsp;does it aim to achieve?	Cryptography is the practice and study of securing information/communication by transforming it into a protected form, typically through the application of mathematical algorithms (ciphers) and keys.<br><br>It is used to ensure <i>confidentiality </i>when storing or transmitting data, usually over networks.	cryptography defn week3
"HC#iZi-NV|"	In the context of <b>Cryptography,&nbsp;</b>what is a&nbsp;<i>Cipher?</i>	A cipher is a (deterministic - always gives same outputs given the same input) mathematical algorithm that transforms plaintext into ciphertext, using a key	cryptography defn week3
"gT6[</)V#u"	In the context of <b>Encryption,&nbsp;</b>what is <i>Plaintext?</i>	Plaintext is the original, readable message/piece of information before encryption (which usually changes it to something unreadable)	cryptography defn week3
B[QM(T9@CB	In <b>Cryptography</b>, what is meant by a&nbsp;<i>one-way function?</i>	A one-way function is a function that is easy/efficient to compute for any input, but is computationally infeasible (takes alot of resources, time) to invert.<br><br>In other words, it is a function for which output values are easy to calculate, but given an output it is very difficult to figure out what the original input was (e.g. figure out what the inverse function would output)	cryptography defn otp week3
g5IFNv.iWo	What is the difference between a <b>code </b>and a <b>cipher </b>in cryptography?	"<div>A <b>code </b>replaces entire words or phrases with other words or symbols to change semantic meaning (e.g. “attack” to “yellow apple”) and requires a <b>codebook</b>.</div><div><br>
A <b>cipher</b>&nbsp;is an algorithm that transforms data at a lower level (e.g. changes individual letters or bits) using a<b>&nbsp;key</b>.&nbsp;It preserves the original meaning of the message but aims to make it&nbsp;unreadable without the key.</div>"	cryptography week3
"j#s|r@~1mm"	In the context of&nbsp;<b>Cryptography</b>, what is a <i>Cryptanalyst</i>?	A cryptanalyst is a person who attempts to break cryptographic systems (they are the adversary)	cryptography defn week3
Q=&&LD/iNL	In the context of&nbsp;<b>Cryptography,&nbsp;</b>what is a <i>Cryptographe</i><b>r</b>?	A cryptographer is a person who designs and analyses cryptographic algorithms and protocols	cryptography defn week3
b<K{%$L`*j	What is a <b>Monoalphabetic substitution </b>cipher?&nbsp;	A cipher that replaces each letter in a given plaintext with another, fixed letter <br><br>e.g. it applies a fixed permutation of the alphabet	cryptography defn week3
An/+5?P(Bk	What is the <b>Caesar cipher</b>, and why is it <i>insecure?</i>	The <strong>Caesar cipher</strong> is a type of <strong>monoalphabetic substitution cipher</strong> in which each letter in the plaintext is shifted by a fixed number of positions (the key) along the alphabet.<br>For example, if the key is 2, then<br>- A is mapped to C,<br>- B is mapped to D,<br>...<br>- Z is mapped to B&nbsp;<br><br>The cipher is <strong>insecure</strong> because the amount of possible keys is extremely small (only 26 possible keys in English), making it easy to break using brute-force by trying all possible shifts<br><br>Note: Caesar cipher (for English) works modulo 26, meaning the alphabet wraps around. For instance, if Z is the 26th letter of the alphabet, and the key is 2, then its mapping is (26 + 2) mod 26 = 2, which corresponds to the letter B.	cipher cryptography defn week3
ITxNu-etp0	In <b>Cryptography</b>, what is <i>frequency analysis</i>, and how can it be used to break certain <i>ciphers?</i>	Frequency analysis examines how often letters or patterns appear in ciphertext and compares them to the known frequency distribution of letters in a given language (e.g. “E” is the most common letter in English). This can be used to infer what the key to a cipher was, especially in the case of monoalphabetic substitution ciphers.<br><br>For example, if an attacker were to look at the ciphertext for the Caesar cipher, it is likely that the most common letter in the ciphertext correlates to E. This information can be used to deduce what the key was	cryptography defn week3
v^CYc`AECz	What security model (binary, risk-based) does Cryptography follow?&nbsp;	Cryptography follows a <b>binary security model:&nbsp;</b>a system/cryptographic algorithm is considered either <i>secure </i>(given its assumptions) or <i>completely insecure </i>(no inbetween)	cryptography week3
su4/MHp.J6	Why is it dangerous to design your own cryptographic algorithms?	It is very easy to introduce subtle mistakes or design flaws when creating a cryptographic algorithm, and even a small flaw can completely invalidate its security and be exploited by attackers. It is much safer to rely on well-established, thoroughly analysed cryptographic algorithms rather than attempting to design our own<br><br>As per the general binary security model used for Cryptography, a cryptographic algorithm that is even slightly insecure is considered completely insecure	cryptography week3
"M#~Rzv`r52"	Why are monoalphabetic substitution ciphers vulnerable to frequency analysis?	Because they preserve the frequency distributions of letters (e.g. statistical patterns are preserved), allowing attackers to match common ciphertext letters to common plaintext letters.&nbsp;<br><br>For instance, 'E' is an extremely common letter in English. So if a letter in the ciphertext appears very often, it is likely that the cipher mapped E to that letter. Similar patterns can be abused to figure out the rest of the mappings	cipher cryptography week3
w^;vWlL>8f	What is a&nbsp;<strong>polyalphabetic substitution cipher</strong>?	A polyalphabetic substitution cipher is a cipher that uses multiple different substitution alphabets instead of just one, meaning the mapping from plaintext letters to ciphertext letters can change throughout the message.<br><br>In other words, a letter may map to a different letter depending on where it is in the message	cipher cryptography defn week3
y|_L|xp:6(	How does a <b>polyalphabetic substitution cipher</b> differ from a <i>monoalphabetic substitution cipher?</i>	A monoalphabetic cipher uses <b>one fixed mapping</b> for the entire message. A given letter always maps to the <i>same</i> fixed ciphertext letter, no matter where it appears in the plaintext.&nbsp;<br><br>On the other hand, a polyalphabetic cipher uses <b>multiple mappings</b>, meaning that the same plaintext letter may map to <i>different </i><b>ciphertext </b>letters depending on its position in the plaintext.	cipher cryptography week3
qAwYAGuFK+	Why do <b>polyalphabetic ciphers</b> make frequency analysis more difficult?	Because the same plaintext letter does not always map to the same ciphertext letter, the original letter frequency distribution is spread out<b> </b>(diffused), making statistical patterns much harder to detect.	cipher cryptography week3
o]%9&f:fRa	What is the <b>Vigenere cipher&nbsp;</b>and how does it work?	"<div>The Vigenere cipher is a <strong>polyalphabetic substitution cipher</strong> that encrypts text by applying a sequence of <b>Caesar shifts </b>determined by a keyword&nbsp;(which acts as the key). We look at the letters of the plaintext and keyword pair-wise (if we run out of keyword letters, go to the beginning of the keyword) and shift the plaintext letter by an amount determined from the current letter of the keyword we are considering.</div><div><br></div><div>e.g. given keyword ""abcd"", the letters in the word correspond to the shift amount. ""a"" means 0, ""b"" means 1 etc.<br></div><div><br></div><div>EXAMPLE:</div><div>For example, given plaintext ""Hi Alice"" and key ""secret"", we do the following steps (first 2 letters. You can try encrypt the rest):</div><div><br></div><div>Look at 1st character of plaintext and keyword, which are H (8th in alphabet) and respectively. S is the 19th character of the alphabet, so apply a caesar shift of 18 to H. This means H maps to the 26th letter, which is Z.</div><div><br></div><div>Look at the 2nd character of plaintext and keyword. ""i"" is the 9th letter of the alphabet, ""e"" the 5th. i maps to the 13th letter, which is m.</div><div><br></div><div>So far, we have mapped ""Hi"" to ""Zm""</div><div><br></div><div>This process is repeated until the entire word is encrypted. Note: there are less letters in the key than the plaintext, so if we reach the key, we simply start from its beginning for the next plaintext letter. The full ciphertext would be ""Zm Ccmvw"". The two C's in the second half correspond to ""A"" and ""l"" in the plaintext - two <i>different </i>letters.</div><div><br></div>"	cipher cryptography defn vigenere week3
uQRC)v&QGv	What is the main weakness of the <b>Vigenere cipher</b>? What is one way of circumventing this weakness?	The main weakness of the Vigenère cipher is that if the keyword is <b>shorter</b> than the plaintext, it must be <strong>repeated</strong> to match the message length. <br><br>This repetition introduces periodic <b>patterns </b>in the ciphertext, which attackers can detect to determine the key length and reduce the cipher to multiple Caesar ciphers, making it vulnerable to <i>frequency analysis</i> or <i>brute force.<br></i><br>One way of circumventing this is by ensuring that the key word is atleast as long as the plaintext.	cipher cryptography vigenere week3
qX2.A|*m.U	How many distinct values can be represented using N bits (<b>b</b>inary digi<b>ts</b>)?	2<sup>N</sup><br><br>Why?<br>If we have N bits, there are N digits, and each digit has 2 possible values.&nbsp;<br><br>So we have N instances where we have to choose from 2 things, meaning there are 2<sup>N</sup> different choices<br><br>For example, if you had 2 shirts and 2 pants to choose from, the total number of combinations would be 2 * 2 = 2<sup>2</sup> = 4. If you also had 2 shoes to choose from, the total number of combinations would now be 2 * 2 * 2 = 2<sup>3</sup> = 8. In general, if we have N choices of 2 things, there are 2<sup>N</sup> possible choices	trivia
KkR^(~/6Sf	What is the largest unsigned value that can be represented using N bits?	2<sup>N&nbsp;</sup>- 1<br><br>Why?<br>We have N bits, so we can represent 2<sup>N</sup> different values. However, we also want to represent 0, which means that we cant represent 2<sup>N</sup> itself. So the largest value is 2<sup>N</sup>&nbsp;- 1<br><br>As an example, if we had 4 bits, and took the maximum value (all 1's), we would get 15. 2<sup>4</sup> is 16. 16-1 is 15, so it checks out:<br><br>8 4 2 1<br>1 1 1 1 = 15 (which is 2<sup>4</sup> - 1)	trivia
NJ8SEX9Cb/	In an <em>N</em>-bit number, what does the most significant bit (MSB) represent, for unsigned and (signed) two’s complement numbers?	For an unsigned number, MSB represents 2<sup>N-1</sup><br>For a signed number, MSB represents -2<sup>N-1</sup><br><br>Why?<br>The most significant bit correlates to the (N-1)th power of 2. This is because the 1st bit correlated to 2<sup>0</sup>, which is the 0th power. So the N'th bit will correlate to the (N-1)th power<br><br>As an example, lets look at the values that each digit of a 4-bit (unsigned) binary number represent:<br><br>1st digit: 2<sup>0&nbsp;&nbsp;</sup>&nbsp;(1 - 1 = 0)<br>2nd digit: 2<sup>1&nbsp;</sup>&nbsp;(2 - 1 = 1)<br>3rd digit: 2<sup>2&nbsp;&nbsp;</sup>&nbsp;(3 - 1 = 2)<br>4th digit: 2<sup>3</sup>&nbsp; &nbsp;(4 - 1 = 3)<br><br>In each case, the N'th digit corresponds to the (N-1)th power of 2, so it checks out<br>	trivia
osQ[iI}qe*	What is the range of values representable with N bits in two’s complement representation?	(-2<sup>N-1</sup>, 2<sup>N-1&nbsp;</sup>- 1)&nbsp; (inclusive range)<br><br>e.g. for 4 bits, the range is (-8, 7)<br><br>NOTE: <br><ul><li>The most negative number is even, the most positive is odd. </li><li>There is one less positive number than there are negative numbers, so the most negative number has no positive counterpart.</li></ul><br>Why?<br>Negative:<br>The most negative number will be the one where the MSB is 1, and the rest is 0's (because to get the most negative number, we would just want the negative portion without adding any positive numbers)<br><br>The MSB of a twos complement number corresponds to a value of -2<sup>N-1</sup>, so that is the lower bound.<br><br>Positive:<br>The most positive number will be the one where MSB is 0 (as we dont it to be negative), and the rest 1's (as the rest of the bits correlate to positive values)<br><br>If the entire number is N bits, then there are N-1 bits dedicated to positive values (so its like we have an unsigned, N-1 bit number). The largest unsigned value we can represent with N-1 bits is 2<sup>N-1&nbsp;</sup>- 1, so that is the upper bound.<br>	trivia
cY.1xYns2h	What is a <i>running key cipher</i> and how does it relate to the <b>Vigenere</b> cipher?	A running key cipher is a <strong>polyalphabetic substitution cipher</strong> similar to the Vigenere cipher, but instead of using a short repeating keyword, it uses a <b>long key</b> (often a piece of text like a book) that is at least as long as the plaintext, so the key does not repeat.	cipher defn running_key vigenere week3
d%fvWQ|4Vg	Why is a <i>running key cipher </i>typically safer than a <b>Vigenere</b> cipher?	Because the key in a running key cipher does <strong>not repeat.</strong>&nbsp;It avoids the ciphertext having a&nbsp;<strong>periodic structure</strong> that would allow attackers to potentially detect the key length and split the cipher into multiple Caesar ciphers (which are easily broken).	cipher vigenere week3
"Ez#M1pZ5BX"	Why is the <b>running key cipher&nbsp;</b><i>not </i>perfectly secure?	"Because the key is usually <b>not truly random</b> (e.g it is taken from natural language text), meaning it contains <b>statistical patterns</b>&nbsp;(like certain words such as ""the"" being more common than others) that attackers can exploit.<br><br>One other reason is that it partially relies on&nbsp;<b>Security by Obscurity</b>. If an attacker figures out what the key is taken from (e.g. which book or text), then the cipher is useless"	cipher week3
m@e0f:LQl[	How do the Vigenere, running key and one-time pad ciphers compare?	Summary:<br><div>Vigenere - usually a short repeating key which leads to strong patterns in the ciphertext, and so it is typically as insecure as caesar shift</div><div><br></div><div>Running key - uses a long, non-repeating key (usually a piece of text like a book) which leads to fewer patterns in the ciphertext, meaning that it is stronger, but still insecure due to language patterns that may arise in the key</div><div><strong><br></strong></div><div>One-time pad - uses a truly random key, so there are no significant patterns, meaning that it is perfectly mathematically secure as long as the random key is not reused</div>	cipher otp running_key vigenere week3
iu(!]%[:,,	What is a&nbsp;<b>one-time pad (OTP)</b>, and how secure is it?	A&nbsp;<strong>one-time pad</strong>&nbsp;(sometimes called <i>perfect substitution cipher</i>) is a&nbsp;<strong>polyalphabetic substitution cipher</strong>&nbsp;similar to the Vigenere cipher, that uses a key that is truly random, atleast as long as the plaintext, and only used&nbsp;once. This makes it produce ciphertext that reveals no statistical information about the original message.<br><br>Because of the random and non-repeating nature of the key, one time pad's are fully secure and mathematically impossible to break (if used correctly e.g. never reuse a key).	cipher defn otp vigenere week3
k*A(NMv^=A	What are the main disadvantages of the one-time pad that make it impractical in real-world systems?	- Truly random keys are difficult to generate, especially at a large scale<br>- Keys must be as long as the plaintext, which can lead to storage requirement issues (storage is expensive)<br>- Securely distributing the key for a given message is as hard as securing the message itself<br>- Ensuring keys are never reused is difficult in practise (e.g. due to human or system error)	cipher otp week3
jsH`^7>vV-	What is symmetric encryption?	Symmetric encryption is a method of encryption where the <b>same </b><i>secret key</i> is used for both encryption <b>and </b>decryption. Both the sender and receiver must share this key in advance and keep it secret.	cryptography week3
P;d;>:v~Nt	In <b>Cryptography</b>, what is the meant by <i>pseudorandom</i> in the context of number generation?	"<div>Pseudorandom means generated deterministically by an algorithm in a way that is <b>not </b>truly random, but designed so that the output number&nbsp;<i>appears&nbsp;</i>truly random and unpredictable to anyone who does not know how it was generated or what seed was used.<br></div><div><br></div><div>Sidenote:</div>
<div>Pseudorandom number generators (PRNGs) typically start from an initial value called a seed. Given the same seed, the generator will always produce the same sequence of outputs. Therefore, if an attacker learns the seed, they can predict all future outputs.&nbsp;</div>"	cryptography week3
Cs_jO)`9zF	What is a stream cipher and how does it work?	A stream cipher is a <b>symmetric </b>encryption method that encrypts data by combining the plaintext with a <i>keystream</i>, typically using bitwise XOR. The same <i>keystream&nbsp;</i>is then used to decrypt the message, again using bitwise XOR<br><br>This works because XOR cancels itself out.&nbsp;<br><br>The keystream is generated using a non-secret&nbsp;<b>Initialisation Vector (IV),</b>&nbsp;and a secret (usually short)&nbsp;<b>secret key</b>.<br><br><br>Sidenote on XOR cancelling itself out:<br><br>(Plaintext) XOR (Stream) XOR (Stream) = Plaintext<br><br>e.g. if we flip the bits of the plaintext using the keystream stream, then flip them again using the same keystream, we effectively unflipped the bits and so we get the original plaintext<br><br>	cipher cryptography defn stream_cipher symmetric_encryption week3
Knn`}8n6KA	What is a keystream in a stream cipher?	The keystream is a sequence of pseudorandom bits generated from a secret key that doesnt change between different messages, as well as an initialisation vector, which does change between different messages.	cipher cryptography defn stream_cipher week3
Mw1PA>jVkz	What is an initialisation vector (IV) in a stream cipher?	An IV is a <b>non-secret</b> input used with the <b>secret key</b> to generate the keystream. It is intended to change between messages, ensuring encryptions across different messages produce different keystreams which makes it so that that the ciphertext is different each time, even if the plaintext was the same or similar.	cipher cryptography defn stream_cipher week3
lCuR1R-KE-	What is the main weakness of the <i>stream cipher?</i>	If the Initialisation Vector is reused (so not changed) between messages, then the keystream generated will be the same (assuming same secret key). <br><br>If an attacker intercepted messages and gained access to ciphertexts that used the same keystream for encryption, they could potentially gain information on what the plaintext contained, because of the properties of bitwise XOR.<br><br>Namely, the fact taht XOR cancels itself out	cipher cryptography stream_cipher week3
x>9>lJ5.m-	How would two people <b>send/receive</b> a message using a <i>stream cipher?</i>	Sender generates a keystream using the&nbsp;<b>secret key</b>&nbsp;and an&nbsp;<b>initialisation vector </b>(non-secret)&nbsp;as input to a&nbsp;<i>deterministic, pseudorandom</i>&nbsp;keystream generator. Sender than XOR's this keystream with their messagge/plaintext, producing ciphertext<br><br>Ciphertext is then sent to the receiver. Receiver generates the same keystream (as the generator is deterministic) using the&nbsp;<b>secret key</b>&nbsp;and the <b>same </b>initialisation vector. Receiver XOR's ciphertext with the keystream, producing the plaintext. <br><br>This works because XOR cancels itself out<br>(Plaintext) XOR (Stream) XOR (Stream) = Plaintext<br><br>	cipher cryptography stream_cipher week3
Qf.qQIUW_P	What is a block cipher and how does it work?	"<div>A block cipher is a <b>symmetric </b>encryption method that encrypts data in <b>fixed-size blocks</b> (e.g. 128 bits) using a secret key. Each block of plaintext is transformed into a block of ciphertext of the same size using a deterministic algorithm controlled by the key.</div><div><br></div>
<div>The cipher applies a series of substitutions and permutations to thoroughly mix the bits, making the output appear random, even though the algorithm is deterministic. Decryption involves applying the inverse transformation with the same key to recover the original plaintext.&nbsp;</div><div><br></div><div>Sidenote: AES (Advanced Encryption Standard) is the most widely used block cipher.</div>"	block_cipher cipher cryptography symmetric_encryption week3
ir-49t.0aH	What are the main types of ciphers used in modern symmetric encryption, and what makes them secure?	"<div>The two main types are <b>block ciphers</b> and <b>stream ciphers</b>. Both are designed so that, without knowledge of the secret key, their output appears indistinguishable from random, ensuring security.</div><div><br></div>
<div>Block ciphers achieve this by acting like a <b>random </b>permutation/transformation on fixed-size blocks of the plaintext, while stream ciphers generate a <strong>pseudorandom keystream</strong> that behaves like true randomness when combined with plaintext provided that initialisation vectors are not reused.</div>"	block_cipher cipher cryptography stream_cipher symmetric_encryption week3
VQRV<s5mn	What is the key distribution problem in symmetric encryption?	The key distribution problem is the challenge of&nbsp;<b>securely</b> sharing a<b> secret key </b>between<b> two parties</b>&nbsp;before communication. If the key is intercepted during exchange, an attacker could decrypt all future messages.<br><br>This is a particular issue for&nbsp;<b>symmetric encryption</b>&nbsp;methods, which depend on the&nbsp;<i>sender </i><b>and </b><i>receiver </i>having access to the same&nbsp;<b>secret key </b>for encryption and decryption respectively	cryptography week3
B{/|<wmo>S	How is the jump address calculated for J-type instructions?	J-type instructions (j, jal) use pseudo-direct addressing<br><br>- Use 26-bit immediate field of the j-type instruction, which represents a word address. One word is 4 bytes, so to get a 28-bit byte address the CPU needs to multiply the word address by 4/shift it left by 2<br>- CPU sets the lower 28-bits of the Program Counter (PC) to be this calculated byte address. Keeps the most significant 4 bits of the PC the same.<br><br>Sideote: If the j-type instruction is located at address A, then the PC's upper 4 bits bits will have the same upper 4 bits as A+4. This is because the PC is incremented by 4 each time an instruction is fetched. In other words, the upper 4 bits of the PC will be the address of the instruction&nbsp;<i>after</i>&nbsp;the jump, not the jump itself.<br><br>More detail:<br>- The PC's 32-bit value represents a byte address, because memory is byte-addressable. This is why we need to convert the 26-bit word address to a byte address.&nbsp;<br>- J-type instructions can only jump to instructions within the same 256 MB memory boundary of the instruction directly after the jump, because the upper 4 bits of the PC do not change<br>	addressing j-type trivia
K<O=O_AJ=)	What is the discrete logarithm problem?	The discrete logarithm problem is the problem of finding x&nbsp;given:<br>- base g <br>- large prime number p<br>- y = g<sup>x&nbsp;</sup>mod  p<br><br>While computing g<sup>x&nbsp;</sup>mod p is computationally easy, finding x from y is computationally very hard, especially for large primes - even if you know what g and p are	cryptography diffie_hellman week3
f`<o[^D[`~	What is the main use of Diffie-Hellman key exchange?	Allows two parties to establish a shared secret key over an insecure channel (e.g a network) without sending the key itself, preventing eavesdroppers from directly obtaining it. <br><br>This is useful for symmetric encryption, where both parties need the same secret key but must first establish it securely over an insecure channel.	cryptography diffie_hellman week3
ih9Q^@73y|	What are the steps of the Diffie–Hellman key exchange?	"<ol><li>Both parties (Alice and Bob) publicly agree on a large prime number p and a base g.
</li>
<li>
Alice chooses a private secret number a, and Bob chooses a private secret number b.
</li>
<li>
Alice computes A = g<sup>a&nbsp;</sup>mod  p and sends it to Bob.
</li>
<li>
Bob computes B = g<sup>b&nbsp;</sup>mod  p and sends it to Alice.
</li>
<li>
Alice computes the shared secret as B<sup>a&nbsp;</sup>= g<sup>ab</sup> mod  p</li><li>Bob computes the shared secret as A<sup>b&nbsp;</sup>= g<sup>ab&nbsp;</sup>mod  p</li>
</ol>
<div>Both parties now have the same shared secret g<sup>ab</sup>, which can be used as a symmetric key.</div><div><br></div><div>An eavesdropper can see g, p, A, and B, but cannot compute the shared secret without recovering a&nbsp;or b, which would require solving the <strong>discrete logarithm problem</strong>, which is computationally infeasible<br></div>"	cryptography diffie_hellman week3
kX*v;Oe=-p	What is the main idea behind the Diffie-Helman key exchange?	"<div>Both parties exchange values computed using <strong>modular exponentiation</strong>, which is easy to compute in one direction but hard to reverse, acting as a one-way function.</div><div><br></div>
<div>This allows them to independently compute the same shared secret key using their own <strong>private values</strong> together with the exchanged values and public information. An eavesdropper cannot do this, as they do not know the private values and would need to solve the <strong>discrete logarithm problem</strong> to recover them, which is computationally infeasible</div>"	cryptography diffie_hellman week3
eo[!0F3fe5	How do you convert a number represented in Two's complement binary to its negated counterpart?	Flips all bits and add 1<br><br>NOTE: The most negative number has no positive counterpart, as there is 1 more negative number than there are positive numbers, given a fixed number of bits. Doing this negation procedure on the most negative number just gives you the same number (which is an edge case to consider)<br><br>Why does this work?<br>given a number x, if we flip all the bits we effectively get not(x)<br><br>x + not(x) will be a binary number consisting of all 1's. this correlates to a value of -1 in two's complement. So,<br>x + not(x) = -1<br>not(x) + 1 = -x<br><br>so to get -x from x, we calculate not(x) + 1, which is exactly what we do when we flip all bits and add 1.<br><br>	trivia
"E~g#$c[1_Q"	What is key establishment in cryptography?	Key establishment is the process by which two parties&nbsp;<i>securely </i>obtain a<b> shared secret key</b>, which can then be used for symmetric encryption. An example of a key establishment protocol is Diffie-Hellman key exchange	cryptography diffie_hellman symmetric_encryption week3
P*eU?r/q)=	What is <b>public-key (asymmetric)&nbsp;</b>cryptography?	Public-key cryptography is a type of encryption mechanism that uses a<b>&nbsp;</b>pair of keys.&nbsp;<br>- A&nbsp;<b>public key</b>, which can be shared openly to the public<br>- And a&nbsp;<b>private key</b>, which is kept secret<br>Data encrypted with the public key can only be decrypted with the corresponding private key.	cryptography public_key week3
Q4rqs%**f,	How would two people send/receive a message using&nbsp;<b>public-key cryptography?</b>	"<div>To send a message, the <b>sender</b> encrypts it using the <b><i>recipient’s public key</i></b> and sends the ciphertext. The <b><i>recipient </i></b>then decrypts it using their <b><i>private key.</i></b></div><div><br></div>
<div>If the <b><i>recipient</i></b> wants to reply, they encrypt their message using the <b>sender’s public key</b>, and the <b>sender </b>decrypts it using their <b>private key</b>.</div>"	cryptography public_key week3
c$<O8+n;b6	How does public-key cryptography differ from symmetric encryption?	Public-key cryptography uses two <b>different keys </b>(public and private), whereas symmetric encryption uses <b>the same shared secret key</b> for both encryption and decryption.<br><br>Public-key cryptography avoids the <b>key distribution problem</b>, since the public key can be shared openly and does not need to be sent over a secure channel	cryptography public_key symmetric_encryption week3
o_!AOc~U.6	What is RSA (Rivest-Shamir-Adleman) encryption? What is its security based on?	RSA is a widely used public-key cryptographic algorithm that uses a public/private <b>key pair</b> for encryption and decryption. It is typically used to securely exchange keys or encrypt small amounts of data, rather than large volumes of data. This is because the encryption and decryption processes are computationally expensive compared to symmetric encryption.<br><br>Its security is based on the (computational) difficulty of factorising large integers into their prime factors	cryptography public_key RSA week3
MWbVJjKC{u	What is a session key and what is one example of when it is used?	A session key is a temporary symmetric key used to encrypt data during a communication session. It is established using a <b>key exchange protocol</b> (e.g. Diffie-Hellman) or encrypted using <b>public-key encryption</b> (e.g. RSA)<br><br>For example, when you connect to a website over <b>HTTPS</b>, your browser and the server might first use a key exchange protocol (e.g. Diffie-Hellman) to establish a session key, which is then used to encrypt all data sent during that session	cryptography networking RSA symmetric_encryption week3
"jp#Gtln[3Z"	Why are session keys/symmetric encryption typically used over RSA encryption?	Symmetric encryption is used because it is much faster and more efficient than public-key encryption, making it suitable for encrypting large volumes of data in real-time. This is important in situations such as web communication, where data is continuously sent between a client and a server<br><br>The temporary nature of session keys also means they do not need to be stored long-term, reducing the risk of key compromise and limiting the impact if a key is exposed	cryptography networking symmetric_encryption week3
"B0rBk;R<U#"	What is <b>hybrid encryption</b> and how does it work?	<b>Hybrid encryption</b> combines public-key and symmetric encryption. A random session key is either generated and encrypted using public-key encryption (e.g. RSA), or established via a key exchange protocol (e.g. Diffie-Hellman). The actual message is then encrypted using symmetric encryption with that session key.	cryptography diffie_hellman networking public_key symmetric_encryption week3
oa7}uU?5Rb	What does <b>HTTPS </b>provide (in terms of security properties), and what are its limitations?<br>	HTTPS provides&nbsp;<b>confidentiality and integrity</b>&nbsp;by encrypting the <i>data </i>transmitted between a client and a server, preventing eavesdropping and tampering. <br><br><i>However</i>, it does not hide metadata such as&nbsp;IP addresses, domain names (in most cases) or timings&nbsp;as this information is required for routing the communication (so the packets can be sent correctly).&nbsp;This means a user can still be identified or tracked (e.g. by their IP address) unless additional measures such as a VPN or onion routing (e.g. Tor browser) are used.	networking week3
c8~2`7B7w*	What does the padlock icon in a browser indicate, and why can it be misleading?	"<div>The padlock indicates that the connection is <b>encrypted </b>using HTTPS and that the server has presented a <b>valid certificate</b>.</div><div><strong><br></strong></div>
<div><i>However</i>, it can be misleading because it does not guarantee that the website is legitimate or safe, only that the communication is encrypted. Malicious or phishing websites can still obtain valid certificates and display the padlock, which may lead users to incorrectly assume that the site is trustworthy</div>"	human_centred week3
H?wW-Z=D-a	What is the role of usability in web security, and how has it influenced browser security indicators?	"<div>Usable (human-centred) security ensures that security mechanisms are designed so users can understand and use them correctly, reducing the likelihood of user error or unsafe behaviour</div><div><br></div>
<div>For example, studies showed that <b>positive </b>security indicators (e.g. padlock icons indicating encryption is being used) were often misunderstood or ignored, so browsers shifted towards <i>negative </i>indicators (e.g. “Not secure” warnings), which are more effective at influencing user behaviour and highlighting potential risks, as it is easier for users to recognise and respond to explicit warnings than to interpret positive signals of security</div>"	human_centred networking week3
tp]qum8hHI	What is a <b>man-in-the-middle</b> (MitM) attack?	A <b>man-in-the-middle attack</b> occurs when an <b>attacker</b> <b>intercepts </b>and <b>potentially alters</b> communication between two parties, while <b>impersonating </b>each party to the other. As a result, both parties believe they are communicating securely with each other, when in reality the attacker is controlling the communication and can read or modify messages.	defn man_in_middle networking week4
M|K740}$}$	Why is encryption alone <i>not </i>sufficient for secure communication?	"<div>Encryption provides <b>confidentiality</b>, meaning it prevents an attacker from reading the contents of a message, but does not guarantee <b>integrity </b>or <b>authenticity</b>. As a result, an attacker may still be able to modify messages, replace them or impersonate another party - all of which break security.</div><div><br></div>
<div>Therefore, a protocol can be encrypted and still be vulnerable to attacks such as man-in-the-middle, where the attacker interferes with communication rather than just observing it.</div>"	authenticity confidentiality cryptography Integrity man_in_middle networking week4
m~:rV:0dLc	Why is the Diffie-Hellman key exchange vulnerable to man-in-the-middle attacks, and how can this be prevented?	"<div>Diffie-Hellman is vulnerable because it provides no authentication of the participants, so an attacker can intercept the exchange and run two separate key exchanges, establishing a different shared key with each party. This allows the attacker to decrypt, modify, and re-encrypt messages without being detected</div><div><br></div>
<div>This can be prevented by adding <strong>authentication</strong>, typically using <strong>digital signatures</strong>, which allow each party to verify the identity of the other and ensure they are communicating with the intended party</div>"	cryptography diffie_hellman man_in_middle networking week4
"e6IU4^=ec#"	What is a <i>digital signature</i> and what <b>security properties</b> does it provide?	"<div>A digital signature is a mechanism where a sender uses their <b>private key </b>to<b> sign a message</b>, and the receiver uses the sender’s<b> public key to verify it.&nbsp;</b>It allows the receiver to authenticate the sender and verify the message has not been altered.</div><div><b><br></b></div>
<div>It provides:</div>
-<b> Authentication</b>: confirms the message came from the claimed sender (only they have the private key)<br>-<b> Integrity</b>: ensures the message has not been altered, since any change invalidates the signature<br>-<b> Non-repudiation</b>: the sender cannot deny sending the message, as the signature is tied to their private key<br>"	defn digital_signature networking week4
sz9(7JAV`Q	What does it mean for a <b>digital signature</b> scheme to be <i>unforgeable</i>, and why is that important?	<b>Unforgeability </b>means an attacker cannot produce a <b>valid signature </b>on a new message without the private key, even if they have seen signatures on other messages. This prevents <b>impersonation</b>, ensuring attackers cannot create fake authenticated messages - which would break <i>authenticity </i>and <i>integrity</i>.	authenticity digital_signature Integrity networking week4
Ih(_M3ma>C	What is a <b>Message Authentication Code (MAC)</b>, and what problem does it solve?	A <b>Message Authentication Code (MAC)</b> is a cryptographic mechanism that uses a&nbsp;<b>shared secret key</b>&nbsp;to generate a <i>tag </i>for a message, which can be verified using the same key. It provides&nbsp;<b>integrity </b>and <b>authenticity</b>, allowing the receiver to check that the message has not been altered and was actually created by someone who knows the secret key.	authenticity defn Integrity MAC networking week4
H08;L6aLuo	How does a <b>MAC </b>differ from a <i>digital signature</i>?	A <b>MAC </b>uses a shared secret key for both generating <i>and</i> verifying the tag, whereas a digital signature uses a <b>private key</b> to sign and a <b>public key</b> to verify<br><br>As a result, MACs are more efficient&nbsp;but do not provide non-repudiation, since both parties share the same key (so it is not possible to prove which party created the message)	digital_signature MAC networking week4
iaXe!<zwgo	Why are <b>MACs</b> more efficient than <i>digital signatures</i>?	MACs use <i>symmetric </i>cryptography, which relies on simple, <b>fast </b>operations, whereas digital signatures use <i>asymmetric</i> cryptography, which involves <i>computationally expensive </i>operations on large prime numbers	digital_signature MAC networking week4
k0aLgaQ?RY	What does it mean for a <b>MAC </b>scheme to be secure (unforgeable)?	A <b>MAC </b>is secure if an attacker cannot produce a <b>valid tag </b>for <b>a new message</b> without knowing the secret key, even if they have seen valid tags for other messages. This ensures attackers cannot <b>forge messages</b> that will be accepted as authentic by the other party	MAC networking week4
q)+mTb9c}t	What is <b>authenticated encryption </b>(AEAD), and what problem does it solve?	Authenticated encryption (AEAD) combines <b>encryption </b>and <b>message authentication</b>, providing <b>confidentiality, integrity and authentication</b> in a single scheme. It ensures that data is both secret and has not been tampered with, avoiding vulnerabilities that can arise when encryption and authentication are combined incorrectly	AEAD authenticity defn Integrity networking week4
CT?m<.v<nI	What are the three main ways to combine encryption and MAC to achieve authenticated encryption (AEAD)?	<div><strong>1.) Encrypt-and-MAC (E&amp;M):</strong> encrypt message and MAC the plaintext<br></div><strong>2.) Encrypt-then-MAC (EtM):</strong> encrypt message, then MAC the ciphertext<br><strong>3.) MAC-then-Encrypt (MtE):</strong> MAC the plaintext, then encrypt both message and MAC<br>	AEAD cryptography MAC week4
N[ce5T=ZsE	What is the key idea behind <b>Encrypt-and-MAC (E&amp;M)</b>, and why is it <i>less </i>secure than other <b>AEAD </b>schemes?	In Encrypt-and-MAC, the message is <strong>encrypted</strong> and a MAC is computed separately over the <strong>plaintext</strong>. Because the MAC is not tied to the ciphertext, it does not directly protect the transmitted data. <br><br>This can allow modified ciphertext to be processed before tampering is detected, leading to potential vulnerabilities.	AEAD cryptography defn MAC week4
tRzr]kyV0u	What is the key idea behind <b>Encrypt-then-MAC (EtM)</b>, and why is it secure?	The message is<b> first encrypted</b> to produce ciphertext, and a MAC is then computed <b>over that ciphertext</b>. The receiver verifies the MAC before decrypting, ensuring the ciphertext has not been altered. <br><br>Because the MAC protects the ciphertext itself, only authentic, unmodified data is ever decrypted, preventing the risk of tampering	AEAD cryptography defn MAC week4
Ei~.vB.Oj[	What is the key idea behind MAC-then-Encrypt (MtE), and why is it vulnerable?	The MAC is computed over the plaintext and then both are encrypted together.<br><br>Since the MAC is inside the ciphertext, it is only verified <b>after decryption</b>, meaning altered ciphertext may be decrypted first, which can lead to <b>vulnerabilities.<br><br></b>Despite this, there are arguments for both MtE and EtM (Encrypt then Mac). Both are considered roughly equal in terms of security	AEAD cryptography defn MAC week4
BtDJBJ],:D	What is a cryptographic<b> Hash Function</b>, and what is its purpose?	"<div>A cryptographic hash function takes an input of <b>arbitrary length</b> and produces a <i>fixed-length</i> output (hash). Its purpose is to provide a compact representation of data that can be used to ensure <b>integrity</b>, since any change to the input is <b>extremely likely</b> to produce a different hash<b>.</b></div><div><strong><br></strong></div>
<div>A sender can send the hash of a message alongside it, and the recipient can recompute the hash from the received message and compare the two. If they do not match, it indicates that the message has been altered or corrupted</div>"	defn hash_functions hashing Integrity week4
rjIjJFju9}	What does <b>pre-image</b> resistance mean for a cryptographic <b>Hash Function</b>?	Pre-image resistance means that given a hash value H(x), where H is the hash function, it is <b>computationally infeasible to find the </b>original input<b> x</b>&nbsp;that produced it<br><br>Note: H(x) is called the <i>image of x</i> (<b>output</b>), and x is called a <i>pre-image</i> <i>of H(x)&nbsp;</i>(<b>input </b>that led to H(x))	defn hash_functions hashing Integrity week4
du%p_S/,{x	What does <i>collision resistance</i> mean for a <b>cryptographic Hash Function</b>?	Collision resistance for a hash functiopn H(X) means it is <b>computationally infeasible</b> to find two <i>different </i>inputs x ≠ y such that H(x)=H(y)<br><br>In other words, it is very difficult to find two inputs that produce the same output (hash)	defn hash_functions hashing Integrity week4
Q@j4:.&CoG	In the context of <b>hash functions</b>, why is <i>collision resistance</i> important?	Because it prevents attackers from finding two different messages with the same hash (since its computationally infeasible), which would allow them to substitute a <b>valid message</b> with a <i>tampered</i> one without detection (e.g. by intercepting the communication)	hash_functions hashing week4
sTJ~v78c9M	Why are <b>hash functions</b> useful for ensuring data <i>integrity</i>?	Because if data is modified, its hash will very likely <b>change</b>, so recomputing the hash allows <i>tampering </i>to be detected - if the hashes do not match, the data has been <b>altered</b>. This works because secure hash functions make it <b>computationally infeasible</b> to find a different input with the same hash	hash_functions hashing Integrity week4
h0w@Tzye;/	What does <i>uniformity </i>mean for a <b>cryptographic Hash Function</b>, and why is it desirable?	Uniformity means that a hash function’s outputs are evenly distributed over the output space, so each possible hash value is approximately equally likely given some input. <br><br>This is desirable because it makes the outputs appear random and prevents patterns or biases that could be exploited in attacks. It ensures that similar inputs do not produce predictably similar outputs, making the hash function harder to analyse or attack.	cryptography hash_functions week4
soHgtj{{st	Why are <b>Hash Function</b> outputs not guaranteed to be unique for a given input?	A hash function cannot assign every input a perfectly unique output because it maps arbitrary-length inputs (any size) to fixed-length outputs, so different inputs must sometimes share the same hash.<br><br>Overall, security relies on collisions being hard to find, not impossible.	cryptography hash_functions week4
Q2:6n*_BGB	What is the difference between <b>pre-image resistance</b> and <b>collision resistance</b> in <i>Hash Functions</i>?	Pre-image resistance means that given a hash value H(x), it is <b>computationally infeasible</b> to find an input x that produces it. Collision resistance means it is computationally infeasible to find two different inputs x ≠ y such that H(x)=H(y) (2 different inputs with the same hash output). <br><br>Overall, pre-image resistance protects against reversing a hash, while collision resistance protects against finding two inputs with the same hash.	cryptography hash_functions week4
GTnALM)fph	What is a <b>checksum</b>, and how is it used to verify data integrity?	"<div>A checksum is a value computed from data (e.g. using a hash function) that is used to detect changes such as tampering or corruption.</div><div><br></div>
<div>The sender computes a checksum and sends it with the data. The receiver recomputes the checksum from the received data and compares it to the original - if they differ, the data has been altered or corrupted</div>"	cryptography hash_functions Integrity week4
yVCgO~$M-$	What is <b>HMAC </b>(Hash-based Message Authentication Code)?	HMAC (Hash-based Message Authentication Code) is a construction that uses a <b>cryptographic hash function </b>and a<b> secret key</b> to produce a tag. It provides <strong>integrity and authentication</strong>, allowing the receiver to verify that a message has not been altered and was created by someone who knows the key.	authenticity cryptography defn hash_functions Integrity MAC week4
GM0h;P%Q^1	How does a <b>HMAC </b>(Hash-based Message Authentication Code) work?	It works by applying a hash function twice, using the key in two slightly different forms (called inner and outer). These forms are created by combining the key with fixed constants before each hash (e.g. via XOR).<br><br>This ensures the key is mixed into the computation in a more complex way, preventing attackers from exploiting the structure of the hash function (via length extension attacks), which can occur in naive constructions such as H(K || m) (the hash of K and m concatenated)	cryptography hash_functions MAC week4
z<:V:^FdI^	What is a <b>length extension attack</b>, and why does it break schemes that compute a MAC as H(K || m)?	A length extension attack allows an attacker, given a valid hash of a message, to append extra data and compute a new valid hash without knowing the secret key. <br><br>This is possible because some hash functions process data in blocks and maintain an internal state. The final hash value is this internal state, so an attacker can use it as a starting point to continue hashing additional data. As a result, they can create a modified message that will still be <strong>accepted as valid by the receiver</strong>, breaking authentication.<br><br>*In other words, given H(K || m), the attacker can compute the hash of the extended message K || m || m'&nbsp;without knowing K (the secret key) itself, producing a valid tag for the modified message.	attacks cryptography hash_functions week4
PfjN@]<gN+	What does the notation x || y mean in cryptography?	|| means <b>concatenation</b>.<b> </b>So the value formed by appending y to the end of x (e.g. writing them one after the other).	defn week4
uxU8Y^i)4m	What is a <b>blockchain</b>?	A blockchain is a data structure consisting of a sequence of blocks, where each block contains data and the <strong>hash of the previous block</strong>, forming a chain.<br><br>*Essentially a special type of linked list&nbsp;	block_chain defn week4
Kt$)/[4>oc	How does a <b>blockchain </b>use hash functions to maintain <b>integrity</b>?	Each block includes the hash of the previous block (alongside data), so if any block is modified, its hash changes and breaks the chain, making tampering detectable. <br><br>To avoid detection, an attacker would need to recompute the hashes of that block and all subsequent (later) blocks, which is <b>computationally infeasible</b> in practise.&nbsp;	block_chain week4
lp^+ctG!,3	What is a <b>Digital Certificate</b>, and what information does it contain?	A digital certificate is a signed data structure that binds an <b>entity’s identity </b>(e.g. a domain name) to its <b>public key</b>. It contains the public key, identity information, and a <b>digital signature </b>from a<b> Certificate Authority (CA)</b>, which allows others to verify that the key belongs to that entity.<br><br>In other words, a digital certificate is what certifies the ownership of a public key by the named subject of the certificate.	defn digital_certificate week4
lfesb}=d13	In the context of <b>Digital Signatures</b>, what is a <b>Certificate Authority (CA)</b>?	A <b>Certificate Authority</b> is a <i>trusted </i>entity that <b>verifies </b>identities and&nbsp;<b>issues </b>digital certificates (as well as store them) by signing them. By signing a certificate, the CA confirms that the public key in the certificate belongs to the claimed identity.	defn digital_certificate week4
kbE(v%gDDm	Why are <b>digital certificates</b> needed, and what problem do they solve?	They solve the problem of <b>authenticating public keys</b>. Without certificates, an attacker could perform a <b>man-in-the-middle attack</b> by substituting their own public key. Certificates prevent this by providing a trusted binding between identity and public key.	digital_certificate man_in_middle week4
pvea)ilJjC	What is <b>Public-Key Infrastructure</b> (PKI)?	Public-Key Infrastructure (PKI) is a system of <b>Certificate Authorities (CAs), digital certificates, </b>and <b>trust relationships</b> that is used to securely bind <b>public keys </b>to <i>identities</i>. It enables clients to verify that a public key belongs to a specific entity (e.g. a website) by relying on certificates signed by <b>trusted </b>authorities	defn digital_certificate week4
Gt3J}qa|Ry	What are the <b>main limitations</b> or <b>risks </b>of the Digital Certificate system (PKI)?	The system relies on <b>trusting </b>Certificate Authorities. If a CA is compromised or issues certificates incorrectly, attackers could create fraudulent but valid-looking certificates, undermining security.	digital_certificate week4
hHB9>l0_*	How does <i>certificate chaining</i> work in <b>Public Key Infrastructure (PKI)</b>?	<b>Certificate chaining</b> works by linking certificates through digital signatures. An entity’s certificate is signed by an intermediate CA, which is signed by another CA and so on. This forms a chain that ends at a trusted <b>root CA.</b> Each step in the chain represents a <i>transfer of trust</i>, allowing a client to trust an entity’s public key indirectly through the chain	defn digital_certificate week4
n|+]h@mGe$	How does a client verify a digital certificate in practice?	The client verifies the <strong>CA’s digital signature</strong> on the certificate using the CA’s public key. Certificates are often part of a <strong>chain</strong>, where each certificate is signed by another CA, ending in a <strong>trusted root CA</strong> whose public key is already known. If all signatures are valid, the certificate is trusted	digital_certificate week4

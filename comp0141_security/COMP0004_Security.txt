#separator:tab
#html:true
#guid column:1
#tags column:4
jFRZ.jecz9	What is the <b>CIA Triad</b>, and what are the names of the three properties it defines?	The CIA Triad is a general set of Information Security (InfoSec) properties that any system&nbsp;processing, storing, or transmitting&nbsp;information must maintain to be secure. A given system will usually have to strike a balance between satisfying these 3 properties, as some may need to be prioritised more than others.<br><br>The three properties are:<br>-<b> C</b>onfidentiality<br>-<b> I</b>ntegrity<br>- <b>A</b>vailability	week1
"kUq19%v#Td"	What is meant by <b>Information Security</b> (InfoSec)?	<b>Information Security&nbsp;</b>(InfoSec) is the practice of protecting information and its systems (things that handle the information) from unauthorised access, use, disclosure, disruption, modification, or destruction. These systems typically <b>process, store, and transmit</b>&nbsp;information.	defn week1
kI6Yf}1Y8C	In the context of the CIA Triad, what is the property of <i>Confidentiality</i>?	Confidentiality is the property that the information in the system is <b>not </b>made avaialble or disclosed to unauthorised individuals, entities or processes (e.g. malware).<br><br>Equivalently, it is the property that ensures information is accessed&nbsp;<b>only</b>&nbsp;by authorised individuals, entities or processes	CIA confidentiality defn week1
"e#VjtjpAQo"	In the context of the CIA Triad, what is the property of&nbsp;<i>Integrity</i>?	<div><b>Integrity</b> is the property that information is <b>not</b> altered or destroyed in an unauthorised manner.</div><div><br></div><div>Equivalently, it is the property that ensures information remains <b>accurate, complete, and trustworthy</b> throughout its entire life cycle (during processing, storage, and transmission).</div>	CIA defn Integrity week1
J@,:]S0RBK	What is meant by the&nbsp;<i>correctness</i>&nbsp;of data/information?	Data/information is <b>correct</b> if it is both <b>accurate</b> (contains the right value) and <b>complete</b> (nothing is missing)	defn week1
mo_Rxe>Srn	In the context of the CIA Triad, what is the property of&nbsp;<i>Availability, </i>and&nbsp;<b>why </b>is it important?	<b>Availability</b> is the property that ensures information and systems are accessible and usable on demand by an authorised entity. It represents the degree of <i>accessibility</i> of a system's resources.<br><br>For an information system to be useful and serve its purpose, its resources must be available when required, which is what the <b>Availability</b> property requires.	availability CIA defn week1
kY/A-aRPkp	In the context of <b>Information Security</b>, what is the property of <i>Privacy</i>?	<div><b>Privacy</b> is the right of an individual or entity to control how their personal information is collected, used, and shared.</div><div><br></div><div>It is the idea that Information should not be visible or accessible to others unless the owner explicitly allows it/provides consent.</div>	InfoSec privacy week1
erYC3ZBViE	In the context of&nbsp;<b>Information Security</b>, what is the difference between Privacy and Confidentiality?	Confidentiality is about the system preventing unauthorised access to entities/individuals, whereas Privacy is about the individual choosing if their data should be accessible in the first place.	CIA confidentiality InfoSec privacy week1
p{)q)H/=t(	In the context of&nbsp;<b>Information Security</b>, what is the property of <i>Authenticity</i>?	<b>Authenticity</b> is the property that ensures the system can prove that a person, entity, or message is actually who or what they claim to be. It allows the system to verify the truth of a claim so that an imposter cannot pretend to be a legitimate user (e.g. via some authentication system).&nbsp;	authenticity defn InfoSec week1
qB(b<|[q;>	In the context of&nbsp;<b>Information Security</b>, what is the property of&nbsp;<i>Non-repudiation</i>?	<b>Non-repudiation</b> is the property that ensures an entity cannot deny performing a specific action, like performing a transaction or sending a message. It requires the system to generate and maintain evidence (like digital signatures or logs) that links the action specifically to that user.	defn InfoSec non-repudiation week1
jc[~y+[[HV	In the context of&nbsp;<b>Information Security</b>, what is the property of&nbsp;<i>Anonimity</i>?	<i>Anonimity</i>&nbsp;is the property that ensures a person or entity using the system cannot be identified. It also means that an individual’s real identity cannot be derived from the information or activities recorded within the system.	anonimity defn InfoSec week1
"d8(#6f?[Ir"	In the context of the <b>Information Security</b>, what is the property of&nbsp;<i>Forward secrecy</i>?	<div><b>Note:&nbsp;</b>Forward secrecy is a property of communications systems in particular</div><div><b><br></b></div><div><b>Forward Secrecy </b>is the property that ensures past communications within the system remain secure even if the system is compromised in the future.</div><div>Equivalently, it ensures that an adversary who records encrypted information today cannot access or read it later, even if they eventually gain full control over the system (e.g. they gain access to the main encryption key).</div>	defn Forward-secrecy InfoSec week1
uz^urEgsOV	What are the 2 main approaches in defining the <b>security </b>of a system?&nbsp;	<ol><li><b>Binary model:</b> The approach that defines a system as either <b>secure</b> or <b>not secure </b>(no inbetween). The rule is that if a system meets every single security requirement/property, it is considered secure. Otherwise, it is considered insecure.</li><li><div><b>Risk-based Model:</b>&nbsp;A system is considered secure if it satisfies the security properties we care about to a degree that is acceptable. The idea is that since we know no system is 100% secure, we focus on making the level of security atleast good enough, balancing the cost of protection against the likelihood and damage of an attack.</div></li></ol>	InfoSec week1
edw4lCcDk}	What is the meaning of&nbsp;<i>threat modelling</i>?	Threat modelling is the process of identifying, categorising and prioritising potential security risks to a system&nbsp;<b>before</b>&nbsp;they happen	defn week1
KPV<y:2XyR	What is the <b>STRIDE</b> acronym/mnemonic in <i>Threat Modelling,&nbsp;and what do the letters stand for?</i>	<b>STRIDE</b> is a model used to identify and categorise threats into six main types of security threats.<br><br>The letters stand for:<br><b>S</b>poofing&nbsp;<br><b>T</b>ampering&nbsp;<br><b>R</b>epudiation<br><b>I</b>nformation Disclosure<br><b>D</b>enial of service&nbsp;<br><b>E</b>levation of privilege&nbsp;	defn STRIDE Threat_modelling week1
vpAJXZ[oH`	In the context of <b>STRIDE</b>, what does&nbsp;<i>Spoofing&nbsp;</i>involve, and what security property is violated by&nbsp;it??	<i>Spoofing </i>violates <b>Authenticity</b>. It occurs when an entity pretends to be a different person, system or message to gain access to a restricted system.	STRIDE Threat_modelling week1
r2r27`*YM.	In the context of <b>STRIDE</b>, what does&nbsp;<i>Tampering&nbsp;</i>involve, and what security property is violated by&nbsp;it??	<i>Tampering </i>violates <b>Integrity</b>. It involves the unauthorised modification of data, code or communication.	STRIDE Threat_modelling week1
OEn!2zMet4	In the context of <b>STRIDE</b>, what does&nbsp;<i>Information Disclosure </i>involve, and what security property is violated by&nbsp;it?	<i>Information Disclosure</i> violates <b>Confidentiality</b>. It involves exposing private or sensitive data to individuals who are not authorised to see it.	STRIDE Threat_modelling week1
ELWzV/E2_H	In the context of <b>STRIDE</b>, what does&nbsp;<i>Denial of Service&nbsp;</i>involve, and what security property is violated by&nbsp;it??	<i>Denial of Service</i> violates <b>Availability</b>. It occurs when an attacker crashes or slows down a system so that legitimate users cannot access it	STRIDE Threat_modelling week1
eL%W*2loK{	In the context of <b>STRIDE</b>, what does&nbsp;<i>Elevation of Privilege&nbsp;</i>involve, and what security property is violated by&nbsp;it??	<i>Elevation of Privilege</i> violates <i>Authorisation</i>. It occurs when a user gains more permissions or access rights to a system than they are supposed to have.	STRIDE Threat_modelling week1
MIFs,Ck$Ug	In the context of <b>STRIDE</b>, what does&nbsp;<i>Repudiation </i>involve, and what security property is violated by&nbsp;it?	<i>Repudiation </i>violates <b>Non-repudiation</b>. It occurs when a user denies performing an action because the system lacks sufficient evidence to prove they did it.	STRIDE Threat_modelling week1
OVEUo{-;%-	What is meant by a&nbsp;<i>threat</i>, and how does it differ from a&nbsp;<i>vulnerability?</i>	"A threat is a potential event or action that could cause harm, while a vulnerability is a weakness or flaw in a system that makes a threat possible. They differ in the sense that a threat is the ""what"", while a vulnerability is the ""how""<br>"	defn week1
E)q/HFc@,O	In the context of a <b>Web Server</b>, give two examples of a security <i>Threat</i>, an associated <i>Vulnerability</i>, and the <i>Security Principle</i> violated.	<div>1.)</div><div><b>Threat:</b> An attacker performing a Denial of Service (DoS) attack to take the web server down.</div><div><b>Vulnerability:</b>&nbsp;A lack of security measures within the system, such as <b>rate limiters</b> or <b>backup servers</b>, to handle excessive traffic or system failure.</div><div><b>Principle violated:</b> Availability</div><div><br></div><div>2.)</div><div><b>Threat:</b> An attacker gaining administrative access&nbsp;to take full control of the server</div><div><b>Vulnerability:</b> Running outdated server software that has a known, unpatched security flaw (a security exploit)</div><div><b>Principle Violated:&nbsp;</b>Authorisation</div>	question week1
"r#Ip#.uO)F"	In <b>Threat modelling</b>, what is <i>Likelihood </i>and what two main factors influence it?	"<div><b>Likelihood</b> is the probability that a threat will successfully exploit a vulnerability in the system.
It is mainly influenced by:</div><ol><li><b>Attacker Motivation: </b>How much will they benefit from exploiting the system? (e.g. For a bank, the likelihood of an attack is high as people want money).&nbsp;</li><li><b>Ease of Exploitation: </b>How easy is exploiting the vulnerability? (e.g. Does the attacker have the tools/skills to break the vault, and is the risk worth it?).</li></ol>"	Threat_modelling week1
C6NlMuLJv|	In <b>Threat modelling</b>, what is&nbsp;<i>Impact</i>?	<b>Impact</b> is the (moreso qualitative) measure of the total harm, loss or damage caused to an organisation if a threat is successfully exploited using a vulnerability within the system.	Threat_modelling week1
on`x2{~4OA	When creating a <i>threat model</i>, what are the 5 main factors we consider?	Threats<br>Vulnerabilities<br>Likelihood (of a threat exploiting a vulnerability)<br>Impact (Qualitative cost)<br>Cost (Quantitative cost)	Threat_modelling week1
y})^$Aj$0V	In <b>Information Security</b>,<i>&nbsp;</i>what is a&nbsp;<i>Security Policy&nbsp;</i>and how does it differ from a <i>Security Mechanism</i>?	A <b>Security Policy</b> is a definition of the set of rules, roles and procedures an organisation must follow to protect its assets and interests, whereas a <b>Security Mechanism</b> is the technical tool or method that enforces the policy	defn InfoSec Threat_modelling week1
Q:EG2oXvIW	"Under what conditions is a system formally considered <b>""secure""</b>?"	A system is considered secure if an <b>adversary</b>, constrained by a specific <i>threat model</i>, cannot violate the <i>security policy</i>.	defn InfoSec Threat_modelling week1
qwnQ)J<4R;	What is a <i>Security Arguement</i>?	A <b>rigorous argument</b> or proof grounded by evidence demonstrating that the chosen <b>security mechanisms</b> are successfully maintaining the <b>security policy</b>, subject to the assumptions of the <b>threat model</b>.	defn InfoSec Threat_modelling week1
PH1^=;<S<A	What six (or seven...) key factors must a <b>Security Policy</b> address to be effective?	"A good policy should address:<br><b>Threats:</b> What/Who are we afraid of? (The Adversary)<br><b>Vulnerabilities:</b> Where are the system's weaknesses? <br><b>Likelihood:</b> How probable is a successful attack?<br><b>Impact:</b> How much harm will an attack cause if it happens?
<br><b>Protection:</b> What specific <b>security mechanisms </b>will we use to enforce the policy?<br>(User) <b>Goals: </b>Why are people using the system? How do we make the policy strict enough to maintain security but lax enough to make the system <b>usable </b>for the user's intended goals?"	human_centred InfoSec week1
D4`ZoMD@0w	In the context of <b>Human-Centred Security,</b><i>&nbsp;</i>what is meant by&nbsp;<i>usability?</i>	Usability is a&nbsp;measure of how well a specific user in a specific context can use a<br>product/design to achieve a defined goal effectively, efficiently and<br>satisfactorily.	human_centred InfoSec week1
DtPeqP`,Z8	What are the risks of making a security policy too restrictive or complex?	Overly secure systems can become <b>unusable,&nbsp;</b>causing:<ul><li><div><b>Workarounds:</b> Users bypass annoying security to save time (e.g., writing passwords on post it notes).</div></li><li><div><b>Non compliance (productivity-first behaviour):</b> Users prioritise <b>productivity</b> over safety to finish their work (e.g. transferring files over a fast tool like dropbox rather than a secure in-house one)</div></li><li><div><b>Human Error:</b> Complex rules increase the likelihood of accidental mistakes or using predictable patterns (e.g. reusing the same password with small changes)</div></li></ul>	human_centred week1
HDdJ%s8;nV	What are the <i>Saltzer and Schroeder</i> principles?	"A foundational set of <b>8 design principles</b> used to build secure and reliable computer systems. They provide a philosophy for security design, focusing on <b>simplicity</b>, <b>least privilege</b>&nbsp;and restricting access to the absolute minimum required for a task.<br><br>Read through this atleast once:&nbsp;<a href=""https://shostack.org/blog/the-security-principles-of-saltzer-and-schroeder"">https://shostack.org/blog/the-security-principles-of-saltzer-and-schroeder</a>"	defn saltzer_schroeder week2
QwmRItLDK{	What are the <b>8 security design principles</b> defined by <i>Saltzer and Schroeder</i>?	1.&nbsp;<b>E</b>conomy of Mechanism<br>2.  <b>L</b>east Privilege<br>3.  <b>L</b>east Common Mechanism<br>4. <b>F</b>ail-Safe Defaults<br><br>5. <b>C</b>omplete Mediation<br>6. <b>O</b>pen Design<br>7.  <b>P</b>sychological Acceptability<br>8. <b>S</b>eparation of Privilege<br><br>Mnemonic (courtesy of professor...): ELLF COPS	defn saltzer_schroeder week2
t3{4LUf]9g	What is the <i>Saltzer and Schroeder&nbsp;</i>pinciple of&nbsp;<b>Economy of Mechanism?</b>	<div>Keep the design and implementation of security measures as <b>simple and small</b> as possible.</div><div><br></div><div>Complex designs are harder to test, verify and more likely to contain hidden bugs or vulnerabilities. The idea is that the simpler the systems, the easier they are to understand, and the less likely they are to contain flaws</div>	defn saltzer_schroeder week2
zH];Fh6A5T	What is the&nbsp;<i>Saltzer and Schroeder&nbsp;</i>pinciple of&nbsp;<b>Fail-safe Defaults?</b>	Base access decisions on <b>permission</b> rather than exclusion. The default action of the system should be to <b>deny access</b> to everyone (denial of service) unless a user is <b>explicitly allowed</b> to enter. <br><br>The idea is that it is better for an <i>authorised</i> entity to accidentally be <b>denied </b>access (easy to detect and fix), than to allow an <i>unauthorised </i>entity to accidentally be <b>granted </b>access (which could happen if the default is to give everyone access unless there is a reason not to, and is hard to detect and fix)	defn saltzer_schroeder week2
oL&L2$jRI-	What is the&nbsp;<i>Saltzer and Schroeder&nbsp;</i>pinciple of&nbsp;<b>Complete Mediation?</b>	Every access to a system resource must be continuously checked for authority. Instead of just verifying permissions at login or initial access, the system must actively mediate and validate all subsequent actions. <br><br>This continuous verification ensures that if a user's rights are revoked while they are still logged in/accessing the system, they are immediately prevented from further interacting with the protected resource	defn saltzer_schroeder week2
PY>4I&l0d4	What is meant by <b>Security by Obscurity, </b>and<b> </b><i>why </i>does it<b> fail?</b>	"<b>Security by Obscurity</b> is a a flawed approach to <b>system security&nbsp;</b>where protection relies entirely on hiding how the system works (its code, architecture or mechanics). It assumes attackers simply won't figure out the hidden flaws.<br><br>It fails because secrecy is fragile. Once an attacker reverse-engineers or leaks the ""secret"" design, the entire defense can collapse since there may be no actual strong barriers (like proper encryption or access controls) stopping them."	defn week2
L}o]e+;!xp	What is the&nbsp;<i>Saltzer and Schroeder&nbsp;</i>pinciple of&nbsp;<b>Open Design?</b>	The security of a system should not depend on the secrecy of its design or implementation. Instead of relying on <b>Security by Obscurity,</b>&nbsp;the architecture should be assumed public, with protection relying entirely on specific, easily changeable secrets like passwords or cryptographic keys.&nbsp;<br><br>The core idea is that a system must remain secure even if an attacker knows exactly how it is designed. Security should rely on them not having the <i>means</i> (e.g. a cryptographic key) to bypass it, rather than not knowing how it works	defn saltzer_schroeder week2
D|6U</1@s!	What is the&nbsp;<i>Saltzer and Schroeder&nbsp;</i>pinciple of&nbsp;<b>Separation of Privilege?</b>	<div>A system should require multiple independent conditions, keys or parties to authorise access to highly sensitive resources. Rather than letting one mechanism or person control the entire decision, authorisation/access is divided into multiple distinct requirements, kind of like a secure bank vault that requires two separate keys held by two different people to open.</div><div><br></div><div>This ensures that a single point of failure, such as one compromised password or a single rogue employee, cannot independently breach the system.<br></div>	defn saltzer_schroeder week2
FV:E-s%^98	What is the&nbsp;<i>Saltzer and Schroeder&nbsp;</i>pinciple of&nbsp;<b>Least Privilege?</b>	Every user, program or process must operate using only the absolute <b>minimum permissions necessary </b>to complete its specific job. By strictly limiting access rights to only what is required, we minimise the <i>impact </i>of a security breach.<br><br>For instance, if an attacker compromises a standard account, they are trapped within those limited, non-admin permissions. This prevents them from causing widespread damage or taking over the entire system.	defn saltzer_schroeder week2
bOp!Txv&[I	What is the&nbsp;<i>Saltzer and Schroeder&nbsp;</i>pinciple of&nbsp;<b>Least Common Mechanism?</b>	The security design should minimise the amount of shared resources or state between different users and processes. The idea is that shared mechanisms, like shared memory, temporary files or network paths, creates channels through which information could leak from one user to another, or where one user's actions could disrupt everyone else's. Isolating users prevents potential cross-interference.<br><br>For example on a computer with multiple user accounts, one user should not be able to see the files of another user, even on the same computer.	defn saltzer_schroeder week2
o{Vh07.n![	What is the&nbsp;<i>Saltzer and Schroeder&nbsp;</i>pinciple of&nbsp;<b>Psychological Acceptability?</b>	Security mechanisms must be easy to use and transparent. If a security requirement is too complex or frustrating, users will find workarounds to bypass it, such as writing passwords on post it notes or leaving secure doors open.<br><br>The core idea is that a system is secure only if the people using it readily accept the measures and follow them routinely without feeling burdened.	defn saltzer_schroeder week2
JVY%0>}Ei?	What is the&nbsp;<b>security design&nbsp;principle</b>&nbsp;of <i>Defense in Depth?</i>	Design the system with multiple layers of security mechanisms so it can resist an attack even if one layer is bypassed. <br><br>This ensures that a single vulnerability or point of failure does not allow an attacker to gain complete control of the system. As a last resort, if an error cannot be handled, intentionally crash or shut down the system. A temporary outage is always safer than remaining online and allowing potential data theft or unauthorised access.	defn week2
OWm9qK:J|g	What is the&nbsp;<b>security design&nbsp;principle</b>&nbsp;of&nbsp;<i>Design for Updating</i><b>?</b>	Plan for the safe and reliable installation of security patches. Since no system remains permanently free from vulnerabilities forever, developers must build a secure, structured mechanism to update the system as new flaws are discovered.	defn week2
j[cQ2PiIuG	What is the&nbsp;<b>security design&nbsp;principle</b>&nbsp;of&nbsp;<i>Prudent Paranoia?</i>	Assume the system operates in a hostile environment and will inevitably face attacks from adversaries.<br><br>Design defenses anticipating that breaches will occur, without underestimatting the effort an adversary will go to. Security design should be proactive rather than reactive.<br><br>Do not trust any user, input or internal component without verification.	defn week2
"s|#Q^?s&Wl"	What is the&nbsp;<b>security design&nbsp;principle</b>&nbsp;of&nbsp;<i>Privacy Promotion?</i>	Design systems to protect user privacy by default (to abide with GDPR laws). Minimise the collection, use and retention of personal data to only what is absolutely necessary.<br><br>Ensure that sensitive information is never exposed, tracked without consent or used beyond its original and intended purpose.	defn week2
gJ&=Rd`[ZJ	What is a <b>URL</b>, and what are its 3 main components?	<div>A <b>URL&nbsp;</b>(Uniform Resource Locator) is a standard string used to locate resources/files on a network. Its 3 main components (with respect to example URL&nbsp;<code>https://website.com/hi</code>) are:&nbsp;</div><div><b>&nbsp;</b></div><div><b>1.) Protocol Specifier:</b> Specifies the network protocol (rules of communication) to be used (e.g.&nbsp;<code>https://)</code></div><div><b>2.) Domain Name (Host):</b> The server's human-readable address (e.g.&nbsp;<code>website.com</code>).</div><div><b>3</b>.)&nbsp;<b>Page Location (Path):</b> The specific file or directory to be requested from that server (e.g.&nbsp;<code>/hi.html</code>)<br></div>	defn networking week2
G|DgU,2:yw	What is the <b>Domain Name System</b> (DNS)?&nbsp;	The DNS is a distributed, hierarchical naming system used to translate human-readable domain names (e.g. google.com) into numerical <b>IP addresses</b>. <br><br>It <b>consists of</b> a global network of <b>domain name servers</b> that partition and store data across different levels (Root, TLD, Authoritative). This allows computers to locate a specific host server so that <b>requests</b> can be <b>routed</b> to it.	defn networking week2
t_]?VEW)r]	What is the difference between&nbsp;the <b>Domain Name System (DNS)</b>&nbsp;and a&nbsp;<i>Domain Name Server?</i>	<div></div>The <b>Domain Name Sysem </b>is&nbsp;the overall system, protocol and global hierarchy used to translate names to IP addresses, whereas a <b>Domain Name Server</b>&nbsp;is a specific <b>computer/server</b>&nbsp;within that system that stores naming data and responds to requests.<br>	defn networking week2
"eP#Ff8w!ne"	In the context of <b>Networking</b>, what is&nbsp;<i>Domain Name Resolution?</i>	Domain Name Resolution is the process performed by the <b>DNS</b> to translate a human-readable domain name into its numerical <b>IP address</b>.<br><br><div>This involves a <b>top-down search</b> through the DNS hierarchy: the <b>Root</b> server redirects the query based on the <b>extension</b> (e.g. .com) to a <b>TLD</b> server, which then redirects the query based on the <b>domain name</b> (e.g. bob.com) until it reaches the <b>Authoritative</b> server, which provides the final <b>IP address</b><br></div>	defn networking week2
vViVUW=IhI	What are the two main <b>Networking </b>steps required to access a <i>website?</i>	<b>1.) Locating the IP address:</b>&nbsp;Using the&nbsp;<b>DNS</b>&nbsp;to translate the human-readable domain name (found within the URL) into the host's numerical&nbsp;<b>IP address</b>.<br><br><b>2.)&nbsp;Requesting the Content:</b>&nbsp;Using the IP address obtained from the&nbsp;<b>DNS&nbsp;</b>to send&nbsp;<b>data packets</b>&nbsp;containing the request (abiding by the network protocol specified in the URL) the host server so it can send the website content back. This is usually either done through&nbsp;<b>Leased Lines&nbsp;</b>or the&nbsp;<b>Internet's infastructure (Internet backbone)</b>	networking week2
Bl`eG!8$^~	In the context of&nbsp;<b>Networking,&nbsp;</b>what is a&nbsp;<i>Packet?</i>	<div>A packet is a <b>formatted </b>(format specified by network protocol) <b>unit of data</b> that travels across a network. Large files or requests are broken down into a sequence of smaller chunks (packets) to make them easier to transmit and route efficiently.</div><div><br></div><div>A packet contains two main things:</div><div>1.)&nbsp;<b>The Payload</b>, which is the&nbsp;actual piece of content being sent (e.g., part of a photo or a line of code)</div><div>2.)&nbsp;<b>The Header</b>, which contains the metadata required for delivery and reassembly. It identifies the <b>source </b>and <b>destination IP addresses</b> for routing and includes <b>sequence numbers</b> so the receiving computer knows how to put the packets back together in the correct order. It also contains other information, like a packet's Time to Live (TTL)</div>	defn networking week2
kd:ggB(f1/	In the context of&nbsp;<b>Networking</b><i>,</i>&nbsp;what are&nbsp;<b><i>Leased Lines</i></b> and what are their trade-offs?	<div>A leased line is a <b>direct, private connection</b> used as a method of transmitting data between two specific locations. It allows data to travel through an uncontended path that bypasses the public internet.<br></div><div><br></div><div><b>Pros:</b></div><ul><li><b>Incredibly fast: </b>Since the line is dedicated, there is no competition for bandwidth</li><li><b>Reliable:&nbsp;</b>Network performance is alot more consistent because there is no typical public network congestion</li><li><b>Secure: </b>Data is transmitted over a private path, making it much harder to intercept</li></ul><div><b>Cons:</b></div><ul><li><b>Incredibly expensive: </b>The cost of renting or maintaining a dedicated physical connection is significantly higher than using existing public infrastructure</li></ul>	defn networking week2
"Dbl(yt*s#q"	What is the <b>Internet Backbone</b>?	Essentially the physical infastructure of the internet. It is the global network of high-speed fiber-optic cables and core routers that provides the main data routes used to connect different networks (global networks, ISPs) across the world.	defn networking week2
w[e{}dJk%8	In&nbsp;<b>Networking</b>, what is a packet's Time to Live (TTL) and why is it necessary?	"<div><b></b>TTL is a value (usually 8 bits) in the <b>packet header</b> that sets a limit on how long a packet can travel through the network.&nbsp;</div><div><br></div><div>Its purpose is to prevent packets from getting stuck in an infinite loop (going around in a circle) if they can't find their destination.</div><div><br></div><div>Every time a packet passes through a router (a ""hop""), the TTL value is decreased by one. If the TTL reaches zero before the packet reaches its destination, the packet is ""killed"" or discarded.</div>"	networking week2
AIB=Y7qHW/	&nbsp;What is the main difference between <i>HTTP </i>and <i>HTTPS?</i>	<div><b>HTTP</b> (Hypertext Transfer Protocol) and <b>HTTPS</b> (Hypertext Transfer Protocol <i>Secure</i>) are both networking protocols, however, unlike HTTP, HTTPS requires that the entire communication be <b>encrypted.</b></div><div><b><br></b></div><div>This means that if HTTPS packets are intercepted/stolen (e.g. via a Man-in-the-middle attack), the data is unreadable to an attacker without the proper decryption key. In contrast, HTTP transmits data in <b>plaintext</b> with no encryption, meaning any intercepted data is immediately readable by unauthorised parties.</div>	networking week2
m:QN.nE4b<	What is the complete end-to-end process of accessing a website, from URL entry to the request being fulfilled? (long answer)	<b></b><div><div>First, the requesting device uses the <b>DNS</b> to translate the human-readable domain name from the <b>URL</b> into the host's numerical <b>IP address</b>.</div><div><br></div><div>Once the IP address is identified, the device sends a request following the rules of the <b>network protocol</b> (like HTTPS) specified in the URL. This request is split into many <b>packets</b>, each containing a <b>header</b> (with its sequence number and <b>Time to Live,&nbsp;</b>among other things) and the <b>payload </b>(actual data) to navigate the physical network infrastructure - either the public <b>Internet Backbone</b> or a private <b>Leased Line</b>.</div><div><br></div><div>When the <b>host server</b> receives the request packets, it processes them and prepares a <b>response</b> containing the website's data. The response is then sent to the requesting device (host server know the requester's <b>IP address</b>). This response is also split into <b>packets</b> with their own headers and TTL values to safely navigate back through the routers without getting stuck.</div><div><br></div><div>When the packets arrive back at the requesting device, it uses the sequence numbers in the headers to <b>reconstruct</b> the data so the browser can <b>render the HTML</b> and display the site.</div></div>	networking week2
xSz>r:MS8x	What are two examples of <b>human limitations</b> that impact security? (4 marks)	<div>One limitation is <b>human memory</b>. Humans can struggle to remember long, complex and unique passwords for every account, often leading to password reuse or writing passwords down, which can be a problem for the security of a system that depends on passwords.</div><div><br></div><div>Another limitation is that humans tend to have a <b>productivity-first mindset</b> (e.g. a preference for efficiency). This means that if a security policy is deemed too slow or inconvenient for a user, they may attempt to circumvent or find workarounds for the security policy, which could create vulnerabilities.</div><div><br></div><div>(sidenote) Other possible limitations:&nbsp;</div><div><ul><li>Susceptibility to social engineering (might reveal information to someone they shouldnt reveal it to)</li><li>Limited technical literacy (humans might not have a good mental model of how things like the internet work, leading to wrong assumptions e.g. the padlock icon doesnt mean that a site is safe, only that it is encrypted)</li><li>Human fatigue e.g. a tired person might press the wrong button and accidentally grant permissions to something they shouldn't have</li><li>Human ego/Optimism bias e.g. a person may think that there is no way&nbsp;<i>they</i>&nbsp;would fall for a cyber attack, causing them to maybe neglect certain security precautions (like having a good password)&nbsp;</li></ul></div>	human_centred question week1
lgF:foF&mK	Requesting more permissions than needed is a violation of which of the&nbsp;<i>Saltzer and Schroeder's</i> design principles?<br><br>Briefly&nbsp;<b>describe&nbsp;</b>the principle, and&nbsp;<b>why&nbsp;</b>it is being violated.	This is a violation of the principle of<b> Least Privilege</b>. The principle states that every user or program should operate using only the <b>minimum set of privileges</b> necessary to complete their task. Requesting excess permissions creates unnecessary security risks if the account or program is compromised, making it a violation of this principle.	question saltzer_schroeder week2
B!,<M(%}:a	A new user account is created with full administrative access by default, requiring an admin to manually restrict them.&nbsp;<br><br>This is a violation of which&nbsp;<i>Saltzer and Schroeder</i>&nbsp;principle<b>?&nbsp;</b>Briefly <b>describe </b>the principle, and <b>why </b>it is being violated.	This is a violation of the <b>Fail-safe Defaults</b> principle. The principle states that access decisions should be based on permission rather than exclusion, meaning the default state should be to&nbsp;<b>deny</b> access rather than grant it. By defaulting to <b>allowing access,&nbsp;</b>the principle is violated.	question saltzer_schroeder week2
tlxZ.pqcj<	A company uses a secret, proprietary encryption algorithm that has never been peer-reviewed, believing secrecy makes it more secure. <br><br>This is a violation of which&nbsp;<i>Saltzer and Schroeder</i>&nbsp;principle<b>?&nbsp;</b>Briefly&nbsp;<b>describe&nbsp;</b>the principle, and&nbsp;<b>why&nbsp;</b>it is being violated.	This is a violation of the <b>Open Design</b> principle. The principle states that the security of a system should not depend on the secrecy of its design or its overall implementation, but rather on specific secrets like cryptographic keys. <br><br>In this example, the company is relying on <b>Security by Obscurity</b>, which violates the principle because the security is tied to the secrecy of the algorithm itself rather than its robustness against an attacker that may have full knowledge of how it works (e.g. in the event that the secret security design is leaked).	question saltzer_schroeder week2
EYGJ?^T64b	A single administrative password allows an employee to access the company’s email server, financial records and employee medical files all at once. <br><br>This is a violation of which&nbsp;<i>Saltzer and Schroeder</i>&nbsp;principle<b>?&nbsp;</b>Briefly&nbsp;<b>describe&nbsp;</b>the principle, and&nbsp;<b>why&nbsp;</b>it is being violated.	"<div>This is a violation of the <b>Separation of Privilege</b> principle. The principle states that access to critical data/functions should depend on more than one condition or ""key"" being met.</div><div><br></div><div>This scenario violates the principle because it utilizes a <b>single point of failure. </b>The system should have instead separated access to the email server, financial records and medical files between different passwords or authorised individuals to ensure one compromise/leak doesn't expose everything.</div>"	question saltzer_schroeder week2
l.e$+Dff~b	Multiple departments in a firm use the same shared network drive to store sensitive data.&nbsp;<br><br>This is a violation of which&nbsp;<i>Saltzer and Schroeder</i>&nbsp;principle<b>?&nbsp;</b>Briefly&nbsp;<b>describe&nbsp;</b>the principle, and&nbsp;<b>why&nbsp;</b>it is being violated.	<div>This is a violation of the <b>Least Common Mechanism</b> principle. The principle states that users or entities should not share mechanisms to access resources to prevent information leakage or interference.</div><div><br></div><div>This violates the principle because the shared drive creates a <b>common path</b> -&nbsp;if an attacker compromises one user's access within a particular department, the shared nature of the mechanism allows them to access every other department's data easily.</div>	question saltzer_schroeder week2
d|Ss%[)L?E	A security architecture is designed with thousands of complex, overlapping rules that are difficult for staff to maintain. <br><br>This is a violation of which&nbsp;<i>Saltzer and Schroeder</i>&nbsp;principle<b>?&nbsp;</b>Briefly&nbsp;<b>describe&nbsp;</b>the principle, and&nbsp;<b>why&nbsp;</b>it is being violated.	This is a violation of the <b>Economy of Mechanism</b> principle. The principle states that the design and implementation of security measures should be kept as <b>simple</b> and <b>small</b> as possible. <br><br>This complex design violates the principle due to the <b>excessively complicated architecture</b>, which is harder to verify and much more prone to <b>human misconfiguration</b> or <b>logical oversight.</b> This makes it inherently less secure than a simpler system.	question saltzer_schroeder week2
fPC2&r8KYF	A system verifies a user's identity at login but does not re-check permissions when they access specific files later in the session. <br><br>This is a violation of which&nbsp;<i>Saltzer and Schroeder</i>&nbsp;principle<b>?&nbsp;</b>Briefly&nbsp;<b>describe&nbsp;</b>the principle, and&nbsp;<b>why&nbsp;</b>it is being violated.	<div>This is a violation of the <b>Complete Mediation</b> principle. The principle states that every access to every object must be continuously checked for authority <b>every single time</b>.</div><div><br></div><div>This violates the principle because the system only checks for authority at <b>login </b>and fails to verify authority at the actual <b>point of access</b>, allowing for unauthorised data access if a user's privileges are changed or revoked while they are still logged in.</div>	question saltzer_schroeder week2
r]T<5QUm,$	A company requires employees to complete a 10-step biometric scan and change their 20-character password every 6 to 7 days.&nbsp;<br><br>This is a violation of which&nbsp;<i>Saltzer and Schroeder</i>&nbsp;principle<b>?&nbsp;</b>Briefly&nbsp;<b>describe&nbsp;</b>the principle, and&nbsp;<b>why&nbsp;</b>it is being violated.	"This is a violation of the <b>Psychological Acceptability</b> principle. The principle states that security mechanisms should be easy to use and not too complex/frustrating, so that users routinely and automatically apply them correctly, rather than feeling encouraged to find workarounds to bypass them.<br><br>This violates the principle because the<b>&nbsp;</b>burden&nbsp;of following these mechanisms may discourage users from following the rules, inevitably leading them to find ""shortcuts"" that bypass the security measures entirely, for instance writing down their passwords on easily accessible pieces of paper."	question saltzer_schroeder week2
j`yr,b5-Q`	What is <b>Cryptography </b>and what core <b>security properties</b>&nbsp;does it aim to achieve?	Cryptography is the practice and study of securing information/communication by transforming it into a protected form, typically through the application of mathematical algorithms (ciphers) and keys.<br><br>It is used to ensure <i>confidentiality </i>when storing or transmitting data, usually over networks.	cryptography defn week3
"HC#iZi-NV|"	In the context of <b>Cryptography,&nbsp;</b>what is a&nbsp;<i>Cipher?</i>	A cipher is a (deterministic - always gives same outputs given the same input) mathematical algorithm that transforms plaintext into ciphertext, using a key	cryptography defn week3
"gT6[</)V#u"	In the context of <b>Encryption,&nbsp;</b>what is <i>Plaintext?</i>	Plaintext is the original, readable message/piece of information before encryption (which usually changes it to something unreadable)	cryptography defn week3
B[QM(T9@CB	In <b>Cryptography</b>, what is meant by a&nbsp;<i>one-way function?</i>	A one-way function is a function that is easy/efficient to compute for any input, but is computationally infeasible (takes alot of resources, time) to invert.<br><br>In other words, it is a function for which output values are easy to calculate, but given an output it is very difficult to figure out what the original input was (e.g. figure out what the inverse function would output)	cryptography defn otp week3
g5IFNv.iWo	What is the difference between a <b>code </b>and a <b>cipher </b>in cryptography?	"<div>A <b>code </b>replaces entire words or phrases with other words or symbols to change semantic meaning (e.g. “attack” to “yellow apple”) and requires a <b>codebook</b>.</div><div><br>
A <b>cipher</b>&nbsp;is an algorithm that transforms data at a lower level (e.g. changes individual letters or bits) using a<b>&nbsp;key</b>.&nbsp;It preserves the original meaning of the message but aims to make it&nbsp;unreadable without the key.</div>"	cryptography week3
"j#s|r@~1mm"	In the context of&nbsp;<b>Cryptography</b>, what is a <i>Cryptanalyst</i>?	A cryptanalyst is a person who attempts to break cryptographic systems (they are the adversary)	cryptography defn week3
Q=&&LD/iNL	In the context of&nbsp;<b>Cryptography,&nbsp;</b>what is a <i>Cryptographe</i><b>r</b>?	A cryptographer is a person who designs and analyses cryptographic algorithms and protocols	cryptography defn week3
b<K{%$L`*j	What is a <b>Monoalphabetic substitution </b>cipher?&nbsp;	A cipher that replaces each letter in a given plaintext with another, fixed letter <br><br>e.g. it applies a fixed permutation of the alphabet	cryptography defn week3
An/+5?P(Bk	What is the <b>Caesar cipher</b>, and why is it <i>insecure?</i>	The <strong>Caesar cipher</strong> is a type of <strong>monoalphabetic substitution cipher</strong> in which each letter in the plaintext is shifted by a fixed number of positions (the key) along the alphabet.<br>For example, if the key is 2, then<br>- A is mapped to C,<br>- B is mapped to D,<br>...<br>- Z is mapped to B&nbsp;<br><br>The cipher is <strong>insecure</strong> because the amount of possible keys is extremely small (only 26 possible keys in English), making it easy to break using brute-force by trying all possible shifts<br><br>Note: Caesar cipher (for English) works modulo 26, meaning the alphabet wraps around. For instance, if Z is the 26th letter of the alphabet, and the key is 2, then its mapping is (26 + 2) mod 26 = 2, which corresponds to the letter B.	cipher cryptography defn week3
ITxNu-etp0	In <b>Cryptography</b>, what is <i>frequency analysis</i>, and how can it be used to break certain <i>ciphers?</i>	Frequency analysis examines how often letters or patterns appear in ciphertext and compares them to the known frequency distribution of letters in a given language (e.g. “E” is the most common letter in English). This can be used to infer what the key to a cipher was, especially in the case of monoalphabetic substitution ciphers.<br><br>For example, if an attacker were to look at the ciphertext for the Caesar cipher, it is likely that the most common letter in the ciphertext correlates to E. This information can be used to deduce what the key was	cryptography defn week3
v^CYc`AECz	What security model (binary, risk-based) does Cryptography follow?&nbsp;	Cryptography follows a <b>binary security model:&nbsp;</b>a system/cryptographic algorithm is considered either <i>secure </i>(given its assumptions) or <i>completely insecure </i>(no inbetween)	cryptography week3
su4/MHp.J6	Why is it dangerous to design your own cryptographic algorithms?	It is very easy to introduce subtle mistakes or design flaws when creating a cryptographic algorithm, and even a small flaw can completely invalidate its security and be exploited by attackers. It is much safer to rely on well-established, thoroughly analysed cryptographic algorithms rather than attempting to design our own<br><br>As per the general binary security model used for Cryptography, a cryptographic algorithm that is even slightly insecure is considered completely insecure	cryptography week3
"M#~Rzv`r52"	Why are monoalphabetic substitution ciphers vulnerable to frequency analysis?	Because they preserve the frequency distributions of letters (e.g. statistical patterns are preserved), allowing attackers to match common ciphertext letters to common plaintext letters.&nbsp;<br><br>For instance, 'E' is an extremely common letter in English. So if a letter in the ciphertext appears very often, it is likely that the cipher mapped E to that letter. Similar patterns can be abused to figure out the rest of the mappings	cipher cryptography week3
w^;vWlL>8f	What is a&nbsp;<strong>polyalphabetic substitution cipher</strong>?	A polyalphabetic substitution cipher is a cipher that uses multiple different substitution alphabets instead of just one, meaning the mapping from plaintext letters to ciphertext letters can change throughout the message.<br><br>In other words, a letter may map to a different letter depending on where it is in the message	cipher cryptography defn week3
y|_L|xp:6(	How does a <b>polyalphabetic substitution cipher</b> differ from a <i>monoalphabetic substitution cipher?</i>	A monoalphabetic cipher uses <b>one fixed mapping</b> for the entire message. A given letter always maps to the <i>same</i> fixed ciphertext letter, no matter where it appears in the plaintext.&nbsp;<br><br>On the other hand, a polyalphabetic cipher uses <b>multiple mappings</b>, meaning that the same plaintext letter may map to <i>different </i><b>ciphertext </b>letters depending on its position in the plaintext.	cipher cryptography week3
qAwYAGuFK+	Why do <b>polyalphabetic ciphers</b> make frequency analysis more difficult?	Because the same plaintext letter does not always map to the same ciphertext letter, the original letter frequency distribution is spread out<b> </b>(diffused), making statistical patterns much harder to detect.	cipher cryptography week3
o]%9&f:fRa	What is the <b>Vigenere cipher&nbsp;</b>and how does it work?	"<div>The Vigenere cipher is a <strong>polyalphabetic substitution cipher</strong> that encrypts text by applying a sequence of <b>Caesar shifts </b>determined by a keyword&nbsp;(which acts as the key). We look at the letters of the plaintext and keyword pair-wise (if we run out of keyword letters, go to the beginning of the keyword) and shift the plaintext letter by an amount determined from the current letter of the keyword we are considering.</div><div><br></div><div>e.g. given keyword ""abcd"", the letters in the word correspond to the shift amount. ""a"" means 0, ""b"" means 1 etc.<br></div><div><br></div><div>EXAMPLE:</div><div>For example, given plaintext ""Hi Alice"" and key ""secret"", we do the following steps (first 2 letters. You can try encrypt the rest):</div><div><br></div><div>Look at 1st character of plaintext and keyword, which are H (8th in alphabet) and respectively. S is the 19th character of the alphabet, so apply a caesar shift of 18 to H. This means H maps to the 26th letter, which is Z.</div><div><br></div><div>Look at the 2nd character of plaintext and keyword. ""i"" is the 9th letter of the alphabet, ""e"" the 5th. i maps to the 13th letter, which is m.</div><div><br></div><div>So far, we have mapped ""Hi"" to ""Zm""</div><div><br></div><div>This process is repeated until the entire word is encrypted. Note: there are less letters in the key than the plaintext, so if we reach the key, we simply start from its beginning for the next plaintext letter. The full ciphertext would be ""Zm Ccmvw"". The two C's in the second half correspond to ""A"" and ""l"" in the plaintext - two <i>different </i>letters.</div><div><br></div>"	cipher cryptography defn vigenere week3
uQRC)v&QGv	What is the main weakness of the <b>Vigenere cipher</b>? What is one way of circumventing this weakness?	The main weakness of the Vigenère cipher is that if the keyword is <b>shorter</b> than the plaintext, it must be <strong>repeated</strong> to match the message length. <br><br>This repetition introduces periodic <b>patterns </b>in the ciphertext, which attackers can detect to determine the key length and reduce the cipher to multiple Caesar ciphers, making it vulnerable to <i>frequency analysis</i> or <i>brute force.<br></i><br>One way of circumventing this is by ensuring that the key word is atleast as long as the plaintext.	cipher cryptography vigenere week3
cY.1xYns2h	What is a <i>running key cipher</i> and how does it relate to the <b>Vigenere</b> cipher?	A running key cipher is a <strong>polyalphabetic substitution cipher</strong> similar to the Vigenere cipher, but instead of using a short repeating keyword, it uses a <b>long key</b> (often a piece of text like a book) that is at least as long as the plaintext, so the key does not repeat.	cipher defn running_key vigenere week3
d%fvWQ|4Vg	Why is a <i>running key cipher </i>typically safer than a <b>Vigenere</b> cipher?	Because the key in a running key cipher does <strong>not repeat.</strong>&nbsp;It avoids the ciphertext having a&nbsp;<strong>periodic structure</strong> that would allow attackers to potentially detect the key length and split the cipher into multiple Caesar ciphers (which are easily broken).	cipher vigenere week3
"Ez#M1pZ5BX"	Why is the <b>running key cipher&nbsp;</b><i>not </i>perfectly secure?	"Because the key is usually <b>not truly random</b> (e.g it is taken from natural language text), meaning it contains <b>statistical patterns</b>&nbsp;(like certain words such as ""the"" being more common than others) that attackers can exploit.<br><br>One other reason is that it partially relies on&nbsp;<b>Security by Obscurity</b>. If an attacker figures out what the key is taken from (e.g. which book or text), then the cipher is useless"	cipher week3
m@e0f:LQl[	How do the Vigenere, running key and one-time pad ciphers compare?	Summary:<br><div>Vigenere - usually a short repeating key which leads to strong patterns in the ciphertext, and so it is typically as insecure as caesar shift</div><div><br></div><div>Running key - uses a long, non-repeating key (usually a piece of text like a book) which leads to fewer patterns in the ciphertext, meaning that it is stronger, but still insecure due to language patterns that may arise in the key</div><div><strong><br></strong></div><div>One-time pad - uses a truly random key, so there are no significant patterns, meaning that it is perfectly mathematically secure as long as the random key is not reused</div>	cipher otp running_key vigenere week3
iu(!]%[:,,	What is a&nbsp;<b>one-time pad (OTP)</b>, and how secure is it?	A&nbsp;<strong>one-time pad</strong>&nbsp;(sometimes called <i>perfect substitution cipher</i>) is a&nbsp;<strong>polyalphabetic substitution cipher</strong>&nbsp;similar to the Vigenere cipher, that uses a key that is truly random, atleast as long as the plaintext, and only used&nbsp;once. This makes it produce ciphertext that reveals no statistical information about the original message.<br><br>Because of the random and non-repeating nature of the key, one time pad's are fully secure and mathematically impossible to break (if used correctly e.g. never reuse a key).	cipher defn otp vigenere week3
k*A(NMv^=A	What are the main disadvantages of the one-time pad that make it impractical in real-world systems?	- Truly random keys are difficult to generate, especially at a large scale<br>- Keys must be as long as the plaintext, which can lead to storage requirement issues (storage is expensive)<br>- Securely distributing the key for a given message is as hard as securing the message itself<br>- Ensuring keys are never reused is difficult in practise (e.g. due to human or system error)	cipher otp week3
jsH`^7>vV-	What is symmetric encryption?	Symmetric encryption is a method of encryption where the <b>same </b><i>secret key</i> is used for both encryption <b>and </b>decryption. Both the sender and receiver must share this key in advance and keep it secret.	cryptography week3
P;d;>:v~Nt	In <b>Cryptography</b>, what is the meant by <i>pseudorandom</i> in the context of number generation?	"<div>Pseudorandom means generated deterministically by an algorithm in a way that is <b>not </b>truly random, but designed so that the output number&nbsp;<i>appears&nbsp;</i>truly random and unpredictable to anyone who does not know how it was generated or what seed was used.<br></div><div><br></div><div>Sidenote:</div>
<div>Pseudorandom number generators (PRNGs) typically start from an initial value called a seed. Given the same seed, the generator will always produce the same sequence of outputs. Therefore, if an attacker learns the seed, they can predict all future outputs.&nbsp;</div>"	cryptography week3
Cs_jO)`9zF	What is a stream cipher and how does it work?	A stream cipher is a <b>symmetric </b>encryption method that encrypts data by combining the plaintext with a <i>keystream</i>, typically using bitwise XOR. The same <i>keystream&nbsp;</i>is then used to decrypt the message, again using bitwise XOR<br><br>This works because XOR cancels itself out.&nbsp;<br><br>The keystream is generated using a non-secret&nbsp;<b>Initialisation Vector (IV),</b>&nbsp;and a secret (usually short)&nbsp;<b>secret key</b>.<br><br><br>Sidenote on XOR cancelling itself out:<br><br>(Plaintext) XOR (Stream) XOR (Stream) = Plaintext<br><br>e.g. if we flip the bits of the plaintext using the keystream stream, then flip them again using the same keystream, we effectively unflipped the bits and so we get the original plaintext<br><br>	cipher cryptography defn stream_cipher symmetric_encryption week3
Knn`}8n6KA	What is a keystream in a stream cipher?	The keystream is a sequence of pseudorandom bits generated from a secret key that doesnt change between different messages, as well as an initialisation vector, which does change between different messages.	cipher cryptography defn stream_cipher week3
Mw1PA>jVkz	What is an initialisation vector (IV) in a stream cipher?	An IV is a <b>non-secret</b> input used with the <b>secret key</b> to generate the keystream. It is intended to change between messages, ensuring encryptions across different messages produce different keystreams which makes it so that that the ciphertext is different each time, even if the plaintext was the same or similar.	cipher cryptography defn stream_cipher week3
lCuR1R-KE-	What is the main weakness of the <i>stream cipher?</i>	If the Initialisation Vector is reused (so not changed) between messages, then the keystream generated will be the same (assuming same secret key). <br><br>If an attacker intercepted messages and gained access to ciphertexts that used the same keystream for encryption, they could potentially gain information on what the plaintext contained, because of the properties of bitwise XOR.<br><br>Namely, the fact taht XOR cancels itself out	cipher cryptography stream_cipher week3
x>9>lJ5.m-	How would two people <b>send/receive</b> a message using a <i>stream cipher?</i>	Sender generates a keystream using the&nbsp;<b>secret key</b>&nbsp;and an&nbsp;<b>initialisation vector </b>(non-secret)&nbsp;as input to a&nbsp;<i>deterministic, pseudorandom</i>&nbsp;keystream generator. Sender than XOR's this keystream with their messagge/plaintext, producing ciphertext<br><br>Ciphertext is then sent to the receiver. Receiver generates the same keystream (as the generator is deterministic) using the&nbsp;<b>secret key</b>&nbsp;and the <b>same </b>initialisation vector. Receiver XOR's ciphertext with the keystream, producing the plaintext. <br><br>This works because XOR cancels itself out<br>(Plaintext) XOR (Stream) XOR (Stream) = Plaintext<br><br>	cipher cryptography stream_cipher week3
Qf.qQIUW_P	What is a block cipher and how does it work?	"<div>A block cipher is a <b>symmetric </b>encryption method that encrypts data in <b>fixed-size blocks</b> (e.g. 128 bits) using a secret key. Each block of plaintext is transformed into a block of ciphertext of the same size using a deterministic algorithm controlled by the key.</div><div><br></div>
<div>The cipher applies a series of substitutions and permutations to thoroughly mix the bits, making the output appear random, even though the algorithm is deterministic. Decryption involves applying the inverse transformation with the same key to recover the original plaintext.&nbsp;</div><div><br></div><div>Sidenote: AES (Advanced Encryption Standard) is the most widely used block cipher.</div>"	block_cipher cipher cryptography symmetric_encryption week3
ir-49t.0aH	What are the main types of ciphers used in modern symmetric encryption, and what makes them secure?	"<div>The two main types are <b>block ciphers</b> and <b>stream ciphers</b>. Both are designed so that, without knowledge of the secret key, their output appears indistinguishable from random, ensuring security.</div><div><br></div>
<div>Block ciphers achieve this by acting like a <b>random </b>permutation/transformation on fixed-size blocks of the plaintext, while stream ciphers generate a <strong>pseudorandom keystream</strong> that behaves like true randomness when combined with plaintext provided that initialisation vectors are not reused.</div>"	block_cipher cipher cryptography stream_cipher symmetric_encryption week3
VQRV<s5mn	What is the key distribution problem in symmetric encryption?	The key distribution problem is the challenge of&nbsp;<b>securely</b> sharing a<b> secret key </b>between<b> two parties</b>&nbsp;before communication. If the key is intercepted during exchange, an attacker could decrypt all future messages.<br><br>This is a particular issue for&nbsp;<b>symmetric encryption</b>&nbsp;methods, which depend on the&nbsp;<i>sender </i><b>and </b><i>receiver </i>having access to the same&nbsp;<b>secret key </b>for encryption and decryption respectively	cryptography week3
K<O=O_AJ=)	What is the discrete logarithm problem?	The discrete logarithm problem is the problem of finding x&nbsp;given:<br>- base g <br>- large prime number p<br>- y = g<sup>x&nbsp;</sup>mod  p<br><br>While computing g<sup>x&nbsp;</sup>mod p is computationally easy, finding x from y is computationally very hard, especially for large primes - even if you know what g and p are	cryptography diffie_hellman week3
f`<o[^D[`~	What is the main use of Diffie-Hellman key exchange?	Allows two parties to establish a shared secret key over an insecure channel (e.g a network) without sending the key itself, preventing eavesdroppers from directly obtaining it. <br><br>This is useful for symmetric encryption, where both parties need the same secret key but must first establish it securely over an insecure channel.	cryptography diffie_hellman week3
ih9Q^@73y|	What are the steps of the Diffie–Hellman key exchange?	"<ol><li>Both parties (Alice and Bob) publicly agree on a large prime number p and a base g.
</li>
<li>
Alice chooses a private secret number a, and Bob chooses a private secret number b.
</li>
<li>
Alice computes A = g<sup>a&nbsp;</sup>mod  p and sends it to Bob.
</li>
<li>
Bob computes B = g<sup>b&nbsp;</sup>mod  p and sends it to Alice.
</li>
<li>
Alice computes the shared secret as B<sup>a&nbsp;</sup>= g<sup>ab</sup> mod  p</li><li>Bob computes the shared secret as A<sup>b&nbsp;</sup>= g<sup>ab&nbsp;</sup>mod  p</li>
</ol>
<div>Both parties now have the same shared secret g<sup>ab</sup>, which can be used as a symmetric key.</div><div><br></div><div>An eavesdropper can see g, p, A, and B, but cannot compute the shared secret without recovering a&nbsp;or b, which would require solving the <strong>discrete logarithm problem</strong>, which is computationally infeasible<br></div>"	cryptography diffie_hellman week3
kX*v;Oe=-p	What is the main idea behind the Diffie-Helman key exchange?	"<div>Both parties exchange values computed using <strong>modular exponentiation</strong>, which is easy to compute in one direction but hard to reverse, acting as a one-way function.</div><div><br></div>
<div>This allows them to independently compute the same shared secret key using their own <strong>private values</strong> together with the exchanged values and public information. An eavesdropper cannot do this, as they do not know the private values and would need to solve the <strong>discrete logarithm problem</strong> to recover them, which is computationally infeasible</div>"	cryptography diffie_hellman week3
eo[!0F3fe5	How do you convert a number represented in Two's complement binary to its negated counterpart?	Flips all bits and add 1<br><br>NOTE: The most negative number has no positive counterpart, as there is 1 more negative number than there are positive numbers, given a fixed number of bits. Doing this negation procedure on the most negative number just gives you the same number (which is an edge case to consider)<br><br>Why does this work?<br>given a number x, if we flip all the bits we effectively get not(x)<br><br>x + not(x) will be a binary number consisting of all 1's. this correlates to a value of -1 in two's complement. So,<br>x + not(x) = -1<br>not(x) + 1 = -x<br><br>so to get -x from x, we calculate not(x) + 1, which is exactly what we do when we flip all bits and add 1.<br><br>	trivia
"E~g#$c[1_Q"	What is key establishment in cryptography?	Key establishment is the process by which two parties&nbsp;<i>securely </i>obtain a<b> shared secret key</b>, which can then be used for symmetric encryption. An example of a key establishment protocol is Diffie-Hellman key exchange	cryptography diffie_hellman symmetric_encryption week3
P*eU?r/q)=	What is <b>public-key (asymmetric)&nbsp;</b>cryptography?	Public-key cryptography is a type of encryption mechanism that uses a<b>&nbsp;</b>pair of keys.&nbsp;<br>- A&nbsp;<b>public key</b>, which can be shared openly to the public<br>- And a&nbsp;<b>private key</b>, which is kept secret<br>Data encrypted with the public key can only be decrypted with the corresponding private key.	cryptography public_key week3
Q4rqs%**f,	How would two people send/receive a message using&nbsp;<b>public-key cryptography?</b>	"<div>To send a message, the <b>sender</b> encrypts it using the <b><i>recipient’s public key</i></b> and sends the ciphertext. The <b><i>recipient </i></b>then decrypts it using their <b><i>private key.</i></b></div><div><br></div>
<div>If the <b><i>recipient</i></b> wants to reply, they encrypt their message using the <b>sender’s public key</b>, and the <b>sender </b>decrypts it using their <b>private key</b>.</div>"	cryptography public_key week3
c$<O8+n;b6	How does public-key cryptography differ from symmetric encryption?	Public-key cryptography uses two <b>different keys </b>(public and private), whereas symmetric encryption uses <b>the same shared secret key</b> for both encryption and decryption.<br><br>Public-key cryptography avoids the <b>key distribution problem</b>, since the public key can be shared openly and does not need to be sent over a secure channel	cryptography public_key symmetric_encryption week3
o_!AOc~U.6	What is RSA (Rivest-Shamir-Adleman) encryption? What is its security based on?	RSA is a widely used public-key cryptographic algorithm that uses a public/private <b>key pair</b> for encryption and decryption. It is typically used to securely exchange keys or encrypt small amounts of data, rather than large volumes of data. This is because the encryption and decryption processes are computationally expensive compared to symmetric encryption.<br><br>Its security is based on the (computational) difficulty of factorising large integers into their prime factors	cryptography public_key RSA week3
MWbVJjKC{u	What is a session key and what is one example of when it is used?	A session key is a temporary symmetric key used to encrypt data during a communication session. It is established using a <b>key exchange protocol</b> (e.g. Diffie-Hellman) or encrypted using <b>public-key encryption</b> (e.g. RSA)<br><br>For example, when you connect to a website over <b>HTTPS</b>, your browser and the server might first use a key exchange protocol (e.g. Diffie-Hellman) to establish a session key, which is then used to encrypt all data sent during that session	cryptography networking RSA symmetric_encryption week3
"jp#Gtln[3Z"	Why are session keys/symmetric encryption typically used over RSA encryption?	Symmetric encryption is used because it is much faster and more efficient than public-key encryption, making it suitable for encrypting large volumes of data in real-time. This is important in situations such as web communication, where data is continuously sent between a client and a server<br><br>The temporary nature of session keys also means they do not need to be stored long-term, reducing the risk of key compromise and limiting the impact if a key is exposed	cryptography networking symmetric_encryption week3
"B0rBk;R<U#"	What is <b>hybrid encryption</b> and how does it work?	<b>Hybrid encryption</b> combines public-key and symmetric encryption. A random session key is either generated and encrypted using public-key encryption (e.g. RSA), or established via a key exchange protocol (e.g. Diffie-Hellman). The actual message is then encrypted using symmetric encryption with that session key.	cryptography diffie_hellman networking public_key symmetric_encryption week3
oa7}uU?5Rb	What does <b>HTTPS </b>provide (in terms of security properties), and what are its limitations?<br>	HTTPS provides&nbsp;<b>confidentiality and integrity</b>&nbsp;by encrypting the <i>data </i>transmitted between a client and a server, preventing eavesdropping and tampering. <br><br><i>However</i>, it does not hide metadata such as&nbsp;IP addresses, domain names (in most cases) or timings&nbsp;as this information is required for routing the communication (so the packets can be sent correctly).&nbsp;This means a user can still be identified or tracked (e.g. by their IP address) unless additional measures such as a VPN or onion routing (e.g. Tor browser) are used.	networking week3
c8~2`7B7w*	What does the padlock icon in a browser indicate, and why can it be misleading?	"<div>The padlock indicates that the connection is <b>encrypted </b>using HTTPS and that the server has presented a <b>valid certificate</b>.</div><div><strong><br></strong></div>
<div><i>However</i>, it can be misleading because it does not guarantee that the website is legitimate or safe, only that the communication is encrypted. Malicious or phishing websites can still obtain valid certificates and display the padlock, which may lead users to incorrectly assume that the site is trustworthy</div>"	human_centred week3
H?wW-Z=D-a	What is the role of <b>usability </b>in web security, and how has it influenced browser security indicators?	"<div>Usable (human-centred) security ensures that security mechanisms are designed so users can understand and use them correctly, reducing the likelihood of user error or unsafe behaviour</div><div><br></div>
<div>For example, studies showed that <b>positive </b>security indicators (e.g. padlock icons indicating encryption is being used) were often misunderstood or ignored, so browsers shifted towards <i>negative </i>indicators (e.g. “Not secure” warnings), which are more effective at influencing user behaviour and highlighting potential risks, as it is easier for users to recognise and respond to explicit warnings than to interpret positive signals of security</div>"	human_centred networking week3
tp]qum8hHI	What is a <b>man-in-the-middle</b> (MitM) attack?	A <b>man-in-the-middle attack</b> occurs when an <b>attacker</b> <b>intercepts </b>and <b>potentially alters</b> communication between two parties, while <b>impersonating </b>each party to the other. As a result, both parties believe they are communicating securely with each other, when in reality the attacker is controlling the communication and can read or modify messages.	defn man_in_middle networking week4
M|K740}$}$	Why is encryption alone <i>not </i>sufficient for secure communication?	"<div>Encryption provides <b>confidentiality</b>, meaning it prevents an attacker from reading the contents of a message, but does not guarantee <b>integrity </b>or <b>authenticity</b>. As a result, an attacker may still be able to modify messages, replace them or impersonate another party - all of which break security.</div><div><br></div>
<div>Therefore, a protocol can be encrypted and still be vulnerable to attacks such as man-in-the-middle, where the attacker interferes with communication rather than just observing it.</div>"	authenticity confidentiality cryptography Integrity man_in_middle networking week4
m~:rV:0dLc	Why is the Diffie-Hellman key exchange vulnerable to man-in-the-middle attacks, and how can this be prevented?	"<div>Diffie-Hellman is vulnerable because it provides no authentication of the participants, so an attacker can intercept the exchange and run two separate key exchanges, establishing a different shared key with each party. This allows the attacker to decrypt, modify, and re-encrypt messages without being detected</div><div><br></div>
<div>This can be prevented by adding <strong>authentication</strong>, typically using <strong>digital signatures</strong>, which allow each party to verify the identity of the other and ensure they are communicating with the intended party</div>"	cryptography diffie_hellman man_in_middle networking week4
"e6IU4^=ec#"	What is a <i>digital signature</i> and what <b>security properties</b> does it provide?	"<div>A digital signature is a mechanism where a sender uses their <b>private key </b>to<b> sign a message</b>, and the receiver uses the sender’s<b> public key to verify it.&nbsp;</b>It allows the receiver to authenticate the sender and verify the message has not been altered.</div><div><b><br></b></div>
<div>It provides:</div>
-<b> Authentication</b>: confirms the message came from the claimed sender (only they have the private key)<br>-<b> Integrity</b>: ensures the message has not been altered, since any change invalidates the signature<br>-<b> Non-repudiation</b>: the sender cannot deny sending the message, as the signature is tied to their private key<br>"	defn digital_signature networking week4
sz9(7JAV`Q	What does it mean for a <b>digital signature</b> scheme to be <i>unforgeable</i>, and why is that important?	<b>Unforgeability </b>means an attacker cannot produce a <b>valid signature </b>on a new message without the private key, even if they have seen signatures on other messages. This prevents <b>impersonation</b>, ensuring attackers cannot create fake authenticated messages - which would break <i>authenticity </i>and <i>integrity</i>.	authenticity digital_signature Integrity networking week4
Ih(_M3ma>C	What is a <b>Message Authentication Code (MAC)</b>, and what problem does it solve?	A <b>Message Authentication Code (MAC)</b> is a cryptographic mechanism that uses a&nbsp;<b>shared secret key</b>&nbsp;to generate a <i>tag </i>for a message, which can be verified using the same key. It provides&nbsp;<b>integrity </b>and <b>authenticity</b>, allowing the receiver to check that the message has not been altered and was actually created by someone who knows the secret key.	authenticity defn Integrity MAC networking week4
H08;L6aLuo	How does a <b>MAC </b>differ from a <i>digital signature</i>?	A <b>MAC </b>uses a shared secret key for both generating <i>and</i> verifying the tag, whereas a digital signature uses a <b>private key</b> to sign and a <b>public key</b> to verify<br><br>As a result, MACs are more efficient&nbsp;but do not provide non-repudiation, since both parties share the same key (so it is not possible to prove which party created the message)	digital_signature MAC networking week4
iaXe!<zwgo	Why are <b>MACs</b> more efficient than <i>digital signatures</i>?	MACs use <i>symmetric </i>cryptography, which relies on simple, <b>fast </b>operations, whereas digital signatures use <i>asymmetric</i> cryptography, which involves <i>computationally expensive </i>operations on large prime numbers	digital_signature MAC networking week4
k0aLgaQ?RY	What does it mean for a <b>MAC </b>scheme to be secure (unforgeable)?	A <b>MAC </b>is secure if an attacker cannot produce a <b>valid tag </b>for <b>a new message</b> without knowing the secret key, even if they have seen valid tags for other messages. This ensures attackers cannot <b>forge messages</b> that will be accepted as authentic by the other party	MAC networking week4
q)+mTb9c}t	What is <b>authenticated encryption </b>(AEAD), and what problem does it solve?	Authenticated encryption (AEAD) combines <b>encryption </b>and <b>message authentication</b>, providing <b>confidentiality, integrity and authentication</b> in a single scheme. It ensures that data is both secret and has not been tampered with, avoiding vulnerabilities that can arise when encryption and authentication are combined incorrectly	AEAD authenticity defn Integrity networking week4
CT?m<.v<nI	What are the three main ways to combine encryption and MAC to achieve authenticated encryption (AEAD)?	<div><strong>1.) Encrypt-and-MAC (E&amp;M):</strong> encrypt message and MAC the plaintext<br></div><strong>2.) Encrypt-then-MAC (EtM):</strong> encrypt message, then MAC the ciphertext<br><strong>3.) MAC-then-Encrypt (MtE):</strong> MAC the plaintext, then encrypt both message and MAC<br>	AEAD cryptography MAC week4
N[ce5T=ZsE	What is the key idea behind <b>Encrypt-and-MAC (E&amp;M)</b>, and why is it <i>less </i>secure than other <b>AEAD </b>schemes?	In Encrypt-and-MAC, the message is <strong>encrypted</strong> and a MAC is computed separately over the <strong>plaintext</strong>. Because the MAC is not tied to the ciphertext, it does not directly protect the transmitted data. <br><br>This can allow modified ciphertext to be processed before tampering is detected, leading to potential vulnerabilities.	AEAD cryptography defn MAC week4
tRzr]kyV0u	What is the key idea behind <b>Encrypt-then-MAC (EtM)</b>, and why is it secure?	The message is<b> first encrypted</b> to produce ciphertext, and a MAC is then computed <b>over that ciphertext</b>. The receiver verifies the MAC before decrypting, ensuring the ciphertext has not been altered. <br><br>Because the MAC protects the ciphertext itself, only authentic, unmodified data is ever decrypted, preventing the risk of tampering	AEAD cryptography defn MAC week4
Ei~.vB.Oj[	What is the key idea behind MAC-then-Encrypt (MtE), and why is it vulnerable?	The MAC is computed over the plaintext and then both are encrypted together.<br><br>Since the MAC is inside the ciphertext, it is only verified <b>after decryption</b>, meaning altered ciphertext may be decrypted first, which can lead to <b>vulnerabilities.<br><br></b>Despite this, there are arguments for both MtE and EtM (Encrypt then Mac). Both are considered roughly equal in terms of security	AEAD cryptography defn MAC week4
BtDJBJ],:D	What is a cryptographic<b> Hash Function</b>, and what is its purpose?	"<div>A cryptographic hash function takes an input of <b>arbitrary length</b> and produces a <i>fixed-length</i> output (hash). Its purpose is to provide a compact representation of data that can be used to ensure <b>integrity</b>, since any change to the input is <b>extremely likely</b> to produce a different hash<b>.</b></div><div><strong><br></strong></div>
<div>A sender can send the hash of a message alongside it, and the recipient can recompute the hash from the received message and compare the two. If they do not match, it indicates that the message has been altered or corrupted</div>"	defn hash_functions hashing Integrity week4
rjIjJFju9}	What does <b>pre-image</b> resistance mean for a cryptographic <b>Hash Function</b>?	Pre-image resistance means that given a hash value H(x), where H is the hash function, it is <b>computationally infeasible to find the </b>original input<b> x</b>&nbsp;that produced it<br><br>Note: H(x) is called the <i>image of x</i> (<b>output</b>), and x is called a <i>pre-image</i> <i>of H(x)&nbsp;</i>(<b>input </b>that led to H(x))	defn hash_functions hashing Integrity week4
du%p_S/,{x	What does <i>collision resistance</i> mean for a <b>cryptographic Hash Function</b>?	Collision resistance for a hash functiopn H(X) means it is <b>computationally infeasible</b> to find two <i>different </i>inputs x ≠ y such that H(x)=H(y)<br><br>In other words, it is very difficult to find two inputs that produce the same output (hash)	defn hash_functions hashing Integrity week4
Q@j4:.&CoG	In the context of <b>hash functions</b>, why is <i>collision resistance</i> important?	Because it prevents attackers from finding two different messages with the same hash (since its computationally infeasible), which would allow them to substitute a <b>valid message</b> with a <i>tampered</i> one without detection (e.g. by intercepting the communication)	hash_functions hashing week4
sTJ~v78c9M	Why are <b>hash functions</b> useful for ensuring data <i>integrity</i>?	Because if data is modified, its hash will very likely <b>change</b>, so recomputing the hash allows <i>tampering </i>to be detected - if the hashes do not match, the data has been <b>altered</b>. This works because secure hash functions make it <b>computationally infeasible</b> to find a different input with the same hash	hash_functions hashing Integrity week4
h0w@Tzye;/	What does <i>uniformity </i>mean for a <b>cryptographic Hash Function</b>, and why is it desirable?	Uniformity means that a hash function’s outputs are evenly distributed over the output space, so each possible hash value is approximately equally likely given some input. <br><br>This is desirable because it makes the outputs appear random and prevents patterns or biases that could be exploited in attacks. It ensures that similar inputs do not produce predictably similar outputs, making the hash function harder to analyse or attack.	cryptography hash_functions week4
soHgtj{{st	Why are <b>Hash Function</b> outputs not guaranteed to be unique for a given input?	A hash function cannot assign every input a perfectly unique output because it maps arbitrary-length inputs (any size) to fixed-length outputs, so different inputs must sometimes share the same hash.<br><br>Overall, security relies on collisions being hard to find, not impossible.	cryptography hash_functions week4
Q2:6n*_BGB	What is the difference between <b>pre-image resistance</b> and <b>collision resistance</b> in <i>Hash Functions</i>?	Pre-image resistance means that given a hash value H(x), it is <b>computationally infeasible</b> to find an input x that produces it. Collision resistance means it is computationally infeasible to find two different inputs x ≠ y such that H(x)=H(y) (2 different inputs with the same hash output). <br><br>Overall, pre-image resistance protects against reversing a hash, while collision resistance protects against finding two inputs with the same hash.	cryptography hash_functions week4
GTnALM)fph	What is a <b>checksum</b>, and how is it used to verify data integrity?	"<div>A checksum is a value computed from data (e.g. using a hash function) that is used to detect changes such as tampering or corruption.</div><div><br></div>
<div>The sender computes a checksum and sends it with the data. The receiver recomputes the checksum from the received data and compares it to the original - if they differ, the data has been altered or corrupted</div>"	cryptography hash_functions Integrity week4
yVCgO~$M-$	What is <b>HMAC </b>(Hash-based Message Authentication Code)?	HMAC (Hash-based Message Authentication Code) is a construction that uses a <b>cryptographic hash function </b>and a<b> secret key</b> to produce a tag. It provides <strong>integrity and authentication</strong>, allowing the receiver to verify that a message has not been altered and was created by someone who knows the key.	authenticity cryptography defn hash_functions Integrity MAC week4
GM0h;P%Q^1	How does a <b>HMAC </b>(Hash-based Message Authentication Code) work?	It works by applying a hash function twice, using the key in two slightly different forms (called inner and outer). These forms are created by combining the key with fixed constants before each hash (e.g. via XOR).<br><br>This ensures the key is mixed into the computation in a more complex way, preventing attackers from exploiting the structure of the hash function (via length extension attacks), which can occur in naive constructions such as H(K || m) (the hash of K and m concatenated)	cryptography hash_functions MAC week4
z<:V:^FdI^	What is a <b>length extension attack</b>, and why does it break schemes that compute a MAC as H(K || m)?	A length extension attack allows an attacker, given a valid hash of a message, to append extra data and compute a new valid hash without knowing the secret key. <br><br>This is possible because some hash functions process data in blocks and maintain an internal state. The final hash value is this internal state, so an attacker can use it as a starting point to continue hashing additional data. As a result, they can create a modified message that will still be <strong>accepted as valid by the receiver</strong>, breaking authentication.<br><br>*In other words, given H(K || m), the attacker can compute the hash of the extended message K || m || m'&nbsp;without knowing K (the secret key) itself, producing a valid tag for the modified message.	attacks cryptography hash_functions week4
PfjN@]<gN+	What does the notation x || y mean in cryptography?	|| means <b>concatenation</b>.<b> </b>So the value formed by appending y to the end of x (e.g. writing them one after the other).	defn week4
uxU8Y^i)4m	What is a <b>blockchain</b>?	A blockchain is a data structure consisting of a sequence of blocks, where each block contains data and the <strong>hash of the previous block</strong>, forming a chain.<br><br>*Essentially a special type of linked list&nbsp;	block_chain defn week4
Kt$)/[4>oc	How does a <b>blockchain </b>use hash functions to maintain <b>integrity</b>?	Each block includes the hash of the previous block (alongside data), so if any block is modified, its hash changes and breaks the chain, making tampering detectable. <br><br>To avoid detection, an attacker would need to recompute the hashes of that block and all subsequent (later) blocks, which is <b>computationally infeasible</b> in practise.&nbsp;	block_chain week4
lp^+ctG!,3	What is a <b>Digital Certificate</b>, and what information does it contain?	A digital certificate is a signed data structure that binds an <b>entity’s identity </b>(e.g. a domain name) to its <b>public key</b>. It contains the public key, identity information, and a <b>digital signature </b>from a<b> Certificate Authority (CA)</b>, which allows others to verify that the key belongs to that entity.<br><br>In other words, a digital certificate is what certifies the ownership of a public key by the named subject of the certificate.	defn digital_certificate week4
lfesb}=d13	In the context of <b>Digital Signatures</b>, what is a <b>Certificate Authority (CA)</b>?	A <b>Certificate Authority</b> is a <i>trusted </i>entity that <b>verifies </b>identities and&nbsp;<b>issues </b>digital certificates (as well as store them) by signing them. By signing a certificate, the CA confirms that the public key in the certificate belongs to the claimed identity.	defn digital_certificate week4
kbE(v%gDDm	Why are <b>digital certificates</b> needed, and what problem do they solve?	They solve the problem of <b>authenticating public keys</b>. Without certificates, an attacker could perform a <b>man-in-the-middle attack</b> by substituting their own public key. Certificates prevent this by providing a trusted binding between identity and public key.	digital_certificate man_in_middle week4
pvea)ilJjC	What is <b>Public-Key Infrastructure</b> (PKI)?	Public-Key Infrastructure (PKI) is a system of <b>Certificate Authorities (CAs), digital certificates, </b>and <b>trust relationships</b> that is used to securely bind <b>public keys </b>to <i>identities</i>. It enables clients to verify that a public key belongs to a specific entity (e.g. a website) by relying on certificates signed by <b>trusted </b>authorities	defn digital_certificate week4
Gt3J}qa|Ry	What are the <b>main limitations</b> or <b>risks </b>of the Digital Certificate system (PKI)?	The system relies on <b>trusting </b>Certificate Authorities. If a CA is compromised or issues certificates incorrectly, attackers could create fraudulent but valid-looking certificates, undermining security.	digital_certificate week4
hHB9>l0_*	How does <i>certificate chaining</i> work in <b>Public Key Infrastructure (PKI)</b>?	<b>Certificate chaining</b> works by linking certificates through digital signatures. An entity’s certificate is signed by an intermediate CA, which is signed by another CA and so on. This forms a chain that ends at a trusted <b>root CA.</b> Each step in the chain represents a <i>transfer of trust</i>, allowing a client to trust an entity’s public key indirectly through the chain	defn digital_certificate week4
n|+]h@mGe$	How does a client verify a digital certificate in practice?	The client verifies the <strong>CA’s digital signature</strong> on the certificate using the CA’s public key. Certificates are often part of a <strong>chain</strong>, where each certificate is signed by another CA, ending in a <strong>trusted root CA</strong> whose public key is already known. If all signatures are valid, the certificate is trusted	digital_certificate week4
dug>l94BUt	In the context of&nbsp;<b>digital certificates</b>, what is <i>X.509?</i>	X.509 is a standard that defines the <i>format </i>and <i>structure </i>of<b> digital certificates</b>, including how identities, public keys, and Certificate Authority signatures are represented. It also defines how certificates can be linked into <strong>certificate chains</strong>.	defn digital_certificate week4
JV)v(`p))k	What is a <b>limitation </b>of digital certificates in terms of <i>identity verification</i>?	Digital certificates typically verify <b>control </b>over a domain, <i>not </i>the real-world <b>identity </b>of an organisation. This means a certificate shows that the server you are communicating with controls the domain name, but does not guarantee that the organisation behind the domain is legitimate or trustworthy. As a result, attackers (e.g. phishing sites) can obtain valid certificates (e.g. via services like Let’s Encrypt) and still appear secure - displaying HTTPS indicators such as the padlock icon in a browser	digital_certificate week4
b,-iBCsb8f	What is meant by a<b> human-centred</b> (usable) approach to security?	A human-centred approach to security designs systems by considering how people<b> actually behave</b> and <b>interact </b>with technology, rather than assuming ideal behaviour. It aims to account for human limitations (e.g. memory, attention, biases, laziness) and design systems that reduce user error and make secure behaviour easy	defn human_centred week5
H{Nh&`|2XM	Why do users often behave insecurely even if they care about security?	Because security is usually not their <b>primary goal</b>. Users prioritise completing their main task, so if security is inconvenient, complex or slows them down, they will ignore it or find workarounds	human_centred week5
Lew>O?r|JS	Why should users <i>not </i>be considered the weakest link in security?	Users are not inherently the problem. They often want to be secure but prioritise their primary tasks over security, may lack awareness (especially if elderly or unfamiliar with technology), or are forced to use systems that are not <b>usable</b>.<br><br>As a result, “human errors” are often predictable responses to poorly designed systems and policies. Security should therefore be designed to be usable and error-tolerant, working with human behaviour rather than relying on users to behave perfectly	human_centred week5
BQa7S<j3gF	What are the two main categories of human-related security failures?	<strong>1.) Unintentional</strong> (e.g. mistakes, poor practices, non-compliance)<br><strong>2.) Intentional</strong> (e.g. malicious insiders)	human_centred week5
mRGoScVAk)	What are common examples of <b>unintentional </b>human security failures?	1.) <b>Human error</b> (e.g. accidentally revealing sensitive information)<br>2.) <b>Poor digital hygiene</b> (e.g. keeping sensitive data unnecessarily)<br>3.) <b>Non-compliance with policies</b> (e.g. password reuse)	human_centred week5
"i#a&(Y)/:v"	What trade-off exists in password security?	There is a trade-off between <b>security </b>and <b>usability</b>. Strong passwords are <b>harder to guess</b> but <i>difficult to remember</i>, leading users to reuse, simplify, or write them down. Simpler passwords are <b>easier to use</b> but <i>less secure</i>, so improving one often weakens the other.	human_centred week5
cS?g!>nu$<	How can systems be designed to help users perform security tasks correctly?	By making actions <strong>simple and low effort</strong>, using clear <strong>signifiers</strong> (sound or visual indicators of what to do) and providing <strong>feedback</strong> so users understand the result of their actions.	human_centred week5
Lu7qEE8De8	What does “efficiency” mean in <b>usable security</b>?	Users can perform tasks quickly and with minimal effort, reducing the likelihood of bypassing security measures. The idea is that the secure way of doing things should also be the most efficient/fast	defn human_centred week5
sA*Yd%No2!	What does “effectiveness” mean in usable security?	Effectiveness means the system allows users to achieve their goals accurately and without mistakes. It focuses on whether users can complete tasks successfully and correctly, rather than how quickly they can do them	human_centred week5
u4ha,9I~90	What is “error tolerance” in usable security, and why is it important?	Error tolerance means designing systems that <strong>anticipate and manage human errors</strong>, rather than penalising users for them. A usable system assumes users will make mistakes and provides ways to <strong>prevent, detect, and recover from errors</strong>, while only showing errors when something is genuinely wrong (so that users dont constantly feel penalised). <br><br>This reduces the likelihood of users becoming frustrated and bypassing security	human_centred week5
p?3/^$2/:o	What does “engagement” mean in usable security?	Engagement means the system is <b>pleasant </b>and <b>appropriate to use for its industry/topic</b>, so users are more willing to interact with it and follow its processes rather than avoiding or ignoring them	human_centred week5
M8YfHRBIBR	What are the main factors that make a system usable in a security context?	"<div>A system is usable if it is:</div>
<ul>
<li>
<strong>Effective</strong> (users can complete tasks correctly and accurately)
</li>
<li>
<strong>Efficient</strong> (tasks can be done quickly and easily)
</li>
<li>
<strong>Engaging</strong> (pleasant and appropriate to use)
</li>
<li>
<strong>Error tolerant</strong> (prevents and recovers from mistakes without penalising user)
</li>
<li>
<strong>Easy to learn</strong> (new users can use it quickly)</li></ul>"	human_centred week5
Fu9/e/<UOJ	What is a <b>limitation </b>or <b>critique </b>of <i>human-centred design</i>?	It may optimise a system for the specific group of users involved in the design process, potentially making it less suitable for others	human_centred week5
w*_j?6|-Bv	What is a cognitive bias in the context of security?	Cognitive biases are <b>systematic patterns in human thinking</b> that influence decisions, often leading to irrational or suboptimal behaviour (wrt security). <br><br>In security, they cause users to underestimate risks, delay protective actions, or follow habits&nbsp;even when they are aware of potential threats.	defn human_centred week5
g$=5o_V[kw	In the context of <b>human-centred security</b>, what is meant by <i>optimism bias?</i>	"Optimism bias is a cognitive bias where individuals believe they are<b> less likely than others to experience negative events</b>, such as security breaches or attacks. This leads users to underestimate their personal risk and ignore or deprioritise security precautions due to their ""It won't happen to me"" mindset"	defn human_centred week5
KUwwRD/PH*	In the context of&nbsp;<b>human-centred security</b>, what is meant by&nbsp;<i>anchoring bias?</i>	Anchoring bias is a cognitive bias where people rely heavily on their <strong>first experience or initial decision</strong>, using it as a reference point for future behaviour. This means users tend to repeat past actions rather than re-evaluating decisions based on new information<br><br>E.g. users may think “I’ve done it before and it was fine, so I can do it again.” If a user has previously connected to an insecure network (e.g. public Wi-Fi) without experiencing any issues, they may base (anchor) future security decisions on that experience, underestimating the risk	defn human_centred week5
K1/{a-qjiF	How does <b>optimism bias</b> affect <i>security behaviour?</i>	It causes users to think “it won’t happen to me,” leading them to ignore warnings, reuse passwords, or take risks, even when they are aware of potential threats	human_centred week5
P&SlAg:}bw	How does<b> anchoring bias</b> affect security behaviour?	Users stick to previous habits (e.g. ignoring 2FA) because their behaviour is <strong>anchored to past choices</strong>, even if those choices may no longer be appropriate	human_centred week5
C9Q=)g;sNo	In the context of&nbsp;<b>human-centred security</b>, what is meant by&nbsp;<i>hyperbolic time discounting?</i>	Hyperbolic time discounting is a <b>cognitive bias</b> where people prioritise <b>immediate rewards over future benefits</b>, even when the future benefits are more important (PROCRASTINATION!!).	defn human_centred week5
d=20V.n:TH	How does <b>hyperbolic time discounting</b> affect security behaviour?	"It causes users to <strong>delay or avoid security actions</strong> (e.g. setting up 2FA, updating passwords, installing updates) because these actions require effort now but only provide benefits in the future. As a result, users often choose immediate convenience, increasing long-term security risk. E.g. users think ""I'll do it later"" but never end up doing it"	human_centred week5
CYNlfm1JhI	In the context of&nbsp;<b>human-centred security</b>, what is meant by&nbsp;<i>consensus bias?</i>	Consensus bias is a cognitive bias where individuals assume that their behaviour is<b> typical or widely shared by others</b>, even without evidence. In a security context, this leads users to believe that their practices (e.g. weak passwords or ignoring updates) are normal, reducing their perception that their behaviour is risky or needs to change.	defn human_centred week5
"gNlC2u#ykH"	How does <b>consensus bias</b> affect security behaviour?	Consensus bias causes users to <strong>justify insecure behaviour by assuming others do the same</strong>, reducing their motivation to adopt stronger practices. For example, a user might think “no one uses complex passwords,” and therefore feel less pressure to improve their own security, even if their behaviour increases risk	human_centred week5
K_b4RORjka	Why are security warnings often ineffective at changing user behaviour?	Security warnings are often ignored because they are&nbsp;<b>too frequent, contain false positives, or interrupt the user’s task</b>, causing users to become desensitised and treat them as routine interruptions rather than meaningful alerts.	human_centred week5
"u.4vX6g#%b"	What can happen if users are frequently shown warning messages? [4 marks]	"1.) Users may become desensitised to warnings, causing them to ignore or automatically dismiss them over time<br>2.) Frequent warnings may interrupt the user’s primary task, leading to frustration (lack of usability) and a desire to bypass them<br>3.) If many warnings are false positives, users may stop trusting them and assume they are not important<br>4.) Users may fail to understand the warnings (due to poor mental models of security), especially if they are unclear or too technical, leading to incorrect decisions<br>


"	human_centred question week5
K5mbl{/>.u	Why might users fail to notice security-relevant changes (e.g. UI changes or warnings)?	Because their attention is directed toward their <b>main goal</b>, and attention is a limited resource. If a security cue is not directly related to that goal, it may not be processed at all.&nbsp;This is because processing all available information would lead to <b>cognitive overload</b>. <br><br>For example, users may fail to notice that a security indicator (e.g. browser security padlock) is missing because they are focused on completing their task	human_centred week5
d8,/?t05(}	Why are traditional security awareness training programmes often ineffective?	Because they focus on <b>raising awareness of risks</b>, but <i>fail </i>to provide users with <i>practical, actionable</i> <i>ways </i>to protect themselves. As a result, users may <b>understand </b>that they are at risk, but<i> lack the knowledge or tools</i> to respond effectively	human_centred week5
l6VB{TSI6T	Why can security awareness campaigns that emphasise risk be counterproductive?	If campaigns focus only on highlighting threats, they can make users feel <strong>fearful or overwhelmed</strong>, without giving them clear actions to take. This creates awareness without empowerment, meaning behaviour does not tend to improve (wrt security)	human_centred week5
bVfx(-t+OX	Why is it problematic to design security systems that expect users to adapt to them?	Because it assumes users can behave perfectly, ignoring human limitations. This leads to errors and non-compliance. Instead, systems should be designed to <b>fit users </b>and <b>their behaviour</b>, not the other way around	human_centred week5
K[Y2^sKC^9	What is the <b>80/20</b> principle in <b>human-centred </b>security design?	Around <b>80%</b> of security should be achieved through system design that <i>supports users</i>, while only about <b>20%</b> should <i>rely on user awareness and training</i>. This reflects the idea that simply trying to get users to behave more securely (behaviour change) is not enough without systems that make secure behaviour easy and natural	defn human_centred week5
DqZ|NaQ;Zf	Why is changing user behaviour more important than simply increasing awareness?	Because security depends on what users do, not what they know. Simply being aware of risks does not guarantee secure behaviour, as users may still prioritise convenience, forget or be influenced by cognitive biases<br><br>Therefore, awareness must translate into actual behavioural change, which requires designing systems and interventions that account for usability, context and human limitations, so that users are able and self-motivated to act securely in practice	human_centred week5
lEGGI<``<n	What are <b>simulated phishing attacks</b>, and how are they used in raising security awareness?	Simulated phishing attacks are controlled exercises where an organisation sends <b>fake phishing emails to users</b> to observe their behaviour (e.g. clicking links or submitting information). They are used to measure vulnerability, identify at-risk users as well as provide targeted training, helping organisations understand how users respond to phishing in practice	defn human_centred simulated_phishing week5
Q/:2Hp((&/	What are the <i>advantages </i>of <b>simulated phishing attacks?</b>	<div>Allows organisations to:</div><div><br></div><div>1.) Identify vulnerable users</div><div>2.) Measure how susceptible users are to phishing</div><div>3.) Deliver targeted training to vulnerable users</div><div>4.) Increase short-term awareness of phishing attacks</div>	human_centred simulated_phishing week5
t977FprU$7	What are the <i>disadvantages </i>of <b>simulated phishing attacks?</b>	1.)&nbsp;Effects may be short-lived, with users reverting to insecure behaviour over time<br>2.)&nbsp;They only measure specific actions (e.g. clicking links), not true understanding. Users may pass simulations without understanding how phishing actually works<br>3.)&nbsp;They place responsibility on users, rather than addressing system design or usability issues.<br>4.)&nbsp;They may not reflect real-world complexity, as actual phishing attacks are more varied and adaptive<br>5.)&nbsp;They can create distrust between employees and the organisation, reducing openness and cooperation	human_centred simulated_phishing week5
^6.sT.eT&	Why is<b> simulated phishing</b> <i>not sufficient</i> as a long-term defence against phishing?	Because attackers/adversaries constantly adapt their techniques, creating new and more sophisticated phishing methods. Simulated phishing typically tests only known or simple attack types, meaning users may still be vulnerable to novel or more complex/new real-world attacks	human_centred simulated_phishing week5
"d$mA#ydB$z"	Why can <b>sanctioning </b>poor security behaviour be <i>ineffective or harmful?</i>	-&nbsp;May lead to non-reporting&nbsp;(users/employees hide mistakes rather than report them) reducing visibility of real incidents<br>- Can also create distrust between users and the organisation, discouraging cooperation<br><br>Overall, While sanctions aim to deter insecure behaviour by increasing perceived cost, they can instead produce negative outcomes such as non-reporting, fear and reduced willingness to engage with security processes, ultimately weakening overall security	human_centred week5
h)41n_L{[0	In the context of <b>human-centred security,</b>&nbsp;what is meant by <i>inclusive security?</i>	Inclusive security is the design of security systems and processes that consider the needs, abilities, and limitations of all users, including those with disabilities, older adults, and people with different cognitive or physical abilities, ensuring systems are usable and secure for everyone<br><br>Sidenote: when designing security systems, the needs of disabled people are often overlooked, despite making up a significant proportion of the population (In 2021, 18% of people in England were disabled)	defn human_centred inclusive_security week5
FA4G_sl9RA	Why is <b>accessibility</b> and <b>inclusive security</b> important in <i>information security?</i>	Because a significant portion of users have accessibility needs (e.g. disabilities, age-related limitations, dyslexia), and systems that do not account for these needs can become <b>unusable or insecure</b> for these groups, leading to exclusion or increased security risk	human_centred inclusive_security InfoSec week5
j{dI8C3!%Z	What are the four key principles of accessible interface design?	"<div>Interfaces should be:</div>
<strong>1.) Perceivable</strong>&nbsp;- Information must be presented in ways that users can perceive, regardless of sensory ability (e.g. not relying solely on colour, so colour-blind users can distinguish warnings)<br><strong>2.) Operable</strong>&nbsp;- Users must be able to interact with and navigate the system (e.g. interfaces should support users with motor impairments who may struggle with precise inputs)<br><b>3.)</b>&nbsp;<strong>Understandable</strong>&nbsp;- Information and system behaviour must be clear and predictable, so users can understand how to use it correctly<br><strong>4.) Robust</strong>&nbsp;- The system should work reliably across a range of devices, browsers, and assistive technologies (e.g. screen readers with text-to-speech)<br>"	human_centred inclusive_security week5
JP/b?+$5o0	What is <i>accessible design</i> in the context of <b>security systems?</b>	<div></div>Accessible design focuses on making systems and processes <b>usable for people with disabilities</b> (e.g. visual, motor, or cognitive impairments). It aims to remove barriers that would otherwise prevent these users from interacting with security mechanisms effectively, ensuring they can use systems safely and independently<br><br>Note: Improvements made to increase&nbsp;<b>usability</b>&nbsp;for disabled users often benefit all users, as they make systems simpler, clearer and easier to use.	defn human_centred inclusive_security week5
L`OJ7Bh:Ow	What is <i>universal design</i> in the context of <b>security systems?</b>	Universal design aims to make systems and processes <b>usable for all users</b>, regardless of their abilities or characteristics. Instead of targeting specific groups, it focuses on creating solutions that work broadly, improving <b>usability </b>and <b>accessibility </b>for everyone	defn human_centred inclusive_security week5
P./%68=`,M	What is the difference between accessible design, universal design and inclusive security?	<div>- Accessible design focuses on making systems usable for people with disabilities, by removing specific barriers (e.g. supporting screen readers or alternative input methods)<br></div><div><br></div>- Universal design aims to make systems usable for everyone from the outset, without needing special adaptations, by designing broadly usable systems<br><br>- Inclusive security is a broader concept that ensures security systems work for all types of users, including those with disabilities, older adults and users with different abilities or levels of technical knowledge. It incorporates both accessible and universal design principles<br>	human_centred inclusive_security week5
BWu[vz<[s8	How do <b>age-related factors</b> affect security <i>usability</i><b> </b>and <i>behaviour?</i>	Age-related changes such as <b>reduced memory, slower cognitive processing, </b>and <b>decreased physical dexterity</b> can make security tasks (e.g. remembering passwords, navigating interfaces) more difficult. This can lead to errors, frustration, or avoidance of security mechanisms.<br><br>Additionally, older users may be more reluctant to adopt new security technologies. This can be influenced by <b>anchoring bias</b>, where users rely on past experience (e.g. not needing 2FA before) and assume their existing behaviour is sufficient, reducing willingness to change	human_centred inclusive_security week5
"xe-=-(#jY{"	What <b>key factors</b> influence <b>older adults’</b> engagement with <i>cybersecurity behaviours?</i>	<strong>1.) Technical competence</strong> (e.g. familiarity with devices)<br><strong>2.) Support from others</strong> (e.g. family or friends)<br>3.)&nbsp;<strong>Effort and demand</strong> of keeping up with security changes (older people tend to not keep up with advancements in technology/security as well as younger people)<br>	human_centred inclusive_security week5
FS4Q`lZuUW	Why are <b>older adults</b> less likely to use <i>two-factor authentication (2FA)?</i>	Older adults may be less likely to use 2FA due to limited technical understanding, concerns about privacy, and usability challenges. <br><br>Physical factors (e.g. difficulty handling small devices) and cognitive factors (e.g. unfamiliarity with new systems) can make 2FA harder to use. Additionally, anchoring bias may cause users to rely on past behaviour (e.g. not needing 2FA before), reducing willingness to adopt it.	human_centred inclusive_security week5
fcyP-UCYA,	What are the advantages of using an authentication app instead of SMS-based 2FA? [2 marks]	<div>Authentication apps are more secure than SMS based 2FA authentication as they not vulnerable to certain attacks like sim-swapping. Additionally, they generate codes locally on the device rather than relying on a network, making them more resistant to network-based attacks</div>	human_centred question week5
Gi*gBRl9{K	What is SIM swapping (attack)?	SIM swapping is an attack (form of identity theft) where an attacker tricks a mobile network provider into transferring a victim’s phone number to a SIM card controlled by the attacker. This allows the attacker to receive calls and SMS messages intended for the victim, including authentication codes used for SMS-based 2FA	attacks week5
lxu-TjEJ/w	Give four reasons why is SMS-based 2FA is more <b>usable </b>compared to authentication apps. [4 marks]	<div><b>Familiarity</b>&nbsp;- users are already familiar with receiving and reading text messages, as well as what SMS is and how it works<br></div><b>No additional software required</b>&nbsp;- does not require installing any additional software<br><b>Works on most devices</b>&nbsp;- does not require a smartphone or specific software<br><b>Simple to use</b>&nbsp;- users just read and enter a code, with minimal steps<br><br>Additional ones:<br><b>No additional setup required&nbsp;</b>- does not require reconfiguring a device&nbsp;<br><strong>Easy recovery</strong>&nbsp;- lost SIM cards can be re-issued easily, so users do not need to back up credentials or app settings<br><br>	question week5
"usy{(cCc#r"	What is a <b>Denial of Service (DoS)</b> attack, and how does it work?	A DoS attack is an <b>attack on availability.</b> It aims to <b>prevent legitimate users from accessing a service</b>, typically by exhausting its resources (e.g. bandwidth, CPU time, memory) or exploiting (network) protocol weaknesses. <br><br>This is typically done by overwhelming the target (e.g. a network or server) with traffic or causing it to waste resources on fake requests, making it unable to respond to real users	attacks availability defn DoS networking week6
J>BBN04^yM	What is <b>amplification </b>in <b>DoS attacks</b>, and why is it powerful?	"Amplification is a <i>technique </i>used in DoS attacks where a small request from an attacker triggers a much larger response from other systems, which is then directed at the victim. This is powerful because even ""small"" attackers with <b>little effort and resources</b> can generate large-scale attacks against much larger organisations"	attacks availability DoS networking week6
y^ve;xX>m|	What are the two main types of amplification-based DoS/DDoS attacks?	<b>1.) DoS bug</b>&nbsp;- exploits a design or implementation flaw in a protocol or system to cause disruption with minimal effort/bandwidth (packets sent)<br><b>2.) DoS flood </b>-&nbsp;uses a large volume of traffic using many machines (often via botnets) to overwhelm a target system&nbsp;<br><br>Main difference is that a DoS bug attack exploits a weakness in the system, whereas a DoS flood attack exhausts system resources through sheer volume of traffic (so its more like brute force)	attacks availability DoS networking week6
iwR;IG&.^C	(This is not a flashcard, more like a case study/example to help understanding) <br>Example of a DoS bug attack: IEEE 802.11b Wi-Fi standard	"<div>One well-known example of a DoS bug is found in the 802.11b Wi-Fi protocol, which contains design flaws that can be exploited to disrupt communication with minimal effort</div><div><br></div>
<div>One such attack is the deauthentication attack. In the 802.11b protocol, devices can send <em>deauthentication packets</em> to disconnect a client from an access point. However, these packets are <b>not authenticated</b>, meaning any device can send them.</div><div><br></div><div>An attacker can exploit this flaw by repeatedly sending fake deauthentication packets to users on the network. Since the protocol treats these packets as legitimate, targeted users are continuously disconnected and cannot maintain a stable connectio</div><div><br></div>
<div>Another example is the NAV (Network Allocation Vector) attack. In 802.11b, devices use a NAV field in the packets to indicate how long they intend to use the wireless channel, during which other devices must remain silent. However, any device can set this value, and it is not properly verified. An attacker can send packets with a very large NAV value, causing all other devices to believe the channel is occupied and stop transmitting, effectively blocking all communication</div><div><br></div>
<div>Both of these attacks exploit design flaws in the protocol, rather than overwhelming the network with traffic (and so they are DoS bug attacks). As a result, an attacker can deny service using minimal effort and bandwidth/resources - which is an example of why DoS bug attacks can be so dangerous</div>"	attacks DoS networking week6
"nK1%m#%Z6e"	What is a ping in networking?	<div>A ping is a type of network request that is used to check if another device on a network is reachable. One device sends a message, and if the other device is online, it sends a reply back.</div><div><br></div><div>Typically used for diagnostics, but can be used in a certain way for DoS attacks</div><div><br></div><div>Note: The actual name for the ping request is an <b>ICMP Echo Request</b>, and the name for the reply is an <b>ICMP Echo Reply</b>&nbsp;</div>	defn networking week6
A-Ry{ko|k6	What is a <b>broadcast address</b> in networking?	A broadcast address is a network address used to send a message to <strong>all devices on a network simultaneously</strong>, rather than a single specific device	defn networking week6
b$uBVoP|gl	What is a Smurf amplification DoS attack?	A Smurf DoS attack is an&nbsp;<b>amplification-based</b>&nbsp;DoS flood attack where an attacker sends ping requests to a broadcast address with a spoofed source IP (the victim's). This causes all devices on the network to send replies to the victim, overwhelming them with traffic	attacks defn DoS networking week6
em)cG25<Ji	How can Smurf amplification DoS attacks be prevented?	Smurf attacks can be prevented by blocking or disabling responses to broadcast requests, particularly from external sources. Network devices (e.g. routers) can be configured to ignore ICMP/ping requests sent to broadcast addresses, so that machines do not reply to them. <br><br>This prevents attackers from triggering many responses from a single request	attacks DoS networking week6
"s#{Yx$J#vN"	What is a DNS amplification DoS attack?	A DNS amplification attack is a type of <strong>DoS flood attack</strong> where an attacker sends small DNS queries with a <strong>spoofed source IP (the victim)</strong>, causing DNS servers to send much larger responses to the victim, overwhelming them	attacks defn DoS networking week6
"H2zjcc{5#7"	What is <b>TCP </b>and how does it ensure reliable communication?	TCP (Transmission Control Protocol) is a <b>connection-based network protocol</b> that provides reliable communication between two devices<br><br>It ensures packets (data) are delivered accurately, in order and without loss by requiring the receiver to send acknowledgements (ACKs) for received data. If the sender detects that data has not been acknowledged, it retransmits it, ensuring reliable delivery<br><br>Note TCP is typically used in situations where correctness of data is important, such as web browsing or file transfer	defn networking week6
wN2[reew2V	What is the TCP three-way handshake and how does it work?	"<div>The TCP three-way handshake is the process used to establish a connection between a client and a server before data is sent.</div><div><br></div>1.) The client sends a <b>SYN </b>(synchronise) packet to request a connection<br>2.) The server responds with a&nbsp;<b>SYN-ACK</b>&nbsp;(synchronise acknowledgement) packet to acknowledge and accept<br>3.) The client sends an <b>ACK</b>&nbsp;(acknowledge) packet to confirm<br><ol>
</ol>
<div>After this, the handhsake is complete, a connection is established and data can be transmitted between the two devices<br></div>"	defn networking week6
z@%_l)MMGa	What is a TCP SYN flood attack?	An SYN flood is a <strong>DoS attack</strong> where an attacker sends many TCP SYN (connection/synchronise request) packets to a server (usually with a spoofed source IP address) but does not complete the handshake, causing the server to run out of resources (as it accumulates alot of half-open connections) and preventing legitimate connections	attacks defn DoS networking week6
O3py)}jS<E	Why don’t simple fixes (e.g. increasing memory capacity, compute power, server amount) stop DoS attacks?	Because DoS attacks can scale with the system. If capacity is increased (e.g. larger queues or more bandwidth), attackers can simply generate more traffic to overwhelm it again. These approaches address the symptoms (lack of resources) rather than the root cause (malicious or unverified requests) of the attacks	attacks mitigation networking week6
e-_LLmpbcn	What is the key principle for defending against resource exhaustion attacks (like SYN floods)?	Systems should avoid allocating resources or storing state about a particular connection until the client proves they are legitimate, preventing attackers from consuming resources using fake or incomplete requests	attacks DoS networking week6
lCIz]O{yoM	What is an SYN cookie and how do they work?	"<div>SYN cookies are a defensive technique against SYN flood attacks where the server does not store connection state after receiving a SYN request.</div><div><br></div><div>Instead, it encodes connection information into the SYN-ACK packet (e.g. within a field in the packet)&nbsp;and sends it to the client</div><div><br></div>
<div>When the client replies with an ACK, the server verifies this encoded value and only then reconstructs the connection and allocates resources.</div>"	defn DoS mitigation networking week6
hakg>4@&{q	What is a TCP connection (TCP con) flood attack?	"<div>A TCP connection flood is a DoS attack where attackers complete the TCP handshake and establish real connections, then send repeated requests to overwhelm the server</div><div><br></div>
<div>Note: this means attackers cannot easily use spoofed IP addresses, as they must receive the server’s responses in order to complete the handshake, which can sometimes reveal the true source of the attack (dangerous for attacker)</div>"	attacks defn networking week6
h*k_le<F5N	What is a botnet?	<strong>A botnet</strong> is a network of compromised devices (bots) that are remotely controlled by an attacker (the <strong>botmaster</strong>), typically via a command-and-control (C2 or C&amp;C) server. Botnets are used to perform coordinated malicious activities such as launching large-scale DDoS attacks, sending spam, conducting data theft or generating fraudulent ad revenue.<br><br>Note: a bot is defined to be an infected (by malware)&nbsp;<b>compromised machine/device</b>&nbsp;that can be&nbsp;<b>controlled</b>&nbsp;by an attacker remotely	attacks botnet defn networking week6
I^d~PY3PPQ	How does a botnet operate?	1.) Internet-connected devices (e.g. computers, IoT devices) are infected with malware, turning them into “bots”<br>2.) The infected devices connect to a command-and-control (C2 or C&amp;C) server, allowing the attacker to manage them remotely<br>3.) The attacker/server sends commands via the C2 infrastructure (e.g. to launch an attack or send spam)<br>4.) All compromised devices act together, for example by sending large volumes of traffic to a target in a DDoS attack.	botnet networking week6
i_+meUUq4R	How can DDoS protection providers such as Cloudflare or Akamai help defend a server from DoS attacks?	"<div>DDoS protection providers&nbsp;as an intermediary layer/protective barrier between users and the origin server</div><div><br></div>
<div>All incoming traffic is routed through the provider’s global network (with massive bandwidth and distributed data centers), where malicious traffic is detected and mitigated using techniques such as rate limiting and filtering malicious packets&nbsp;</div><div><br></div>
<div>Only legitimate, clean traffic is then forwarded to the origin server, preventing it from being overwhelmed during a DoS/DDoS attack</div>"	attacks networking week6
wS!eq<G2yP	In the context of <b>network security</b>, what is&nbsp;<i>rate limiting?</i>	"<div>Rate limiting is a technique used to control the number of requests a client (e.g. an IP address or user) can make to a server within a given time period.</div><div><br></div>
<div>If the limit is exceeded, additional requests are delayed, throttled or blocked. This helps&nbsp;prevent server overload and abuse (e.g. DDoS attacks).</div>"	defn mitigation networking week6
BfIF-G_Yo[	In the context of&nbsp;<b>DoS mitigation</b>, what are&nbsp;<i>client puzzles?</i>	"Client puzzles are a DDoS mitigation technique that require clients to solve a <strong>computational challenge</strong>&nbsp;(called a ""proof-of-work"") before their request is processed. This increases the cost of sending many requests, slowing down attackers<br><br>The goal is to make large-scale attacks computationally expensive, as each request requires significant computational resources to generate but is quick for the server to verify"	defn DoS mitigation networking week6
vW(3Op{f4V	What are the <i>limitations </i>of <b>client puzzles</b> as a DoS mitigation technique?	- They can penalise legitimate users, especially those with low-powered devices (e.g. phones), making it harder for them to access the service<br>- They require modifications to both client and server behaviour, as clients must solve puzzles and servers must generate and verify them, making deployment difficult in practice<br>- They may not fully stop attackers, as adversaries may still use powerful machines or botnets to solve puzzles at scale<br>- They can degrade user experience, increasing latency and effort for every request during an attack	DoS mitigation networking week6
"lh#>_Mu_/S"	What is a <i>memory-bound puzzle</i> in <b>DoS mitigation?</b>	Memory-bound puzzles are a type of <b>client puzzle</b> designed so that solving them requires many main memory accesses, making them more fair across different devices, since memory speed differs less than CPU speed<br><br>This helps prevent powerful attackers from having a large advantage over weaker clients. However, memory-bound puzzles still suffer from similar limitations to other client puzzles: they can penalise legitimate users, require changes to both clients and servers&nbsp;and may still be overcome by attackers using powerful machines or botnets	defn DoS mitigation networking week6
b5oRZ3iNhk	What are<i> CAPTCHAs</i> in <b>DoS mitigation?</b>	<b>CAPTCHAs</b> (Completely Automated Public Turing tests to tell Computers and Humans Apart) are challenges designed to <strong>distinguish human users from automated bots</strong>. In DoS mitigation, they help filter out malicious automated traffic by requiring users to perform tasks that are easy for humans but may be difficult for machines (e.g. recognising distorted text or images, performing certain actions)	defn DoS mitigation networking week6
vUz{RXtY%~	What are the <i>limitations of CAPTCHAs</i> in <b>DoS defence?</b>	- They degrade user experience, adding friction and delay for legitimate users (reduces usability)<br>- They may disadvantage users with accessibility needs (e.g. visual impairments)<br>- They can be bypassed by advanced bots&nbsp;or human-solving services<br>- They only protect at the application layer, not against network-level flooding	DoS mitigation networking week6
A820|F{}2O	What is the goal of <i>source identification</i> in <b>DoS defence?</b>	The goal of source identification is to determine the true origin of attack traffic, even when attackers attempt to hide it (e.g. using spoofed IP addresses). This allows defenders to block, filter or trace back attacks closer to their source, reducing their impact and potentially stopping them entirely	defn DoS networking week6
b~&Jq6ydrG	What is <i>ingress filtering</i> in <b>DoS defence?</b>	"<div><strong>Ingress filtering</strong> is a network-level defence used in computer networking to ensure that incoming or outgoing packets are actually from the networks from which they claim to originate. Routers or ISPs perform checks on the source IP address of each packet, allowing only those that match legitimate and expected address ranges. Packets with spoofed, invalid, or unexpected source IP addresses are dropped</div><div><br></div>
<div>This acts as a countermeasure against attacks that reply on spoofing, such as DoS/DDoS attacks</div>"	defn DoS mitigation networking week6
fWo>zz6IMC	Why is ingress <i>filtering difficult</i> to <b>implement effectively in practise?</b>	- It requires global cooperation across all ISPs, which is hard to enforce<br>- If even a small number of networks do not implement it, attackers can still send spoofed traffic through those networks<br>- There is no global body that can require all networks to implement ingress filtering, so adoption is inconsistent<br>- Configuring and maintaining accurate filtering rules (e.g. valid IP ranges per customer) can be complex, error-prone and resource-intensive for providers	attacks DoS networking week6
"xT92~SF#Sg"	What is <i>traceback </i>in <b>DoS defence?</b>	Traceback is a technique used to reconstruct the path that attack packets took through the network, allowing defenders to identify the origin or intermediate sources of a DoS attack and take action against them<br><br>Some traceback methods require routers to record or embed path information within packets as they get transferred, while others rely on logging or traffic analysis without modifying packets themselves	attacks defn DoS networking week6
cK.P/B^Uew	In the context of DoS defence, why is <i>traceback </i>difficult to <b>implement effectively in practise?</b>	- It requires modifications to routers or network protocols, which is difficult to deploy globally<br>- Network paths can be long and dynamic (unstable routing), making accurate reconstruction challenging<br>- There is very little space in packet headers to store traceback information without affecting normal operation<br>- Attackers often send large volumes of traffic,&nbsp;so collecting enough data to reconstruct paths can be resource-intensive<br>- Assumes most routers are trusted and uncompromised, but compromised or malicious routers can provide false or misleading information	DoS mitigation networking week6
I@m;Nd&E&X	In the context of the <b>traceback DoS defense technique</b>, what is <i>edge sampling?</i>	<strong>Edge sampling</strong> is a traceback technique in which routers probabilistically mark packets with information about the network link (edge), typically the two routers forming that link. Not every router marks every packet. Instead, markings are applied randomly so that different packets carry different fragments of the path. By collecting many packets, the victim can combine these fragments to reconstruct the full route taken by the attack traffic (if enough packets are sent - like in the event of a DoS attack)&nbsp;<br><br>This approach significantly reduces the amount of data each packet must store. Rather than recording the entire path in a single packet, only small fragments are included, making the technique much more scalable and practical for deployment	defn DoS mitigation networking week6
CwvAEeX4|2	How does <i>edge sampling</i> work?	"1.) Each router processes packets and, with some probability, marks a packet by writing its own address as the start of an edge and setting a distance field to 0<br>2.) If a packet is already marked and not re-marked, subsequent routers increment the distance field as it travels<br>3.) The next router (when distance == 0) writes its own address as the end of the edge, completing the edge information (unless the packet is re-marked)<br>4.) The victim collects many packets containing different edge fragments and uses the edge and distance information to reconstruct the full attack path<ol>
</ol>
<div>Note: Edge and distance information are typically encoded in unused or repurposed fields in the IP header (e.g. the identification field), not TCP</div>"	DoS mitigation networking week6
NBXN:Vfl-G	Why is the Internet not well-equipped to handle DDoS attacks?	<div>The Internet was not designed with strong DoS resistance in mind. It assumes that most traffic is legitimate and therefore has the followiing weaknesses (against DoS):</div><div><br></div>1.) <b>Open connectivity</b> - Any device can send traffic to any other device <b>without </b>prior authentication<br>2.) <b>Lack of source verification</b> - There are limited built-in mechanisms to verify packet source addresses, enabling IP spoofing<br>3.) <b>Decentralised architecture</b> - The Internet consists of many independently managed networks, making coordinated, global defence difficult.<br>4.) <b>Difficulty distinguishing traffic</b> - Makes it hard to distinguish malicious traffic from legitimate traffic<br>5.)&nbsp;<b>Resource asymmetry</b> -&nbsp;Attackers can use botnets to generate far more traffic than a single target can reasonable handle	attacks DoS mitigation networking week6
rw3xs,D&sN	In the context of&nbsp;<b>botnets,&nbsp;</b>what is C&amp;C (Command and Control)?	Command and Control (C&amp;C) is a server (or system) controlled by an attacker that communicates with compromised devices (bots), allowing the attacker to send commands and receive data (e.g. status updates, stolen information) from them. The C&amp;C server/system is what enables coordinated control of the botnet	botnet networking week6
(Vb^h6,GP	What types of actions can bots (e.g. within a botnet) typically perform?	- <b>Data theft and surveillance</b>:&nbsp;Log keystrokes, take screenshots, sniff network traffic and steal sensitive data<br>- <b>System control</b>:&nbsp;Execute shell commands, start services (e.g. an HTTP server to browse files) and manage files on the system<br>- <b>Malware management</b>:&nbsp;Download and execute additional malware, update themselves and remove competing processes (e.g. antivirus or other malware)<br>- <b>Abuse of network and system resources</b>:&nbsp;Send spam, act as proxies or be used as stepping stones for further attacks. Can also use the infected machine to mine crypto<br>-&nbsp;<b>Distributed attacks</b>:&nbsp;Launch coordinated DDoS attacks against targets	botnet networking week6
E^2lIT>=0B	What is a centralised C&amp;C architecture?	A centralised C&amp;C architecture is one where bots communicate with one or more central servers that issue commands and coordinate all bot activity. Bots must know the address of the C&amp;C server controlling them, which may be stored in code or configuration and updated over time<br><br>There are two main communication approaches in this architecture: <strong>push</strong> and <strong>pull</strong>	botnet defn week6
z(W(gP_fhA	What is push-style centralised C&amp;C and how does it work?	In push-style centralised C&amp;C, the C&amp;C server actively sends commands to bots as soon as they are available. Bots maintain a persistent connection to the server (e.g. via the IRC protocol), allowing them to receive instructions immediately<br><br>This enables rapid coordination of actions such as DDoS attacks.	botnet defn week6
"Dk#5e~.r}8"	What are the <b>advantages and disadvantages</b> of<b>&nbsp;</b><i>push-style centralised C&amp;C?</i>	"<div>Advantages:</div>- <b>Immediate command delivery:</b>&nbsp;Commands are sent to bots as soon as they are issued<br>- <b>Fast, synchronised attacks:</b>&nbsp;Bots can act simultaneously, enabling highly coordinated attacks (e.g. DDoS)<br><ul>
</ul>
<div>Disadvantages:</div>-&nbsp;<strong>Requires persistent connections, increasing detectability:</strong> Bots must remain continuously connected to the C&amp;C server, creating continuous or unusual communication patterns (e.g. IRC traffic) that can be identified by defenders<br>-&nbsp;<b>Centralised points of failure:</b> The C&amp;C server(s) can be targeted and taken down, potentially disrupting large parts of the botnet<br>"	botnet week6
LKJ/1Yke!$	What is pull-style C&amp;C and how does it work?	In pull-style C&amp;C, bots periodically contact the C&amp;C server to request commands rather than receiving them directly (i.e. polling the C&amp;C server at regular intervals). This is typically done using protocols such as HTTP or HTTPS, causing the communication to resemble normal web traffic. This makes it more stealthy and harder to detect	botnet defn week6
"tcF7=.#v0e"	What are the advantages and disadvantages of pull-style C&amp;C?	Advantages:<br>-&nbsp;<b>More stealthy:</b> Communication resembles normal web traffic (e.g. HTTP/HTTPS), making detection more difficult<br>-&nbsp;<b>No need for persistent connections:</b> Bots connect periodically rather than continuously, reducing suspicious traffic and blending in with normal processes<br><br>Disadvantages:<br>- <b>Slower response to commands:</b>&nbsp;Bots only receive instructions when they next contact the server<br>- <b>Less synchronised attacks:</b>&nbsp;Bots act at different times depending on when they poll the C&amp;C server	botnet week6
k_Eg+4q6e`	What is a peer-to-peer (P2P) C&amp;C architecture?	"<div>Peer-to-peer (P2P) C&amp;C is a distributed architecture in which bots communicate with each other rather than relying on central C&amp;C server(s). Commands are propagated through the network from bot to bot, allowing control to be maintained without a single point of failure, unlike in centralised C&amp;C</div><div><br></div>
<div>The botmaster injects commands into one or more bots, and these commands are then propagated across the P2P network between bots</div>"	botnet defn week6
p3R~9=/^C)	What are the <b>advantages and disadvantages</b> of <i>peer-to-peer (P2P) C&amp;C botnets?</i>	"<div>Advantages:<br></div>
<b>- No single point of failure</b>: There is no central server to target, so taking down individual nodes does not disable large parts of the entire botnet<br><b>- More resilient:</b> The botnet can continue operating even if many bots are removed or go offline<br><b>-&nbsp;Harder to trace the botmaster:</b> Commands are relayed through multiple bots, making it difficult for defenders to identify the original source<br><br>Disadvantages:<br><b>- More complicated to design and manage:</b>&nbsp;Requires custom protocols and mechanisms for communication and coordination between bots<br><b>- Peer discovery and maintenance:</b> Bots must locate other peers and maintain connections, which can be unreliable<br><b>- Noisy or unusual traffic:</b> Custom or non-standard communication patterns may stand out and be detected by defenders/ISPs<br><b>- Slower and less synchronised command propagation:</b> Commands spread gradually across the botnet network, reducing coordination compared to centralised control<br>"	botnet week6
zV1.$}C%aw	In the context of&nbsp;<b>botnets</b>, what is&nbsp;<i>bulletproof hosting?</i>	"<div>Bulletproof hosting is a type of hosting service where the provider knowingly allows malicious activity (e.g. botnet C&amp;C servers, spam or malware) to operate on its infrastructure and refuses to take action when abuse is reported</div><div><br></div>
<div>Unlike legitimate hosting providers, which remove malicious content or shut down abusive services when notified, bulletproof hosts ignore complaints or legal requests, allowing attackers/botmasters to keep their systems running. These providers are often located in jurisdictions with weak regulation or limited law enforcement</div>"	botnet defn week6
C8<ffuid|)	What is the main strategeis for defending against botnets?	"<div>The main strategy for defending against botnets is to <b>attack or disrupt the Command and Control</b> <b>(C&amp;C) infrastructure</b>, preventing bots from receiving commands and coordinating their actions. This can involve taking C&amp;C servers offline or blocking communication</div><div><br></div>
<div>Another approach is the use of <strong>honeypots</strong>, which are controlled systems/machines designed to attract attackers and study botnet activity in a controlled environment, helping defenders understand and mitigate attacks.<br></div>"	botnet defn mitigation week6
JpzG.WIqFw	In the context of&nbsp;<b>botnet defense</b>, what is a&nbsp;<i>honeypot?</i>	A <strong>honeypot</strong> is a deliberately vulnerable system designed to attract attackers. When it is compromised, it allows defenders to observe botnet behaviour in a controlled environment and collect intelligence/information on attacker actions	botnet defn mitigation week6
M%a_.6*9T]	What is malware?	<b>Malware (malicious software)</b> is software intentionally designed to perform harmful or unwanted actions, such as stealing data, damaging systems, or gaining unauthorised access. While many types of malware exist, they all share the common goal of carrying out the attacker’s malicious intent<br><br>Note: The term “virus” is often incorrectly used as a synonym for malware. This is wrong, as a virus is only one specific type of malware	defn malware week6
i3lf/yCNgC	What are the key properties used to classify malware?	<div>Malware can be systematically classified based on two main properties:</div><div><br></div><div>Propagation method (the way that it spreads)</div><div>-&nbsp;<strong>Self-spreading:</strong> spreads automatically without user intervention (e.g. worms, viruses)<br>-&nbsp;<strong>Non-self-spreading (user-triggered):</strong> requires user action to spread (e.g. trojan horses)<br></div><div><br></div><div>Mode of operation (the way that it operates)</div><div>-&nbsp;<strong>Requires a host program:&nbsp;</strong>attaches to or depends on another program to run (e.g. viruses, trojans, rootkits)<br>-&nbsp;<strong>Self-contained:</strong>&nbsp;runs independently as its own program (e.g. worms, spyware, keyloggers, dialers)<br></div>	defn malware week6
y2RfxD@akg	In the context of <b>malware, </b>what is a&nbsp;<i>virus?</i>	A computer virus is a type of malware that replicates itself by infecting other programs or files, inserting its own (possibly modified) code into them. It requires a host program to run and spreads when the infected program is executed<br><br>Distinguishing features:<br>- Reproduce its own code<br>- Attach itself to other files (viruses cannot survive on their own)<br>- Get executed when host (executable) file is executed	defn malware virus week6
DxeG[}8Y_v	What are the main techniques used by antivirus software to detect malware?	"<div>Antivirus software typically uses:</div>
<strong>- Signature-based detection:&nbsp;</strong>Compares files against a database of known malware signatures (e.g. byte patterns or hashes) to identify known threats<br><strong>- Heuristic analysis:&nbsp;</strong>Looks for suspicious code structures or characteristics that are commonly associated with malware, allowing detection of previously unseen threats<strong><br></strong><strong>- Behavioural detection (behavioural signatures):&nbsp;</strong>Monitors program behaviour during execution (e.g. modifying system files, unusual network activity) to detect malicious actions<br><div><br>
<strong>Sandboxing</strong> may also be used to analyse suspicious programs in a controlled environment. Sandboxing is where untrusted programs are executed in a restricted, isolated environment to safely observe their behaviour without risking the real system</div>"	malware mitigation week6
u),A}B<H~)	In the context of&nbsp;<b>malware,&nbsp;</b>what is a&nbsp;<i>worm?</i>	A computer worm is a type of malware that self-replicates and spreads automatically across networks (by sendings copies of itself to other nodes), without requiring a host program or user interaction<br><br>Distinguishing features:<br>- <b>Autonomous spread over network:</b>&nbsp;spreads automatically across networks without user intervention<br>- <b>Self-contained:</b>&nbsp;does not need to be embedded in another program (unlike a virus)<br>- <b>Exponential rate of spread:</b> The rate of infection can increase over time as more infected machines continue spreading the worm	malware mitigation week6
cav:VK[q&	In the context of <b>malware,</b>&nbsp;what are common <i>worm propagation methods?</i>	"<strong>- Email propagation:</strong> Sends infected attachments or links to contacts, spreading via user interaction<br><strong>- Network scanning:</strong> Probes random or sequential IP addresses to find vulnerable systems (e.g. printers)<br><strong>- Exploiting network services:</strong> Targets vulnerabilities in services (e.g. web servers, databases) to infect systems automatically<br><strong>- Network shares:</strong> Spreads across shared drives or file systems within local networks<br>


"	malware mitigation week6
zPfnu]OFnW	In the context of&nbsp;<b>malware</b>, how do&nbsp;<i>email-based worms</i>&nbsp;spread?	<div>Email-based worms spread by sending infected attachments or malicious links to users, using <strong>social engineering</strong> to trick them into opening the content. Once executed, the worm can infect the system and often send itself to other contacts<br></div><div><br></div><div>Common techniques are:</div>-&nbsp;<strong>Faking sender identity</strong> (e.g. appearing to come from a trusted contact)<br>-&nbsp;<strong>Disguising file types</strong> (e.g. masking .exe files as harmless .jpg files)<br>-&nbsp;<strong>Promising interesting or urgent content</strong> (e.g. invoices, alerts, important emails)<br>	malware mitigation week6 worm
LI@ISb73%o	In the context of <b>malware</b>, what are <i>exploit-based worms?</i>	Exploit-based worms are worms that spread by automatically exploiting vulnerabilities in network services or software. They require no user interaction and can propagate rapidly by scanning for and infecting vulnerable systems<br><br>Their propagation speed is typically limited by factors such as network latency or available bandwidth, unlike email-based worms, which depend on human interaction	defn malware week6 worm
iZ*`I=64qG	Why are <b>email-based</b> worms typically&nbsp;<i>slower </i>to spread than exploit-based worms?	Because email-based worms require a human in the loop. They rely on users to open attachments or click links, which limits propagation speed and introduces delays. As a result, their spread often follows human behaviour patterns (e.g. time of day) rather than rapid, fully automated growth	malware week6 worm
"rGT#hj3%2."	What are the two main categories of defenses against worms?	"<div>Two main categories are host-level defenses and network-level defenses.</div><div><br></div>
1.) <b>Host-level defenses </b>act on individual machines, aiming to prevent worms from exploiting vulnerabilities (e.g. stack protection, ASLR).<br><ul>
</ul>2.) <b>Network-level defenses </b>act across the network, aiming to detect and limit worm propagation (e.g. intrusion detection systems, limiting outgoing connections via a firewall).<br>"	malware mitigation week6 worm
"ym7%#quDJ$"	In the context of <b>worm defence</b>, what is the <i>main difference</i> between Host-level defense and Network-level defense?	Main difference is that <b>host-level defenses</b> try to stop new devices being infected, while <b>network-level defenses</b> try to contain spread	malware mitigation week6 worm
r{*ln)f|Xp	What is ASLR (Address Space Layout Randomisation)?	"ASLR (Address Space Layout Randomisation) is a <b>host-level security mechanism</b> that randomises the memory locations of key parts of a program (such as the stack, heap, and shared libraries) each time it runs<br><br>
This removes predictable memory addresses, making it difficult for an attacker to reliably locate and exploit vulnerabilities<br><br>Note: Because worms often rely on the same exploit across many machines, ASLR can make such attacks unreliable at scale by reducing the success rate of exploitation and limiting their ability to spread efficiently"	defn malware mitigation week6 worm
i=,oj6Gdxf	In the context of&nbsp;<b>malware,&nbsp;</b>what is a&nbsp;<i>Trojan Horse?</i>	A Trojan horse is a type of malware that appears to perform a legitimate or desirable function but secretly performs malicious actions. Examples include fake software installers, cracked programs (e.g. games), or malicious email attachments.&nbsp;<br><br>Unlike worms or viruses, it does not self-replicate and instead relies on the user to execute it, typically using social engineering to trick the user into running it<br><br>Note: Trojan horses often act as a delivery mechanism for other malware, installing additional malicious software once executed.	defn malware mitigation trojan_horse week6
eeZT%qlh3X	What are the two main ways Trojan horses are structured, and how do they deceive users?	1.) <b>Embedded Trojans</b>: Malicious code hidden inside a legitimate program (e.g. games, utilities), exploiting trust in the application<br>2.) <b>Standalone disguised programs:</b> Malicious files that pretend to be something harmless (e.g. fake file names or extensions), exploiting user misidentification<br><br>Both rely on deception (social engineering), but one hides inside trusted software while the attempts to imitate it	defn malware mitigation trojan_horse week6
QlS`K>(-Yc	What are common functionalities of <b>Trojan horses</b>?	<strong>- Spyware</strong>: secretly monitors user activity and steals sensitive information (e.g. passwords, browsing data, banking information). One common form of spyware is a keylogger<br><strong>- Rootkits</strong>: hide the presence of malware and maintain persistent, privileged access to the system<br><strong>- Backdoors</strong>: allow remote access to the system without normal authentication. RAT (Remote Access Trojans) are a more advanced version of backdoors<br><strong>- Data destruction tools</strong>: corrupt or delete files	malware trojan_horse week6
lHxg5<&kWT	In the context of&nbsp;<b>malware,&nbsp;</b>what is <i>Spyware?</i>	Spyware is malware that secretly monitors a user’s behaviour and collects sensitive information without their knowledge, such as passwords, browsing activity or banking information<br><br>Distinguishing features:<br>- Acts as an attack on user privacy<br>- Hidden from the user, usually difficult to detect<br>- Tend to leak sensitive information to someone else over the internet<br>- Often implemented using some sort of simple, seemingly harmless software e.g. a browser plugin	defn malware spyware week6
"tpmj.P{n#o"	In the context of&nbsp;<b>malware,&nbsp;</b>what is a&nbsp;<i>Keylogger?</i>	"<div>A <b>keylogger </b>is a type of malware that records the keystrokes typed by a user, typically running silently in the background</div><div><br></div>
<div>It is particularly effective because it captures data at the point of input (before encryption), allowing attackers to obtain passwords and sensitive information regardless of how securely the data is later transmitted or stored</div><div><br></div>
<div>Note: Keyloggers can be difficult to detect because many legitimate programs also capture keyboard input, making malicious activity harder to distinguish from normal activity</div>"	defn keylogger malware week6
xY3r+ti7Y!	In the context of&nbsp;<b>malware,&nbsp;</b>what is a&nbsp;<i>Rootkit?</i>	A rootkit is malware designed to hide the presence of an attacker or malicious software on a system while maintaining persistent, privileged access, allowing the attacker to remain undetected and continue accessing the system over time<br><br>Distinguishing features:<br>- Typically installed by an attacker&nbsp;<i>after</i>&nbsp;a system has been compromised<br>- Hides the presence of an attacker and allows them to return at a later time<br>- Difficult to detect&nbsp;as they can modify or intercept operating system functions, altering how the system reports information (e.g. hiding files, processes, or network activity)	defn malware rootkit week6
oPP%_]5`}Y	What is a&nbsp;<strong>user-space rootkit</strong>, and how does it hide malicious activity?	"A user-space rootkit is a type of rootkit that operates at the user application level by replacing or modifying standard system programs (e.g. ls, netstat)<br><br>
It hides malicious activity by making these tools report false information, so processes, files, or network connections controlled by the attacker are not visible to the user<br><br>For example, it may modify the ls&nbsp;command so that certain (malicious) files are not displayed, tricking the user into believing the system is clean"	defn malware rootkit week6
L>TmOwM`H[	What is a&nbsp;<strong>kernel rootkit</strong>, and why is it more powerful than a user-space rootkit?	"<div>&nbsp;A kernel rootkit is a type of rootkit that operates at the operating system (kernel space) level by modifying kernel data structures or behaviour.It is typically installed by exploiting system vulnerabilities or loading malicious kernel modules/drivers</div><div><br></div>
<div>It is more powerful than a user-space rootkit because it has full control over the system, allowing it to hide processes, files and network activity at a deeper level, and potentially manipulate or bypass security mechanisms, making it much harder to detect</div>"	defn malware rootkit week6
Ne6ZQI2Vl;	What are common&nbsp;methods for detecting or defending against rootkits?	"<div>Rootkit generally detection relies on checking the integrity of system components using specialised tools:</div>
- User-space tools (e.g. Tripwire, chkrootkit): detect modified system files or programs<br>- Kernel-level tools: check the integrity of the running kernel and look for unexpected changes<br><ul>
</ul>
<div>Note: These approaches aim to identify signs that system files or behaviour have been tampered with. However, detection is challenging because rootkits may operate at a low level and can manipulate system responses, potentially hiding their presence from detection tools</div>"	mitigation rootkit week6
HzVy`qNsOX	What are the three main&nbsp;<strong>factors of authentication</strong>, and what does each represent?	"<div><strong>Factors of authentication</strong> refer to different types of evidence used to verify a user’s identity:<br></div><div><br></div>
<strong>1.) What you know:</strong>&nbsp;something you remember (e.g. password, PIN)<br><strong>2.) What you have:</strong>&nbsp;something you possess (e.g. security token, phone)<br><strong>3.) What you are:</strong>&nbsp;something inherent (e.g. fingerprint, biometrics, DNA)<br><br>These factors represent different methods of authentication and are often combined (multi-factor authentication) to provide stronger security.<br>"	authentication defn week7
t2/u%B6LGe	Why is storing passwords in plaintext <b>insecure</b>, and what is the <i>basic/naive solution?</i>	Storing plaintext passwords is insecure because if the database is leaked, attackers immediately gain all user credentials<br><br>Instead, passwords should be stored as <strong>cryptographic hashes</strong>. When a user submits a password, the system hashes it and compares it to the stored hash, so the original password is never stored directly	authentication passwords week7
Ei&r<JO|Ih	What is&nbsp;password cracking, and how is it typically performed on hashed passwords?	"<div>Password cracking is the process (an attack) of recovering a user’s password from its stored hash by guessing possible passwords, hashing them, and comparing the results to the stored hash.</div><div><br></div>
<div>This is typically done using:</div>
<b>- Dictionary attacks:</b> trying common passwords and known wordlists (list of statistically probable passwords)<br><b>- Brute force:</b> systematically trying all possible combinations<br><ul>
</ul>
<div>If a guessed password produces the same hash, the original password is recovered.</div>"	attacks authentication defn passwords week7
Qgu]Fsk/DR	What is the limitation of storing passwords as simple hashes?	"If two users have the same password, they will have the same hash, revealing when passwords are shared<br><br>
This allows attackers to perform password cracking using precomputed hashes (e.g. for common passwords) and match them against stored hash values. If a match is found, the attacker can recover the <strong>original plaintext password</strong>, enabling large-scale compromise of accounts<br><br>Note: The precomputed hash tables are sometimes also called&nbsp;<i>rainbow tables</i>"	authentication mitigation passwords week7
sOJ?TF~Pvz	What is a&nbsp;<b>salt&nbsp;</b>in password storage, and how does it defend against <i>password cracking?</i>	"<div>A salt is a random value generated for each user and stored alongside their password hash. When a password is set, the system computes the hash of the password concatenated with the salt (e.g. H(password || salt)) and stores both the salt and the resulting hash</div><div><br></div>
<div>Salting defends against password cracking by ensuring that identical passwords produce different hashes for different users. This prevents the use of precomputed hash tables (e.g. rainbow tables) and forces attackers to recompute hashes for each user individually, significantly increasing the computational cost of the attack<br></div><div><br></div><div><strong>Note:</strong>&nbsp;The salt is not secret and is stored in plaintext. Even if an attacker obtains it, they must still crack each password individually<br></div>"	authentication defn mitigation passwords week7
w.2WG=IX@h	How does salting impact the computational cost of password cracking?	"<div><strong>Without salting</strong>, an attacker can compute hashes for a dictionary of size <em>d</em> once (e.g. using a rainbow table) and reuse them to crack many password hashes efficiently. That is, they only need to compute <em>d</em> hashes in total<br></div><div><br></div>
<div><strong>With salting</strong>, each password is combined with a (typically unique) salt, so the attacker must recompute the dictionary for each salt. If there are <em>n</em> users (and thus up to <em>n</em> distinct salts), the attacker must perform approximately <em>d × n</em> hash computations<br></div><div><br></div>
<div>This increases the computational cost significantly and prevents reuse of precomputed hashes.</div><div><br></div><div><strong>Note:</strong> If some users share the same salt, the attacker can reuse computations for those users. In that case, the total cost is closer to <em>d × s</em>, where <em>s</em> is the number of distinct salts (with <em>s ≤ n</em>)<br></div>"	authentication mitigation passwords week7
YuZIz$XE4	How did traditional Unix systems store and verify user passwords?	"Unix systems stored a hash of the password (not the plaintext) in a file (e.g. /etc/passwd). However, this file was world-readable, meaning any user on the system could access the password hashes<font face=""monospace""><br></font><br>
When a user logged in, the system hashed the entered password and compared it to the stored hash. If the hashes matched, authentication succeeded<br><br>This mechanism relies on the hash function being one-way and having pre-image resistance, so that the original password could not easily be recovered from the hash"	authentication passwords unix week7
xT`c!bqHka	How were passwords hashed in old Unix systems, and what were the key limitations?	"<div>Old Unix systems used <b>DES-based hashing</b>, where the password was used as a key to encrypt a fixed value</div><div><br>
Key limitations:</div><b>- Passwords were truncated to 8 characters:&nbsp;</b>only the first 8 characters were used, reducing the effective password space and making brute-force attacks easier<br><b>- DES was relatively weak:&nbsp;</b>DES has a relatively small key size and is now considered insecure, allowing attackers to try many possible passwords efficiently<br><b>- Users tended to choose predictable passwords:</b>&nbsp;users often chose common or guessable passwords (e.g. common dictionary words), making dictionary attacks highly effective<br><br>
These factors made password cracking more feasible<br>"	authentication passwords unix week7
F_<X.nzn;V	Why are human-chosen passwords a weakness even if the password space is large?	"Although the theoretical password space may be large, users tend to choose <strong>predictable, non-random passwords</strong> (e.g. dictionary words, names, or common patterns, birthdays)<br><br>This significantly reduces the <strong>effective search space</strong>, allowing attackers to prioritise likely passwords using dictionary or rule-based attacks, making password cracking much easier in practice<br><br><strong>Note:</strong> The ""password space"" refers to the set of all possible passwords, while the ""effective search space"" is the subset actually used by humans"	authentication passwords week7
F`nS)uNx4	How did Unix improve password storage using&nbsp;/etc/shadow and salting?	"<div>Unix improved password storage by moving password hashes from the world-readable <b>/etc/passwd</b>&nbsp;file to a protected file<b>&nbsp;(/etc/shadow)</b>, restricting access to privileged users only</div><div><br></div>
<div>It also introduced <strong>salting</strong>, where each user is assigned a random salt that is combined with their password before hashing, with both the salt and hash stored</div><div><br></div>
<div>These changes improved security by preventing unauthorised access to password hashes and by ensuring identical passwords produce different hashes, making dictionary attacks much less effective and increasing the cost of password cracking</div>"	authentication passwords unix week7
HF[MgwV<R!	What are the<i>&nbsp;trade-offs</i> of increasing the salt size&nbsp;in <b>password storage?</b>	"<div>Increasing the salt size reduces the probability that two users share the same salt, and more importantly ensures that password hashes are unique per user, even if they have the same password. This prevents attackers from reusing precomputed hashes (e.g. rainbow tables) across multiple accounts</div><div><br></div>
<div>In practice, larger salts slightly increase storage requirements, but have negligible impact on computation, since hashing cost is dominated by the hash function itself</div><div><br></div>
<div>However, there are diminishing returns: once salts are sufficiently large (e.g. 64+ bits), collisions are already extremely unlikely, so further increases provide little additional security benefit</div><div><br></div><div>Side note: Increasing salt size increases the number of possible salts (e.g. 8-bit - 2<sup>8</sup>, 16-bit - 2<sup>16</sup>), but the attack cost depends on the number of distinct salts in use, not the total possible space. Attackers must perform work per distinct salt (approx. per user), not per possible salt value, so doubling the number of bits per salt&nbsp;<i>does not</i>&nbsp;double the number of work an attacker has to do<br></div>"	authentication passwords unix week7
n^j|`v0ZV1	What technique did traditional UNIX systems use to slow down offline dictionary attacks?	"<div>Traditional UNIX systems used <b>artificial slowdown</b> by iterating the hash function multiple times (e.g. running DES around 25 times) for each password guess</div><div><br></div>
<div>This made each password guess computationally more expensive, so attackers performing offline dictionary or brute-force attacks could test fewer guesses per second, significantly slowing password cracking</div>"	authentication passwords unix week7
fdSOcMzk-{	Why is password-based authentication fundamentally difficult for users?	"<div>Because it requires unaided recall of complex information, with no tolerance for mistakes and no helpful feedback on failure, making passwords hard to remember and easy to get wrong (in other words, password auth systems tend to lack <b>usability</b>)</div><div><br></div>
<div>Note: Graphical passwords (e.g. drawing a pattern) were introduced to reduce reliance on recall by making authentication more intuitive and based on recognition and muscle memory</div>"	authentication passwords week7
j{X((qx/Z2	What is the principle of&nbsp;“recognition rather than recall”, and why is it important in authentication?	"<div>The principle states that systems should allow users to recognise information rather than recall it from memory, as recognition is easier and less error-prone</div><div><br></div>
<div>This is important in authentication because systems that rely on recall (e.g. passwords) place a higher cognitive burden on users, leading to errors and insecure behaviours such as choosing weak passwords, reusing passwords, or writing them down<br></div>"	authentication passwords week7
H?+6y[8:mM	In the context of&nbsp;<b>Authentication</b>, what are examples of <i>what/something you have?</i>	"-&nbsp; Physical tokens (e.g. hardware security keys like YubiKey)<br>- Phones (used for OTP apps like Google Authenticator or Authy)<br>- Smart cards (e.g. ID cards, access badges, oyster cards)<br>- Bank cards (e.g. debit/credit cards with chip)<br>- One-time password (OTP) tokens (hardware or app-based)<br>- SIM cards (for SMS-based authentication)<br>- USB tokens or dongles used for login<br><ul>
</ul>
<div><strong>Note:</strong> These methods rely on the user possessing a physical or digital object. Authentication is based on proving possession of the device/thing that you have, not remembering a secret</div>"	authentication week7
n{%JhHv=kr	What is a replay attack?	"<div>A replay attack is a type of attack in which an attacker records/captures/intercepts valid authentication data (e.g. a login message or token) and replays/reuses it later to impersonate a legitimate user<br></div><div><br></div>
<div>Because the reused message is still valid, the system may accept it and grant access, even though the attacker does not know the secret data</div><div><br></div>"	attacks authentication defn week7
L-+5-b,g2g	What is a challenge-response authentication protocol, and what problem does it solve?	"<div><div>A challenge-response authentication protocol is a method where the verifier sends a random challenge (a fresh, unpredictable value), and the user computes a response using a shared secret and the challenge. The verifier then checks whether the response is correct without the secret being transmitted</div><div><br></div>
<div>This solves the problem of replay attacks, because each authentication attempt uses a fresh, unpredictable challenge. As a result, intercepted responses cannot be reused to gain access</div><div><br></div>
<div>Note: This approach is used in systems such as contactless smart cards (e.g. OYSTER CARDS!!!)</div></div>"	authentication defn mitigation week7
lDRY`m52W/	Why is <b>simple authentication</b> using a&nbsp;<i>fixed message</i> authenticated with a shared key&nbsp;insufficient?	"<div>Because the authenticated message is static (does not change), it remains the same for every authentication attempt</div><div><br></div>
<div>An attacker who intercepts this message can replay it later to impersonate the user without knowing the secret key</div><div><br></div>
<div>Since the verifier only checks the validity of the message and not its freshness (whether or not it is new, hasn't been reused), it cannot distinguish between a legitimate authentication and a replayed one, making the system vulnerable to replay attacks</div>"	authentication mitigation week7
yKqo+p<gl<	What is a&nbsp;two-way challenge-response protocol?	"<div>A two-way challenge-response protocol is an authentication method where both parties authenticate each other by exchanging challenges</div><div><br></div>
<div>Each side sends a random challenge and verifies the other’s response using a shared secret, ensuring that both participants prove their identity rather than just one</div>"	authentication defn week7
i?p<L-jcoF	Why is&nbsp;two-way challenge-response authentication&nbsp;more complex than it first appears?	Two-way challenge-response authentication is more complex than it appears because both sides must prove knowledge of the shared secret <i>(without leaking it)</i> while preventing replay and reflection attacks<br><br>Simply combining two one-way protocols can introduce subtle vulnerabilities, even if each is secure in isolation	authentication week7
"kGaja$#wO1"	What is a&nbsp;<i>reflection attack</i>&nbsp;in <b>challenge-response protocols?</b>	A reflection attack occurs when an attacker tricks a party into answering its own challenge by relaying it through a second session. The attacker takes the victim’s challenge from one session, sends it back to the victim in another session as if it were their own, receives the valid response, and uses it to authenticate in the first session<br><br>This happens when the same protocol is used in both directions without proper separation, allowing the attacker to reuse messages across sessions	attacks authentication defn week7
"O#/RS6%*]A"	What is a one-time password (OTP), and what problem does it solve?	"<div>A one-time password (OTP) is a password that is valid for only a single authentication attempt or for a short time window. Unlike static passwords, each OTP changes for every login or time interval</div><div><br></div>
<div>It solves the <b>replay attack</b> problem because even if an attacker intercepts an OTP, it cannot be reused - this is because OTPs are typically generated using time-based or challenge-based mechanisms, ensuring freshness and preventing attackers from authenticating with previously captured/stolen credentials</div>"	authentication defn one_time_password week7
j3AO^>38tK	How does a&nbsp;<i>hash-chain</i> <b>one-time password</b> scheme&nbsp;work?	"<div><div>A user starts with a secret value and applies a hash function repeatedly to form a chain. The final value in the chain is stored by the server during setup</div><div><br></div>
<div>During login, the user reveals the next value in the chain in reverse order. The server hashes this value once and checks that it matches the previously stored value. If it matches, authentication succeeds and the server updates the stored value to the newly revealed value</div><div><br></div>
<div>This works because of pre-image resistance: it is easy to verify by hashing forward, but <b>computationally infeasible </b>to compute previous values in the chain</div></div>"	authentication one_time_password week7
rp{V9<:!&|	In the context of&nbsp;<b>Authentication,</b>&nbsp;what is a&nbsp;<i>biometric?</i>	A biometric (<i><b>bio</b></i>logical <i><b>metric</b></i>) is a measurable property of an individual (e.g. fingerprint, iris, voice, typing behavior) from which distinguishing and repeatable features can be extracted to recognise or verify identity<br><br>Note: Not all physical traits are suitable biometrics. A good biometric must be sufficiently distinctive and consistent over time. For example, hair colour is usually not used because it is not unique (not <i>distinguishing</i>) and can easily change (not <i>repeatable</i>)	authentication biometrics defn week7
w7+Tx0g(X!	In the context of <b>Biometrics, </b>what is a&nbsp;<i>biometric sample?</i>	"A biometric sample is the raw data captured from a sensor (e.g. a fingerprint image, voice recording, image of an eye) during enrolment or authentication<br><br>
It may vary each time due to noise, environment, or user behaviour"	authentication biometrics defn week7
g(^?/|&5p`	In the context of&nbsp;<b>Biometrics,</b>&nbsp;what is a&nbsp;<i>biometric template?</i>	A biometric template is a processed representation of distinctive features extracted from a biometric sample using <b>feature extraction algorithms</b>, and stored for comparison<br><br>It is designed to be compact, consistent, and suitable for matching during authentication	authentication biometrics defn week7
